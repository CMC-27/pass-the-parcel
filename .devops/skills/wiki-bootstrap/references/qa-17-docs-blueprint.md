# Q&A: 17 — Docs Blueprint (The Standard)

**Why this doc matters to AI agents:**
Agents use this as a quick in-repo reference for the wiki standard itself. A drift between this doc and the `wiki-bootstrap` skill = agents follow the wrong rules.

**Required sections:**
- Core philosophy summary (one short paragraph)
- Folder taxonomy (a brief reminder of the structure)
- Naming conventions (the prefix patterns)
- A pointer to the full `wiki-bootstrap` skill (do not duplicate it)

## Questions to ask
1. Does the doc summarize the core philosophy in one short paragraph (not a full copy of the skill)?
2. Does it show the folder taxonomy in a compact table (not the full 19-slot breakdown)?
3. Does it list the naming conventions (prefix patterns) concisely?
4. Does it link to the full `wiki-bootstrap` skill for the detailed checklist and Q&A workflows?
5. Has this doc drifted from the current `wiki-bootstrap` skill? (The skill is the source of truth; this doc must mirror it.)
6. Is the doc kept concise (under ~150 lines), or has it ballooned into a full duplication?

## Sources of truth
- `.devops/skills/wiki-bootstrap/SKILL.md` — the source of truth
- This doc should be a pointer, not a duplicate
