---
code: T1-E5.01
sprint: none
claim_status: QUEUED
owner: ""
claimed_at: ""
last_touch: "2026-09-24"
touches: [".devops/rules/plan-lifecycle.md", ".devops/skills/pass-the-parcel/SKILL.md", ".devops/plans/template-plan.md", ".devops/skills/sprint-plan/SKILL.md", "scripts/sprint_eligible.py", ".opencode/plans/base-context.md", ".devops/templates/base-context.template.md"]
depends_on: []
triage: MULTI
type: backlog
---
# Backlog: T1-E5.01 - MICRO topology: the sanctioned small-change pathway

## Theme-Epic: T1 - Parcel Pipeline Machinery, E5 - Change-Scale Pathway & Records

**Discovered:** operator direction 2026-09-24 (the overhead-reduction pathway), field-calibrated by the GRID-Link audit: **~45% of 346 archived parcels ran `SINGLE` (155 recorded) and 35 explicit ad-hoc runs** — the light path exists in practice everywhere, sanctioned nowhere. See [`.devops/audits/grid-link-field-audit-2026-09-24.md`](../audits/grid-link-field-audit-2026-09-24.md) finding 6.

## Findings

| # | Gap | Evidence | Impact |
|---|---|---|---|
| 1 | There is no sanctioned path smaller than `SINGLE` — yet the template itself lands direct fixes with recorded skips (`f4bd88b`), and the satellite runs ad-hoc parcels. | AGENTS.md rule 7 ("multi-step tasks MUST use `@pass-the-parcel`"); changelog direct-fix entries; GRID-Link archive: 35 `ad-hoc` mentions. | The de facto practice is a drifting exception — the exact failure mode the 2026-09-16 lesson names ("a stated non-negotiable is reversed as a mode, never by drift"). |
| 2 | The demand is measured, not hypothetical: nearly half of all real parcels chose the lightest available topology. | GRID-Link archive: 155/346 `N/A — SINGLE` markers. | Without a MICRO tier, small changes either pay full ceremony or drift outside the record. |

## Operator rulings locked at intake (2026-09-24)

1. **`Agents: MICRO` is a third value on the existing topology axis** — not a new axis, not a lane outside the lifecycle. No new lifecycle states.
2. **The plan file stays the record** ("plans are our source of truth"): a micro plan is a collapsed parcel — phases 2-8 render as explicitly-marked `N/A — MICRO` sections carrying the eligibility line, so the archive shows exactly what was bypassed and why (formalizes the recorded-skip pattern the 2026-09-23 audit run used).
3. **Gate set:** A and C `N/A` (no scope questions or peer review by definition of eligibility); **Gate B carries the eligibility assertion + the micro plan; Gate D always halts.**
4. **`triage` gains `MICRO`** as a third recommendation value (backward compatible: absent tolerated; `sprint_eligible.py` currently fails unrecognized values open to the mechanical backstop — MICRO needs explicit handling).
5. **The batch preset is untouched** — MICRO is manual-path only; micro items are exactly the between-sprint changes.
6. **Wrap-up on a micro plan = patch bump only** (composes with T1-E2.08's tiered versioning).

## Eligibility (initial bounds — calibrate in Phase 3 from the audit's ad-hoc set)

≤ 3 files, no reserved surface, no prefix-embed cascade, reversible, no contract change — mirrors the Complexity Triage LOW bound; the **inverse of `sprint_eligible.py`'s existing `multi_worthy` computation** (reuse the signal set, do not fork a second dialect).

## What a fix would touch

`plan-lifecycle.md` (one canonical § Micro Lane), `pass-the-parcel` SKILL (topology table + per-topology flow), `template-plan.md` (the collapsed rendering / marker guidance), `sprint-plan` SKILL (§4c triage scoring gains MICRO), `scripts/sprint_eligible.py` (MICRO triage handling + advisory), the shared prefix + seed (serial lane — `-Sync` ×9).

## Open questions for Phase 3

- [ ] Does MICRO get its own plan-file variant (a micro-template) or marker guidance inside `template-plan.md` — one home vs a second template surface?
- [ ] Is the eligibility check a **script** (a pass over the diff vs the reserved set, the `touches`-predicate precedent) or a **wrap-up-gate assertion** (presence-based, the AUTO Gate Evidence Contract precedent)?
- [ ] What does Gate B's positive evidence look like for a micro plan (the Phase 6 self-review row precedent, adapted)?
- [ ] Should the 35 archived ad-hoc runs be sampled to tune the bounds before the predicate is frozen?

## Why NEXT

Operator priority, and the demand is field-proven. The plan itself is a reserved-surface parcel (prefix cascade) — it should run `MULTI` and eat its own dog food.
