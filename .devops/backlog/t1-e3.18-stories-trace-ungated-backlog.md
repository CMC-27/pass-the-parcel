---
code: T1-E3.18
sprint: none
claim_status: QUEUED
owner: ""
claimed_at: ""
last_touch: "2026-09-23"
touches: [".devops/skills/sprint-plan/SKILL.md", ".devops/skills/sprint-close/SKILL.md", ".devops/plans/template-plan.md"]
depends_on: []
triage: SINGLE
type: backlog
---
# Backlog: T1-E3.18 - The `stories:` trace has no gate behind it

## Theme-Epic: T1 - Parcel Pipeline Machinery, E3 - Concurrency & Sprint Lifecycle

**Discovered:** 2026-09-23, closing `T1-E3.16` (sprint 10 wave 3). **Not a defect in that parcel** — it delivered exactly what it specified (three prose surfaces, smallest form). This is the *next* link, surfaced by the parcel's own Phase 3 Q1 ruling.

## Findings

| # | Gap | Evidence | Impact |
|---|---|---|---|
| 1 | The loop's first link is honour-system. `@sprint-plan` v7 §6 step 3 drafts a `stories:` row per committed plan and says *"leave the row off only when the plan is pure machinery with no owner effect — a skipped story is a silent one, so record the reason in the plan body instead."* Nothing asserts the row exists, and nothing asserts the recorded reason is real. | `sprint-plan/SKILL.md:140`; `sprint-close/SKILL.md:92`; `template-plan.md:99-100`. | A plan can be committed with no story and no reason, and no gate notices — the same "declared vs loaded" gap `T1-E1.05` records for `opencode.json`. The consumer degrades gracefully (criteria fallback, Q5), so the failure is invisible rather than loud. |
| 2 | The criteria→story trace is prose instruction only. Phase 4 tells the author to check each criterion against the story; no surface can tell whether a criterion traces to anything. | `template-plan.md` Phase 4 "User Story (criteria trace to it)" block. | The trace is real only where the author complies. Worth measuring before gating: if committed plans reliably carry stories, a gate buys little. |

> **Accepted, not overlooked.** `T1-E3.16`'s Phase 3 Q1 deliberately declined the persona/agent cascade (11 files, `-Sync` ×9) for one guidance line the template scaffold already carries. The question here is different: whether the *contract row* needs a check, not whether the *personas* need edits.

## Why it is not urgent

No gate is red and no plan is blocked. The `stories:` row is additive and the consumer falls back to acceptance criteria, so a missing story costs trace quality, not correctness.

## What a fix would touch

`sprint-plan/SKILL.md` §6 (the commit-time step) and `sprint-close/SKILL.md` §5 (the reader) — either a soft check at commit time or a wrap-up-gate assertion that a committed plan carries `stories:` or a recorded skip reason. `template-plan.md` Phase 4 if the trace gains a verifiable shape. Per-skill version bumps.

## Open question for Phase 3

Gate it, or measure first? The cheapest first move is a read-only count over the sprint-10 queue: how many committed plans carry a `stories:` row versus a recorded skip reason. If compliance is already high, the honest answer may be *accept the honour system* — the same conclusion `T1-E2.02` reached on `check-parcel-prefix.ps1`.
