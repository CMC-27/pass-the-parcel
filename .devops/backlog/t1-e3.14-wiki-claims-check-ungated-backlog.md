---
code: T1-E3.14
sprint: none
claim_status: QUEUED
owner: ""
claimed_at: ""
last_touch: "2026-09-22"
touches: [".devops/skills/agent-wrap-up/SKILL.md", ".devops/plans/template-plan.md", ".devops/skills/pass-the-parcel/SKILL.md"]
depends_on: []
triage: SINGLE
type: backlog
---
# Backlog: T1-E3.14 - `wiki_claims.py check` is gated nowhere, so stale grounded claims pass as verified

## Theme-Epic: T1 - Parcel Pipeline Machinery, E3 - Concurrency & Sprint Lifecycle

**Discovered:** 2026-09-22, at the GRID-Link satellite, during the Sprint 13 wave-3 **batch wrap-up**. Found by the wiki-reconciliation step, which ran `check` on its own initiative - no gate does.

## Findings

| # | Gap | Evidence | Impact |
|---|-----|----------|--------|
| 1 | **The drift gate is invoked by nothing.** `wiki_claims.py` ships two subcommands; only `coverage` is gated. `@agent-wrap-up` Phase 7a - an explicit hard stop - gates `wiki_lint.py --quiet` + `wiki_claims.py coverage` and never `check`; `plans/template-plan.md`'s Phase 9 scaffold names no claims gate at all. | `agent-wrap-up/SKILL.md:159-163`; `plans/template-plan.md:236-247`; the template ships `scripts/wiki_claims.py` (both subcommands). | **`coverage` green + `check` red is a false "verified" signal:** the wiki is asserted to be complete while its grounded claims have drifted. Drift accumulates silently until someone runs `check` by hand. |
| 2 | **A live instance.** In the same batch, `T19-E2.04` changed `reviewBridge.js#applyReviewToFormData` - a claim-bound symbol. Neither runner ran `check` (both Phase 9 evidence tables list only lint / test / build / size), and Phase 7a would not have either. At wrap-up it was **red: 5 stale claims** across `01-vision-north-star`, `03-glossary-of-terms`, `05-core-architecture`, `06-directory-structure`, `08-user-journey`. | GRID-Link `T19-E2.04` Phase 9 table; `wiki_claims.py check` output at wrap-up; re-stamped via `wiki_claims.py update`. | All five propositions still **held**, so this was latent rather than wrong - but nothing in the pipeline distinguished "held" from "drifted". |

> Finding 2 is the instance; finding 1 is the class. The class is the fix.

## What the fix does

1. **Gate it at the hard stop.** Add `python scripts/wiki_claims.py check` to `@agent-wrap-up` Phase 7a alongside `coverage`, so both must exit 0.
2. **Name one home for Phase 9.** Either add `check` to the plan template's Phase 9 evidence scaffold, or state explicitly that claim re-stamping is a **wrap-up duty** - the cheaper single-writer option. One home, cited, never restated (`surface-budget.md`).
3. **Name the repair.** `wiki_claims.py update` is the re-stamp path and is currently discovered by hand. If wrap-up owns re-stamping, say so where wrap-up can act on it.

## What a fix touches

`.devops/skills/agent-wrap-up/SKILL.md` (Phase 7a hard-stop pair + the Hard Stop paragraph); `.devops/plans/template-plan.md` (Phase 9 scaffold, if that is the chosen home); `.devops/skills/pass-the-parcel/SKILL.md` (the Phase 9 gate owner, if the gate list lives there rather than in the plan template); per-skill `version` bumps.

## Open question for Phase 3

**Phase 9 or Phase 7a - one home, or two?** If Phase 9 runs it, a runner that changes a claimed symbol must re-stamp **before** its `plan:` commit (drift is caught at the earliest point, but every runner pays for it). If only Phase 7a runs it, the wrap-up owns all re-stamping (one writer, no runner cost - but a red gate then blocks the **whole** batch wrap-up, which is the blast radius `T1-E3.04` deliberately keeps small). Pick one and cite it.

## Reference

GRID-Link 2026-09-22, Sprint 13 wave 3: `.opencode/plans/run-sprint-13/sprint_run_report.md` (satellite-local); the plans' Completion Notes (`T19-E2.04`, `T19-E2.05`, archived) each record this gap under "Gate-set gap found at wrap-up".
