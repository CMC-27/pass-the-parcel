---
code: T3-E1.03
sprint: none
claim_status: QUEUED
owner: ""
claimed_at: ""
last_touch: "2026-09-24"
touches: [".github/workflows/validate.yml", ".devops/sync-manifest.yaml", ".devops/templates/SATELLITE-BOOTSTRAP.md", "README.md"]
depends_on: []
triage: SINGLE
type: backlog
---
# Backlog: T3-E1.03 - The deterministic CI gates stop at the template border

## Theme-Epic: T3 - Template Distribution & Onboarding, E1 - Repository Hygiene & Presentation

**Discovered:** 2026-09-24 GRID-Link field audit, finding 7. See [`.devops/audits/grid-link-field-audit-2026-09-24.md`](../audits/grid-link-field-audit-2026-09-24.md).

## Findings

| # | Gap | Evidence | Impact |
|---|---|---|---|
| 1 | `validate.yml` is not in `portable_files`, and satellite CI is app-only. | GRID-Link `.github/workflows/ci.yml`: npm lint/test/build, bundle budget, functions tests, deploy — **none** of the machinery gates (prefix, UTF-8, wiki lint, claims). | In the field, the "deterministic guardrails" instrument is a **local convention** again (invoked at wrap-up / test-and-deploy, not enforced per push) — the exact failure the instrument exists to prevent. |
| 2 | README's product-page claim is template-side only. | `README.md` "What you get": *"🔒 Deterministic CI gates — prefix integrity, UTF-8, wiki lint, coverage, claims drift, and a transport self-test on every push."* | A satellite adopter reads a guarantee the transport does not deliver. |
| 3 | The satellite's local enforcement is weaker than the template's, and lagging. | GRID-Link's `agent-wrap-up` is v16 — one behind the v17 3-gate claims hard stop (it hasn't pulled since ~machinery 63-66). | Gate enforcement quality silently differs per satellite. |

## Design fork (Phase 3 decides — smallest surface that a satellite can adopt without restructuring its own CI)

1. **Reusable workflow** (`workflow_call`) shipped in the template repo and referenced by satellite CI — machine-enforced, but couples satellites to the template repo's CI stability.
2. **Composite action** — self-contained, but implies a second repo or a branch convention.
3. **Documented wiring block** in `SATELLITE-BOOTSTRAP.md` (the step the satellite pastes into its own CI) — zero transport machinery, zero enforcement (the status quo's honesty fix).
4. **Drift-job precedent**: `wiki-refresh.yml` is already transportable — does it already point the way?

## What a fix would touch

`.github/workflows/` (the reusable surface, whichever fork wins), `.devops/sync-manifest.yaml` (`portable_files` — note `.github/workflows` is currently outside the walk by construction in the surface-budget report too), `SATELLITE-BOOTSTRAP.md` (the wiring step), `README.md` (scope the claim honestly or make it true).

## Open questions for Phase 3

- [ ] Which surface wins — enforcement (reusable workflow) vs portability (documented block) vs both (workflow + bootstrap wires it by default)?
- [ ] Does the machinery-gate job need a satellite-mode (skip `machinery-version` discipline and the self-test, which are template-identity checks)?
- [ ] Is `validate.yml`'s own gate list split into "invariant everywhere" (prefix, UTF-8, lint, claims) vs "template-identity" (self-test, version discipline)?

## Why NEXT

Truth correction on the product page plus the field integrity of the template's third supporting instrument. Small-to-medium effort with an existing precedent (`wiki-refresh.yml`).
