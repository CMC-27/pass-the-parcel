---
code: T1-E3.20
sprint: none
claim_status: QUEUED
owner: ""
claimed_at: ""
last_touch: "2026-09-24"
touches: [".devops/skills/sprint-run/SKILL.md", "scripts/sprint_eligible.py", "scripts/tests/test_sprint_eligible.py"]
depends_on: []
triage: SINGLE
type: backlog
---
# Backlog: T1-E3.20 - The batch preset silently overrules a sprint's recorded Mode rulings

## Theme-Epic: T1 - Parcel Pipeline Machinery, E3 - Concurrency & Sprint Lifecycle

**Discovered:** the 2026-09-13 process lesson (`@sprint-run` batch preset silently overrules a sprint's recorded plan settings); promoted to a parcel by the 2026-09-24 GRID-Link field audit — no surface re-checks it, and the audit confirms the batch path is the satellite's primary execution mode (Sprint 14 in flight).

## Findings

| # | Gap | Evidence | Impact |
|---|---|---|---|
| 1 | The locked `AUTO`+`SINGLE` preset overwrites each plan's frozen `## ⚙️ Plan Settings` at claim time, so a sprint that ruled a bundle `USER-MANAGED` gets those gates auto-cleared regardless. | `process-lessons.md` [2026-09-13]; `plan-lifecycle.md` § Deviations (the preset is a named deviation, not a check). | The operator's explicitly recorded gate decision is silently overruled — the exact class of bypass the four-gate architecture exists to make impossible. |
| 2 | The MULTI-worthy fork catches **topology** (`triage: MULTI` or >3 `touches`) but not **Mode** — a plan recorded `USER-MANAGED` with `triage: SINGLE` passes the fork unflagged. | `sprint_eligible.py` `complexity` block (signals: blast-radius only); `sprint-run` § 1 step 7. | Half the overrule risk is invisible to the one operator checkpoint that exists. |

## What a fix would touch

`scripts/sprint_eligible.py` (an advisory `mode_conflicts` key per queued plan — a plan whose authored Plan Settings `Mode` disagrees with the batch preset; the `parallel_groups`/`complexity` advisory precedent, never a predicate clause), `scripts/tests/test_sprint_eligible.py` (fixtures), `sprint-run` SKILL § 1 (the preview presents mode conflicts in the **same** operator answer as the MULTI-worthy fork — one pause, both signals).

## Open questions for Phase 3

- [ ] One consolidated fork (topology + mode in a single accept/defer answer) or a separate pause per signal class?
- [ ] Should the check read the plan's frozen `Plan Settings` block (authored intent) or only the `triage` front-matter (commit-time recommendation)? The lesson says the former is what gets overruled.

## Why LATER

The lesson is recorded and the operator is aware; the batch path is used deliberately. Small effort (S), no current breakage — but it closes a standing exception on the template's core promise (hard gates, not vibes).
