---
code: T1-E3.15
sprint: sprint-10-owner-loop
claim_status: QUEUED
owner: ""
claimed_at: ""
last_touch: "2026-09-23"
touches: [".devops/skills/sprint-close/SKILL.md", ".devops/skills/q-and-a/SKILL.md", ".devops/skills/true-or-false/SKILL.md"]
depends_on: ["T1-E3.12"]
triage: SINGLE
type: backlog
---
# Backlog: T1-E3.15 - Sprint-close user manual testing, surfaced and walked one-by-one

## Theme-Epic: T1 - Parcel Pipeline Machinery, E3 - Concurrency & Sprint Lifecycle

**Discovered:** 2026-09-23, operator direction: "prior to the business report, surface user manual testing, carried out like our q-and-a / true-or-false skills — we surface the tests to be performed, and respond one by one with the ask questions tool."

## Findings

| # | Gap | Evidence | Impact |
|---|-----|----------|--------|
| 1 | **No structured user-acceptance step exists in the close cycle.** Per-plan Gate D sign-off is informal chat ("try it out"), and batch wrap-up presents a consolidated Gate D report but runs no interrogation of the operator's actual manual test results. | `agent-wrap-up/SKILL.md` batch wrap-up flow; `sprint-close/SKILL.md` steps 1–4 (retro → kill-list scan → capacity → report/archiving). | User testing happens (or doesn't) without a record; the business report then narrates a sprint nobody formally accepted. |

## What the fix does (operator-authored intent)

New step in `@sprint-close`, **before** the business-report step (E3.12) so the report can credit passed testing:

1. **Surface the test queue.** Collect candidate manual tests from each executed plan's acceptance criteria + Phase 10 tweak notes (the same sources Gate D sign-off reads), consolidated into one sprint-wide testing menu grouped by outcome/theme, not parcel code.
2. **Walk the tests one-by-one via the ask-questions tool.** Each test presented with what to do, what to expect; the operator reports pass/fail/blocked per question — the exact interaction shape in `@q-and-a` / `@true-or-false` (one call per question where the ask surface is single-question, one batched call where it accepts an array; the final confirse validation question always on its own per the T1-E3.08 pattern).
3. **Record the verdicts.** Outcomes land in the sprint folder (e.g. `user-testing.md`) alongside `sprint.md` so Step 6's archive moves it, and failed tests add actionable rows to the backlog or business report rather than evaporating.

## What a fix touches

`.devops/skills/sprint-close/SKILL.md` (new step + renumber + register/archive linkage), `.devops/templates/sprint.template.md` (`user-testing.md` row), `HOW-TO.md` (one mention). Cites — never restates — the ask-tool questionnaire mechanics from `@q-and-a` (`surface-budget.md`).

## Open question for Phase 3

Per-plan Gate D already halts for the user; does a sprint-level test walk **duplicate** Gate D, or does it answer a different question (acceptance vs verification)? If duplicate, this may shrink to "record + carry the Gate D verdicts into the report."

## Reference

Operator direction 2026-09-23 (this conversation). Pattern precedents: `@q-and-a`, `@true-or-false`, T1-E3.08 (batched clarification questionnaire).
