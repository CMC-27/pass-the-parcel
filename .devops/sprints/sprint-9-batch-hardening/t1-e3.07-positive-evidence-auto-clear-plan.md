---
code: T1-E3.07
sprint: sprint-9-batch-hardening
claim_status: QUEUED
owner: ""
claimed_at: ""
last_touch: "2026-09-16"
touches: [".devops/skills/pass-the-parcel/SKILL.md", ".devops/skills/ptp-parcel-fast/SKILL.md", ".devops/skills/sprint-run/SKILL.md", ".devops/plans/template-plan.md", ".opencode/plans/base-context.md", ".devops/templates/base-context.template.md", ".devops/sync-manifest.yaml", ".devops/logs/version-history.md"]
depends_on: []
---
# T1-E3.07 - Positive-evidence auto-clear for AUTO gates

> **Origin:** AUTO gates clear on the *absence* of a rejection — an empty or vague self-review passes by omission. This parcel replaces that negative test with a positive-evidence contract and makes an unproven gate a stop-the-line. Wave 1 of the batch-hardening sequence.

## ⚙️ Plan Settings (FROZEN — set at plan start, read before any phase)

| Setting | Value | Meaning |
|---|---|---|
| **Mode** | `USER-MANAGED` | Every gate halts for the user; `AUTO` auto-clears Gates A-C on mechanical verification — Gate D always halts. |
| **Agents** | `MULTI` | Comprehensive plan; chosen at plan start by the complexity triage. |

> **Frozen config — read before executing ANY phase.** Mutable runtime state lives ONLY in the bottom `## 📍 State & Gates` section.

---

## 1 Phase 1: Expansion & Scoping
**Skill Executed:** `ptp-context-hunter`
**Intent:**
Make every AUTO gate-clear require positive, presence-based evidence instead of the absence of a rejection, so a vague or empty self-review can no longer pass by omission.

**In Scope:**
- The AUTO-clear test for Gate A, Gate B, and the inline Phase 6 self-review.
- One written **evidence contract** per gate, stated once and cited elsewhere (no restatement).
- Template evidence shapes: Phase 4 acceptance criteria, the Phase 6 self-review row-per-criterion, the Phase 9 command + exit-code table.
- `sprint-run` § 5: an unproven gate-critical section becomes a stop-the-line cause, not just a `REJECTED` verdict.
- The `base-context.md` line describing AUTO ("after mechanical verification") if its wording must change — with the prefix `-Sync` cascade that implies.

**Out of Scope:**
- The meaning of the gates, or the human Gate D.
- The claim-eligibility predicate (that is T1-E3.09).
- The `MULTI`/`SINGLE` gate set, or adding an independent reviewer to `SINGLE`.

**Perimeter Notes:**
- The `SINGLE` self-review is written by the same agent that authored the plan. This parcel cannot make it *independent* — only make its *absence* detectable.
- The evidence test must be mechanical (presence + reference), never a quality judgement, or it will produce false halts.

---

## 2 Phase 2: Requirements & Context
**Skill Executed:** `ptp-context-hunter`
**Forensic Context Inventory:**
> [ ] Wiki core docs read (`.wiki/core/00-system-index.md` + relevant docs)
> [ ] Knowledge capture read (`.wiki/core/18-knowledge-capture.md`)
> [ ] Source verified (files below)

**Current implementation (verified 2026-09-16):**
- `pass-the-parcel/SKILL.md` § Review Gates (AUTO clause): "auto-clears Gates A-C after mechanical verification (outputs present, no `REJECTED` verdict line, no `Unresolvable:` entries)."
- `ptp-parcel-fast/SKILL.md` § Auto-clear test mirrors it verbatim: "outputs exist **and** contain no `REJECTED` verdict line and no `Unresolvable:` entry".
- `sprint-run/SKILL.md` § 5 stop-the-line lists only a Phase 6 self-review `**REJECTED:**` as a gate halting cause.
- `.opencode/plans/base-context.md:43`: "`AUTO` (orchestrator auto-clears Gates A-C after mechanical verification; Gate D always requires the human)."
- `template-plan.md` Phases 6/7 carry the `N/A — SINGLE self-review` note; Phase 9 has a checkbox list but no command + exit-code evidence table.

**Architectural considerations:**
- The pass condition is a **negative** ("not rejected"), so an empty or placeholder block passes. Under `SINGLE` + batch, Gate C is `N/A` and no adversarial reviewer runs — the self-review is the only check before Gate D.
- `@sprint-run` batches Gate D into one human verdict, so weak A/B evidence propagates to a single end-of-batch decision.
- Changing the `base-context` prefix line drags the `-Sync` cascade (9 locked agents) plus a `machinery-version` bump.

**Relevant existing decisions:**
- `T1-E3.06` records the Sprint 8 coverage gaps (G1/G2/G3) but **not** this one — the negative auto-clear test is currently unflagged.

---

## 3 Phase 3: User Clarification
**Skill Executed:** `ptp-context-hunter`
> [ ] Q1: Is an unproven gate a batch **HALT** (stop-the-line, consistent with `REJECTED`) or a per-plan **carry-forward skip**? *Recommended: halt.*
> [ ] Q2: Does the contract cover Gate A, Gate B, **and** the inline self-review — or only the self-review (the weakest link)? *Recommended: all three, stated once.*
> [ ] Q3: Where is the evidence contract canonical — `pass-the-parcel` § Review Gates (proposed) or `plan-lifecycle.md`?
> [ ] Q4: Does the `base-context.md` AUTO line need rewording, dragging the `-Sync` cascade + version bump? *Recommended: yes.*
> [ ] Q5: What counts as "evidence present" while staying mechanical? Proposed: (a) no unresolved placeholders (`[ ]`, `<…>`, `TBD`, `TODO`) in gate-critical sections; (b) Gate A — every Phase 3 question has an answer line and the final validation is recorded; (c) Gate B — ≥1 acceptance-criterion row with a test target, **or** a recorded "no wiki delta — rationale"; (d) self-review — ≥1 row citing an acceptance criterion.

**Architectural Conflict Warnings Raised:**
- None known yet. Flag if the evidence test cannot be kept free of quality judgement.

---

> **HALT POINT (Gate A — Scope):** Phases 1-3 complete. Present the scope perimeter + Phase 3 Q&A record; user approves the scope before planning begins.

---

## 4 Phase 4: Wiki Requirements & Acceptance Criteria
**Skill Executed:** `ptp-high-visionary` + `@wiki-writer`
> **Parked — authored at execution time.** Spec-first: wiki docs written `status: in-progress` before code; acceptance criteria table with test targets.

---

## 5 Phase 5: High-Visionary Standard Implementation Plan
**Skill Executed:** `ptp-high-visionary`
> **Parked — authored at execution time.** No code snippets except exact string literals.

---

## 6 Phase 6: Grumpy Architect Spec & Logic Audit
**Skill Executed:** `ptp-grumpy-architect` (`SINGLE`: orchestrator inline)
> **Parked — run at execution time.**

---

## 7 Phase 7: Smooth Operator Product Review
**Skill Executed:** `ptp-smooth-operator` (`SINGLE`: orchestrator inline)
> **Parked — run at execution time.**

---

## 8 Phase 8: Execute Changes
**Skill Executed:** `ptp-code-surgeon`
> **Parked — executed at execution time.**

---

## 9 Phase 9: Verify Changes
**Skill Executed:** `ptp-code-surgeon`
> **Parked — verified at execution time.**

---

## 10 Phase 10: User Review & Tweaks
**Skill Executed:** `knowledge-capture`
> **Parked — captured at execution time.**

---

## Completion Note (Wrap Up)
**Skill Executed:** `agent-wrap-up`
> **Parked — completed at wrap-up.**

---

## 📍 State & Gates (CACHE-ANCHORED — update ONLY this section at gate transitions)

| Metric | Value |
|---|---|
| **Status** | `QUEUED` |
| **Version** | `v0.1.0` |
| **Active Persona** | `Planner` |
| **Depends On** | none (canonical: claim front-matter `depends_on`) |
| **Blocks** | none |

> **Settings pointer:** `Mode` and `Agents` live in the frozen **Plan Settings** block at the TOP of this file.

| Gate | Requirement | Status |
|---|---|---|
| A | Scope approved (Phases 1-3, + 3.5 in AUTO) | `OPEN` |
| B | Spec & plan approved (Phases 4-5) | `OPEN` |
| C | Peer reviews passed (Phases 6-7) | `OPEN` |
| D | Implementation verified (Phases 8-9) | `OPEN` |

> Rejection routes: A → `PHASE_1`; B/C → `PHASE_5_REVISION`; D/rollback → `PHASE_8_FAILED`. Parked items carry `Status: QUEUED` until `@sprint-plan` commits them.
