---
code: T1-E3.11
sprint: sprint-9-batch-hardening
claim_status: QUEUED
owner: ""
claimed_at: ""
last_touch: "2026-09-16"
touches: [".devops/skills/sprint-run/SKILL.md", ".devops/skills/ptp-parcel-fast/SKILL.md", ".devops/agents/parcel-sprint.agent.md", ".devops/skills/agent-wrap-up/SKILL.md", ".devops/skills/pass-the-parcel/SKILL.md", ".devops/rules/plan-lifecycle.md", ".opencode/plans/base-context.md", ".devops/templates/base-context.template.md", ".devops/templates/SPRINTS.template.md", "HOW-TO.md", ".devops/sync-manifest.yaml", ".devops/logs/version-history.md"]
depends_on: ["T1-E3.09", "T1-E3.10"]
---
# T1-E3.11 - Two-lane concurrent batch + version-bump ownership

> **Origin:** `@sprint-run` is trunk-sequential: one plan at a time, no worktree. This parcel adds two-lane concurrency (parallel worktree runs for disjoint plans, serial for shared-machinery surfaces) and moves the `machinery-version` bump + prefix `-Sync` to the host/wrap-up so machinery plans stop colliding on shared counters. Wave 5 of the batch-hardening sequence.

## ⚙️ Plan Settings (FROZEN — set at plan start, read before any phase)

| Setting | Value | Meaning |
|---|---|---|
| **Mode** | `USER-MANAGED` | Every gate halts for the user; `AUTO` auto-clears Gates A-C on mechanical verification — Gate D always halts. |
| **Agents** | `MULTI` | Comprehensive plan; chosen at plan start by the complexity triage. |

> **Frozen config — read before executing ANY phase.** Mutable runtime state lives ONLY in the bottom `## 📍 State & Gates` section.

---

## 1 Phase 1: Expansion & Scoping
**Skill Executed:** `ptp-context-hunter`
**Intent:**
Run disjoint plans concurrently in isolated worktrees while serialising shared-machinery surfaces in a serial lane — and move the `machinery-version` bump + prefix `-Sync` to the host/wrap-up so machinery plans stop colliding on shared counters.

**In Scope:**
- Reserved-surface classification: lane A (serial) when `touches` intersects the shared/global set; lane B (parallel) otherwise.
- Parallel groups computed from T1-E3.09's group output; worktree + `plan/<code>-<slug>` branch per lane-B plan; concurrent spawn; **serialised merge** back to trunk in queue order.
- Version-bump ownership: `machinery-version` + its `version-history.md` literal + the prefix `-Sync` move out of individual plans into the host/batch wrap-up (one bump per batch).
- Segment-wise glob overlap (upgrades the prefix-or-equal heuristic that becomes a correctness risk under concurrency).
- `plan-lifecycle.md` § Deviations: `trunk-sequential` replaced by `group-parallel`.

**Out of Scope:**
- A full independent-set scheduler beyond two lanes.
- Relaxing the `touches` predicate or the batched Gate D.
- Concurrency as a cost lever — it is wall-clock only and may cost slightly more in re-verification.

**Perimeter Notes:**
- In **this** template nearly every authoritative plan touches machinery, so lane B is thin here; the payoff is in satellites with a disjoint `src/**`. State that honestly rather than selling speed the template won't see.
- `touches` fidelity stops being scheduling-only and becomes correctness: a mis-declared write set becomes a merge conflict or a lost edit.
- Shared surfaces (`sync-manifest.yaml`, `version-history.md`, `agent-changelog.md`, `sprint.md`, `backlog-index.md`, `.devops/agents/**` + `base-context.md`) remain trunk-only and host-owned.

---

## 2 Phase 2: Requirements & Context
**Skill Executed:** `ptp-context-hunter`
**Forensic Context Inventory:**
> [ ] Wiki core docs read (`.wiki/core/00-system-index.md` + relevant docs)
> [ ] Knowledge capture read (`.wiki/core/18-knowledge-capture.md`)
> [ ] Source verified (files below)

**Current implementation (verified 2026-09-16):**
- `plan-lifecycle.md` § Deviations #3: **trunk-sequential** — drops `git worktree add`, keeps `git mv` + `claim: <code>`; no branch, no merge, no prune. § Claim Protocol steps 3–5 keep the worktree + branch + merge/prune for the normal path the batch dropped.
- Version discipline: `sync-manifest.yaml` currently `machinery-version: 47`; `validate.yml` requires that value as a literal row in `.devops/logs/version-history.md`.
- Sprint 8 retro documents the version collision across **serial** waves (each plan bumps a stale base); under concurrency it is fatal.
- `touches` overlap is prefix-or-equal; a mid-path wildcard (e.g. `src/*/db`) is **not** detected — a documented `ponytail:` ceiling with a named upgrade path (segment-wise glob intersection).
- `check-parcel-prefix.ps1 -Sync` rewrites **9** agent files atomically.

**Architectural considerations:**
- Shared/global surfaces (`sync-manifest.yaml`, `version-history.md`, `agent-changelog.md`, `sprint.md`, `backlog-index.md`, `.devops/agents/**` + `base-context.md`) must stay trunk-only and host-owned — the version counter is the hard blocker.
- Concurrency raises `touches` fidelity from **scheduling** to **correctness**: a miss becomes a merge conflict or a lost edit.
- In this template, plans are near-total-overlap on machinery, so lane B is thin **here**; the payoff is satellites with disjoint `src/**`. Say so rather than selling speed the template won't see.
- Failure policy improves for free: a HALTed lane-B plan stays on its branch while the others merge — unlike today, where one halt stops the whole line.
- Parallelism reduces wall-clock, not tokens; expect equal or slightly higher cost from post-merge re-verification.

---

## 3 Phase 3: User Clarification
**Skill Executed:** `ptp-context-hunter`
> [ ] Q1: Define the exact **reserved-surface set** (the globs that force lane A) — must include the version counter surfaces and the prefix.
> [ ] Q2: Where does the version bump move — **batch wrap-up** (recommended; `version-history.md` rows are already written there) vs host-at-claim? Does a mid-batch push become a CI-failing window, or does the batch never push until wrap-up?
> [ ] Q3: Does the host own the prefix `-Sync` entirely (one regenerate per batch), and what happens if a lane-A plan edits `base-context.md` mid-batch?
> [ ] Q4: Failure policy for a lane-B member that HALTs — merge survivors and leave the failed branch (recommended), or abort the group?
> [ ] Q5: Merge order and re-verification — merge in queue order, re-run repo gates once per group or once per merge?
> [ ] Q6: Group size cap (concurrent-spawn resource limit) and whether lanes truly run simultaneously or groups are serialised.
> [ ] Q7: Does concurrency change the consolidated Gate D report shape?
> [ ] Q8: Interaction with `T1-E3.10`'s yield — a deferred plan is removed **before** groups are computed, so the two are orthogonal; confirm.

**Architectural Conflict Warnings Raised:**
- The version counter is **global and monotonic**; without moving its ownership, concurrent machinery plans collide and fail the CI predicate. This is the prerequisite, not a nice-to-have.
- `touches` mis-declaration becomes a merge conflict under concurrency — the segment-wise glob upgrade is **required**, not optional.

---

> **HALT POINT (Gate A — Scope):** Phases 1-3 complete. Present the scope perimeter + Phase 3 Q&A record; user approves the scope before planning begins.

---

## 4 Phase 4: Wiki Requirements & Acceptance Criteria
**Skill Executed:** `ptp-high-visionary` + `@wiki-writer`
> **Parked — authored at execution time.** Spec-first: wiki docs written `status: in-progress` before code; acceptance criteria table with test targets.

---

## 5 Phase 5: High-Visionary Standard Implementation Plan
**Skill Executed:** `ptp-high-visionary`
> **Parked — authored at execution time.** No code snippets except exact string literals.

---

## 6 Phase 6: Grumpy Architect Spec & Logic Audit
**Skill Executed:** `ptp-grumpy-architect` (`SINGLE`: orchestrator inline)
> **Parked — run at execution time.**

---

## 7 Phase 7: Smooth Operator Product Review
**Skill Executed:** `ptp-smooth-operator` (`SINGLE`: orchestrator inline)
> **Parked — run at execution time.**

---

## 8 Phase 8: Execute Changes
**Skill Executed:** `ptp-code-surgeon`
> **Parked — executed at execution time.**

---

## 9 Phase 9: Verify Changes
**Skill Executed:** `ptp-code-surgeon`
> **Parked — verified at execution time.**

---

## 10 Phase 10: User Review & Tweaks
**Skill Executed:** `knowledge-capture`
> **Parked — captured at execution time.**

---

## Completion Note (Wrap Up)
**Skill Executed:** `agent-wrap-up`
> **Parked — completed at wrap-up.**

---

## 📍 State & Gates (CACHE-ANCHORED — update ONLY this section at gate transitions)

| Metric | Value |
|---|---|
| **Status** | `QUEUED` |
| **Version** | `v0.1.0` |
| **Active Persona** | `Planner` |
| **Depends On** | `T1-E3.09`, `T1-E3.10` |
| **Blocks** | none |

> **Settings pointer:** `Mode` and `Agents` live in the frozen **Plan Settings** block at the TOP of this file.

| Gate | Requirement | Status |
|---|---|---|
| A | Scope approved (Phases 1-3, + 3.5 in AUTO) | `OPEN` |
| B | Spec & plan approved (Phases 4-5) | `OPEN` |
| C | Peer reviews passed (Phases 6-7) | `OPEN` |
| D | Implementation verified (Phases 8-9) | `OPEN` |

> Rejection routes: A → `PHASE_1`; B/C → `PHASE_5_REVISION`; D/rollback → `PHASE_8_FAILED`. Parked items carry `Status: QUEUED` until `@sprint-plan` commits them.
