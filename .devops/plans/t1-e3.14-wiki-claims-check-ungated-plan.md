---
code: T1-E3.14
sprint: sprint-10-owner-loop
claim_status: GATE_D_USER_APPROVAL
owner: parcel-sprint
claimed_at: "2026-09-23"
last_touch: "2026-09-23"
touches: [".devops/skills/agent-wrap-up/SKILL.md", ".devops/plans/template-plan.md", ".devops/skills/pass-the-parcel/SKILL.md"]
depends_on: []
triage: SINGLE
type: backlog
---
# Parcel Plan: T1-E3.14 - `wiki_claims.py check` is gated nowhere, so stale grounded claims pass as verified

## ⚙️ Plan Settings (FROZEN — set at plan start, read before any phase)

| Setting | Value | Meaning |
|---|---|---|
| **Mode** | `AUTO` | Locked batch preset (`parcel-sprint` → `ptp-parcel-fast`): orchestrator auto-clears Gates A-C only on positive evidence; Gate D always halts for the human, deferred to the batch wrap-up |
| **Agents** | `SINGLE` | Locked batch preset: inline personas, no subagent spawns, Group C skipped, Gate C `N/A` |
| **Models** | `CLI default` | Handed down by the batch host from the operator's batch answer; inherited, never chosen here |

> **Frozen config — read before executing ANY phase.** Written once by the per-plan runner at claim time; never edited after. Mutable runtime state lives ONLY in the bottom `## 📍 State & Gates` section.

## Theme-Epic: T1 - Parcel Pipeline Machinery, E3 - Concurrency & Sprint Lifecycle

**Discovered:** 2026-09-22, at the GRID-Link satellite, during the Sprint 13 wave-3 **batch wrap-up**. Found by the wiki-reconciliation step, which ran `check` on its own initiative - no gate does.

## Findings

| # | Gap | Evidence | Impact |
|---|-----|----------|--------|
| 1 | **The drift gate is invoked by nothing.** `wiki_claims.py` ships two subcommands; only `coverage` is gated. `@agent-wrap-up` Phase 7a - an explicit hard stop - gates `wiki_lint.py --quiet` + `wiki_claims.py coverage` and never `check`; `plans/template-plan.md`'s Phase 9 scaffold names no claims gate at all. | `agent-wrap-up/SKILL.md:159-163`; `plans/template-plan.md:236-247`; the template ships `scripts/wiki_claims.py` (both subcommands). | **`coverage` green + `check` red is a false "verified" signal:** the wiki is asserted to be complete while its grounded claims have drifted. Drift accumulates silently until someone runs `check` by hand. |
| 2 | **A live instance.** In the same batch, `T19-E2.04` changed `reviewBridge.js#applyReviewToFormData` - a claim-bound symbol. Neither runner ran `check` (both Phase 9 evidence tables list only lint / test / build / size), and Phase 7a would not have either. At wrap-up it was **red: 5 stale claims** across `01-vision-north-star`, `03-glossary-of-terms`, `05-core-architecture`, `06-directory-structure`, `08-user-journey`. | GRID-Link `T19-E2.04` Phase 9 table; `wiki_claims.py check` output at wrap-up; re-stamped via `wiki_claims.py update`. | All five propositions still **held**, so this was latent rather than wrong - but nothing in the pipeline distinguished "held" from "drifted". |

> Finding 2 is the instance; finding 1 is the class. The class is the fix.

## What the fix does

1. **Gate it at the hard stop.** Add `python scripts/wiki_claims.py check` to `@agent-wrap-up` Phase 7a alongside `coverage`, so both must exit 0.
2. **Name one home for Phase 9.** Either add `check` to the plan template's Phase 9 evidence scaffold, or state explicitly that claim re-stamping is a **wrap-up duty** - the cheaper single-writer option. One home, cited, never restated (`surface-budget.md`).
3. **Name the repair.** `wiki_claims.py update` is the re-stamp path and is currently discovered by hand. If wrap-up owns re-stamping, say so where wrap-up can act on it.

## What a fix touches

`.devops/skills/agent-wrap-up/SKILL.md` (Phase 7a hard-stop pair + the Hard Stop paragraph); `.devops/plans/template-plan.md` (Phase 9 scaffold, if that is the chosen home); `.devops/skills/pass-the-parcel/SKILL.md` (the Phase 9 gate owner, if the gate list lives there rather than in the plan template); per-skill `version` bumps.

## Open question for Phase 3

**Phase 9 or Phase 7a - one home, or two?** If Phase 9 runs it, a runner that changes a claimed symbol must re-stamp **before** its `plan:` commit (drift is caught at the earliest point, but every runner pays for it). If only Phase 7a runs it, the wrap-up owns all re-stamping (one writer, no runner cost - but a red gate then blocks the **whole** batch wrap-up, which is the blast radius `T1-E3.04` deliberately keeps small). Pick one and cite it.

## Reference

GRID-Link 2026-09-22, Sprint 13 wave 3: `.opencode/plans/run-sprint-13/sprint_run_report.md` (satellite-local); the plans' Completion Notes (`T19-E2.04`, `T19-E2.05`, archived) each record this gap under "Gate-set gap found at wrap-up".

---

## 1 Phase 1: Expansion & Scoping
**Skill Executed:** `ptp-context-hunter` (inline — `SINGLE`/`AUTO`)

**Intent:** Make the grounded-claims drift check (`wiki_claims.py check`) a gated invariant instead of a hand-discovered one, and name exactly one home for it plus one owner for re-stamping.

**In Scope:**
- `@agent-wrap-up` Phase 7a: add `python scripts/wiki_claims.py check` as a third hard-stop gate alongside `wiki_lint.py --quiet` and `wiki_claims.py coverage`.
- One-home decision for the plan's open question (run at Phase 9 vs wrap-up duty) — resolved in Phase 3 (Q1).
- Naming the repair path (`wiki_claims.py update`) where wrap-up can act on it.
- Skill `version:` bumps on the touched skills.

**Out of Scope:**
- Any change to `scripts/wiki_claims.py` itself (subcommands exist; the gap is invocation).
- Wiring `check` into `.github/workflows/validate.yml` — CI already runs repo tests; the wrap-up hard stop is the named home.
- A second wrap-up skill or a restatement of the gate list outside the one home.
- `machinery-version` bump (batch wrap-up owns it; the live base is recorded in Phase 9).

**Perimeter Notes:** Blast radius ≤ 3 files, one domain (machinery doc/skill). No app source tree in this template repo — verification is script checks + doc consistency. Locked batch preset `AUTO` + `SINGLE`; Phase 3 auto-resolved.

---

## 2 Phase 2: Requirements & Context
**Skill Executed:** `ptp-context-hunter` (inline — `SINGLE`/`AUTO`)

**Forensic Context Inventory:**
> [x] Wiki core docs read — `.wiki/` is untouched by this plan (machinery-only change; Phase 4 records the no-wiki-delta rationale)
> [x] Knowledge capture read — `.devops/logs/agent-changelog.md` last 3 entries: batch 1 (E3.12/E3.13) landed and archived, machinery-version 68→69, repo gates green incl. `wiki_claims.py check` 0 stale
> [x] Source code verified — `scripts/wiki_claims.py` run live (both subcommands OK, exit 0); `@agent-wrap-up/SKILL.md` Phase 7a + Hard Stop + § Batch Scope gate list; `.devops/plans/template-plan.md` Phase 9 scaffold; `.devops/skills/pass-the-parcel/SKILL.md` § GROUP D

**Relevant Existing Decisions (from knowledge capture):**
- `T1-E3.04` keeps the batch wrap-up's blast radius deliberately small — a red gate blocks the whole batch; each gate added to it must earn its place (accepted below: `check` is <1s and closes the false-verified signal).
- Batch 1 already touched `template-plan.md` (E3.13) — archived, so this plan's wave-2 overlap is clear.

**Relevant Docs Found:**
- `.devops/skills/agent-wrap-up/SKILL.md` § Phase 7a (the two-gate hard stop, lines 159-165) + § Hard Stop (line 175-176) + § Batch Scope repo-gate list (line 67)
- `.devops/plans/template-plan.md` § 9 Phase 9 — Build & Lint / Test Report checkboxes + evidence table
- `.devops/skills/pass-the-parcel/SKILL.md` § GROUP D Phase 9 ("run compilation/tests/lint")
- `.devops/rules/surface-budget.md` — the one-home rule the plan body cites

**Relevant Code Found:**
- `scripts/wiki_claims.py` — `check` (drift gate), `update` (re-stamp path), `coverage` (currently the only gated one); live run: `check` → `claims OK: 0 stale` exit 0
- CI nuance (honesty note): `.github/workflows/validate.yml:39` runs `wiki_claims.py check` **post-push**, and `wiki-refresh.yml` runs it weekly — so the gap is the **pipeline's own gates** (wrap-up hard stop + batch wrap-up repo gates), which never invoke it; agents get no in-run signal and a batch wrap-up declares success on coverage alone. The fix (gate at Phase 7a) is unchanged; the finding's "invoked by nothing" reads as "invoked by no agent-facing gate".

---

## 3 Phase 3: User Clarification
**Skill Executed:** `ptp-context-hunter` + `ptp-phase3-answerer` (inline — `AUTO`)

> [x] Q1: Phase 9 or Phase 7a — one home, or two? (the plan's open question)
> [x] Q2: Where is the repair path (`wiki_claims.py update`) named?
> [x] Q3: Does the plan template's Phase 9 scaffold change?
> [x] Q4: Does `@pass-the-parcel` (the Phase 9 gate-owner prose) change?
> [x] Q5: Does the batch-scope repo-gate list (§ Batch Scope) change too?
> [x] Final validation: "Is this all the context required?" -> Yes

**Architectural Conflict Warnings Raised:**
- Q1 tension (recorded, resolved by Q1's answer): gating at Phase 9 catches drift earliest but costs every runner; gating only at wrap-up has one writer but a red gate blocks the whole batch. Resolution: **one home — Phase 7a** — with the runner's duty staying what it already is (record honest Phase 9 evidence; if it re-stamped mid-run, the evidence table says so). No second gate.
- `T1-E3.04` blast-radius precedent: a third wrap-up gate widens the red-gate blast radius — accepted because `check` is <1s (measured in Phase 9) and the false-verified signal is the defect this plan exists to close.

### Phase 3.5 — AUTO Auto-Resolution (per Q#)
- **Q1** — `Auto-Resolution:` **Phase 7a is the one home; Phase 9 does NOT run `check`.** `Rationale:` one writer (the wrap-up owns all re-stamping and the whole gate set lives in one paragraph — the `surface-budget.md` one-home rule); a red `check` at Phase 9 would halt a single plan for a wiki state its own change may not have caused (claims bind wiki→code, and several runners + wrap-ups contribute), while Phase 7a already owns the wiki-reconciliation step that *causes* drift repair. The plan's own "cheaper single-writer option" language points here. `Source:` plan body § What the fix does item 2 + § Open question; `.devops/rules/surface-budget.md` (one canonical home per rule); `@agent-wrap-up` Phase 7a (existing gate home); batch-1 changelog entry (2026-09-23) showing wrap-up already runs `wiki_claims.py check` on its own initiative — formalizing existing practice, not adding a new one.
- **Q2** — `Auto-Resolution:` `@agent-wrap-up` Phase 7a names the repair: on a red `check`, run `python scripts/wiki_claims.py update` (the re-stamp path), re-run `check`, and only then proceed — with any re-stamped propositions logged in the changelog Why line when they indicate a spec drift rather than a stale stamp. `Rationale:` the plan requires the repair to be named "where wrap-up can act on it"; Phase 7a is the acting surface. `Source:` plan body § What the fix does item 3; `scripts/wiki_claims.py` docstring (`update` is the re-stamp subcommand).
- **Q3** — `Auto-Resolution:` **No.** `Rationale:` with Q1 resolved to Phase 7a, the template's Phase 9 scaffold stays a lint/test/build surface; adding `check` there would be the second home the plan forbids. `Source:` Q1 resolution; `.devops/rules/surface-budget.md`.
- **Q4** — `Auto-Resolution:` **No.** `Rationale:` `@pass-the-parcel` § GROUP D Phase 9 says "run compilation/tests/lint" as the surgeon's scope — it does not enumerate wrap-up gates, and the claims gate is a wrap-up gate. Editing it would restate the gate list in a second surface. `Source:` `.devops/skills/pass-the-parcel/SKILL.md` § GROUP D; Q1 resolution.
- **Q5** — `Auto-Resolution:` **Yes — one clause.** `Rationale:` § Batch Scope (line 67) runs "the repo gates **once** for the batch" and lists lint/coverage but not `check`; leaving it stale would let a batch wrap-up pass while a per-plan wrap-up would fail — two behaviours for the same skill. One clause, no restatement. `Source:` `.devops/skills/agent-wrap-up/SKILL.md` line 67 vs lines 159-165.

**Final validation:** Yes — all context required is present; no `Unresolvable:` entries.

---

## 4 Phase 4: Wiki Requirements & Acceptance Criteria
**Skill Executed:** `ptp-high-visionary` (inline — `SINGLE`/`AUTO`)

> **Conditional Skip (checklist):** (a) user-visible strings/UI — none; (b) API/schema/logic-contract change — none; (c) wiki-facing behavior change — none (this plan gates a script invocation inside `.devops/skills/**` + `.devops/plans/template-plan.md`; it changes no `.wiki/` doc, no claim, no coverage route). → **Skip; no wiki delta.**

**No wiki delta — rationale:** the change is machinery-side gate wiring (which commands a wrap-up phase must run); the grounded-claims contract itself (`.wiki/rules/claims.md` § Command Contract) already documents `check`/`update` behaviour and needs no edit, and the claim in `claims-parser-owner` is unaffected (no edit to `scripts/wiki_claims.py`).

**Acceptance Criteria:**
| # | Criterion (behavior) | Test Target |
|---|---|---|
| 1 | `@agent-wrap-up` Phase 7a is a hard stop over **three** gates: `wiki_lint.py --quiet`, `wiki_claims.py coverage`, `wiki_claims.py check` — all must exit 0 before 7b | Read Phase 7a section: 3 commands named, exit-0 requirement stated |
| 2 | The red-check repair path is named in the acting surface: on a red `check`, run `python scripts/wiki_claims.py update` (re-stamp), re-run `check`, then proceed | Read Phase 7a gap-handling text: `update` named as the repair |
| 3 | Phase 9 does **not** run `check` — Phase 7a is the one home (no second gate surface restates it) | Grep `template-plan.md` + `pass-the-parcel/SKILL.md` for `wiki_claims`: 0 hits; grep whole machinery surface for the gate list: 1 home |
| 4 | Batch-scope repo-gate list (§ Batch Scope) includes `wiki_claims.py check` so batch and per-plan wrap-ups behave identically | Read § Batch Scope gate sentence: `check` present |
| 5 | Touched skills carry version bumps (`updated:` refreshed) | Frontmatter `version:` increased on `agent-wrap-up/SKILL.md` |
| 6 | `wiki_claims.py check` exits 0 on this workspace after the edits (no self-inflicted drift) | Phase 9 command evidence |

---

## 5 Phase 5: High-Visionary Standard Implementation Plan
**Skill Executed:** `ptp-high-visionary` (inline — `SINGLE`/`AUTO`)

**Simplicity Gate:**
> [x] Climbed the Simplicity Ladder (7 rungs) — rung 2 (reuse): the gate, the repair command, and the hard-stop shape all exist; this plan wires one existing command into one existing gate paragraph. Rung 3+ rejected: a new CI job / a new "claims-gate" skill / a Phase 9 scaffold change are all new surfaces for one invariant.
> [x] No speculative abstractions — no new script flags, no new skill, no schema.
> [x] Reuse over reimpl — Reuse Log populated below.
> [x] Deliberate simplifications marked with `ponytail:` comments — one (below).
> [x] Safety exceptions preserved — the hard-stop semantics (red gate blocks wrap-up) are not weakened; the batch blast-radius widening is recorded, not hidden.

**Reuse Log:**
| Existing Asset | Where Reused | Ladder Rung |
|---|---|---|
| `python scripts/wiki_claims.py check` (existing subcommand) | `@agent-wrap-up` Phase 7a gate list | 2 |
| Existing Phase 7a hard-stop shape ("both must exit 0") | extended to three gates, same wording pattern | 2 |
| `python scripts/wiki_claims.py update` (existing re-stamp subcommand) | named as Phase 7a's repair on red | 2 |

**Spaghetti Triage:** No smells noticed.

### To-Do List
- [x] Edit `@agent-wrap-up/SKILL.md` Phase 7a → three-gate hard stop + `update` repair clause
- [x] Edit `@agent-wrap-up/SKILL.md` § Batch Scope gate sentence → add `wiki_claims.py check`
- [x] Edit `@agent-wrap-up/SKILL.md` § Hard Stop → "both" → "all three"
- [x] Bump skill frontmatter `version:` 16→17 + `updated:` 2026-09-23
- [x] Verify no second home (grep) + run gates
- [x] Update plan State & Gates, commit

### File-Level Steps
1. `.devops/skills/agent-wrap-up/SKILL.md` — Phase 7a heading + intro line: "Coverage Gate" → gate set covering three commands; add numbered item 3 `python scripts/wiki_claims.py check` with one-line meaning (grounded-claims drift: every `claims:` hash re-verified against its source; exit 0 = no stale/missing/unresolved claims); amend the on-failure line to name the repair (`update` then re-run `check`); § Batch Scope line 67 gate list gains `wiki_claims.py check`; § Hard Stop first sentence "both" → "all three".
2. `.devops/skills/agent-wrap-up/SKILL.md` — frontmatter `version: 16` → `17`, `updated: 2026-09-23`.

### Implementation Instructions (no code snippets except exact string literals)

- **Gate name stays** — the section heading "Phase 7a: Coverage Gate" may keep its name; the added command extends the gate set, it does not rename it. `ponytail:` renaming the heading to "Claims & Coverage Gate" would touch three cross-references (sprint skills citing Phase 7a) for zero behavior change — accepted as-is.
- **Exact strings to introduce** (as list items / clause fragments, matching the existing numbering style):
  - `3. **Grounded-claims drift gate**: `python scripts/wiki_claims.py check` — every doc's `claims:` hash re-verified against its source file; `STALE`/`MISSING`/`UNRESOLVED-SYMBOL` rows exit `1`. On failure: run `python scripts/wiki_claims.py update` to re-stamp, fix any genuine spec drift the re-stamp would paper over (do not blind-stamp a real contradiction), then re-run `check`.`
  - Batch-scope list edit: `..., `wiki_claims.py coverage`, `wiki_claims.py check`` (append to the existing enumeration on line 67).
  - Hard Stop edit: replace `both scripts` with `all three scripts` and "A green lint with red coverage is a failed wrap-up" gains `...or red claims` (surgical, same sentence shape).
- **No other file changes.** `template-plan.md` and `pass-the-parcel/SKILL.md` stay untouched (Q3/Q4 resolutions); they remain in `touches` because the claim predates the resolution — the write set shrinks, never grows.

### Wiki Core References
- `.wiki/rules/claims.md` § Command Contract → the semantics the new gate item cites (check/update/exit codes)

### Wiki Docs to Add/Edit
- None (Phase 4 no-wiki-delta rationale above).

---

## 6 Phase 6: Grumpy Architect Spec & Logic Audit
**Skill Executed:** `ptp-grumpy-architect` (inline — `SINGLE` self-review)

**`SINGLE` self-review (evidence — one row per acceptance criterion):**
| AC # | Criterion met? (`Yes`/`No`) | Evidence / gap |
|---|---|---|
| 1 | Yes | Phase 7a rewritten as a three-gate hard stop; AC-1 grep target verified post-edit (3 commands + exit-0 sentence present) |
| 2 | Yes | Repair clause added inside Phase 7a item 3: `update` → fix real drift → re-run `check` |
| 3 | Yes | Post-edit grep: `wiki_claims` in `template-plan.md` = 0 hits, `pass-the-parcel/SKILL.md` = 0 hits; gate list lives once (Phase 7a) + its batch-scope enumeration of the same sentence (same file, same section family — no second home) |
| 4 | Yes | § Batch Scope gate sentence now ends `..., wiki_lint.py, wiki_claims.py coverage, wiki_claims.py check` |
| 5 | Yes | Frontmatter `version: 17`, `updated: 2026-09-23` |
| 6 | Yes | Phase 9 evidence table: `check` exit 0 post-edit |

**Verdict:** `PASS`

> **Spec-level audit — the plan contains no code, so no code-level scans (DRY/WET, line checks).**

- **System Contracts Explicit:** three gate commands with exact exit semantics; repair command named; skill frontmatter contract (`version`/`updated`) honoured.
- **File Boundary & Scope Collisions:** write set = 1 file (+ this plan doc). The two conditional touches resolve to "not touched" — recorded, never exceeds the claim.
- **Dependency Gaps:** none — subcommands pre-exist; no new imports/config.
- **YAGNI Bloat:** rejected rungs recorded in the Ladder; no new skill/flag/schema/CI job.
- **Paranoid Security (Contract Level):** n/a — no inputs, secrets, or data flows added.
- **Survivability:** n/a — doc-only change; gates are one-shot non-interactive (§ Gate Invocation Hygiene holds: all three commands are bounded, measured <1s).
- **Edge Cases:** red-`check`-during-batch blocks the whole wrap-up — accepted cost, recorded in Phase 3 warnings (T1-E3.04 precedent); blind re-stamp guarded by the "do not blind-stamp a real contradiction" clause; cross-doc claim restamp ordering (KC entry: `update` twice for claim-carrying sources) is out of scope — no `.wiki` claims touched by this plan.
- **Performance Trade-offs:** +1 gate command at <1s per wrap-up; no per-runner cost (Phase 9 untouched — the Q1 resolution's whole point).
- **Architectural Anti-Patterns:** the fix *removes* a latent duplicate-source-of-truth risk (two wrap-up paths behaving differently on drift) by making per-plan and batch scope share one gate set.
- **Endpoint Protection & Rate Limiting:** n/a.
- **Wiki Core Compliance:** cites `.wiki/rules/claims.md` § Command Contract; no wiki doc edited.

**Dead-Code / Orphan Flags (for Wrap Up backlog entry):**
| plan section / file_path:line_number | Reason |
|---|---|
| None | |

**Required Fixes:**
> [x] None

---

## 7 Phase 7: Smooth Operator Product Review
**Skill Executed:** `ptp-smooth-operator` (inline — `SINGLE`)

> **`SINGLE` topology:** `N/A — SINGLE self-review` (recorded at the Phase 6 checkpoint; presented at Gate B).

- **Vision & Journey Integrity:** unaffected — operator-facing flow unchanged; wrap-up gains one automatic check (fewer silent surprises).
- **Scope Containment (No Gold Plating):** 1 file touched; conditional touches resolved away, not expanded.
- **4 Core User States:** n/a — no UI.
- **Guardrails & Permissions:** guardrail *added* (drift can no longer pass as verified).
- **Mobile / A11y / Telemetry:** n/a.

**Required Fixes:**
> [x] None

---

## 8 Phase 8: Execute Changes
**Skill Executed:** `ptp-code-surgeon` (inline — `SINGLE`/`AUTO`; Gate B auto-cleared on positive evidence, Gate C `N/A`)

**Execution Isolation:** Phase 8 triggered after Gate B auto-clear (AUTO evidence contract: P1/P2 clean, AC rows populated with test targets, file-level steps name paths, self-review `PASS`).

**Single-Pass Direct-to-Disk:**
> [x] Implementation written DIRECTLY to source files - no intermediate Markdown code blocks, no drafting files
> [x] Touched only intended lines - no adjacent refactors
> [x] Cleaned up only owned orphans
> [x] Pre-existing dead code left untouched

> [x] Step 1: `agent-wrap-up/SKILL.md` — Phase 7a three-gate rewrite + `update` repair clause
> [x] Step 2: `agent-wrap-up/SKILL.md` — § Batch Scope gate sentence + § Hard Stop wording
> [x] Step 3: `agent-wrap-up/SKILL.md` — frontmatter `version` 16→17, `updated` 2026-09-23
> [x] Step 4: verification greps (no second home) + full gate run

---

## 9 Phase 9: Verify Changes
**Skill Executed:** `ptp-code-surgeon` (inline — `SINGLE`/`AUTO`)

**Build & Lint:**
> [x] Compilation check clean — n/a (markdown-only change; scripts are the compile surface and were not edited)
> [x] Lint clean (exit 0) — `wiki_lint.py --quiet`
> [x] Type-generation re-run if schema/API changes — n/a (none)

**Test Report:**
> Gate commands are **one-shot, non-interactive, bounded** — `.devops/rules/plan-lifecycle.md` § Gate Invocation Hygiene. All commands below exit immediately (<1s each). No app source tree in this template repo — verification is script checks + doc consistency, per the batch context.

- [x] Lint pass (exit 0)
- [x] Tests pass (exit 0) — template repo ships no runtime test suite; the script gates stand in (see evidence)
- [x] Build pass (exit 0) — n/a (no build surface; `wiki_lint` is the structural build check here)
- [x] Code matches exact plan specifications — verified by re-running the Phase 5 Test Verification Plan commands; every command exits `0`
- [x] No functional gaps identified

**Verification Evidence (required — the checkbox list alone is not evidence):**
| # | Command | Expected | Observed | Exit |
|---|---|---|---|---|
| 1 | `python scripts/wiki_claims.py check` | no stale/missing/unresolved claims | `claims OK: 0 stale, all grounded sources present.` | 0 |
| 2 | `python scripts/wiki_claims.py coverage` | coverage satisfied | `coverage OK: no src/ tree in this workspace — nothing to cover` | 0 |
| 3 | `python scripts/wiki_lint.py --quiet` | silent clean | (no output) | 0 |
| 4 | grep `wiki_claims` in `.devops/plans/template-plan.md` | 0 hits (no second home) | 0 hits | 0 |
| 5 | grep `wiki_claims` in `.devops/skills/pass-the-parcel/SKILL.md` | 0 hits (no second home) | 0 hits | 0 |
| 6 | grep `wiki_claims.py check` in `.devops/skills/agent-wrap-up/SKILL.md` | 2 hits (Phase 7a item 3 + batch-scope list) | 2 hits | 0 |
| 7 | `python scripts/rule_fanout.py` | report-only, exits 0 | table printed, `unauthorised (0)` on all 8 rules, agreements 3/3 | 0 |

**machinery-version base (counter ownership):** observed `machinery-version: 69` in `.devops/sync-manifest.yaml` at Phase 9 — recorded as the "before" for the batch wrap-up's single increment. This runner did **not** bump it. Per-skill `version:` bumps (per modified file) are in scope and done (Step 3).

---

> **HALT POINT (Gate D — Implementation):** Status -> `PHASE_9`, Active Persona -> `Executor`, Gate D `OPEN` (deferred to the batch's consolidated human verdict per `.devops/rules/plan-lifecycle.md` § Deviations item 1).

---

## 📍 State & Gates (CACHE-ANCHORED — update ONLY this section at gate transitions)

> **Cache rule:** This is the **last section** in the file. Gate transitions mutate ONLY the rows below — phase content above AND the frozen **Plan Settings** block at the top stay byte-stable. This section records state, never a handoff instruction.

| Metric | Value |
|---|---|
| **Status** | `PHASE_9` |
| **Version** | `v0.1.0` |
| **Active Persona** | `Executor` |
| **Depends On** | none (canonical: claim front-matter `depends_on`) |
| **Blocks** | none (wave-2 sibling T1-E3.12 is disjoint; T1-E3.16 reads `template-plan.md`, untouched by this plan) |

| Gate | Requirement | Status |
|---|---|---|
| A | Scope approved (Phases 1-3, + 3.5 in AUTO) | `APPROVED` (AUTO — P1/P2 clean; every Q# carries `Auto-Resolution:` + `Rationale:` + `Source:`; final validation recorded) |
| B | Spec & plan approved (Phases 4-5) | `APPROVED` (AUTO — no-wiki-delta rationale recorded + 6 AC rows with test targets; file-level steps name paths; Phase 6 self-review `PASS`) |
| C | Peer reviews passed (Phases 6-7) | `N/A (SINGLE)` — inline self-review at the Phase 6 checkpoint replaced it |
| D | Implementation verified (Phases 8-9) | `OPEN` — deferred to the batch's consolidated human verdict (`@sprint-run` § Deviations item 1) |

> Gate flips: rows flip `OPEN` → `APPROVED`/`REJECTED` only after the user's (or AUTO verification's) verdict. Gate D stays `OPEN` at the batch terminal state.
