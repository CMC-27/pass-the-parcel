# T1-E1.01 — Reconcile Model Registry with opencode.json Runtime Models

> **📍 State & Gates**

## 1 Phase 1: Scope Perimeter

**In Scope:**
- Resolve the mismatch between `.opencode/plans/base-context.md` Model Registry (documents `mimo-2.5` for orchestrator + Phases 1-3, 4-5, 7 + Wrap Up, `v4-flash-max` for Phase 6) and `opencode.json` runtime config (assigns `opencode-go/deepseek-v4-flash` to every agent; `opencode-go/deepseek-v4-flash-max` for Grumpy Architect only).
- Decide the canonical mapping, then update exactly one side: either the Model Registry (if the runtime is truth) or `opencode.json` (if the documented registry is the intent).

**Out of Scope:**
- Any model/vendor migration beyond reconciling identifiers.
- Agent runbook bodies (already prefix-synced from base-context).

**Blocks:** none · **Blocked by:** user decision on which mapping is intended (mimo-2.5 availability on the `opencode-go` provider is the open question).

## 2 Phase 2: Requirements & Context

- `opencode.json` was imported verbatim from GRID-Deploy on 2026-09-03; GRID's runtime has no `mimo-2.5` agent assignment anywhere, suggesting the provider dropped the model and GRID switched silently without updating the registry docs.
- Files touched by any fix: `opencode.json` (agent `model` fields) OR `.opencode/plans/base-context.md` (Model Registry + Delegation Map) + `check-parcel-prefix.ps1 -Sync` re-inline.

## 3 Phase 3: User Clarification

- [ ] Is `mimo-2.5` still available/targeted on the `opencode-go` provider, or retired?
- [ ] Which model should own orchestrator + Phases 1-3, 4-5, 7 + Wrap Up going forward?

## 4 Phase 4: Wiki Requirements & Acceptance Criteria

- No wiki delta — rationale: internal config reconciliation; no user-visible behavior or logic contract changes. (Recorded per spec-first conditional-skip rule.)

## 5 Phase 5: Standard Implementation Plan

1. Read the user's Phase 3 answers.
2. Apply the chosen mapping to the losing side (either 5 `model` fields in `opencode.json`, or the Registry/Delegation Map rows in `base-context.md`).
3. If `base-context.md` changed: run `powershell -ExecutionPolicy Bypass -File scripts\check-parcel-prefix.ps1 -Sync`.
4. Verify: prefix check PASS, `check-utf8-agents.ps1` ALL CLEAN, `opencode.json` parses as JSON.

## 8 Phase 8: Execute Changes

_(pending pickup)_

## 9 Phase 9: Verify Changes

_(pending pickup)_

## 10 Phase 10: User Review & Tweaks

_(pending pickup)_

## Completion Note

_(archived on completion)_

---

> **📍 State & Gates (LAST section — all gate bookkeeping here)**

| Field | Value |
|---|---|
| **Status** | `BACKLOG` |
| **Active Persona** | `Planner` |
| **Mode** | `USER-MANAGED` |
| **Last Updated** | 2026-09-03 |

> Valid states: `BACKLOG`, `PHASE_1`, `PHASE_3`, `PHASE_5`, `PHASE_5_REVISION`, `PHASE_7`, `PHASE_9`, `COMPLETE`.

| Gate | Requirement | Status |
|---|---|---|
| A | Spec & plan approved (Phases 4-5) | `OPEN` |
| B | Reviews passed (Phases 6-7) | `OPEN` |
| C | Implementation verified (Phases 8-9) | `OPEN` |
