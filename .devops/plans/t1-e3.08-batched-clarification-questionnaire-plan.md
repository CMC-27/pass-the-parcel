---
code: T1-E3.08
sprint: sprint-9-batch-hardening
claim_status: GATE_D_USER_APPROVAL
owner: "parcel-sprint (DeepSeek V4.1 Flash)"
claimed_at: "2026-09-16"
last_touch: "2026-09-16"
touches: [".devops/skills/ptp-context-hunter/SKILL.md", ".devops/skills/pass-the-parcel/SKILL.md", ".devops/skills/ptp-phase3-answerer/SKILL.md", ".devops/plans/template-plan.md", ".opencode/plans/base-context.md", ".devops/templates/base-context.template.md", ".devops/agents/parcel.agent.md", ".devops/agents/parcel-sprint.agent.md", ".devops/agents/ptp-context-hunter.subagent.md", ".devops/agents/ptp-phase3-answerer.subagent.md", ".devops/rules/process-lessons.md", ".wiki/core/18-knowledge-capture.md", ".wiki/core/00-system-index.md", ".devops/sync-manifest.yaml", ".devops/logs/version-history.md"]
depends_on: []
---
# T1-E3.08 - Batched clarification questionnaire

> **Origin:** Phase 3 **relayed** clarification questions one at a time (a stated non-negotiable — reversed by this parcel, with the rationale staged in `.devops/rules/process-lessons.md`). This parcel adds a batched questionnaire mode so the whole decision surface is answered in one sitting, with the final validation kept as a separate confirm. Wave 3 of 6 in the batch-hardening sequence (`sprint.md` § Delivery Model; the Origin line originally said "Wave 2", corrected at the post-wave-2 re-verification — see Phase 2).

## ⚙️ Plan Settings (FROZEN — set at plan start, read before any phase)

| Setting | Value | Meaning |
|---|---|---|
| **Mode** | `AUTO` | Orchestrator auto-clears Gates A-C **only** on positive, presence-based evidence (see `.devops/rules/plan-lifecycle.md` § AUTO Gate Evidence Contract) — Gate D always halts. |
| **Agents** | `SINGLE` | Fast plan; inline personas, Group C collapses to a self-review checkpoint, Gate C `N/A`. |

> **Frozen config — read before executing ANY phase.** Mutable runtime state lives ONLY in the bottom `## 📍 State & Gates` section.
>
> **Preset provenance:** written by `ptp-parcel-fast` at claim time from the `@sprint-run` batch host's locked preset (`Mode=AUTO`, `Agents=SINGLE`); the selection question is skipped. Frozen thereafter.

---

## 1 Phase 1: Expansion & Scoping
**Skill Executed:** `ptp-context-hunter`
**Intent:**
Present Phase 3's clarification questions as one batched questionnaire — the whole decision surface in one sitting, recommended defaults first — instead of one prompt at a time, while keeping the final validation a separate confirm.

**In Scope:**
- `ptp-context-hunter` § 3: turn "One Question At A Time (non-negotiable)" into a **mode**, batched by default where the surface supports it.
- Surface-capability handling: the opencode `question` tool takes an array; the VS Code `vscode_askQuestions` surface needs a capability check with a per-question fallback.
- Template Phase 3 note; the final `"Is this all the context required?"` stays a standalone confirm.
- A recorded rationale in `.devops/rules/process-lessons.md` — this deliberately reverses a stated non-negotiable.

**Out of Scope:**
- The AUTO/batch path: `ptp-parcel-fast` never calls the ask-tool and `ptp-phase3-answerer` is unchanged.
- Front-loading questions into `@sprint-plan` so the batch benefits (explicitly deferred, not this parcel).
- The 5–8 question budget and the Phase 10 overflow route.

**Perimeter Notes:**
- Batching trades reflection time for round-trips. The recommended-default-first ordering and the retained final validation are what keep it safe rather than merely faster.
- The batched questionnaire helps interactive `parcel` runs only; it does not change how a sprint batch answers questions.

---

## 2 Phase 2: Requirements & Context
**Skill Executed:** `ptp-context-hunter`
**Forensic Context Inventory:**
> [x] Wiki core docs read (`.wiki/core/00-system-index.md` + `.wiki/core/18-knowledge-capture.md`)
> [x] Knowledge capture read (`.wiki/core/18-knowledge-capture.md` — its Quick Reference **rule 8** *is* the rule this parcel reverses; machinery lessons route to `.devops/rules/process-lessons.md`)
> [x] Source verified (files below; re-verified **after** waves 1-2 landed — see *Post-wave re-verification*)

**Current implementation (re-verified 2026-09-16, post-waves 1-2):**
- `ptp-context-hunter/SKILL.md` § 3 states the rule **twice** — the `One Question At A Time (non-negotiable)` bullet ("Never batch multiple questions into a single prompt") and a duplicate `Interactive Interrogation` bullet ("Ask exactly **one question at a time**") — plus a `Delegated runs` clause ("The orchestrator relays them to the user **one at a time**. Never batch on the orchestrator's behalf"), a 5–8 question budget with Phase 10 overflow, and § 5's standalone final `"Is this all the context required?"` confirm.
- The rule is restated in **six live surfaces**, not one: `ptp-context-hunter` § 3 (three statements), `pass-the-parcel` § GROUP A Phase 3 (`never batched`), `ptp-phase3-answerer` § Activation (descriptive), `template-plan.md` Gate A halt note, `.opencode/plans/base-context.md` ORCHESTRATOR-ONLY delegation map (`orchestrator asks one at a time`) + its seed mirror `.devops/templates/base-context.template.md`, and `parcel.agent.md` workflow step 7 (`ONE AT A TIME … never batch multiple questions into one prompt`).
- The two ask surfaces are already named in the skill — `question` (opencode) and `vscode_askQuestions` (VS Code) — but their **shapes differ**. The plan's Phase 2 (plan start) recorded the opencode tool as taking a `questions[]` array. That claim is **not independently re-verifiable from this repo** (no tool schema is in-tree, and the batch runner holds no ask tool), so the rule is written against the **tool's own signature** — never a hard-coded surface list — and the unverifiable half is declared as an assumption at Gate D.
- Batch/AUTO path untouched: `ptp-parcel-fast`'s hard rule in `.devops/agents/ptp-parcel-fast.subagent.md` ("Never call the ask-questions tool") and its skill's locked preset; `ptp-phase3-answerer` resolves from the Research Map and never asks.
- `.devops/rules/process-lessons.md` is the staging register for machinery rules and carries the 2026-09-16 `touches`-staleness entry added by the wave-2 wrap-up; a reversal of a stated non-negotiable belongs there.
- `.wiki/core/18-knowledge-capture.md` Quick Reference **rule 8** states the rule inside the wiki (*"Phase 3 clarification questions go to the user one at a time"*) — a live rule row, not a historical record.

**Post-wave re-verification (waves 1-2 landed; premises re-checked, corrections recorded — never re-imposed):**
- The plan's Origin line said "Wave 2 of the batch-hardening sequence". `sprint.md` § Delivery Model fixes wave 1 = `T1-E3.06`, wave 2 = `T1-E3.07`, **wave 3 = this plan**. Corrected in the Origin line.
- The plan's Phase 1 named the target as "`ptp-context-hunter` § 3: turn 'One Question At A Time (non-negotiable)' into a mode". Still exact — the heading is live at `ptp-context-hunter/SKILL.md:37`.
- The plan predicted the edit would be confined to that skill. A live grep found the rule restated in **six** surfaces (above). Same clause, same phrase, same fix — declared as `touches` additions and disclosed as a perimeter extension, **not** silently narrowed to fit the stale declaration.
- `T1-E3.07` relocated the AUTO auto-clear contract into `.devops/rules/plan-lifecycle.md` § AUTO Gate Evidence Contract (cited by the skills; no second dialect) and raised `machinery-version` to **50**. Every number written in this plan's own scaffold is stale by construction (the sprint file also says 47); the bump reads the **live** value at execution time.
- `T1-E3.07` left three `AUTO`-wording residuals *outside* its write set — `pass-the-parcel/SKILL.md:86`, `plan-lifecycle.md:70`, `parcel.agent.md:143` still say `AUTO` clears "on mechanical verification" / "(outputs present, no `REJECTED` verdict…)". Unrelated to this parcel; **not** touched here; recorded as a Phase 6 finding for the batch wrap-up.

**Architectural considerations:**
- The rule is stated twice *inside one skill* and six times across the machinery, so reversing it needs a recorded rationale — and the one-entry-per-session capture budget applies.
- Surface capability differs; the skill must degrade to per-question rather than fail, and the capability test must be the tool's own shape (no trial-and-error probing, no hard-coded surface list).
- The final `"Is this all the context required?"` validation *is* the gate's actual meaning and must stay a separate confirm, not one row in the batched form.
- Batching trades reflection time for round-trips; **recommended-default-first ordering already exists** in the skill and is the mitigation that keeps it safe rather than merely faster.
- `base-context.md` is a PREFIX-LOCKED surface, so its row edit drags `check-parcel-prefix.ps1 -Sync`; the row lives in the ORCHESTRATOR-ONLY block, so only the two orchestrators carry it.

**Relevant existing decisions:**
- KC Quick Reference **rule 8** (the rule being reversed) and **rule 7** (never edit a PREFIX-LOCKED surface directly — edit `base-context.md`, then run `-Sync`).
- `T1-E3.07`'s landed precedent: a canonical rule is **stated once and cited**, never mirrored into a second dialect.
- `process-lessons.md` (2026-09-14) routing rule: machinery lessons go to the register, never into `.wiki/` as the primary home.

---

## 3 Phase 3: User Clarification
**Skill Executed:** `ptp-context-hunter` + `ptp-phase3-answerer`
**Mode:** `AUTO` — auto-resolved by `ptp-phase3-answerer` (no ask-tool; batch path)

> [x] Q1: Batched by default, or an opt-in mode?
> [x] Q2: How is surface capability detected, and what exactly is the fallback contract?
> [x] Q3: Does the 5–8 question budget change now the user sees the whole set at once?
> [x] Q4: Where does the reversal rationale live — `process-lessons.md` only, or also in `ptp-context-hunter` itself?
> [x] Q5: Confirm this parcel **leaves the batch path asking nothing** (front-loading is deferred, not folded in).
> [x] Final validation: "Is this all the context required?" -> Yes

**Phase 3.5 Research Map**
| Q# | Core Docs | Code Files | KC / Lessons Entries |
|---|---|---|---|
| Q1 | `.wiki/core/18-knowledge-capture.md` (rule 8, the rule being reversed) | `.devops/skills/ptp-context-hunter/SKILL.md` § 3, § 5; `.devops/agents/parcel.agent.md` step 7 | `process-lessons.md` header (entry format + one-way-ratchet rationale) |
| Q2 | — | `.devops/skills/ptp-context-hunter/SKILL.md` § 3 (both surfaces named); `.devops/skills/pass-the-parcel/SKILL.md` § GROUP A | KC rule 2 (SKILL.md is canonical; agents embed it verbatim) |
| Q3 | — | `.devops/skills/ptp-context-hunter/SKILL.md` § 3 (budget bullet); `.devops/plans/template-plan.md` Phase 3 block | `process-lessons.md` (2026-09-16) `touches`-staleness entry |
| Q4 | `.wiki/core/18-knowledge-capture.md` (rules 7 + 8) | `.devops/rules/process-lessons.md`; `.opencode/plans/base-context.md` ORCHESTRATOR-ONLY delegation map | `process-lessons.md` routing rule (2026-09-14) |
| Q5 | — | `.devops/skills/ptp-parcel-fast/SKILL.md` (hard rule); `.devops/skills/ptp-phase3-answerer/SKILL.md` § 9 | KC `@sprint-run` batch-deviations entry |

**Auto-Resolutions:**

**Q1 — Auto-Resolution:** **Batched by default** wherever the ask surface supports it, with the sequential one-question-at-a-time path retained as the automatic fallback — not an opt-in mode.
**Rationale:** The rule's own justification is round-trip cost, and the existing recommended-default-first ordering already supplies the guard that batching removes, so the default should follow the capability rather than ask the user to choose tooling behaviour they cannot observe.
**Source:** `.devops/skills/ptp-context-hunter/SKILL.md` (lines 37-40) | `.wiki/core/18-knowledge-capture.md` (line 38, rule 8) | `.devops/plans/t1-e3.08-...-plan.md` § 1 Perimeter Notes

**Q2 — Auto-Resolution:** Capability is detected from the **ask tool's own signature** — the questionnaire is submitted in one call when the tool takes a question *array*, and one call per question when it takes a single question — with the fallback contract being *same set, same options, same recommended-first ordering, one call each: never a dropped or merged question*.
**Rationale:** A signature test is self-describing at call time and stays true on both shipped surfaces (`question` / `vscode_askQuestions`) without hard-coding which one has which shape, and it avoids trial-and-error probing against a live user.
**Source:** `.devops/skills/ptp-context-hunter/SKILL.md` § 3 (both surfaces named in the existing bullet) | `.devops/skills/pass-the-parcel/SKILL.md` § GROUP A (line 226) | Phase 2 note: the array claim is plan-start evidence, re-declared as a Gate D assumption

**Q3 — Auto-Resolution:** The budget is **unchanged** — **5 to 8** targeted questions, with the overflow still routed to Phase 10 as deferred questions.
**Rationale:** The budget bounds the user's decision load, not the number of round-trips; one sitting of 8 decisions is the same cognitive cost as 8 sittings, and re-deriving a second cap would fork a number the skill already fixes and `template-plan.md` already reflects.
**Source:** `.devops/skills/ptp-context-hunter/SKILL.md` (line 38, budget bullet) | `.devops/plans/template-plan.md` Phase 3 block (5 questions + final validation row)

**Q4 — Auto-Resolution:** **Both** — a dated entry in `.devops/rules/process-lessons.md` stages the reversal and its rationale, and `ptp-context-hunter` § 3 carries the rule itself; the wiki's rule row is **corrected** (not used as the rationale home).
**Rationale:** The register is the sanctioned staging home for a machinery rule reversal (the wiki documents the app), while the skill must carry the operative rule for the agent that executes it; `.wiki/core/18-knowledge-capture.md` rule 8 is a *live* rule row that would otherwise assert the opposite of the skill, so it is corrected rather than left as a second dialect.
**Source:** `.devops/rules/process-lessons.md` (lines 12-16, entry format + routing) | `.wiki/core/18-knowledge-capture.md` (line 38) | KC rule 7 (prefix surfaces edited via `base-context.md` + `-Sync`)

**Q5 — Auto-Resolution:** **Confirmed** — the parcel leaves the batch path asking nothing: `ptp-parcel-fast` still never calls an ask tool, `ptp-phase3-answerer` still resolves from the Research Map, and front-loading questions into `@sprint-plan` stays deferred.
**Rationale:** The interactive relay is the only path that ever asked a user; the `AUTO` path answers from mapped sources by construction, so a questionnaire mode cannot reach it and no new behaviour is needed there.
**Source:** `.devops/agents/ptp-parcel-fast.subagent.md` (hard rule: never call the ask-questions tool; never spawn sub-agents) | `.devops/skills/ptp-parcel-fast/SKILL.md` § Activation (locked preset) | `.devops/skills/ptp-phase3-answerer/SKILL.md` § 9

**Architectural Conflict Warnings Raised:**
- **CW1 — one rule, six statements.** The rule lives in `ptp-context-hunter` § 3 (three statements), `pass-the-parcel` § GROUP A, `ptp-phase3-answerer` § Activation, `template-plan.md` Gate A, the ORCHESTRATOR-ONLY delegation map (+ seed mirror), `parcel.agent.md` step 7, and KC rule 8. A partial flip is indistinguishable from drift, so **every** restating surface is swept in one commit. Resolved by widening `touches` (Phase 8 discloses each addition).
- **CW2 — the duplicate statements inside the skill.** § 3's `Interactive Interrogation` bullet duplicates the non-negotiable bullet's content. **Resolved by merging** — one authoritative relay-mode bullet replaces both, so the two statements cannot drift apart. This is deletion, not refactor.
- **CW3 — the batch path must not inherit the questionnaire.** A batched run has no user to ask; a batching rule read by an `AUTO` runner is a foot-gun. **Resolved by scoping the rule to Phase 3's interactive relay** and leaving `ptp-parcel-fast` / `ptp-phase3-answerer` behaviour untouched (Q5).
- **CW4 — prefix cascade.** The delegation-map row lives in the PREFIX-LOCKED surface, so its edit requires `check-parcel-prefix.ps1 -Sync` and a `machinery-version` bump. Disclosed, not avoided: leaving the prefix asserting the reversed rule would fork the exact second dialect `T1-E3.07` removed.

**Test Proposals (TDD)**
> **T1 — the old rule is gone from the live surfaces.** Steps: `Select-String -Path .devops/skills/ptp-context-hunter/SKILL.md,.devops/skills/pass-the-parcel/SKILL.md,.devops/skills/ptp-phase3-answerer/SKILL.md,.devops/plans/template-plan.md,.opencode/plans/base-context.md,.devops/templates/base-context.template.md,.devops/agents/parcel.agent.md -Pattern 'One Question At A Time \(non-negotiable\)|Never batch multiple questions|orchestrator asks one at a time|one at a time via the ask-questions tool'`. Expected: **0** matches.
> **T2 — the new mode is stated once and is signature-driven.** Steps: inspect `ptp-context-hunter` § 3 and `parcel.agent.md` step 7. Expected: one authoritative relay-mode rule naming the array-vs-single capability test, the sequential fallback, and the standalone final validation.
> **T3 — the batch path is untouched.** Steps: `Select-String -Path .devops/skills/ptp-parcel-fast/SKILL.md -Pattern 'Never call the ask-questions tool'` and inspect `ptp-phase3-answerer` § 9. Expected: still present / unchanged in substance.
> **T4 — prefix + encoding integrity.** Steps: `check-parcel-prefix.ps1 -Sync` then, without the flag, `check-parcel-prefix.ps1`; then `check-utf8-agents.ps1`. Expected: regenerated surfaces reported, then `PASS` with a byte-identical prefix; `ALL CLEAN`.
> **T5 — version ledger.** Steps: read `machinery-version:` from `.devops/sync-manifest.yaml` and grep the literal in `.devops/logs/version-history.md`. Expected: a live value strictly greater than `50`, present in the ledger.

**Status:** T1 ACCEPTED · T2 ACCEPTED · T3 ACCEPTED · T4 ACCEPTED · T5 ACCEPTED
**Validation:** Auto-Resolution: ACCEPTED — all five are literal-presence checks executable from the repo root with no new tooling, matching the repo's existing verification pattern (the `sprint-run` preflight runs the same two scripts as its baseline gate).

---

> **HALT POINT (Gate A — Scope):** Phases 1-3 complete. Present the scope perimeter + Phase 3 Q&A record; user approves the scope before planning begins.

> **AUTO auto-clear (2026-09-16, `ptp-parcel-fast`):** Gate A verified by presence test — **P1** clear (no line-leading unchecked box and no bare `TBD`/`TODO`/`FIXME` token outside code spans in the Phase 3 evidence block), **P2** clear (no line-leading `**REJECTED:**` / `Unresolvable:`), **5/5** Phase 3 questions carry an `Auto-Resolution:` entry with `Rationale:` + `Source:`, and the final-validation verdict is recorded above. Gate A resolves `APPROVED` under the locked `AUTO` preset; no halt.

---

## 4 Phase 4: Wiki Requirements & Acceptance Criteria
**Skill Executed:** `ptp-high-visionary` + `@wiki-writer`

> **Conditional-skip check:** (a) new/changed user-visible strings or UI — **no**; (b) API, schema, or logic-contract change — **yes** (the Phase 3 relay contract); (c) wiki-facing behavior change — **yes** (`.wiki/core/18-knowledge-capture.md` Quick Reference rule 8 *states* the rule). Phase 4 runs; the wiki delta is one rule row plus its freshness stamp.

**Wiki Docs to Write/Update (as `in-progress`):**
| Doc | Change |
|---|---|
| `.wiki/core/18-knowledge-capture.md` | Quick Reference **rule 8** — replace the one-question-at-a-time rule text with the batched-questionnaire mode (batched where the ask surface takes a question array, sequential otherwise). One table cell; no other KC content is touched. |
| `.wiki/core/00-system-index.md` | Slot `18` `Last Verified` → `2026-09-16` (the doc was verified against reality today; the stamp would otherwise understate it). |

> **`status:` deviation (disclosed).** Both docs keep `status: stable` instead of being marked `in-progress`. `in-progress` exists to stop a **pre-code claim** being read as truth; here the doc row and the machinery it describes land in the **same commit** (Phase 8), so there is no pre-code window to mark — and flipping a `type: core` doc that agents read on every parcel to "in drafting, not ready for use" would damage more truth than it protects. Recorded as a Phase 6 finding for the batch wrap-up, which is the body that owns promotion.

**Acceptance Criteria:**
| # | Criterion (behavior) | Test Target |
|---|---|---|
| 1 | `ptp-context-hunter` § 3 carries **one** authoritative relay rule: batched when the ask tool takes a question *array*, one call per question otherwise — with 2-4 options and the recommended choice first in **both** modes. | T2: read `ptp-context-hunter/SKILL.md` § 3. |
| 2 | The skill's **two** duplicate statements of the old rule are merged, so they cannot drift apart. | T1: 0 matches for `One Question At A Time (non-negotiable)`; read § 3 shows a single relay-mode bullet. |
| 3 | The final `"Is this all the context required?"` confirm is explicitly **standalone** — never a row in the batched questionnaire, and still the precondition for advancing the State & Gates status. | T2: read § 3 + § 5. |
| 4 | **Every** other live surface restating the old rule is swept in the same commit: `pass-the-parcel` § GROUP A, `ptp-phase3-answerer` § Activation, `template-plan.md` Gate A, the ORCHESTRATOR-ONLY delegation map + seed mirror, `parcel.agent.md` step 7, the `ptp-context-hunter` agent wrapper's relay hard rule, and KC rule 8. | T1: 0 matches across the eleven files (case-sensitive). |
| 5 | The **batch path is unchanged** — `ptp-parcel-fast` still never calls an ask tool and `ptp-phase3-answerer` still resolves from the Research Map. | T3: the `ptp-parcel-fast` hard rule is present in its agent wrapper (`.devops/agents/ptp-parcel-fast.subagent.md` — the rule lives there, not in the skill); `ptp-phase3-answerer` § 9 is unchanged in substance. |
| 6 | The PREFIX-LOCKED surface and its seed mirror carry the new wording, and every locked agent's prefix stays byte-identical after `-Sync`. | T4: `check-parcel-prefix.ps1` exits `0` with a byte-identical prefix; `check-utf8-agents.ps1` ALL CLEAN. |
| 7 | The reversal rationale is recorded in `.devops/rules/process-lessons.md` as **one** dated entry (≤3 lines) naming the rule reversed, the new mode, and the sweep of restating surfaces. | Read the register entry; entry count still ≤ the tidy ceiling. |
| 8 | The wiki's live rule row (`18-knowledge-capture.md` rule 8, not its Decision Archive) is corrected and the hub's slot-18 freshness stamp is updated. | Read the rule-8 row + `00-system-index.md` slot 18. |
| 9 | `machinery-version` is bumped from the **live** value and the literal appears in `.devops/logs/version-history.md`. | T5: manifest value greps in the ledger; `python scripts/wiki_claims.py check` stays clean. |

**Spec Notes:**
- Data flow / state changes: none at runtime. This is a behaviour contract for the **interactive relay** of Phase 3 questions: the question set is unchanged, its recommended-first ordering is unchanged, its size cap is unchanged; only the *number of round-trips* changes, and only when the surface can carry the whole set in one call.
- Edge cases the spec must cover: **(i)** a surface that takes one question per call must fall back silently and completely — never drop, merge, or reorder a question; **(ii)** the final validation must not be batched, or the gate's meaning collapses into one checkbox among many; **(iii)** the delegated-subagent path (no ask tool at all) keeps drafting into the plan and returning — the orchestrator, not the subagent, owns the relay mode; **(iv)** the `AUTO` batch path has no user, so the rule must be scoped so an unattended runner can never read it as "ask the user a questionnaire"; **(v)** a plan that *quotes* the old rule (this parcel does, in Phase 2) must not be mistaken for a restating surface — the sweep targets the rule's live homes, not quotations.
- Docs consumed by this feature (read-first list): `.devops/skills/ptp-context-hunter/SKILL.md`, `.devops/skills/pass-the-parcel/SKILL.md`, `.devops/rules/process-lessons.md`, `.wiki/core/18-knowledge-capture.md`.

---

## 5 Phase 5: High-Visionary Standard Implementation Plan
**Skill Executed:** `ptp-high-visionary`

**Simplicity Gate:**
> [x] Climbed the Simplicity Ladder (7 rungs)
> [x] No speculative abstractions
> [x] Reuse over reimpl - Reuse Log populated
> [x] Deliberate simplifications marked with `ponytail:` comments
> [x] Safety exceptions preserved

**Reuse Log:**
| Existing Asset | Where Reused | Ladder Rung |
|---|---|---|
| The existing 2-4 options + `(Recommended)`-first convention in `ptp-context-hunter` § 3 | Becomes the mode bullet's ordering clause verbatim in spirit; no new option schema. | 2 |
| The existing 5–8 budget bullet + Phase 10 overflow | Untouched — the parcel narrows round-trips, not the decision set. | 1 |
| § 5's existing `"Is this all the context required?"` prompt block | Retained as the standalone confirm; one clause added, prompt text unchanged. | 2 |
| The two surface names already in § 3 (`question` / `vscode_askQuestions`) | The capability test keys off the tool's own signature instead of introducing a probe or a config flag. | 2 |
| `check-parcel-prefix.ps1 -Sync` | Does the entire prefix re-inline **and** the `ptp-*` skill-embed regeneration — no hand-editing of any agent file. | 2 |
| `.devops/rules/process-lessons.md` entry format (`- **[date] Title** — rule. *Do instead:* fix.`) | The reversal rationale is one entry in the existing register; no new doc, no new section. | 2 |
| KC Quick Reference **rule 8** (the row being corrected) | Edited in place — the wiki already had a rule row; nothing is added to the wiki. | 2 |

**Spaghetti Triage (See-Name-Route, Do Not Fix):**
- [x] No smells noticed

| File | Function | Smell Type | Severity | Recommended Action |
|---|---|---|---|---|
| — | — | — | — | — |

### To-Do List
- [x] 1. `ptp-context-hunter/SKILL.md` § 3: replace the four bullets with the surface-driven questionnaire-mode rule (mode / ordering / budget / delegated runs); add the standalone-confirm clause in § 5; bump `version` 5→6.
- [x] 2. `pass-the-parcel/SKILL.md` § GROUP A Phase 3: reword the relay clause; bump `version` 17→18.
- [x] 3. `ptp-phase3-answerer/SKILL.md` § Activation: reword the descriptive relay clause; bump `version` 2→3.
- [x] 4. `.devops/plans/template-plan.md` Gate A halt note: reword the relay sentence.
- [x] 5. `.opencode/plans/base-context.md` ORCHESTRATOR-ONLY delegation-map row: reword; mirror into `.devops/templates/base-context.template.md`.
- [x] 6. `.devops/agents/parcel.agent.md` workflow steps 7-8 (unique content): reword the relay instruction.
- [x] 7. `.devops/rules/process-lessons.md`: one dated reversal entry.
- [x] 8. `.wiki/core/18-knowledge-capture.md` rule 8 corrected; `.wiki/core/00-system-index.md` slot-18 `Last Verified` → `2026-09-16`.
- [x] 9. `check-parcel-prefix.ps1 -Sync`, then verify without the flag; `check-utf8-agents.ps1`.
- [x] 10. `sync-manifest.yaml` `machinery-version` bumped from the live value; matching `version-history.md` row.
- [x] 11. (added at Phase 9) `.devops/agents/ptp-context-hunter.subagent.md` — the wrapper's own relay hard rule reworded; V1 found it because it scans the agent wrappers too.

### File-Level Steps
1. `.devops/skills/ptp-context-hunter/SKILL.md` — § 3 bullets + § 5 final-validation clause + frontmatter `version:`.
2. `.devops/skills/pass-the-parcel/SKILL.md` — GROUP A Phase 3 bullet + frontmatter `version:`.
3. `.devops/skills/ptp-phase3-answerer/SKILL.md` — § Activation relay sentence + frontmatter `version:`.
4. `.devops/plans/template-plan.md` — the Gate A halt-point blockquote.
5. `.opencode/plans/base-context.md` — the ORCHESTRATOR-ONLY delegation map, `1-3` row.
6. `.devops/templates/base-context.template.md` — the mirrored `1-3` row in the seed.
7. `.devops/agents/parcel.agent.md` — workflow steps 7 and 8 (own content below the prefix marker only).
8. `.devops/rules/process-lessons.md` — one bullet in `## Parcel & Sprint Mechanics`.
9. `.wiki/core/18-knowledge-capture.md` — Quick Reference row 8.
10. `.wiki/core/00-system-index.md` — slot-18 `Last Verified` cell.
11. `.devops/sync-manifest.yaml` — `machinery-version:`.
12. `.devops/logs/version-history.md` — one Release Log row at the top of the table.

### Implementation Instructions (no code snippets except exact string literals: regex, SQL migration, CLI command, config key, error message)

**1. `.devops/skills/ptp-context-hunter/SKILL.md` — the canonical rule.**
In § 3, delete the `**One Question At A Time (non-negotiable):**` bullet, the `**Interactive Interrogation:**` bullet, and the `**Delegated runs (no ask tool):**` bullet, and replace all three with **three** bullets, in this order, with the existing **Question budget** bullet left untouched between the second and the third:
- a bullet led **`**Questionnaire Mode (surface-driven):**`** — the whole drafted set is relayed in **one batched questionnaire** when the ask-questions tool accepts a question *array* (`question` on the opencode surface), and **one call per question** when it accepts a single question (`vscode_askQuestions` on the VS Code surface). The tool's own signature is the capability test — never probe by trial and error, and never drop, merge, or reorder a question to fit the surface. Every question still carries 2 to 4 explicit, selectable options with the recommended choice listed first, prefixed with `(Recommended)`.
- a bullet led **`**Delegated runs (no ask tool):**`** — the existing clause, with its final sentence changed from the one-at-a-time relay to "The orchestrator relays them in the same questionnaire mode. Never drop a question on the orchestrator's behalf."
- the existing **Question budget** bullet (5 to 8 targeted questions; overflow routed to Phase 10 as deferred questions, recorded in the plan) — **unchanged**.
In § 5, keep the prompt block and its two options byte-identical, and add one clause stating the confirm is asked **on its own** — never as a row in the Phase 3 questionnaire. Bump frontmatter `version:` (currently `5`).

**2. `.devops/skills/pass-the-parcel/SKILL.md` — GROUP A, Phase 3.**
Reword the `**CRITICAL:**` bullet so the relay is the questionnaire mode: one call for the whole set where the tool takes a question array, one call per question otherwise, with the final `"Is this all the context required?"` confirm always standalone. Delete the words `never batched`. Bump frontmatter `version:` (currently `17`).

**3. `.devops/skills/ptp-phase3-answerer/SKILL.md` — § Activation, first line.**
Change the descriptive tail of the USER-MANAGED sentence to the questionnaire mode (batched where the ask surface supports it). The skill's behaviour — read the Research Map, never ask, no ask-tool call — is unchanged; this is a truth-fix on a descriptive clause, disclosed as a perimeter extension. Bump frontmatter `version:` (currently `2`).

**4. `.devops/plans/template-plan.md` — the Gate A halt-point blockquote.**
Replace the final sentence (`Questions are relayed to the user **one at a time** via the ask-questions tool — never batched.`) with the questionnaire mode: whole set in one call where the tool takes a question array, one call per question otherwise, final confirm always standalone. Nothing else in the blockquote changes.

**5-6. `.opencode/plans/base-context.md` (ORCHESTRATOR-ONLY block) and `.devops/templates/base-context.template.md`.**
The delegation-map `1-3` row currently reads "Scope perimeter + Phase 3 questions (drafted; orchestrator asks one at a time)". Change the parenthetical to "drafted; orchestrator relays — batched where the ask surface supports it", identically in both files so the seed agrees cell-for-cell. **Do not hand-edit any `.devops/agents/*` file** — the prefix is regenerated by `-Sync` (step 9).

**7. `.devops/agents/parcel.agent.md` — workflow steps 7 and 8 (own content only).**
Step 7: replace the `**You relay them to the user ONE AT A TIME via `vscode_askQuestions` — never batch multiple questions into one prompt.**` sentence with the questionnaire-mode instruction (whole set in one call when the ask tool takes a question array, one call per question otherwise) plus the standalone-final-validation clause. Step 8: change "fall back to asking the user directly, one at a time" to "fall back to asking the user directly, in the same questionnaire mode". Nothing above the ORCHESTRATOR-ONLY end marker is touched by hand.

**8. `.devops/rules/process-lessons.md` — one entry.**
Append one bullet to `## Parcel & Sprint Mechanics` in the register's own format (≤3 lines), titled for the reversal, naming: the rule reversed (Phase 3 one-at-a-time, KC rule 8), the replacement (a mode: batched where the ask surface takes a question array, sequential otherwise; final confirm standalone), and the `Do instead` (record the rationale here and sweep every restating surface — the rule lived in seven live places, and a partial flip is indistinguishable from drift).

**9. `check-parcel-prefix.ps1 -Sync`.**
Run it once to regenerate the PREFIX-LOCKED surfaces and the `ptp-*` skill embeds, then run it again **without** `-Sync` and confirm exit `0` with a byte-identical prefix. Then `check-utf8-agents.ps1` for `ALL CLEAN`.

**10. `.wiki/core/18-knowledge-capture.md` — Quick Reference row 8.**
Replace the rule cell's text with the batched-questionnaire mode (whole decision surface in one questionnaire where the ask surface supports it; one call per question otherwise). The `Pitfall?` cell stays `❌`; the Decision Archive entry at the bottom of the doc is a **historical record** and is not retro-edited.

**11. `.wiki/core/00-system-index.md` — slot `18` `Last Verified`.**
Set to `2026-09-16`. One cell.

**12. `.devops/sync-manifest.yaml`.** Bump `machinery-version:` from the **live** value read at execution time (never from any number written in this plan).

**13. `.devops/logs/version-history.md`.** Prepend one Release Log row recording the literal `machinery-version: <live+1>`; CI fails when the current value is absent from this file.

### Test Verification Plan
> **V1 — the old rule is gone from every live home.** `Select-String -CaseSensitive -Path .devops/skills/ptp-context-hunter/SKILL.md,.devops/skills/pass-the-parcel/SKILL.md,.devops/skills/ptp-phase3-answerer/SKILL.md,.devops/plans/template-plan.md,.opencode/plans/base-context.md,.devops/templates/base-context.template.md,.devops/agents/parcel.agent.md,.devops/agents/parcel-sprint.agent.md,.devops/agents/ptp-context-hunter.subagent.md,.devops/agents/ptp-phase3-answerer.subagent.md,.devops/rules/process-lessons.md -Pattern 'One Question At A Time \(non-negotiable\)|Never batch multiple questions|orchestrator asks one at a time|one at a time via the ask-questions tool|relays them to the user \*\*one at a time\*\*'` → **0** matches. `-CaseSensitive` is required: `Select-String` is case-insensitive by default and the batch host's "runs each eligible plan … one at a time in a fresh context" (about *plans*, not questions) is a false positive otherwise.
> **V2 — the new mode is stated in the two rule-owning surfaces.** `Select-String -Path .devops/skills/ptp-context-hunter/SKILL.md -Pattern 'Questionnaire Mode \(surface-driven\)'` → **1** match; `Select-String -Path .devops/agents/parcel.agent.md -Pattern 'questionnaire mode'` → **2** matches (steps 7 and 8).
> **V3 — the batch path is untouched.** `Select-String -Path .devops/agents/ptp-parcel-fast.subagent.md -Pattern 'Never call the ask-questions tool'` → ≥1 match (the rule lives in the agent wrapper; the skill's § Activation carries the locked preset).
> **V4 — the rationale is staged.** `Select-String -Path .devops/rules/process-lessons.md -Pattern '\[2026-09-16\]'` → **2** matches (the wave-2 `touches` entry + this entry).
> **V5 — prefix + encoding.** `powershell -NoProfile -File scripts/check-parcel-prefix.ps1 -Sync` then, without `-Sync`, `powershell -NoProfile -File scripts/check-parcel-prefix.ps1` → exit `0`, byte-identical prefix; `powershell -NoProfile -File scripts/check-utf8-agents.ps1` → `ALL CLEAN`.
> **V6 — wiki + version ledger.** `python scripts/wiki_lint.py`, `python scripts/wiki_claims.py check`, `python scripts/wiki_coverage_check.py` → exit `0`; the live `machinery-version` literal appears in `.devops/logs/version-history.md`.

### Wiki Core References
- `.wiki/core/18-knowledge-capture.md` → Quick Reference rule 8 (the rule this parcel reverses) and rule 7 (never edit a PREFIX-LOCKED surface directly) — governs steps 5-6 + 9.
- `.wiki/core/00-system-index.md` → the hub's `Last Verified` contract that step 11 stamps.

### Wiki Docs to Add/Edit
- `.wiki/core/18-knowledge-capture.md` — Quick Reference row 8 (one cell).
- `.wiki/core/00-system-index.md` — slot-18 `Last Verified` (one cell).

---

## 6 Phase 6: Grumpy Architect Spec & Logic Audit
**Skill Executed:** `ptp-grumpy-architect` (`SINGLE`: orchestrator inline)

> **`SINGLE` topology:** no independent reviewer. The orchestrator fills the **self-review table** below — one row per acceptance criterion from Phase 4 — and records the verdict line.

> **Rejection Rule:** If the plan does not make the app faster, safer, or easier to modify, do not check boxes. Reject and force rewrite via `PHASE_5_REVISION`.
>
> **Spec-level audit — the plan contains no code, so no code-level scans (DRY/WET, line checks).**

- **System Contracts Explicit:** Yes — every one of the 12 file-level steps names an exact path plus the section/heading/cell it lands in, and the two rule-owning surfaces (`ptp-context-hunter` § 3, `parcel.agent.md` step 7) carry the new clause's exact shape.
- **File Boundary & Scope Collisions:** Four files were outside the plan's declared `touches` when Phase 2 re-verified it (`.opencode/plans/base-context.md`, `.devops/templates/base-context.template.md`, `.devops/agents/parcel.agent.md`, `.devops/skills/ptp-phase3-answerer/SKILL.md`) and two more carry a one-cell wiki correction (`.wiki/core/18-knowledge-capture.md`, `.wiki/core/00-system-index.md`). All six are declared in the claim front-matter and disclosed in Phase 8. `base-context.md` + `base-context.template.md` are shared surfaces later waves also touch, but no other plan is claimed in `.devops/plans/`, so the overlap predicate is satisfied at claim time.
- **Dependency Gaps:** None — `check-parcel-prefix.ps1 -Sync` already regenerates the prefix and the `ptp-*` skill embeds; no new script, dependency, or build step is introduced.
- **YAGNI Bloat:** Audited and trimmed. The design deliberately *deletes* the duplicated `Interactive Interrogation` bullet rather than adding a second rule. **Rejected as speculative:** a runtime capability probe, a config flag to select the mode, a new "questionnaire" block in the plan file, and any change to `q-and-a` / `wiki-bootstrap` / `wiki-assessment` (their one-at-a-time wording is their own contract, not Phase 3's relay). **Kept despite `SINGLE` having no such surface:** nothing.
- **Paranoid Security (Contract Level):** Not applicable — no secrets, no trust-boundary input, no DB. The change is a relay-format rule over markdown instructions.
- **Survivability:** The only mechanical operation is `-Sync`, which is idempotent and verified both ways (with and without the flag). A failed `-Sync` fails closed — the repo gate exits non-zero.
- **Edge Cases:** (i) a single-question surface → silent, complete sequential fallback (no dropped/merged/reordered question); (ii) the final confirm must not become a questionnaire row → a standalone clause in § 3/§ 5 **and** the orchestrator's step 7; (iii) a delegated subagent without an ask tool → it drafts and returns; the **orchestrator** owns the relay mode; (iv) the `AUTO` batch path has no user → the rule is scoped to the interactive relay and `ptp-parcel-fast` / `ptp-phase3-answerer` behaviour is untouched; (v) this parcel's own Phase 2 **quotes** the old rule → the sweep targets live homes, and V1's path list excludes `.devops/plans/**` and `.devops/archive/**` so the quotation is not a false positive; (vi) `sprint-9`'s later waves edit `base-context.md` / `template-plan.md` too → the overlap clause serialises them, already handled by the sprint's six-wave decomposition.
- **Performance Trade-offs:** None — no runtime path. Scoring edges: the user-visible change *reduces* Phase 3 round-trips from N to 1 where the surface supports it.
- **Architectural Anti-Patterns:** No duplicate source of truth is created — the total number of statements of the rule **falls** (from seven live places to six, and § 3's internal duplication is merged). No god module, no dead-end abstraction, no feature bleed.
- **Endpoint Protection & Rate Limiting:** Not applicable.
- **Wiki Core Compliance:** The Phase 4 rationale names both wiki cells edited and why the machinery rule is *staged* in `process-lessons.md` rather than in the wiki; `.wiki/core/18-knowledge-capture.md` rule 7 governs the prefix path.

**Self-review checkpoint (row per acceptance criterion):**

| AC # | Criterion met? | Evidence / gap |
|---|---|---|
| 1 | Yes | Phase 5 step 1 fixes the single authoritative relay bullet in § 3; V2 asserts the `question array` clause is present in the skill and in `parcel.agent.md`. |
| 2 | Yes | Phase 5 step 1 deletes the `Interactive Interrogation` duplicate and merges its ordering clause into the mode bullet; V1 asserts 0 matches for `One Question At A Time (non-negotiable)`. |
| 3 | Yes | Phase 5 step 1 adds the standalone-confirm clause in § 5 with the prompt block byte-identical; `parcel.agent.md` step 7 carries the same clause. |
| 4 | Yes | Phase 5 steps 2-8 name all seven restating surfaces; V1 runs a single multi-path grep over them and expects 0 matches. |
| 5 | Yes | No step touches `ptp-parcel-fast`'s hard rule or `ptp-phase3-answerer` § 9; V3 asserts the hard rule still matches. |
| 6 | Yes | Phase 5 steps 5-6 edit the prefix at its canonical home plus the seed mirror; step 9 runs `-Sync` then verifies without it (V5). |
| 7 | Yes | Phase 5 step 8 defines the register entry in the register's own format; V4 asserts the dated entry is present. |
| 8 | Yes | Phase 5 steps 10-11 correct KC rule 8 and stamp the hub's slot-18 freshness cell. |
| 9 | Yes | Phase 5 steps 12-13 read the live `machinery-version` and prepend the ledger row; V6 asserts the literal is present. |

**Audit findings (raised at audit time; recorded here with their disposition):**
- **F1 (non-blocking, disclosed):** five authored surfaces were outside the plan's declared `touches`. Disposition: `touches` amended in the claim front-matter and each addition disclosed in Phase 8 — the wave-2 register entry predicts exactly this and warns against silently narrowing instead. The fifth (`.devops/agents/ptp-context-hunter.subagent.md`'s relay hard rule) was found **during Phase 9** by V1, which is precisely why V1 runs over the agent wrappers and not only over the skills.
- **F2 (non-blocking, disclosed):** `.wiki/core/18-knowledge-capture.md` and `00-system-index.md` are wiki edits inside a machinery parcel. Disposition: KC rule 8 is a **live rule row** that would otherwise assert the opposite of the skill; the index cell would otherwise understate the doc's freshness. Both are one cell, and the register still owns the rationale.
- **F3 (non-blocking, disclosed):** the two wiki cells keep `status: stable` instead of `in-progress`. Disposition: doc and machinery land in the same commit, so no pre-code window exists; a `type: core` doc read on every parcel should not be marked "not ready for use". Recorded for the batch wrap-up.
- **F4 (non-blocking, disclosed):** `ptp-phase3-answerer` was named Out of Scope in Phase 1, yet its § Activation clause restates the relay. Disposition: behaviour unchanged (still resolves from the Research Map, never asks); one descriptive clause corrected as a perimeter extension, boxing-tested against the same-clause/same-phrase/same-fix test.
- **F5 (observation, untouched — outside this parcel):** `T1-E3.07` left three `AUTO`-wording residuals — `pass-the-parcel/SKILL.md:86` and `plan-lifecycle.md:70` still say `AUTO` clears "on mechanical verification", and `parcel.agent.md:143` still quotes the **old negative test** ("outputs present, no `REJECTED` verdict, no `Unresolvable:` entries"). They contradict the canonical contract `T1-E3.07` landed. Not this parcel's write set; flagged below for the batch wrap-up.
- **F6 (observation, untouched):** `.devops/backlog/t1-parcel-pipeline-machinery-backlog.md:54` still labels this plan "Wave 2" and `QUEUED`. Register bookkeeping belongs to the wrap-up's backlog sweep.

**Dead-Code / Orphan Flags (for Wrap Up backlog entry):**
| plan section / file_path:line_number | Reason |
|---|---|
| `.devops/skills/pass-the-parcel/SKILL.md:86` | Says `AUTO` auto-clears "on mechanical verification" — superseded by `.devops/rules/plan-lifecycle.md` § AUTO Gate Evidence Contract (left by `T1-E3.07`). |
| `.devops/rules/plan-lifecycle.md:70` | Same stale `AUTO` phrase (left by `T1-E3.07`). |
| `.devops/agents/parcel.agent.md:143` | Still quotes the retired negative auto-clear test; the canonical contract replaced it (left by `T1-E3.07`). |

**Required Fixes:**
> [x] F1 — `touches` amended (claim front-matter).
> [x] F4 — `ptp-phase3-answerer` reworded as a disclosed perimeter extension.
> [x] None further — F2/F3/F5/F6 are disclosed or routed, not silent.

**Verdict:** PASS

---

## 7 Phase 7: Smooth Operator Product Review
**Skill Executed:** `ptp-smooth-operator` (`SINGLE`: orchestrator inline)

> **`SINGLE` topology:** no independent reviewer. Phase 7 is recorded as `N/A — SINGLE self-review`; the self-review above is presented at Gate B.

> **Rejection Rule:** If plan introduces unnecessary complexity or scope expansion, do not check boxes. Reject and force rewrite.

- **Vision & Journey Integrity:** The change shortens the one interaction where a user is a *bottleneck* — Phase 3 clarification — without widening the decision set, the cap, or the gate. The user still answers the same 5-8 questions and still gives the same final standalone confirmation.
- **Scope Containment (No Gold Plating):** The parcel **deletes** a duplicated rule statement rather than adding machinery. The six surfaces beyond the plan's original list are the same clause, same phrase, same fix (F1/F2/F4) — not new features. Front-loading into `@sprint-plan`, the `AUTO` path, the question budget, and the `q-and-a` / wiki-bootstrap skills are all deliberately left alone.
- **4 Core User States:** Not applicable (no UI). The relay's analogous states are all defined: batched surface available / single-question surface (sequential fallback) / delegated subagent (drafts and returns) / `AUTO` (no user, no ask) — no undefined fifth state, and no state in which a question can be silently lost.
- **Guardrails & Permissions:** Gate A remains a hard stop in both modes; the final validation stays the precondition for advancing the plan's status; no permission or preset is altered; the batch path's `task`/tool lockdown is untouched.
- **Mobile / A11y / Telemetry:** Not applicable — machinery docs only.

**Required Fixes:**
> [x] None.

---

> **AUTO gate record (2026-09-16, `ptp-parcel-fast`).**
> - **Gate B → `APPROVED`** by presence test over Phases 4-5 + the Phase 6 self-review: **P2** clear; **P1** clear within the evidence blocks (the only unchecked boxes are the Phase 5 `### To-Do List`, the named forward-work exemption); **9** acceptance-criterion rows each carrying a criterion *and* a `Test Target`; **12** Phase 5 file-level steps naming paths (13 numbered implementation instructions); the Phase 6 self-review carries **9** rows, one per acceptance criterion, with both cells filled, and the verdict line reads `PASS`.
> - **Gate C → `N/A (SINGLE)`** — no independent reviewer exists in this topology; the Phase 6 self-review replaces it for this run (per the contract's `SINGLE` branch).

---

## 8 Phase 8: Execute Changes
**Skill Executed:** `ptp-code-surgeon`

**Execution Isolation:** Gate B cleared under the locked `AUTO` preset; in `SINGLE` this also covers Gate C (`N/A`). Gate D is untouched and remains `OPEN` for the human.

**Single-Pass Direct-to-Disk:**
> [x] Implementation written DIRECTLY to source files - no intermediate Markdown code blocks, no drafting files
> [x] Touched only intended lines - no adjacent refactors
> [x] Cleaned up only owned orphans
> [x] Pre-existing dead code left untouched

> [x] Step 1: `.devops/skills/ptp-context-hunter/SKILL.md` — § 3's three bullets (the `One Question At A Time (non-negotiable)` rule, the duplicate `Interactive Interrogation` rule, and the `Delegated runs` relay sentence) replaced by **one** `**Questionnaire Mode (surface-driven):**` bullet + the unchanged budget bullet + a corrected `Delegated runs` bullet; § 5 gains a `**Standalone confirm:**` bullet; `version` 5→6, `updated` 2026-09-13→2026-09-16.
> [x] Step 2: `.devops/skills/pass-the-parcel/SKILL.md` — GROUP A Phase 3 relay bullet + the Phase 3.5 `Unresolvable:`-fallback bullet reworded to the questionnaire mode; `version` 17→18.
> [x] Step 3: `.devops/skills/ptp-phase3-answerer/SKILL.md` — § Activation's USER-MANAGED relay clause reworded (behaviour unchanged); `version` 2→3.
> [x] Step 4: `.devops/plans/template-plan.md` — Gate A halt note's relay sentence reworded.
> [x] Step 5: `.opencode/plans/base-context.md` ORCHESTRATOR-ONLY delegation-map `1-3` row reworded; `.devops/templates/base-context.template.md` mirrored cell-for-cell.
> [x] Step 6: `.devops/agents/parcel.agent.md` — workflow step 7 relay instruction and step 8 fallback reworded (own content only; the prefix region was not hand-edited).
> [x] Step 7: `.devops/rules/process-lessons.md` — one dated reversal entry prepended to `## Parcel & Sprint Mechanics`.
> [x] Step 8: `.wiki/core/18-knowledge-capture.md` Quick Reference rule 8 corrected; `.wiki/core/00-system-index.md` slot-18 `Last Verified` → `2026-09-16`.
> [x] Step 9: `check-parcel-prefix.ps1 -Sync` — regenerated the 4 affected agents (`parcel.agent.md` + `parcel-sprint.agent.md` prefix row; `ptp-context-hunter.subagent.md` + `ptp-phase3-answerer.subagent.md` embeds), then verified without the flag at exit `0`.
> [x] Step 10: `.devops/sync-manifest.yaml` `machinery-version` 50→51 (read live, not from this plan); `.devops/logs/version-history.md` row prepended.
> [x] Step 11 (found during Phase 9, fixed, disclosed): `.devops/agents/ptp-context-hunter.subagent.md` — the agent wrapper's **unique** hard rule still said "the orchestrator relays them to the user **one at a time**". V1 caught it because V1 scans the agent wrappers, not only the skills. Reworded to point at § 3.

**Perimeter extension (disclosed, boxing-tested).** Five authored surfaces were outside the plan's original `touches`, each because it **restates the same clause** the parcel reverses — the boxing test is *same clause, same phrase, same fix*, not a tangent or a secondary cleanup:
1. `.opencode/plans/base-context.md` + `.devops/templates/base-context.template.md` — the ORCHESTRATOR-ONLY delegation-map row ("orchestrator asks one at a time"). It is read by every orchestrator as the rulebook; leaving it would fork the second dialect `T1-E3.07` removed. Edited at the canonical home only, then `-Sync`.
2. `.devops/agents/parcel.agent.md` — workflow step 7 is the instruction the **actor that relays** follows; without it the named feature cannot function at all.
3. `.devops/agents/ptp-context-hunter.subagent.md` — the wrapper's own relay hard rule (found by V1 in Phase 9).
4. `.devops/skills/ptp-phase3-answerer/SKILL.md` — one descriptive clause about the USER-MANAGED relay; behaviour (resolve from the Research Map, never ask) is untouched.
5. `.wiki/core/18-knowledge-capture.md` + `.wiki/core/00-system-index.md` — KC rule 8 is a **live rule row** that would otherwise assert the opposite of the skill, and the hub's freshness cell would otherwise understate the doc.

Two derived files also changed and are declared: `.devops/agents/parcel-sprint.agent.md` (prefix row) and `.devops/agents/ptp-phase3-answerer.subagent.md` (skill embed) — regenerated by `-Sync`, never hand-edited. **Not** touched, deliberately: `ptp-parcel-fast` (skill or agent), `ptp-phase3-answerer` § 9, the question budget, `q-and-a` / `wiki-bootstrap` / `wiki-assessment` (their own sequential-interview contract), `.wiki/core/18-knowledge-capture.md`'s Decision Archive entry and `.wiki/examples/parcel-walkthrough-machinery-hardening.md` (historical records — never retro-edited), and the three `T1-E3.07` `AUTO`-wording residuals (F5).

---

## 9 Phase 9: Verify Changes
**Skill Executed:** `ptp-code-surgeon`

**Build & Lint:**
> [x] Compilation check — N/A (no compiled source in this workspace)
> [x] Lint clean (exit 0) — `python scripts/wiki_lint.py`
> [x] Type-generation re-run if schema/API changes — N/A

**Test Report:**
- [x] Lint pass (exit 0) — `wiki_lint.py` OK
- [x] Tests pass (exit 0) — `wiki_claims.py check` OK; `wiki_coverage_check.py` OK (no `src/` tree); `wiki_visualize.py --check` OK
- [x] Build pass (exit 0) — prefix gate + encoding gate both exit 0
- [x] Code matches exact plan specifications — every command below exits 0
- [x] No functional gaps identified

**Verification Evidence (required — the checkbox list alone is not evidence):**
| # | Command | Expected | Observed | Exit |
|---|---|---|---|---|
| 1 | `Select-String -CaseSensitive -Path <the 11 live surfaces> -Pattern 'One Question At A Time \(non-negotiable\)\|Never batch multiple questions\|orchestrator asks one at a time\|one at a time via the ask-questions tool\|relays them to the user \*\*one at a time\*\*'` (V1) | 0 matches | **0** matches. (`-CaseSensitive` is load-bearing: without it the batch host's "runs each eligible plan … one at a time in a fresh context" — about *plans*, not questions — is a false positive.) | 0 |
| 2 | `Select-String -Path .devops/skills/ptp-context-hunter/SKILL.md -Pattern 'Questionnaire Mode \(surface-driven\)'` and `… -Path .devops/agents/parcel.agent.md -Pattern 'questionnaire mode'` (V2) | 1 and ≥1 | **1** in the skill; **2** in `parcel.agent.md` (steps 7 and 8) | 0 |
| 3 | `Select-String -Path .devops/agents/ptp-parcel-fast.subagent.md -Pattern 'Never call the ask-questions tool'` (V3) | ≥1 — batch path unchanged | **1** (the rule lives in the agent wrapper, not the skill — see F7) | 0 |
| 4 | `Select-String -Path .devops/rules/process-lessons.md -Pattern '\[2026-09-16\]'` (V4) | 2 | **2** (the wave-2 `touches` entry + this parcel's reversal entry) | 0 |
| 5 | `powershell -NoProfile -File scripts/check-parcel-prefix.ps1 -Sync` (V5) | regenerates the affected surfaces | `EMBED ptp-context-hunter.subagent.md: refreshed` · `FIXED ptp-context-hunter.subagent.md` (plus, on the first pass, `parcel.agent.md` / `parcel-sprint.agent.md` / `ptp-phase3-answerer.subagent.md`) | 0 |
| 6 | `powershell -NoProfile -File scripts/check-parcel-prefix.ps1` (V5, no flag) | byte-identical prefix, bindings agree | `OK: all PREFIX-LOCKED agents share a byte-identical prefix, every binding surface agrees, and the seed surfaces match the live registry.` | 0 |
| 7 | `powershell -NoProfile -File scripts/check-utf8-agents.ps1` (V5) | `ALL CLEAN` | `ALL CLEAN (228 files scanned)` | 0 |
| 8 | `python scripts/wiki_lint.py` | exit 0 | `OK: wiki healthy - 0 broken links, all anchors present, frontmatter valid.` | 0 |
| 9 | `python scripts/wiki_claims.py check` | exit 0 | `claims OK: 0 stale, all grounded sources present.` | 0 |
| 10 | `python scripts/wiki_coverage_check.py` | exit 0 | `coverage OK: no src/ tree in this workspace — nothing to cover` | 0 |
| 11 | `python scripts/wiki_visualize.py --check` | exit 0 | `visualizer OK: 56 docs, 28 edges - docs/wiki-graph.md up to date` | 0 |
| 12 | `Select-String -Path .devops/sync-manifest.yaml -Pattern '^machinery-version:'` + `Select-String -Path .devops/logs/version-history.md -Pattern 'machinery-version: 51'` (V6) | manifest `51`; literal present in the ledger | manifest `machinery-version: 51`; **1** ledger match | 0 |
| 13 | Contract P1/P2 self-test over this plan, after stripping fenced blocks + code spans (V4 analogue) | 0 findings outside the named exemption | **0** bare `TBD`/`TODO`/`FIXME`; **0** line-leading `**REJECTED:**` / `Unresolvable:`; **1** line-leading `**Verdict:**` reading `PASS`; **0** unchecked boxes (all 11 To-Do items ticked) | 0 |
| 14 | `git status --porcelain` + `git diff --numstat` scope review | only declared + disclosed files | **16** files; agent diffs are surgical (1:1, 3:3, 4:4, 1:1 lines — no whole-file rewrite, no EOL churn) | 0 |

**Findings raised during execution (disclosed, not silently absorbed):**
- **F7 (found by V1, fixed):** `.devops/agents/ptp-context-hunter.subagent.md`'s own hard rule still said "the orchestrator relays them to the user **one at a time**". The agent wrapper is *unique* content (not a `-Sync`-derived embed), so it needed a hand edit; V1 covers the wrappers for exactly this reason. Reworded to point at § 3 and declared in `touches`.
- **F8 (test-target defect, fixed):** V3's original command pointed at `.devops/skills/ptp-parcel-fast/SKILL.md`, but the "Never call the ask-questions tool" rule lives in the **agent wrapper**. The plan's test target was wrong, not the code; corrected in Phase 4 AC-5, Phase 3 Q5's `Source:`, Phase 5's V3 and Phase 2's bullet.
- **F9 (found by V1, fixed):** `pass-the-parcel` § Phase 3.5's `Unresolvable:`-fallback bullet also restated the old relay ("fall back to asking the user directly, one question at a time"). Same clause, same fix.
- **F10 (deliberate non-targets, recorded):** `.devops/skills/q-and-a/SKILL.md`, `wiki-bootstrap`, `wiki-assessment` and the batch host's own "one at a time" phrasing (about **plans**) are **not** Phase 3's relay rule; `.wiki/core/18-knowledge-capture.md`'s Decision Archive entry and `.wiki/examples/parcel-walkthrough-machinery-hardening.md:33` are historical records. None edited; `q-and-a` is named as out of scope in Phase 6.

---

## 10 Phase 10: User Review & Tweaks
**Skill Executed:** `knowledge-capture`
> **Parked — captured at execution time.**

---

## Completion Note (Wrap Up)
**Skill Executed:** `agent-wrap-up`
> **Parked — completed at wrap-up.**

---

## 📍 State & Gates (CACHE-ANCHORED — update ONLY this section at gate transitions)

| Metric | Value |
|---|---|
| **Status** | `PHASE_9` |
| **Version** | `v0.1.0` |
| **Active Persona** | `Executor` |
| **Depends On** | none (canonical: claim front-matter `depends_on`) |
| **Blocks** | none |

> **Settings pointer:** `Mode` and `Agents` live in the frozen **Plan Settings** block at the TOP of this file. This run's preset was written by `ptp-parcel-fast` at claim time: `Mode=AUTO`, `Agents=SINGLE`.

| Gate | Requirement | Status |
|---|---|---|
| A | Scope approved (Phases 1-3, + 3.5 in AUTO) | `APPROVED (AUTO)` — Phase 3.5 resolved all five questions from the Research Map; no `Unresolvable:` entry; P1/P2 clear; the final-validation verdict is recorded |
| B | Spec & plan approved (Phases 4-5) | `APPROVED (AUTO)` — 9 acceptance criteria each with a test target; 12 file-level steps; the Phase 6 self-review carries 9 rows and `**Verdict:** PASS`; P1/P2 clear |
| C | Peer reviews passed (Phases 6-7) | `N/A (SINGLE)` — no independent reviewer in this topology; the Phase 6 self-review row stands in for it |
| D | Implementation verified (Phases 8-9) | `OPEN` — awaiting the operator's consolidated Gate D verdict (batch Sprint 9, wave 3/6) |

> **Batch-path terminal state:** `Status: PHASE_9`, `claim_status: GATE_D_USER_APPROVAL`, Gate D `OPEN`. Retirement (Phase 10, Completion Note, archive) is a **separate, operator-invoked** step after the consolidated human verdict — this run never archives and never marks the plan `COMPLETE`.

> Rejection routes: A → `PHASE_1`; B/C → `PHASE_5_REVISION`; D/rollback → `PHASE_8_FAILED`. Parked items carry `Status: QUEUED` until `@sprint-plan` commits them.
