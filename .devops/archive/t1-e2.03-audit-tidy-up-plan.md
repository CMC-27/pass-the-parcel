---
code: T1-E2.03
sprint: ""
claim_status: COMPLETE
owner: parcel-fast
claimed_at: "2026-09-13"
last_touch: "2026-09-13"
touches: ["scripts/**", ".devops/sync-manifest.yaml", ".devops/agents/**", ".devops/templates/**", ".devops/skills/pass-the-parcel/**", ".devops/skills/sync-architecture/**", ".devops/skills/model-routing/**", ".devops/rules/**", ".devops/backlog/**", ".devops/logs/**", ".opencode/plans/base-context.md", "HOW-TO.md", "CHANGELOG.md"]
depends_on: []
---
# Parcel Plan: T1-E2.03 - Pre-satellite-sync audit tidy-up (F1-F7) + machinery 40

## Theme-Epic: T1 - Parcel Pipeline Machinery, E2 - Governance & Transport Integrity

## ⚙️ Plan Settings (FROZEN — set at plan start, read before any phase)

| Setting | Value | Meaning |
|---|---|---|
| **Mode** | `AUTO` | Locked preset (`parcel-fast`). Gates A–C auto-clear on mechanical verification; Gate D always halts. |
| **Agents** | `SINGLE` | Locked preset. Orchestrator plays each persona inline; no `task` spawns. Group C skipped (inline self-review); Gates B+C merge at Gate B (Gate C `N/A`). |

> **Preset provenance:** set from the `parcel-fast` **Orchestrator Presets** row (`.opencode/plans/base-context.md`). Selection questions skipped. Trunk-direct deviation: this repo has no ACTIVE sprint and no `.devops/sprints/`, so the claim is trunk-sequential (no `git worktree add`) — same deviation class as `@sprint-run`, recorded here.

> **RULES:**
> - Claim front-matter at the top governs claiming; `claim_status` is NOT the bottom `Status`.
> - Halt points are HARD STOPS. In `AUTO`, Gates A–C auto-clear on mechanical verification; **Gate D always halts.**
> - Persona matches Status. Read **Plan Settings** above before acting.

> **🔒 CACHE-ANCHORED:** the Plan Settings block above is frozen; mutable State Dashboard + Gate Log live in the last section (`## 📍 State & Gates`).

---

## 1 Phase 1: Expansion & Scoping
**Skill Executed:** `ptp-context-hunter` (`SINGLE`: inline)

**Intent:** Close the seven findings from the pre-satellite-sync architecture audit (F1–F7) and cut machinery-version 40, so the portable surface can be pushed to satellites cleanly.

**In Scope:**
- F1 — sync propagates *new* Model Registry rows to an existing satellite's `base-context.md` (plus migration note).
- F2 — `prune_files` gains the retired `.devops/agents/parcel-compactor.md`.
- F3 — `check-utf8-agents.ps1` scans `.devops/rules/**` + `.devops/templates/**`.
- F4 — T1 theme register records completed `T1-E1.03`.
- F5 — `MATURITY.md` reassessed for machinery 38–40.
- F6 — root `CHANGELOG.md` "Earlier releases" filled (`v0.7.6`, `v0.7.8`, `v0.7.9`, `v0.7.10`, `v0.7.11`).
- F7 — reconcile the Gate-A-in-AUTO contradiction (CW2): "AUTO auto-clears Gates A–C; only Gate D always halts".
- Bump `machinery-version` 39 → 40 + version-history + changelog + affected skill/seed versions.

**Out of Scope:**
- Splitting `check-parcel-prefix.ps1` (T1-E2.02, parked).
- Any change to the pipeline's phase structure, gate set, or delegation map beyond F7 wording.
- Satellite-side edits (this release only changes the template surface).

**Perimeter Notes:** All findings are template-surface (portable machinery, seeds, registers). F1/F3 change the sync engine + a guard script → `machinery-version` bump required. F7 edits the PREFIX-LOCKED prefix → `-Sync` + re-verify required.

---

## 2 Phase 2: Requirements & Context
**Skill Executed:** `ptp-context-hunter` (`SINGLE`: inline)

**Forensic Context Inventory:**
> [x] Wiki core docs read (`.wiki/core/00-system-index.md`, `18-knowledge-capture.md`)
> [x] Knowledge capture read (`.wiki/core/18-knowledge-capture.md`)
> [x] Source verified — `scripts/sync-architecture.ps1` (`Update-TargetModelBindings`), `scripts/check-utf8-agents.ps1` (`$targets`), `.devops/sync-manifest.yaml`, `base-context.md`, seed mirrors, skills, registers

**Relevant Existing Decisions (from knowledge capture):**
- T1-E1.01 said "binding is satellite configuration"; **reversed** by T1-E1.03 (registry is the single source, sync force-stamps). F1 completes the clone contract for *new* keys.
- T1-E3.02 CW2 recorded the Gate-A contradiction as an observation, deferred non-blocking, to be resolved if it became blocking. F7 resolves it.

**Relevant Docs Found:** `SATELLITE-BOOTSTRAP.md` (migration note), `model-routing/SKILL.md` §3, `sync-architecture/SKILL.md`, `.devops/rules/agents-and-skills.md` § Sync Protocol.

**Relevant Code Found:** `Update-TargetModelBindings` rewrites existing rows only (lines 151–171) — reproduces `TARGET_CHECK_EXIT=1` on a pre-v39 satellite.

---

## 3 Phase 3: User Clarification
**Skill Executed:** `ptp-context-hunter` (`SINGLE`: inline)

- Q1: F1 fix — append missing rows in sync, or docs-only? → **Sync appends missing rows + doc note.**
- Q2: Include F7 (Gate-A contradiction)? → **Yes — reconcile.**
- Final validation: "Is this all the context required?" → **Yes** (user answered both forks; no further questions).

**Architectural Conflict Warnings Raised:**
- F7 is the recorded **CW2** conflict (Gate A "always halts" vs `AUTO` auto-clears A–C). Resolution requires prefix regen — flagged and accepted.

---

> **HALT POINT (Gate A — Scope):** Phases 1-3 complete (+ 3.5). Set Status -> `PHASE_3`, Active Persona -> `Scoper`. **AUTO: Gate A auto-cleared** — user supplied both design forks and the mandate; mechanical verification passed.

---

## 4 Phase 4: Wiki Requirements & Acceptance Criteria
**Skill Executed:** `ptp-high-visionary` (spec-first) (`SINGLE`: inline)

> **Conditional Skip:** a wiki delta IS in scope — `.wiki/core/18-knowledge-capture.md` and/or the walkthrough may need the F1/F7 clarification. Rationale for partial skip below.

**Wiki Docs to Write/Update (as `in-progress`):**
| Doc | Change |
|---|---|
| — | No new wiki doc. F6/F5 are `.devops/` state; F7 wording is machinery. Any KC note handled at Wrap Up. |

**Acceptance Criteria:**
| # | Criterion (behavior) | Test Target |
|---|---|---|
| 1 | A satellite whose `base-context.md` lacks a registry row gains it after sync and passes its own `check-parcel-prefix.ps1` | temp-target repro (was exit 1) |
| 2 | `-SelfTest` still OK (materialise + prune + binding propagation + post-sync `-Check`) | `sync-architecture.ps1 -SelfTest` |
| 3 | `check-utf8-agents.ps1` scans `.devops/rules/**` + `.devops/templates/**` and stays ALL CLEAN | gate exit 0 |
| 4 | Prefix PASS ×10 + MODEL ×12 after F7 + `-Sync` | `check-parcel-prefix.ps1` |
| 5 | `machinery-version: 40` present in `sync-manifest.yaml` and recorded in `version-history.md` | CI discipline block |
| 6 | T1 register lists `T1-E1.03`; `CHANGELOG` lists v0.7.6/8/9/10/11; `MATURITY` has a machinery 38–40 row | read-back |
| 7 | No live surface says "Gate A ... always halt" | grep |

**Spec Notes:**
- Data flow: template registry (source) → `Update-TargetModelBindings` → target rows (rewrite + **insert**) → target prefix regen → target `check-parcel-prefix`.
- Edge cases: target registry absent (skip, unchanged); target already has all keys (no writes); insertion anchor = last registry-pattern row inside/after the table.

---

> **NO HALT after Phase 4:** spec reviewed with the plan at Gate B.

---

## 5 Phase 5: High-Visionary Standard Implementation Plan
**Skill Executed:** `ptp-high-visionary` (`SINGLE`: inline)

**Simplicity Gate:**
> [x] Climbed the Simplicity Ladder — edit existing surfaces, no new files except the plan + registers
> [x] No speculative abstractions
> [x] Reuse over reimpl — extend `Update-TargetModelBindings`, extend the existing `$targets` list
> [x] Deliberate simplifications marked `ponytail:` (row insert anchored on the last registry row; no table re-layout)
> [x] Safety exceptions preserved (prefix contract, Gate D human sign-off)

**Reuse Log:**
| Existing Asset | Where Reused | Ladder Rung |
|---|---|---|
| `Get-RegistryBindings` | extended to capture the capability-class cell for inserted rows | 2 |
| `Update-TargetModelBindings` step 1 | add missing-row insertion after existing rewrite | 2 |
| `check-utf8-agents.ps1` `$targets` accumulation | append `.devops\rules` + `.devops\templates` | 2 |

**Spaghetti Triage:** none noticed.

### To-Do List
- [x] F1: `sync-architecture.ps1` — append missing registry rows + help/comment; extend `Get-RegistryBindings` with `class`.
- [x] F1 docs: `SATELLITE-BOOTSTRAP.md` migration note (v11) + `model-routing` §3 note (v7) + `sync-architecture` skill (v6) + `agents-and-skills.md` bullet.
- [x] F2: `sync-manifest.yaml` — add `parcel-compactor.md` to `prune_files`; correct the comment.
- [x] F3: `check-utf8-agents.ps1` — add `.devops\rules` + `.devops\templates`; update header.
- [x] F7: reconcile Gate-A wording — `base-context.md` + seed, `pass-the-parcel` (v13), `plan-lifecycle.md`, `HOW-TO.md`, `parcel.agent.md`, `parcel-fast.agent.md`.
- [x] F4: T1 register — add `T1-E1.03` completed row + this plan's row.
- [x] F5: `MATURITY.md` — history row + `last-reviewed`.
- [x] F6: `CHANGELOG.md` — add v0.7.6/8/9/10/11.
- [x] Bump: `machinery-version` 40; `version-history.md` v0.7.11 row; `agent-changelog.md` entry.
- [x] Verify: `-Sync`, all gates, `-SelfTest`, temp-target repro.

### File-Level Steps
1. `scripts/sync-architecture.ps1`
2. `.devops/sync-manifest.yaml`
3. `scripts/check-utf8-agents.ps1`
4. `.opencode/plans/base-context.md` + `.devops/templates/base-context.template.md`
5. `.devops/skills/pass-the-parcel/SKILL.md`, `.devops/rules/plan-lifecycle.md`, `HOW-TO.md`
6. `.devops/agents/parcel.agent.md`, `.devops/agents/parcel-fast.agent.md` (unique content), then `-Sync`
7. docs: `.devops/templates/SATELLITE-BOOTSTRAP.md`, `.devops/skills/model-routing/SKILL.md`, `.devops/skills/sync-architecture/SKILL.md`, `.devops/rules/agents-and-skills.md`
8. registers: `.devops/backlog/t1-parcel-pipeline-machinery-backlog.md`, `.devops/backlog/MATURITY.md`, `CHANGELOG.md`
9. logs: `.devops/logs/version-history.md`, `.devops/logs/agent-changelog.md`

### Implementation Instructions (no code snippets except exact string literals)
- F1 insertion anchor: the last line matching `^\|\s*([a-z0-9-]+)\s*\|` whose key is a registry key. `ponytail:` naive insert after the last present row; a re-laid-out table is not detected (validator fails loud instead).
- F1 new-row literal: `| <key> | <capability-class> | <vscode> | <opencode> |`.
- F7 replacement literal: `Gate D always halts for the human` (drop Gate A from the always-halt clause); keep `AUTO` auto-clears `Gates A-C`.
- F2 prune literal: `.devops/agents/parcel-compactor.md`.

### Wiki Core References
- `.wiki/core/18-knowledge-capture.md` → F1/F7 durable lessons (Wrap Up)

### Wiki Docs to Add/Edit
- None required this plan (state/machinery); Wrap Up evaluates KC admission.

---

> **HALT POINT (Gate B — Spec & Plan Review):** Phases 4-5 complete. Status -> `PHASE_5`, Active Persona -> `High-Visionary`. **AUTO: Gate B auto-cleared** — outputs present, inline self-review clean, no `REJECTED`, no `Unresolvable:`. Gate C recorded `N/A (SINGLE)`.

---

## 6 Phase 6: Grumpy Architect Spec & Logic Audit
**Skill Executed:** `ptp-grumpy-architect` (`SINGLE`: orchestrator inline)

> **`SINGLE` topology:** no independent reviewer. Inline self-review checkpoint.

**Verdict:** `N/A — SINGLE self-review`

- **System Contracts Explicit:** `Get-RegistryBindings` map gains `class`; back-compat for `vscode`/`opencode` consumers (`SelfTest`, binding stamp).
- **File Boundary & Scope Collisions:** F1 touches the sync engine (portable); F7 touches the PREFIX-LOCKED prefix — both require `-Sync` + gate re-run.
- **Dependency Gaps:** none — no new files, no new deps.
- **YAGNI Bloat:** insertion is a few lines; no table parser.
- **Paranoid Security:** no secrets; no network.
- **Survivability:** absent target registry → skip (fail-safe); no writer on `-Check`/`-Verify`/`-DryRun`.
- **Edge Cases:** registry absent (skip); all keys present (no-op); orchestrator-presets table must NOT be mistaken for registry rows (backticked first cell) — verified.
- **Performance Trade-offs:** negligible.
- **Architectural Anti-Patterns:** keeps one source (registry) + derived surfaces; no duplicate.
- **Wiki Core Compliance:** N/A (machinery).

**Dead-Code / Orphan Flags:** `parcel-compactor.md` (→ F2 prune entry).

**Required Fixes:** None.

---

## 7 Phase 7: Smooth Operator Product Review
**Skill Executed:** `ptp-smooth-operator` (`SINGLE`: orchestrator inline)

> **`SINGLE` topology:** `N/A — SINGLE self-review`; presented at Gate B.

- **Vision & Journey Integrity:** resolves a real satellite-onboarding failure; keeps the clone contract honest.
- **Scope Containment:** exactly F1–F7 + bump; no pipeline redesign.
- **4 Core User States:** sync fail-loud path retained (validator), now avoided for new keys.
- **Guardrails & Permissions:** Gate D still halts; no destructive effect on satellites beyond the intended prune.
- **Telemetry:** none.

**Required Fixes:** None.

---

> **HALT POINT (Gate C — Peer Reviews):** Reviews complete. **`SINGLE` topology:** Gate C `N/A (SINGLE)` — approval happens once at Gate B. Status -> `PHASE_7`, Active Persona -> `Reviewer`.

---

## 8 Phase 8: Execute Changes
**Skill Executed:** `ptp-code-surgeon` (`SINGLE`: orchestrator inline)

**Execution Isolation:** triggers after the plan is approved (Gate B in `SINGLE`).

**Single-Pass Direct-to-Disk:**
> [x] Implementation written DIRECTLY to source files
> [x] Touched only intended lines
> [x] Cleaned up only owned orphans (compactor prune entry)
> [x] Pre-existing dead code left untouched

> [x] Step 1-9: all edits applied (see Phase 5 to-do list).

---

## 9 Phase 9: Verify Changes
**Skill Executed:** `ptp-code-surgeon` (`SINGLE`: orchestrator inline)

**Build & Lint:**
> [x] `check-parcel-prefix.ps1` (after `-Sync`) — PASS ×10, MODEL ×12, SEED/OC agreement
> [x] `check-utf8-agents.ps1` — ALL CLEAN (now includes rules + templates)
> [x] `sync-architecture.ps1 -SelfTest` — the transport contract, incl. binding propagation + prune
> [x] `wiki_lint` / `wiki_claims` / `wiki_coverage` / `wiki_visualize --check` — exit 0
> [x] F1 temp-target repro — previously exit 1, now exit 0
> [x] Version discipline — `machinery-version: 40` recorded in `version-history.md`

**Test Report:**
- [x] Lint pass (exit 0)
- [x] Tests pass (exit 0)
- [x] Code matches the plan specifications — every gate re-run; each exits `0`
- [x] No functional gaps identified

**Evidence (2026-09-13):**
- `check-parcel-prefix.ps1` (after `-Sync`) → **PASS ×10**, MODEL ×12, seed registry + seed config agree, exit 0.
- `check-utf8-agents.ps1` → **ALL CLEAN (213 files)**; was 202 → **+11** = 3 `.devops/rules` + 8 `.devops/templates` (F3 proof).
- `sync-architecture.ps1 -SelfTest` → `SELFTEST OK: 5 dirs, 38 skills, 11 files materialised correctly` (F1 engine intact).
- **F1 repro:** pre-v39 target (10 registry rows) → re-sync printed `BINDINGS: inserted 2 new registry row(s): wiki-verifier, wiki-writer` → 12 rows, target gate **exit 0** (was exit 1 pre-fix).
- **F2 repro:** planted `.devops/agents/parcel-compactor.md` → `PRUNE`, `OUT OF SYNC`, exit 1, **no `DRIFT`**.
- `wiki_lint --quiet` / `wiki_claims check` / `wiki_coverage_check` / `wiki_visualize --check` → all exit 0.
- Version discipline: `machinery-version: 40` in `sync-manifest.yaml` **and** recorded in `version-history.md`.
- **F7 grep:** no live surface says "Gate A … always halt" (remaining hits are archive/history + the correct "topology-invariant" phrasing).

---

> **HALT POINT (Gate D — Implementation):** Implementation and verification complete. Status -> `PHASE_9`, Active Persona -> `Executor`. **Gate D `OPEN`** — awaiting user sign-off. On rollback: Status -> `PHASE_8_FAILED`.

---

## 10 Phase 10: User Review & Tweaks
**Skill Executed:** `knowledge-capture`

**Tweak Discipline:**
> Tweaks are surgical, not architectural. `expansion` and `refactor` are HALT conditions.

- [ ] Tweak classified: `fix` / `expansion` / `refactor`
- [ ] `expansion` and `refactor` routed to new parcel
- [ ] `fix` touches <= 3 files
- [ ] `fix` introduces no new abstraction absent from original plan

**Capture Categories:** Recurring / Tribal / One-off / Cosmetic-skip

| Round | Feedback | Action Taken | Capture Flag | Result |
|---|---|---|---|---|
| 1 | No tweaks requested | — | Cosmetic-skip | COMPLETE |

**Sign-Off:** 2026-09-13 - User approved ("wrap up, commit and push").

---

## Completion Note (Wrap Up)
**Skill Executed:** `agent-wrap-up`

> [ ] Read `.wiki/core/18-knowledge-capture.md` for existing related decisions
> [ ] Themed tweaks synced to knowledge capture doc
> [ ] Wiki docs updated per Phase 10 tweaks
> [ ] Code reconciled against Phase 4 spec — deviations logged
> [ ] `status: in-progress` wiki docs promoted to `stable` (or deviation logged)

**Themed Tweaks:** None — approved at Gate D with no tweak rounds.

**Knowledge Capture Entries:** 1 pitfall → `.wiki/core/18-knowledge-capture.md` § Pitfalls: *a force-stamped projection needs an insert path, not just a rewrite path* (F1's generalized lesson). F7 **rejected** by the Admission Gate — a documentation reconciliation already mirrored across five surfaces, covered by the existing "One state machine, five mirrors" rule. Consolidation tidy: +1 entry, no cuts/merges/promotions; KC 91 lines.

**Spec Reconciliation:** Implementation matches the Phase 4/5 spec (F1–F7 + bump). No deviations; the only variance is verification breadth — F1 was proven with a throwaway pre-v39 target in addition to `-SelfTest`.

**In-Progress Promotions:** None — no wiki docs were authored `in-progress` this plan.

**Dead-Code Backlog Entries (from Phase 6):** None. `parcel-compactor.md` is disposed via the `prune_files` manifest entry (F2), not a backlog item.

**Wiki Updates:** `.wiki/core/18-knowledge-capture.md` (pitfall). No other `.wiki/` doc changed (machinery-only release).

**Plan Archiving:** Plan archived to `.devops/archive/t1-e2.03-audit-tidy-up-plan.md` (via `git mv`, no stub).

**Backlog Review:** T1-E2.03 added to the T1 register E2 Open at execution, moved to E2 Completed at wrap-up. T1-E1.03 row backfilled (F4). No pre-existing backlog item resolved by this session; T1-E2.02 (parked) untouched.

---

## 📍 State & Gates (CACHE-ANCHORED — update ONLY this section at gate transitions)

> **Cache rule:** This is the **last section**. Gate transitions mutate ONLY the rows below.

| Metric | Value |
|---|---|
| **Status** | `COMPLETE` |
| **Version** | `v0.1.0` |
| **Active Persona** | `—` |
| **Depends On** | none |
| **Blocks** | none |

> **Settings pointer:** `Mode` and `Agents` live in the frozen **Plan Settings** block at the TOP.

> Valid states: `QUEUED`, `CLAIMED`, `PHASE_1`, `PHASE_3`, `PHASE_5`, `PHASE_5_REVISION`, `PHASE_7`, `PHASE_8_FAILED`, `PHASE_9`, `COMPLETE`.

| Gate | Requirement | Status |
|---|---|---|
| A | Scope approved (Phases 1-3, + 3.5 in AUTO) | `APPROVED` (AUTO auto-clear — mandate + forks confirmed) |
| B | Spec & plan approved (Phases 4-5) | `APPROVED` (AUTO auto-clear — outputs present, self-review clean, no `REJECTED`) |
| C | Peer reviews passed (Phases 6-7) | `N/A (SINGLE)` |
| D | Implementation verified (Phases 8-9) | `APPROVED` |

> Gate flips: rows flip `OPEN` → `APPROVED`/`REJECTED` ONLY after the user's (or AUTO verification's) verdict, recorded by the orchestrator. Rejection routes: A -> `PHASE_1`; B/C -> `PHASE_5_REVISION`; D/rollback -> `PHASE_8_FAILED`.

> **This section is the LAST section in the file.**
