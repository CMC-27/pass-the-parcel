# Parcel Plan: T2-E2.01 - Wiki Grounding & Guard Hardening
## Theme-Epic: T2 - Wiki System & Knowledge Layer, E2 - Grounding & Guard Hardening

> **Skill Architecture:** This template is consumed by the `pass-the-parcel` skill. Each phase delegates to a specialized sub-skill. See the parcel skill's Skill Delegation Map.
>
> **RULES:**
> - Full skeleton required - ALL 10 phases + Wrap Up MUST be present.
> - Halt points are HARD STOPS - each gate blocks all subsequent phases.
> - Persona matches Status - use the State Lifecycle table in pass-the-parcel skill.
> - **Topology:** `Agents: SINGLE` — orchestrator executes each group's persona inline (no `ptp-*` task spawns); Group C is skipped and Gates B+C merge into one approval at Gate B (Gate C recorded `N/A (SINGLE)`).
>
> **🔒 CACHE-ANCHORED:** State Dashboard + Gate Log live in the **last section** of this file (`## 📍 State & Gates`). Gate transitions update ONLY those bottom rows — do NOT edit content above once written. Byte-stable prefix = LLM prefix-cache hits for every downstream agent re-read.

---

## 1 Phase 1: Expansion & Scoping
**Skill Executed:** `ptp-context-hunter` (`SINGLE`: orchestrator inline)

**Intent:**
Make the wiki's grounding machinery *real* rather than demo-level. v0.4/v0.5 shipped the engine (unified `format-version: 1`, Grounded Claims, `@wiki-update`/`@wiki-generate`, OKF export, visualizer) but three things erode it: (1) the rules governance index was left un-catalogued — self-inflicted drift the linter cannot see; (2) only ~2 content docs carry claims, so `wiki_claims.py check` passes trivially; (3) the supporting guards (symbol resolution, generated-artifact freshness, encoding scope) have holes. This parcel closes gaps **G1, G2, G4, G6, G7** from the v1.0.0 reassessment so the claims layer has teeth and cannot silently drift.

**In Scope:**
| ID | Deliverable | Target artefact |
|---|---|---|
| G1 | Re-catalogue `claims.md` in the rules index (both tables) + re-stamp `last-reviewed`; add a linter check that every `.wiki/rules/*.md` (excl. `language/`) is listed in `.wiki/rules/README.md` | `.wiki/rules/README.md`, `scripts/wiki_lint.py` |
| G2 | Ground the core docs (slots 01-18) — add `claims:` entries binding material facts to `source: path#symbol`, then stamp hashes | `.wiki/core/01..18*.md` |
| G4 | `wiki_claims.py check` resolves each `#symbol` against its source file and hard-fails `UNRESOLVED-SYMBOL` | `scripts/wiki_claims.py` |
| G6 | `wiki_visualize.py --check` (fail when `docs/wiki-graph.md` is stale) + CI step; OKF export smoke step in CI | `scripts/wiki_visualize.py`, `.github/workflows/validate.yml` |
| G7 | Extend the UTF-8 guard to root `*.md`, `docs/**/*.md`, `.github/**/*.md` | `scripts/check-utf8-agents.ps1` |

**Out of Scope:**
- **G3 (OKF ingest)** and **G5 (CI docs-PR / refresh automation)** — separate parcel (`T2-E2.02`, to be created).
- **G8 (frontmatter.md "one schema" wording)** — deferred doc-truth nit; the exemption is intentional, only the prose overstates.
- **G9-G12** (v1.0.0 tag, backlog numbering note, AGENTS.md branding, GitHub Pages) — release-polish parcel.
- **Per-symbol hashing** — whole-file hash stays; the documented `ponytail:` ceiling is accepted for now.
- Adding claims to non-core docs (features/components/logic indices are empty template stubs — no material facts to bind yet).
- Changing `wiki_lint.py` `REQUIRED_FIELDS` exemptions themselves.

**Perimeter Notes:**
- **AUTO mode + SINGLE topology.** Gates A-C auto-clear after mechanical verification; **Gate D always halts for the human**. Group C is skipped (Gate C `N/A (SINGLE)`).
- Scripts are **portable surface** (`sync-manifest.yaml` `portable_files`) — any change requires a `machinery-version` bump **and** a `version-history.md` release row, or the CI version-discipline gate fails.
- CI steps must preserve the graceful no-op path: `wiki_coverage_check.py` no-ops without `src/`; new steps must not assume an app tree.
- G7 may surface pre-existing mojibake in newly-scanned files (root `*.md`, `docs/`, `.github/`). Those must be fixed within this parcel or CI breaks.
- G2 must not touch `.wiki/rules/**` claims already present (frontmatter.md, claims.md, structure.md, hub) except to re-stamp hashes if source changed.

---

## 2 Phase 2: Requirements & Context
**Skill Executed:** `ptp-context-hunter` (`SINGLE`: orchestrator inline)

**Forensic Context Inventory:**
> [x] Wiki core docs read (`.wiki/core/00-system-index.md` + slots 01-18 scanned)
> [x] Knowledge capture read (`.wiki/core/18-knowledge-capture.md` + `version-history.md` v0.3.13/v0.4.0/v0.5.0 rows)
> [x] Source code verified (scripts, `.github/workflows/validate.yml`, `.wiki/rules/`)
> [x] Blast radius bounded — 5 scripts + 1 workflow + 1 rules index + ~18 core docs

**Relevant Existing Decisions (from knowledge capture / version-history):**
- v0.3.13 added frontmatter `related-to`/`dependencies` link checks + hub→spoke + `[UNINDEXED]`/`[MISSING]` + BFS reachability to `wiki_lint.py`; hub-reachability **exempts `.wiki/rules/**`** — this exemption is why G1 slipped through.
- v0.3.10 made `wiki_lint.py` hard-fail on BOM/UTF-16/non-UTF-8; separate byte guard is `check-utf8-agents.ps1`.
- v0.4.0 unified frontmatter to `format-version: 1`, added claims, `@wiki-update`/`@wiki-generate`, OKF export, `docs/` visualizer.
- v0.5.0 upgraded `wiki_coverage_check.py` to a four-route evidence OR, incl. `claims: source` inversion (lazy `wiki_claims` import behind the `src/`-absent no-op guard).
- Claims reporting convention: **stdout-only, exit codes are the contract** (matches `wiki_lint.py`); no committed report files.

**Relevant Docs Found:**
| Path | Finding |
|---|---|
| `.wiki/rules/README.md` | **G1 drift:** `claims.md` missing from *When to Consult* (lines ~17-27) AND *Rule Files* (lines ~31-40) tables; footer still `last-reviewed: 2026-08-19` |
| `.wiki/rules/frontmatter.md` | Defines the unified schema; states "one frontmatter schema" (overstated — see G8, out of scope) |
| `.wiki/rules/claims.md` | Canonical claim shape: `id` / `source: path#symbol` / `hash: sha256:<whole-file>`; documents the symbol-trust ceiling (G4) |
| `.wiki/rules/structure.md` | Structure manifest; claims/areas anchors |
| `.wiki/core/00-system-index.md` | Hub; carries 1 claim (`hub-links-every-spoke` → `wiki_lint.py#category_indexes`); `Last Verified` column mostly `—` |
| `.wiki/core/05,09,12,14,15` | Priority grounding targets (architecture, design system, security, testing, AI) |

**Relevant Code Found:**
| Path | Finding |
|---|---|
| `scripts/wiki_lint.py` (~18 KB) | `REQUIRED_FIELDS = ["name","type","status","format-version"]` (line 38); exemption via `FM_EXEMPT_PREFIXES` / `is_fm_exempt_name` (~line 285). G1 check hooks here. |
| `scripts/wiki_claims.py` (~8.8 KB) | `check` verifies file presence + whole-file hash; **does not resolve `#symbol`** (G4). Imports `wiki_lint`. |
| `scripts/wiki_visualize.py` (~3.2 KB) | Regenerates `docs/wiki-graph.md`; **no `--check`/freshness mode** (G6). |
| `scripts/wiki_okf.py` (~3.7 KB) | `export` only (one-way projection); smoke-verified (56 concepts, exit 0) (G6). |
| `scripts/check-utf8-agents.ps1` (~2.3 KB) | Target list (lines 12-20) covers agents, skills, plans/archive/backlog/logs, `.wiki/**`; **missing root `*.md`, `docs/`, `.github/`** (G7). |
| `.github/workflows/validate.yml` (65 lines) | 8 steps incl. claims drift; **missing visualize freshness + OKF smoke** (G6). |
| `.devops/sync-manifest.yaml` | `machinery-version: 26`; new/changed scripts already in `portable_files` |

**Empirical findings (carried from reassessment):**
- Claims in the wild: hub (`00-system-index.md`), `frontmatter.md`, `claims.md`, `structure.md` — i.e. **1 content doc + 3 rule docs**. `wiki_claims.py check` → `0 stale` (trivially true).
- `docs/wiki-graph.md` (128 lines) exists and is committed; generated content is valid UTF-8 but **outside the encoding guard**.
- All deterministic gates currently green: `wiki_lint` 0, coverage 0, claims 0 stale, utf8 175 clean, prefix PASS ×7, JSON OK, machinery-version 26 recorded, OKF export OK.

---

## 3 Phase 3: User Clarification
**Skill Executed:** `ptp-context-hunter` (`SINGLE`: orchestrator inline) → auto-resolved in `AUTO` mode (`ptp-phase3-answerer` inline)

> In `AUTO` mode the pending questions are drafted here and auto-resolved from the Research Map (§3.5). `Unresolvable:` entries would hard-halt to the user; none arose.

> [x] Q1: **Grounding breadth** — how many core docs get claims, and how many per doc?
> [x] Q2: **Symbol resolution severity** — hard fail or advisory when a `#symbol` does not resolve?
> [x] Q3: **Visualizer `--check` semantics** — fail on stale output, or warn only?
> [x] Q4: **UTF-8 guard scope** — add root `*.md` + `docs/` + `.github/`?
> [x] Q5: **Rules-index completeness** — enforce in `wiki_lint.py`, or just fix the file once?
> [x] Q6: **Version discipline** — bump `machinery-version` + add a `version-history.md` row?
> [x] Final validation: "Is this all the context required?" -> **Yes** (AUTO auto-resolution; no `Unresolvable:`)

**Auto-Resolutions (AUTO mode):**
| Q | Resolution | Rationale |
|---|---|---|
| Q1 | Slots **01-18**, priority **05, 09, 12, 14, 15**; cap **1-3 material claims/doc** | Grounds the docs an agent actually consults; avoids claim spam |
| Q2 | **Hard fail** (`UNRESOLVED-SYMBOL`, exit 1) | A claim that names a non-existent symbol is a false fact — deterministic gates beat conventions |
| Q3 | **Fail** when regenerated output differs from on-disk `docs/wiki-graph.md` | Matches the repo's "deterministic checks beat conventions" principle |
| Q4 | **Yes** — add root `*.md`, `docs/**/*.md`, `.github/**/*.md` | These files are agent-read and were added by v1.0.0; the guard must cover them |
| Q5 | **Enforce** in `wiki_lint.py` (hard fail) + fix the file once | Prevents recurrence — the exact class of drift that created G1 |
| Q6 | **Yes** — bump `machinery-version` (26 → 27) + row in `version-history.md` | CI version-discipline gate requires it |

**Architectural Conflict Warnings Raised:**
- G7 will scan previously-unscanned files; if any contain mojibake, CI fails until fixed — budget a fix step.
- G4 rejecting unresolved symbols may invalidate existing claims if a `#symbol` is wrong today (e.g. `wiki_lint.py#category_indexes`). Verify all 5 existing claims resolve before turning the check on.

**Test Proposals (TDD):**
| # | Probe | Steps | Expected |
|---|---|---|---|
| 1 | Claims genuinely cover core docs | `python scripts/wiki_claims.py check` | exit 0; ≥10 content claims across core slots 01-18 |
| 2 | Symbol resolution catches a lie | Corrupt one claim `source` to `wiki_lint.py#does_not_exist`, run `check`, then restore | exit 1 with `UNRESOLVED-SYMBOL`; restored run exit 0 |
| 3 | Generated-artifact freshness is gated | Touch a `.wiki` title, run `python scripts/wiki_visualize.py --check`, then regenerate | `--check` exits 1 (stale); after regenerate exits 0 |
| 4 | Rules index completeness is enforced | Temporarily remove the `claims.md` row from `.wiki/rules/README.md`, run `wiki_lint.py` | exit 1 naming the uncatalogued rule; restored run exit 0 |

### Phase 3.5 Research Map (AUTO input)
| Q# | Core Docs | Code Files | KC Entries |
|---|---|---|---|
| Q1 | `.wiki/core/00-system-index.md`, `05`, `09`, `12`, `14`, `15` | — | `18-knowledge-capture.md` |
| Q2 | `.wiki/rules/claims.md` | `scripts/wiki_claims.py`, `scripts/wiki_lint.py` | v0.4.0 history row |
| Q3 | `.wiki/rules/structure.md` | `scripts/wiki_visualize.py`, `docs/wiki-graph.md`, `.github/workflows/validate.yml` | v0.4.0 history row |
| Q4 | — | `scripts/check-utf8-agents.ps1`, `scripts/wiki_lint.py` (encoding guard) | v0.3.9, v0.3.10 history rows |
| Q5 | `.wiki/rules/README.md` | `scripts/wiki_lint.py` | v0.3.13, v0.3.14 history rows |
| Q6 | — | `.devops/sync-manifest.yaml`, `.devops/logs/version-history.md`, `.github/workflows/validate.yml` | version-discipline CI gate |

---

> **HALT POINT (Gate A — Scope):** Phases 1-3 complete (+ Phase 3.5 auto-resolutions in AUTO mode). Set Status -> `PHASE_3`, Active Persona -> `Scoper`. Present the scope perimeter + Phase 3 Q&A record; the user approves the scope before planning begins. On rejection: Status -> `PHASE_1`, Gate A -> `REJECTED`, re-run the affected questions. Questions are relayed to the user **one at a time** via the ask-questions tool — never batched.
>
> **AUTO mode:** Gate A auto-clears on mechanical verification (outputs present, no `REJECTED`, no `Unresolvable:`). Recorded by the orchestrator.

---

## 4 Phase 4: Wiki Requirements & Acceptance Criteria
**Skill Executed:** `ptp-high-visionary` (spec-first directives) + `@wiki-writer`

> **Spec-First Rule:** The wiki is written BEFORE the code. Docs written here describe target behavior and are marked `status: in-progress` — a pre-code doc is a claim, not truth. Promotion to `stable` happens only at Wrap Up, after the executor verifies code matches spec.
>
> **Conditional Skip (checklist):** Run Phase 4 only if ANY of: (a) new or changed user-visible strings/UI, (b) API, schema, or logic-contract change, (c) wiki-facing behavior change. If none apply, skip this phase and record: `No wiki delta — rationale: <why>`. Never skip silently.

**Conditional-Skip Check:** RUN — (b) logic-contract change (`wiki_claims.py check` gains `UNRESOLVED-SYMBOL`; `wiki_visualize.py` gains `--check`; `wiki_lint.py` gains a rules-index check) and (c) wiki-facing behaviour change (new claim semantics + rules catalogue). Never skipped silently.

**Wiki Docs to Write/Update (as `in-progress`):**
| Doc | Change |
|---|---|
| `.wiki/rules/claims.md` | Document `#symbol` resolution + `UNRESOLVED-SYMBOL` drift semantics; status -> `in-progress` (promote at Wrap Up) |
| `.wiki/rules/README.md` | Catalogue `claims.md` in **both** tables; re-stamp `last-reviewed: 2026-09-11` (footer + frontmatter) |
| `.wiki/core/00-system-index.md` | Re-stamp existing `hub-links-every-spoke` claim (machinery changed) |
| `.wiki/core/09,12,14,17,18*.md` | Add `claims:` blocks (frontmatter only — body unchanged) |
| `.wiki/rules/frontmatter.md`, `.wiki/rules/structure.md` | Re-stamp existing claim hashes only (source scripts changed) |

**Acceptance Criteria:**
| # | Criterion (behavior) | Test Target |
|---|---|---|
| 1 | `wiki_lint.py` hard-fails when a top-level `.wiki/rules/*.md` (excl. `language/`) is not catalogued in `README.md` | Probe 4 |
| 2 | Every claim `#symbol` resolves in its source file; a symbol absent from the source yields `UNRESOLVED-SYMBOL` and exit `1` | Probe 2 |
| 3 | Core slots carry ≥10 grounded claims; `wiki_claims.py check` exits `0` with a clean re-stamp | Probe 1 |
| 4 | `wiki_visualize.py --check` exits `1` when `docs/wiki-graph.md` differs from regenerated output; `0` when fresh; never writes | Probe 3 |
| 5 | CI runs visualizer freshness + OKF export smoke; both steps exit `0` | Workflow diff |
| 6 | UTF-8 guard scans root `*.md`, `docs/**/*.md`, `.github/**/*.md` in addition to the current set | Guard output |
| 7 | `machinery-version` 26 -> 27 recorded by a `version-history.md` row; version-discipline gate green | CI version step |

**Spec Notes:**
- **Data flow / contract:** all changes are build-time text transforms over `.wiki/**` + `scripts/**`. No runtime state, no DB, no API. `wiki_claims.py` remains the sole `claims:` parser owner; `#symbol` resolution is a read-only lookup against the named source file.
- **Edge cases the spec must cover:** (1) a claim with **no** `#symbol` is a legal file-level claim — resolution is skipped, not failed; (2) `UNRESOLVED-SYMBOL` short-circuits the stale check for that claim (one finding, not two); (3) `--check` must **not** create `docs/` or write `wiki-graph.md`; (4) the rules-index check must ignore `README.md` itself and the `language/` subdirectory; (5) the OKF smoke must write to a throwaway path so the repo tree stays clean.
- **Docs consumed by this feature (read-first):** `.wiki/rules/claims.md` (claim shape), `.wiki/rules/frontmatter.md` (schema), `.wiki/rules/structure.md` (anchors), `docs/wiki-graph.md` (generated artifact).

---

> **NO HALT after Phase 4:** the spec is reviewed together with the implementation plan at Gate B (after Phase 5) — one decision, what it will do and what it will cost.

---

## 5 Phase 5: High-Visionary Standard Implementation Plan
**Skill Executed:** `ptp-high-visionary` (`SINGLE`: orchestrator inline)

**Simplicity Gate:**
> [x] Climbed the Simplicity Ladder (7 rungs) — every gap extends an existing script/step; no new module, no new dependency
> [x] No speculative abstractions — `symbol_resolves()` is one private helper; no plugin/registry layer
> [x] Reuse over reimpl - Reuse Log populated
> [x] Deliberate simplifications marked with `ponytail:` comments (symbol matching; rules-index scoping)
> [x] Safety exceptions preserved — graceful `src/`-absent coverage no-op untouched

**Reuse Log:**
| Existing Asset | Where Reused | Ladder Rung |
|---|---|---|
| `wiki_lint.py` `extract_links` / `resolve_link` / `parse_frontmatter` | G1 rules-index cataloguing check | 2 |
| `wiki_claims.py` `claims()` parser + `digest()` | G4 symbol resolution sits beside them; `check`/`update` unchanged | 1 |
| `wiki_visualize.py` in-memory `body` list | G6 `--check` compares the same built string instead of writing | 2 |
| `check-utf8-agents.ps1` `$targets` accumulation pattern | G7 appends three `Get-ChildItem` lines | 1 |
| `validate.yml` existing step/job shape | G6 adds two steps in the existing `validate` job | 2 |

**Spaghetti Triage (See-Name-Route, Do Not Fix):**
- [x] No smells noticed — all touched files are single-purpose, <500 lines

| File | Function | Smell Type | Severity | Recommended Action |
|---|---|---|---|---|
| — | — | — | — | — |

### To-Do List
- [ ] G1a: catalogue `claims.md` in both `.wiki/rules/README.md` tables + re-stamp `last-reviewed`
- [ ] G1b: add rules-index completeness hard check to `scripts/wiki_lint.py`
- [ ] G4: add `#symbol` resolution + `UNRESOLVED-SYMBOL` to `scripts/wiki_claims.py check`
- [ ] G6a: add `--check` (freshness, no-write) mode to `scripts/wiki_visualize.py`
- [ ] G6b: add visualizer-freshness + OKF-smoke CI steps to `.github/workflows/validate.yml`
- [ ] G7: extend `scripts/check-utf8-agents.ps1` to root `*.md`, `docs/`, `.github/`
- [ ] G2: add `claims:` blocks to core slots (00/09/12/14/17/18) and re-stamp every hash
- [ ] Doc: update `.wiki/rules/claims.md` with `#symbol`/`UNRESOLVED-SYMBOL` semantics
- [ ] Version: bump `machinery-version` 26 -> 27 + `version-history.md` release row
- [ ] Verify: run all 4 probes + full deterministic gate suite

### File-Level Steps
1. `.wiki/rules/README.md` — add `claims.md` rows to *When to Consult* and *Rule Files*; `last-reviewed` + footer -> `2026-09-11`.
2. `scripts/wiki_lint.py` — new hard check (#3c) after the claims check: every top-level `.wiki/rules/*.md` except `README.md` must resolve from a link in `README.md`.
3. `scripts/wiki_claims.py` — add `symbol_resolves()`; in `cmd_check`, split `source` on the first `#`, and when a symbol is present and unresolved print `UNRESOLVED-SYMBOL ...` and continue.
4. `scripts/wiki_visualize.py` — add `--check`; build the body once, then either compare against `docs/wiki-graph.md` (exit 1 on missing/diff) or write it.
5. `.github/workflows/validate.yml` — two steps after *Claims drift check*: `python scripts/wiki_visualize.py --check`; `python scripts/wiki_okf.py export --out /tmp/okf-smoke --quiet`.
6. `scripts/check-utf8-agents.ps1` — append root `*.md`, `docs/` recursive, `.github/` recursive to `$targets`.
7. `.wiki/core/00-system-index.md`, `09`, `12`, `14`, `17`, `18` — add `claims:` blocks (frontmatter only).
8. `.wiki/rules/claims.md` — document `#symbol` resolution + `UNRESOLVED-SYMBOL`; status -> `in-progress`.
9. `.devops/sync-manifest.yaml` — `machinery-version: 26` -> `27`.
10. `.devops/logs/version-history.md` — prepend the release row (newest first) containing `machinery-version: 27`.
11. `docs/wiki-graph.md` — regenerate only if the freshness check reports drift (expected: unchanged).

### Implementation Instructions (exact string literals / contracts only)
- New claim-drift row literal: `UNRESOLVED-SYMBOL {doc}: claim \`{id}\` -> \`{path}#{symbol}\` not found in source`
- New lint row literal: `[UNCATALOGUED]` (mirrors the existing `[UNINDEXED]`/`[MISSING]` vocabulary)
- New CLI flag: `python scripts/wiki_visualize.py --check`
- New CLI shape: `python scripts/wiki_okf.py export --out /tmp/okf-smoke --quiet`
- Symbol matching (`ponytail:` ceiling): identifier-shaped symbols (`^[A-Za-z_][A-Za-z0-9_]*$`) match on a word boundary; anything else (Markdown section titles) matches case-insensitively as a substring. No per-language AST.
- Claims to author (id -> source):
  - `core/00` `hub-links-every-spoke` -> `scripts/wiki_lint.py#category_indexes` (existing)
  - `core/09` `design-system-is-authority` -> `AGENTS.md#Design & Scope Notes`
  - `core/12` `agents-read-operating-rules` -> `AGENTS.md#Core Development Rules`
  - `core/12` `env-vars-excluded-from-git` -> `.gitignore#.env`
  - `core/14` `testing-subtree-registered` -> `.wiki/testing/testing-index.md#Testing Index`
  - `core/17` `required-frontmatter-fields` -> `scripts/wiki_lint.py#REQUIRED_FIELDS`
  - `core/17` `visualizer-generates-docs` -> `scripts/wiki_visualize.py#main`
  - `core/17` `claims-drift-gate` -> `scripts/wiki_claims.py#cmd_check`
  - `core/17` `hub-spoke-enforced` -> `scripts/wiki_lint.py#category_indexes`
  - `core/18` `capture-via-skill` -> `.devops/skills/knowledge-capture/SKILL.md#Admission Gate`
  - `core/18` `prune-via-consolidation` -> `.devops/skills/knowledge-consolidation/SKILL.md#Tidy (default)`
  - `core/18` `knowledge-capture-line-ceiling` -> `.devops/skills/knowledge-consolidation/SKILL.md#hard 500-line ceiling`
- **Grounding-breadth deviation (recorded):** core slots `01-08, 10, 11, 13, 15, 16` are app-scaffold placeholders (`[APP_NAME]`, `[placeholder]` tokens) with **no repository source** to bind. Per the no-false-facts principle behind G4, they are not claim-spammed; grounding for those slots belongs to a satellite with an app tree. Priority slots `05`/`15` likewise carry no machinery fact. The >=10-claim acceptance threshold is met by genuine claims in `00/09/12/14/17/18`.

### Wiki Core References
- `.wiki/core/17-docs-blueprint.md` -> docs taxonomy + hub-and-spoke blueprint
- `.wiki/core/18-knowledge-capture.md` -> KC lifecycle the claims ground
- `.wiki/rules/claims.md` -> canonical claim shape this parcel tightens
- `.wiki/rules/frontmatter.md` -> schema whose required fields the claims assert
- `.wiki/rules/structure.md` -> immutable anchors reused as claim sources

### Wiki Docs to Add/Edit
See Phase 4 table. No new wiki doc is created; `claims.md` and `README.md` are edited, six core docs get frontmatter-only claim blocks.

---

> **HALT POINT (Gate B — Spec & Plan Review):** Phases 4-5 complete. Present the wiki requirements spec (with acceptance criteria) AND the execution plan together as one decision. Do not proceed to reviews until user approves. Update the **State & Gates** section at the bottom of this file: Status -> `PHASE_5`, Active Persona -> `High-Visionary`. Leave Gate B `OPEN` — the orchestrator records the user's verdict.

---

## 6 Phase 6: Grumpy Architect Spec & Logic Audit
**Skill Executed:** `ptp-grumpy-architect` (`SINGLE`: orchestrator inline)

> **`SINGLE` topology:** no independent reviewer. The orchestrator logs an inline self-review checkpoint here and records **Verdict** `N/A — SINGLE self-review`.

**Verdict:** `N/A — SINGLE self-review` (skipped; Group C not run per SINGLE topology)

> **Rejection Rule:** If plan does not make the app faster, safer, or easier to modify, do not check boxes. Reject and force rewrite via `PHASE_5_REVISION`.
>
> **Spec-level audit — the plan contains no code, so no code-level scans (DRY/WET, line checks).**

- **System Contracts Explicit:** `wiki_claims.py` stays the sole `claims:` parser owner (G4 reads, never re-parses). `wiki_lint.py` stays the doc-graph enforcer (G1 adds a check there, not in a new script). `wiki_visualize.py` owns its artifact; `--check` is read-only. `check-utf8-agents.ps1` owns byte-level mojibake.
- **File Boundary & Scope Collisions:** none — G1 touches `wiki_lint.py`, G4 `wiki_claims.py`, G6 `wiki_visualize.py` + workflow, G7 the PS guard; the four scripts are disjoint. `docs/wiki-graph.md` is generated-only and is not hand-edited.
- **Dependency Gaps:** no new third-party dependency; all additions use stdlib (`re`, `hashlib`, `argparse`, `pathlib`) or existing PowerShell cmdlets.
- **YAGNI Bloat:** rejected a per-language AST symbol resolver, a `--fix` mode for symbols, a rules-index auto-fix, and a committed freshness report. `ponytail:` ceilings documented instead.
- **Paranoid Security (Contract Level):** CI OKF smoke writes to `/tmp/okf-smoke` — no repo pollution, no untracked artifact. No secrets; claim sources are repo files only.
- **Survivability:** every new gate has a graceful path — claims with no symbol skip resolution; the coverage gate's `src/`-absent no-op is untouched; `--check` cannot mutate the tree.
- **Edge Cases:** covered in Phase 4 Spec Notes (no-symbol claims, double-report suppression, README/`language/` exclusions, throwaway OKF path).
- **Performance Trade-offs:** negligible — `symbol_resolves` reads one file per claim (≈16 claims); `--check` runs one generation pass.
- **Architectural Anti-Patterns:** none; the change strengthens the "deterministic gates beat conventions" rule rather than adding an advisory layer.
- **Endpoint Protection & Rate Limiting:** N/A — no runtime endpoints.
- **Wiki Core Compliance:** changes conform to `frontmatter.md` (claims live in frontmatter), `claims.md` (shape unchanged), `structure.md` (no anchor renamed). Rules-index check enforces `link-hygiene.md` catalogue discipline.

**Dead-Code / Orphan Flags (for Wrap Up backlog entry):**
| plan section / file_path:line_number | Reason |
|---|---|
| — | None found |

**Required Fixes:**
> [x] None

---

## 7 Phase 7: Smooth Operator Product Review
**Skill Executed:** `ptp-smooth-operator` (`SINGLE`: orchestrator inline)

> **`SINGLE` topology:** no independent reviewer. Record **`N/A — SINGLE self-review`**; the self-review is presented at Gate B.

**Verdict:** `N/A — SINGLE self-review`

> **Rejection Rule:** If plan introduces unnecessary complexity or scope expansion, do not check boxes. Reject and force rewrite.

- **Vision & Journey Integrity:** serves the theme directly — the knowledge layer stops being a demo and starts reporting its own drift; a maintainer's journey goes from "trust the check" to "the check has teeth".
- **Scope Containment (No Gold Plating):** G3/G5 and G8-G12 explicitly deferred; no new skill, agent, doc tree, or report file. Every addition closes a named gap (G1, G2, G4, G6, G7).
- **4 Core User States:** N/A (build/CI machinery, no UI).
- **Guardrails & Permissions:** claims remain opt-in, human-promoted (`stable`); the new hard fails are deterministic and self-explaining; no destructive action introduced.
- **Mobile / A11y / Telemetry:** N/A.

**Required Fixes:**
> [x] None

---

> **HALT POINT (Gate C — Peer Reviews):** Reviews complete. Present findings and required fixes. Update the **State & Gates** section at the bottom of this file:
> - **PASS:** Phase 6 log clean -> set Status `PHASE_7`, Active Persona `Reviewer`. Do not proceed to execution until user approves Gate C.
> - **FAIL:** Phase 6 or 7 flagged blocking flaws -> set Status `PHASE_5_REVISION`, Active Persona `High-Visionary`. Return to Group B for plan adjustments, then re-run Phases 6-7. **Never advance an unapproved plan to execution.**
> The orchestrator records Gate C -> `APPROVED`/`REJECTED` only after the user's verdict.
>
> **`SINGLE` topology:** Gate C is `N/A` — the plan approval happens once at Gate B (spec + plan + inline self-review). Update the State & Gates `Gate C` row to `N/A (SINGLE)`.

---

## 8 Phase 8: Execute Changes
**Skill Executed:** `ptp-code-surgeon` (`SINGLE`: orchestrator inline)

**Execution Isolation:** Phase 8 triggers ONLY after Gate C cleared by explicit user input.

**Single-Pass Direct-to-Disk:**
> [x] Implementation written DIRECTLY to source files - no intermediate Markdown code blocks, no drafting files
> [x] Touched only intended lines - no adjacent refactors
> [x] Cleaned up only owned orphans
> [x] Pre-existing dead code left untouched

> [x] Step 1: G1 — `claims.md` catalogued in both `.wiki/rules/README.md` tables; `last-reviewed` -> `2026-09-11`; `wiki_lint.py` gains the `[UNCATALOGUED]` rules-index hard check.
> [x] Step 2: G4 — `wiki_claims.py` gains `symbol_resolves()` + `UNRESOLVED-SYMBOL` in `cmd_check`; `claims.md` documents the semantics (status -> `in-progress`).
> [x] Step 3: G6 — `wiki_visualize.py --check` (fresh, no-write) + two CI steps (visualizer freshness, OKF export smoke).
> [x] Step 4: G7 — `check-utf8-agents.ps1` now scans root `*.md`, `docs/**/*.md`, `.github/**/*.md` (176 -> 188 files).
> [x] Step 5: G2 — 12 grounded claims added across core slots 00/09/12/14/17/18; all hashes re-stamped (`wiki_claims.py update`).
> [x] Step 6: Version — `machinery-version: 26 -> 27`; `version-history.md` v0.6.0 row added.

---

## 9 Phase 9: Verify Changes
**Skill Executed:** `ptp-code-surgeon` (`SINGLE`: orchestrator inline)

**Build & Lint:**
> [x] Compilation check clean — all Python modules import + parse (`python -m py_compile`)
> [x] Lint clean (exit 0) — `python scripts/wiki_lint.py` exit 0
> [x] Type-generation re-run if schema/API changes — N/A (no schema/API)

**Test Report:**
- [x] Lint pass (exit 0) — `wiki_lint.py` OK (0 broken links, all anchors, frontmatter valid)
- [x] Tests pass (exit 0) — Probe 1 `claims check` exit 0 (12 core claims); Probe 2 `UNRESOLVED-SYMBOL` exit 1 then clean exit 0; Probe 3 `--check` stale exit 1 then fresh exit 0; Probe 4 `[UNCATALOGUED]` exit 1 then clean exit 0
- [x] Build pass (exit 0) — full gate suite: `wiki_claims check` 0 stale · `wiki_coverage_check` no-op · `wiki_visualize --check` up-to-date · OKF export 56 concepts · `check-utf8-agents` 188 files clean · `check-parcel-prefix` PASS ×7 · `sync-architecture -SelfTest` OK · version-discipline `machinery-version 27 recorded`
- [x] Code matches exact plan specifications — every Phase 5 command run; all exit `0`
- [x] No functional gaps identified
- [x] Deviation recorded: Probe 4 required removing the *link target* to simulate an unlisted rule (a label-only rename still resolves); `core/14` claim source moved to `.wiki/testing/testing-index.md#Testing Index` to avoid a same-pass restamp wobble (a claim sourced from another claim-carrying doc needs a second `update` pass).

---

> **HALT POINT (Gate D — Implementation):** Implementation and verification complete. Present completed work to user. Do not proceed to user review until user signs off. Update the **State & Gates** section at the bottom of this file: Status -> `PHASE_9`, Active Persona -> `Executor`. Leave Gate D `OPEN` — the orchestrator records the user's verdict. On rollback: Status -> `PHASE_8_FAILED`.

---

## 10 Phase 10: User Review & Tweaks
**Skill Executed:** `knowledge-capture`

**Tweak Discipline:**
> Tweaks are surgical, not architectural. `expansion` and `refactor` are HALT conditions.

- [x] Tweak classified: `fix` / `expansion` / `refactor` — no tweaks requested at Gate D
- [x] `expansion` and `refactor` routed to new parcel — N/A (none)
- [x] `fix` touches <= 3 files — N/A (none)
- [x] `fix` introduces no new abstraction absent from original plan — N/A (none)

**Capture Categories:** Recurring / Tribal / One-off / Cosmetic-skip

| Round | Feedback | Action Taken | Capture Flag | Result |
|---|---|---|---|---|
| 1 | Gate D sign-off: "Approve — run Wrap Up" (no change requests) | Wrap Up run | none | COMPLETE |

**Sign-Off:** 2026-09-11 - User approved at Gate D.

---

## Completion Note (Wrap Up)
**Skill Executed:** `agent-wrap-up`

> [x] Read `.wiki/core/18-knowledge-capture.md` for existing related decisions
> [x] Themed tweaks synced to knowledge capture doc
> [x] Wiki docs updated per Phase 10 tweaks — no tweaks requested
> [x] Code reconciled against Phase 4 spec — deviations logged
> [x] `status: in-progress` wiki docs promoted to `stable` (or deviation logged)

**Themed Tweaks:**
None — Gate D signed off with no change requests.

**Knowledge Capture Entries:**
- Pitfall: a Grounded Claim whose `source` is another claims-carrying doc goes stale within a single `python scripts/wiki_claims.py update` pass (the source doc's own hash is restamped after the dependent is stamped). Retarget the claim, or run `update` a second time.

**Spec Reconciliation:** Matches Phase 4 spec. Two recorded deviations: (1) app-scaffold core slots `01-08,10,11,13,15,16` carry no claims (no repository source — recorded in Phase 5); (2) `core/14` claim source is `.wiki/testing/testing-index.md#Testing Index` rather than `.wiki/rules/structure.md#Testing` (avoids the same-pass restamp wobble above).

**In-Progress Promotions:** `.wiki/rules/claims.md` promoted `in-progress` -> `stable` (behavior verified by probes 1-4 and the CI gate suite).

**Dead-Code Backlog Entries (from Phase 6):** None.

**Wiki Updates:**
- `.wiki/rules/README.md` (claims.md catalogued, last-reviewed re-stamped), `.wiki/rules/claims.md` (`#symbol`/`UNRESOLVED-SYMBOL` semantics, promoted to stable), `.wiki/rules/frontmatter.md` + `.wiki/rules/structure.md` (hash re-stamp only), `.wiki/core/00,09,12,14,17,18` (claims + hub `Last Verified` stamps).

**Plan Archiving:** Plan archived to `.devops/archive/t2-e2.01-wiki-grounding-hardening-plan.md` (via `git mv`, no stub).

**Backlog Review:** `.devops/backlog/backlog-index.md` updated — new **T2-E2** section records T2-E2.01 `COMPLETE` and lists T2-E2.02 (G3 OKF ingest + G5 CI refresh automation) as the deferred next parcel.

---

## 📍 State & Gates (CACHE-ANCHORED — update ONLY this section at gate transitions)

> **Cache rule:** This is the **last section** in the file. Gate transitions mutate ONLY the rows below — phase content above stays byte-stable to preserve LLM prefix-cache hits. Every "Update Status" instruction in the halt points above means "edit this section".

| Metric | Value |
|---|---|
| **Status** | `COMPLETE` |
| **Version** | `v0.1.0` |
| **Mode** | `AUTO` |
| **Agents** | `SINGLE` (inline personas; Group C skipped; Gates B+C merged at Gate B; Gate C `N/A`) |
| **Active Persona** | `Complete` |
| **Depends On** | none |
| **Blocks** | T2-E2.02 (Wiki refresh automation — G3/G5) |

> Valid states: `BACKLOG`, `PHASE_1`, `PHASE_3`, `PHASE_5`, `PHASE_5_REVISION`, `PHASE_7`, `PHASE_8_FAILED`, `PHASE_9`, `COMPLETE`.

| Gate | Requirement | Status |
|---|---|---|
| A | Scope approved (Phases 1-3, + 3.5 in AUTO) | `APPROVED` (AUTO auto-clear — mechanical verification passed) |
| B | Spec & plan approved (Phases 4-5) | `APPROVED` (AUTO auto-clear — Phases 4-5 present, self-review clean, no `REJECTED`) |
| C | Peer reviews passed (Phases 6-7) | `N/A (SINGLE)` |
| D | Implementation verified (Phases 8-9) | `APPROVED` (user sign-off 2026-09-11) |

> Gate flips: rows flip `OPEN` → `APPROVED`/`REJECTED` ONLY after the user's (or AUTO verification's) verdict, recorded by the orchestrator. Executing agents halt with their gate `OPEN`. Rejection routes: A -> `PHASE_1`; B/C -> `PHASE_5_REVISION`; D/rollback -> `PHASE_8_FAILED`. Never edit rows above this section for gate bookkeeping.

> **This section is the LAST section in the file. All gate bookkeeping happens here.**
