---
code: T1-E1.03
sprint: none
claim_status: COMPLETE
owner: parcel-orchestrator
claimed_at: "2026-09-13"
last_touch: "2026-09-13"
touches: [".devops/templates/**", ".opencode/plans/base-context.md", "opencode.json", "scripts/**", ".devops/skills/model-routing/**", ".devops/agents/**", ".devops/sync-manifest.yaml", ".github/workflows/validate.yml", "AGENTS.md", "HOW-TO.md", ".devops/README.md", ".devops/rules/**", ".wiki/core/18-knowledge-capture.md", ".wiki/core/00-system-index.md", ".devops/logs/version-history.md"]
depends_on: ["T1-E3.02"]
---
# Parcel Plan: T1-E1.03 - Model Binding Alignment & Template→Satellite Propagation
## Theme-Epic: T1 - Parcel Pipeline Machinery, E1 - Model & Config Integrity

> **Sprint note:** No sprint is currently open (`.devops/sprints/` does not exist; Triage Panel empty). `sprint: none`. This plan was instantiated directly and claimed on the trunk — the `@sprint-plan` adoption path is deferred until the sprint cycle is adopted in this repo.

## ⚙️ Plan Settings (FROZEN — set at plan start, read before any phase)

| Setting | Value | Meaning |
|---|---|---|
| **Mode** | `USER-MANAGED` | `USER-MANAGED` (every gate halts for the user) or `AUTO` (orchestrator auto-clears Gates A-C; Gate D always halts) |
| **Agents** | `SINGLE` | `MULTI` (comprehensive — full `ptp-*` delegation, 4 gates) or `SINGLE` (fast — inline personas, Group C skipped, Gates B+C merged at Gate B, Gate C `N/A`) |

> **Frozen config — read before executing ANY phase.** These two settings govern the entire pipeline and are never edited after plan start; they sit at the TOP so no session can miss them. Mutable runtime state (Status / Active Persona / gates) lives ONLY in the cache-anchored `## 📍 State & Gates` section at the bottom. See `@pass-the-parcel` § Agent Topology.

> **Selection record (orchestrator, 2026-09-13):** Agents `SINGLE` was **user-selected** (not a locked preset) — the orchestrator recommended `MULTI` on the complexity signals below and the user overrode it. Recorded because `SINGLE` removes independent review: the Phase 6/7 checkpoints are inline self-reviews only.
>
> **Mode correction (plan-start, 2026-09-13):** `Mode` was first selected as `AUTO`, then corrected to `USER-MANAGED` **before any phase work began**. Reason: Phase 3's open forks are *contract* decisions — what is the canonical binding source; does a template-workspace binding **force** or **seed** a satellite binding; how is satellite divergence handled. `ptp-phase3-answerer` would have auto-resolved them from repo docs that contradict each other (T1-E1.01's "binding is satellite configuration" vs the KC rule *Farthest-evolved consumer wins*). Under `USER-MANAGED` Phase 3 puts those forks to the user directly, one at a time. Gates A and D halt in both modes, so this changes only **who answers Phase 3**.
>
> Complexity signals at selection: blast radius **HIGH** (validator + sync engine + manifest + the binding surfaces below + CI), contract change **HIGH** (redefines what "the registry" is and adds a transported surface), ambiguity **HIGH**, novelty **MEDIUM-HIGH**.

> **Skill Architecture:** This template is consumed by the `pass-the-parcel` skill. Each phase delegates to a specialized sub-skill. See the parcel skill's Skill Delegation Map.
>
> **RULES:**
> - **Claim front-matter** at the very top (`code` / `sprint` / `claim_status` / `owner` / `claimed_at` / `last_touch` / `touches` / `depends_on`) governs claiming. See `.devops/rules/plan-lifecycle.md` § Claim Protocol. `claim_status` is NOT the bottom `Status` (pipeline phase).
> - Full skeleton required - ALL 10 phases + Wrap Up MUST be present.
> - Halt points are HARD STOPS - each gate blocks all subsequent phases.
> - Persona matches Status - use the State Lifecycle table in pass-the-parcel skill.
> - Read **Plan Settings** above before acting; the topology there governs which gates apply. See `@pass-the-parcel` § Agent Topology.
>
> **🔒 CACHE-ANCHORED:** The **Plan Settings** block above is frozen config; the mutable State Dashboard + Gate Log live in the **last section** (`## 📍 State & Gates`). Gate transitions update ONLY those bottom rows — do NOT edit content above once written. Byte-stable prefix = LLM prefix-cache hits for every downstream agent re-read.

---

## 1 Phase 1: Expansion & Scoping
**Skill Executed:** `ptp-context-hunter`

**Intent:**
Keep declarative model routing (do NOT remove it). Add two capabilities the pipeline lacks today:

1. **Cross-surface alignment checks.** Today only the live trio is validated (registry ↔ agent frontmatter ↔ `opencode.json`, via `check-parcel-prefix.ps1`). The **seed** surfaces are never checked against each other or against the live surfaces, so a template can ship a registry and a seed config that disagree with each other and with what the template workspace actually runs. Add deterministic validation so every surface that carries a model binding is proven to agree, or fails loudly.
2. **Template → satellite binding propagation.** A model binding chosen in *this* template workspace must be carried to satellites by `@sync-architecture`. Today it is explicitly not: the live registry and `opencode.json` are excluded from the portable surface, while the agent frontmatter that *is* copied keeps the template's model.

**Pre-populated reconnaissance (orchestrator, 2026-09-13 — Phase 3 still re-runs interactively):**

*Binding surfaces that exist today:*

| # | Surface | Role | Live? | Transported? | Validated? |
|---|---|---|---|---|---|
| 1 | `.opencode/plans/base-context.md` → `## Model Registry` | canonical registry | live | **No** | yes (target of check) |
| 2 | `.devops/agents/*.agent.md` / `*.subagent.md` → frontmatter `model:` | VS Code runtime binding | live | **Yes** (`.devops/agents` is a `portable_dir`) | yes (subject of check) |
| 3 | `opencode.json` → `agent.<key>.model` | opencode runtime binding | live | **No** | yes (subject of check) |
| 4 | `.devops/templates/base-context.template.md` → registry table | seed registry (**blank cells**) | seed | yes | **No** |
| 5 | `.devops/templates/opencode.template.json` → `agent.<key>.model` | seed binding (`<your provider/model>` ×11) | seed | yes | **No** |
| 6 | `.devops/templates/AGENTS.template.md`, `SATELLITE-BOOTSTRAP.md`, `HOW-TO.md` | prose describing the routing model | seed/docs | yes | **No** |

*The latent bug (verified by reading, not assumed):* `sync-architecture.ps1:54-58` states the repo-specific surface (`base-context.md`, `opencode.json`, `AGENTS.md`) is **not** copied, and after the portable surface is copied the script regenerates each agent's PREFIX-LOCKED prefix from the **target's own** `base-context.md`. But the frontmatter `model:` line sits *above* the prefix and is copied verbatim from the template. Therefore a satellite that legitimately rebinds its own registry receives template frontmatter and `check-parcel-prefix.ps1` fails **by construction**. Sync cannot detect it either: `Get-ItemHashes` (`sync-architecture.ps1:271-292`) strips frontmatter for `parcel*`/`ptp-*` before hashing, so model drift is invisible to `-Check`.

*Contract tension to resolve in Phase 3:* T1-E1.01 (completed 2026-09-03) concluded *"concrete model binding is satellite configuration in `opencode.json`"*, and KC `.wiki/core/18-knowledge-capture.md` § Agents & Models records **Dual-surface agent binding**. A single propagating source of truth is a third surface unless the design deliberately makes one surface canonical and derives the others. The requirement **partially reverses** T1-E1.01; the reversal must be an explicit recorded decision, not an accidental drift.

**In Scope:**
- A deterministic alignment check covering all six surfaces above, wired into `scripts/` and the `validate.yml` gate set.
- A propagation mechanism so a template-workspace binding choice reaches satellites coherently (registry + runtime + frontmatter agreed on arrival, or an explicit documented reconciliation step).
- Reconciliation of the T1-E1.01 decision and the "Dual-surface agent binding" KC rule with the new propagating-source design.
- `@model-routing` skill update so the rebinding procedure stays the single documented path.
- Seed template updates (`base-context.template.md`, `opencode.template.json`) so a bootstrapped satellite starts aligned.
- Version/state discipline for every touched `.devops/skills/**`, `.devops/templates/**`, `scripts/**` artifact.

**Out of Scope:**
- Removing model routing (explicitly rejected by the user — routing stays declarative per-capability-class).
- Per-agent model *selection* policy or capability-class redesign — the class→model decision matrix in `@model-routing` §2 is not in question.
- Any satellite's local model choice retroactively — propagation defines how a template binding arrives, not that satellites must accept it (acceptance semantics are a Phase 3 question).
- The `wiki-writer` / `wiki-verifier` agents — they carry bindings but have no Model Registry row; whether they gain rows is a Phase 3 question.
- Any change to the `parcel-sprint` / `ptp-parcel-fast` batch behaviour shipped by T1-E3.02.

**Perimeter Notes:**
- This plan **depends on T1-E3.02** (now archived) because it edits the same sync/validator/registry surface that plan just rewrote.
- `SINGLE` topology ⇒ Phase 6/7 are inline self-reviews; the sync-engine change ships without an independent adversarial reviewer (user-accepted at selection).
- Phase 3 must interrogate, at minimum: (a) *what* is the single canonical binding source; (b) whether a template binding **forces** or **seeds** a satellite binding; (c) whether a satellite's divergence is a hard failure or a recorded reconciliation; (d) how the documented `opencode.json` opt-out (VS Code-only satellite) interacts with propagation; (e) whether `wiki-writer`/`wiki-verifier` gain registry rows.

---

## 2 Phase 2: Requirements & Context
**Skill Executed:** `ptp-context-hunter`

**Forensic Context Inventory:**
> [x] Wiki core docs read (`.wiki/core/00-system-index.md` + drilled into relevant docs)
> [x] Knowledge capture read (`.wiki/core/18-knowledge-capture.md`)
> [x] Source code verified (components / utilities / hooks / types in blast radius)

**Relevant Existing Decisions (from knowledge capture):**
- KC § Sync & Versioning — **Farthest-evolved consumer wins**: when template and satellite diverge, port the satellite's battle-tested machinery back; app-specific content stays in the consumer. *(2026-09-03)* — bears directly on whether a template binding may overwrite satellite learning.
- KC § Sync & Versioning — **Post-sync bookkeeping self-heals**: sync stamps the target manifest's `machinery-version:` in place; `-Check` hashes only agent-unique content (frontmatter stripped), so per-repo prefix regeneration never reports phantom DRIFT. *(2026-09-06)* — the frontmatter-stripping rule is exactly what hides model drift today.
- KC § Agents & Models — **Dual-surface agent binding**: agents live once in `.devops/agents/*.agent.md`; VS Code reads per-file frontmatter, opencode reads `opencode.json`. Per-file binding means the orchestrator cannot mis-spawn a model; use the provider's exact picker casing. *(2026-09-06)*
- KC § Agents & Models — **Set-level agent versioning**: agents are versioned as a coordinated set via `machinery-version:` — no per-file `version:`; only skills carry per-file versions. *(2026-09-04)*
- KC § Parcel Pipeline — **`@sprint-run` batch deviations** *(2026-09-13, added at T1-E3.02 wrap-up)* — not directly in blast radius; noted because this plan inherits the same `touches` surface.

**Relevant Docs Found:**
- `.devops/skills/model-routing/SKILL.md` (v5) — §1 binding architecture (two runtime surfaces + canonical registry), §3 the only safe rebinding procedure, §4 validation contract.
- `.opencode/plans/base-context.md:78-96` — `## Model Registry` (10 rows, uniform `DeepSeek V4.1 Flash` / `opencode-go/deepseek-v4.1-flash`) inside the `ORCHESTRATOR-ONLY` block; inlined into every locked agent by `-Sync`.
- `.devops/templates/base-context.template.md:91-109` — seed registry with **blank** model cells.
- `.devops/templates/opencode.template.json` — seed bindings, `<your provider/model>` placeholder ×11, plus the documented opt-out paragraph ("Satellites that use VS Code agents only may delete the 'agent' block — check-parcel-prefix prints SKIP").
- `.devops/sync-manifest.yaml:23-30` — `machinery-version: 38`; `portable_dirs` includes `.devops/agents` + `.devops/templates`; `portable_files` includes both check scripts.
- `.github/workflows/validate.yml` — the deterministic gate set this change must extend (prefix, UTF-8, wiki lint/coverage/claims/visualize/OKF, JSON parse, `-SelfTest`, version discipline).
- `HOW-TO.md:154` — the prose claim that "the parcel pipeline routes by capability class … never by vendor model name in prose" and "extending/rebinding is a config edit, not a machinery sync."

**Relevant Code Found:**
- `scripts/check-parcel-prefix.ps1` — Model Registry parsed at `:95-101` (regex matches `| parcel… |` / `| ptp-… |` rows **without** backticks); per-file binding check `:194-216`; `opencode.json` ↔ registry validation `:222-255` (present block ⇒ fail on missing key / mismatch / `<your provider/model>` placeholder; absent or empty block ⇒ documented `SKIP`).
- `scripts/sync-architecture.ps1` — `-SelfTest` (throwaway target smoke test), `-Verify` (structural checks of the satellite-authored surface: AGENTS.md markers, `opencode.json` wiring, `base-context.md` presence, machinery presence, `.ptp-source`), `-Check` (hash comparison); `Get-ItemHashes:262-313` with the frontmatter-stripping branch at `:271-292`; prefix regeneration from the target's base-context.
- `.vscode/settings.json:9-13` — `chat.agentFilesLocations` maps `.devops/agents` (now deprecated upstream but still honoured for the Local agent), confirming surface #2 is live.

**Constraint discovered during reconnaissance (must survive any redesign):**
`check-parcel-prefix.ps1:100`'s registry regex requires a bare `| parcel… |` / `| ptp-… |` row (no backticks). The existing `Orchestrator Presets` rows survive only because their keys are backtick-wrapped. Any new table added to the `ORCHESTRATOR-ONLY` block risks being mis-parsed as a model-binding row and producing a false `model binding mismatch`.

---

## 3 Phase 3: User Clarification
**Skill Executed:** `ptp-context-hunter` (`SINGLE` — orchestrator inline; all questions relayed one at a time via the ask-questions tool)

> ✅ **Q1 — Canonical binding source:** **Registry-canonical, no new artifact.** `base-context.md` → `## Model Registry` stays the single source of truth (live copy + seed mirror); agent frontmatter and `opencode.json` are **derived** surfaces. Sync parses the **source** repo's registry as a read-only input — `base-context.md` itself stays repo-specific and is never copied.
> ✅ **Q2 — Propagation semantics:** **Always force.** Sync stamps the template binding into every target surface on every sync — no fingerprint, no preservation branch, no bootstrap-only path. Satellite-side rebinds are transient (reverted by the next sync); recorded as intended behaviour, not a defect.
> ✅ **Q3 — Divergence handling:** **No reporting (status quo).** `-Check` stays a hash verdict over agent-unique content; binding divergence from the source stays invisible to sync. Enforcement is the target's own registry↔frontmatter↔`opencode.json` agreement, checked by Q7's extended validator.
> ✅ **Q4 — VS Code-only opt-out:** **Retired.** `opencode.json` *and* its `agent` block become mandatory in every satellite; the absent/empty-block `SKIP` becomes a `FAIL` naming the seed file; the opt-out paragraph is deleted from `opencode.template.json`; already-block-less satellites restore the block (documented in `version-history.md` + HOW-TO).
> ✅ **Q5 — Registry coverage:** **Both non-parcel agents gain rows** — `wiki-writer` → `deep planning/authoring`, `wiki-verifier` → `independent audit`. Registry grows to **12 rows**; the `no Model Registry row` SKIP path disappears.
> ✅ **Q6 — Seed contents:** **Concrete binding.** `base-context.template.md` ships all 12 rows bound to `DeepSeek V4.1 Flash` / `opencode-go/deepseek-v4.1-flash`; `opencode.template.json` replaces all 11 `<your provider/model>` values with the concrete id. Blank cells / placeholders become a **`FAIL`** state — no legal "unbound seed".
> ✅ **Q7 — Check placement:** **Extend `scripts/check-parcel-prefix.ps1`.** One registry parser, one portable file, one `validate.yml` step. No new script, no new wiring points.
> ✅ **Q8 — CI transport:** **Leave CI local.** `.github/workflows/validate.yml` stays out of `portable_files`; the alignment check reaches satellites because it rides inside the already-transported `check-parcel-prefix.ps1`. Wiring the gate set into a satellite's own CI is documented as a per-satellite bootstrap step (`SATELLITE-BOOTSTRAP.md` + HOW-TO:154 corrected).
> [x] Final validation: "Is this all the context required?" -> **Yes** (2026-09-13)

**Fleet contract (Phase 3 outcome, one line):** Satellites are **clones** of the template machinery — bindings flow one way, template registry → sync → target registry + target frontmatter + target `opencode.json`; no satellite-side binding is durable.

**Architectural Conflict Warnings Raised:**

1. **T1-E1.01 reversal — must be an explicit recorded decision.** T1-E1.01 (2026-09-03) concluded *"concrete model binding is satellite configuration in `opencode.json`"*. Q1+Q2 replace that with template-owned, registry-canonical, force-propagated binding. The reversal must land in the knowledge-capture log (§ Agents & Models) and in the wiki during Wrap Up — never as accidental drift.
2. **KC "Dual-surface agent binding" is now a derived triple.** The 2026-09-06 rule says agents live once in `.devops/agents/*` (VS Code reads per-file frontmatter) and opencode reads `opencode.json`. Under Q1 the **registry** is canonical and both runtime surfaces are projections; the rule must be amended, or the next agent re-derives a contradictory model.
3. **Registry prose contradicts clone semantics.** `base-context.md:81` and `base-context.template.md:94` call the binding table *"an example binding, not a mandate"* and say each satellite *"authors its own `base-context.md` and rebinds per its available models"*. Q2/Q4 invalidate that sentence — both copies must be rewritten to "mandate, propagated by sync".
4. **`@model-routing` §3 is now wrong for satellites.** Its procedure edits the **satellite's** frontmatter + registry row + `opencode.json`. Under Q2 that edit is transient — the correct procedure is template-side rebind → `-Sync` → fleet stamp. §1/§3 must be rewritten or every future rebind is mis-documented.
5. **Phase 1 Out-of-Scope line ("acceptance semantics are a Phase 3 question") is now answered** — acceptance is *not* optional (force, Q2/Q4). Phase 5 must reconcile that bullet; the Phase 1 body is left byte-stable by design and this Phase 3 record is authoritative.
6. **Q2 + Q3 combine into a silent-revert contract.** Force-stamp + no divergence reporting means a satellite's local rebind is reverted on the next sync with **no log line**. Intended under clone semantics; must be stated plainly in `@model-routing` and HOW-TO so it is never mistaken for a bug.
7. **Provider reachability is unvalidated.** A forced binding names a model id; nothing checks that the target can reach that provider — string equality passes, runtime fails. Accepted limit; record the template-side rule "the propagated binding must be a model every satellite can reach".
8. **`wiki-*` regex/loop constraint (must survive the redesign).** `check-parcel-prefix.ps1:100`'s registry regex matches only keys starting `parcel`/`ptp-`, and `$agentFiles` (`:87-90`) globs only `parcel*.agent.md` / `ptp-*.subagent.md`. Q5 therefore requires (a) a widened key pattern and (b) `wiki-writer.agent.md` + `wiki-verifier.subagent.md` to be picked up **by the binding check only**. They must **not** enter the prefix-integrity loop (`:105-192`): verified by reading, neither file carries the shared PREFIX-LOCKED prefix (bare frontmatter + `You are the …` body), so adding them there would fail every run and/or rewrite their bodies. Decouple the binding pass from the prefix pass.
9. **Registry-row regex fragility.** Any new 4-cell table added to the `ORCHESTRATOR-ONLY` block with a bare `parcel*`/`ptp-*` first cell is mis-parsed as a binding row. The 12-row registry is the only table allowed in that shape.

**Research Map (Phase 3.5):** `N/A — USER-MANAGED mode` — no auto-resolution pass ran; every fork was put to the user directly, one at a time.

**Test Proposals (TDD):**
- **T1 — seed↔live alignment (proves surface #4 is now validated).** Corrupt one model cell in `.devops/templates/base-context.template.md` → run `check-parcel-prefix.ps1` → expect non-zero exit naming the seed row and the live registry value; restore → expect exit 0.
- **T2 — placeholder is fatal (proves Q6's no-unbound-seed rule).** Set one `agent.<key>.model` in `.devops/templates/opencode.template.json` back to `<your provider/model>` → expect non-zero exit naming the seed file; restore → exit 0.
- **T3 — force propagation lands coherently (proves the template→satellite path end-to-end).** `sync-architecture.ps1 -Target <throwaway>` then the target's `check-parcel-prefix.ps1` → expect exit 0 with `MODEL DeepSeek V4.1 Flash` for all **12** agents and `OC-MODEL` for all 12 `opencode.json` keys. Prefer folding this into `-SelfTest` if cheaper.

---

> **HALT POINT (Gate A — Scope):** Phases 1-3 complete (+ Phase 3.5 auto-resolutions in AUTO mode). Set Status -> `PHASE_3`, Active Persona -> `Scoper`. Present the scope perimeter + Phase 3 Q&A record; the user approves the scope before planning begins. On rejection: Status -> `PHASE_1`, Gate A -> `REJECTED`, re-run the affected questions. Questions are relayed to the user **one at a time** via the ask-questions tool — never batched.

---

## 4 Phase 4: Wiki Requirements & Acceptance Criteria
**Skill Executed:** `ptp-high-visionary` (spec-first directives) + `@wiki-writer`

> **Spec-First Rule:** The wiki is written BEFORE the code. Docs written here describe target behavior and are marked `status: in-progress` — a pre-code doc is a claim, not truth. Promotion to `stable` happens only at Wrap Up, after the executor verifies code matches spec.
>
> **Conditional Skip (checklist):** Run Phase 4 only if ANY of: (a) new or changed user-visible strings/UI, (b) API, schema, or logic-contract change, (c) wiki-facing behavior change. If none apply, skip this phase and record: `No wiki delta — rationale: <why>`. Never skip silently.

**Conditional Skip — NOT applied.** Two conditions hit: (b) logic-contract change (what the registry *is*, where a binding comes from, what a satellite may do with it) and (c) wiki-facing behavior change (the documented rebinding procedure and the documented opt-out both change meaning). Spec written below.

**Wiki Docs to Write/Update (as `in-progress`):**
| Doc | Change |
|---|---|
| `.wiki/core/18-knowledge-capture.md` | Amend the `Dual-surface agent binding` entry *(2026-09-06)* into the **registry-canonical triple**: the Model Registry is the source; per-file frontmatter and `opencode.json` are derived projections. Record the **T1-E1.01 reversal** + the clone-fleet contract (strict force-propagation, no durable satellite-side binding) as one dated decision. Frontmatter `status: in-progress` until Wrap Up verifies the shipped code. |
| *machinery docs — authored with the code in Phase 8; listed here because they are this template's spec surface, versioned by `machinery-version`* | `.devops/rules/agents-and-skills.md` § Agent Home + § Sync Protocol gain the **binding contract** (registry-canonical; sync force-stamps; a registry key with no file, or a binding file with no row, is a hard failure). `.devops/skills/model-routing/SKILL.md` §1/§3 rewritten to a **template-side** rebind procedure. `HOW-TO.md:134` (v20 opt-out note) and `:154` (CI/provider prose) corrected. `README.md:57` bootstrap step corrected. `.devops/templates/SATELLITE-BOOTSTRAP.md` §2 + v20 migration note corrected; per-satellite CI wiring documented (Q8). |

**Acceptance Criteria:**
| # | Criterion (behavior) | Test Target |
|---|---|---|
| 1 | `check-parcel-prefix.ps1` validates **12** registry rows and reports `MODEL <value>` for all 12 agent files (3 orchestrators + 7 `ptp-*` + `wiki-writer` + `wiki-verifier`) and `OC-MODEL` for all 12 `opencode.json` keys. Exit 0. | Template repo, script run |
| 2 | One registry row per binding file and one binding file per registry row — a declared key with no file, or a discovered file with no row, is a hard failure naming the key. | Synthetic: rename an agent file / delete a registry row |
| 3 | **Seed↔live agreement:** a model cell in `.devops/templates/base-context.template.md` that is blank or differs from the live registry fails the check, naming the seed file, the key and both values. | T1 |
| 4 | **No unbound seed:** a `agent.<key>.model` value of `<your provider/model>` in `.devops/templates/opencode.template.json` fails the check, naming the seed file and key. | T2 |
| 5 | **Opt-out retired:** a target `opencode.json` with no `agent` block (or an empty one) fails with a message pointing at `.devops/templates/opencode.template.json` — no `SKIP`. An absent `opencode.json` file still fails as today. | T5 |
| 6 | `wiki-writer.agent.md` / `wiki-verifier.subagent.md` are validated **by the binding pass only**; the prefix-integrity pass leaves their bodies untouched (`-Sync` must not rewrite them). | Template repo: `-Sync` then `git diff` shows no change to those two files |
| 7 | **Force propagation lands coherent:** after `sync-architecture.ps1 -Target <throwaway>`, the target's registry rows, all 12 frontmatter `model:` lines and all 12 `opencode.json` models equal the template's, and the *target's* `check-parcel-prefix.ps1` exits 0. | T3 |
| 8 | **Force has no preservation branch:** a target whose registry cell and agent frontmatter were locally edited is re-stamped on the next sync (both edits reverted). | T4 |
| 9 | **Q3 regression guard:** `-Check` exit code and verdict lines are unchanged for a target with diverged bindings — no new failure mode, no `BINDING-*` lines in `-Check`. | T6 |
| 10 | Every touched doc states the clone contract: no satellite-side rebinding instruction, no opt-out, no placeholder to fill. Machinery discipline holds: `sync-manifest.yaml` `machinery-version` bumped and recorded in `version-history.md`. | Doc grep + `validate.yml` version gate |

**Spec Notes:**
- **Data flow / state changes:** binding flows one way — `base-context.md` `## Model Registry` (template) → sync → target registry table → target agent frontmatter (`model:`) → target `opencode.json` (`agent.<key>.model`). The registry is the only source; the other two are projections. `-Sync` re-inlines the ORCHESTRATOR-ONLY block (which contains the registry) into the 3 orchestrator files; the 7 `ptp-*` files carry only the shared prefix, so their inlined bodies do not change.
- **Ordering constraint:** stamping must run **before** prefix regeneration in sync, so the regenerated orchestrator prefixes carry the stamped registry.
- **Decoupled passes:** prefix integrity (existing, `parcel*`/`ptp-*` only, and `wiki-*` files must never enter it — verified: neither carries the shared prefix) vs binding integrity (all 12 files, frontmatter `model:` only).
- **Edge cases the spec must cover:** target with no `base-context.md` yet (fresh clone — sync currently SKIPs prefix regen; stamping must not throw); target with no `opencode.json` (FAIL, unchanged); `agent` block present but a key missing (`BINDING-SKIP <key>` from sync + validator FAIL); registry row present but agent file absent (FAIL); agent file matching the binding glob with no row (FAIL); registry table with a reordered or 3-cell row (fails loudly, never silently mis-parses); a satellite-local extra agent file outside the binding glob (invisible to the check — deliberate).
- **Deliberate non-goals (spec level):** no reporting of binding divergence in `-Check` (Q3); no credential/provider reachability check (Phase 3 warning 7); no creation/restructuring of the target's `agent` block (Q4 = FAIL instead); no CI file transport (Q8).
- **Docs consumed by this feature (read-first list):** `.devops/skills/model-routing/SKILL.md`; `.devops/skills/pass-the-parcel/SKILL.md` § Model Registry; `.devops/rules/agents-and-skills.md`; `.devops/rules/plan-lifecycle.md` § Deviations; `.wiki/core/18-knowledge-capture.md` § Agents & Models + § Sync & Versioning; `HOW-TO.md` § Sync; `.devops/templates/SATELLITE-BOOTSTRAP.md`.

---

> **NO HALT after Phase 4:** the spec is reviewed together with the implementation plan at Gate B (after Phase 5) — one decision, what it will do and what it will cost.

---

## 5 Phase 5: High-Visionary Standard Implementation Plan
**Skill Executed:** `ptp-high-visionary`

**Simplicity Gate:**
> [x] Climbed the Simplicity Ladder (7 rungs)
> [x] No speculative abstractions
> [x] Reuse over reimpl - Reuse Log populated
> [x] Deliberate simplifications marked with `ponytail:` comments
> [x] Safety exceptions preserved (the check must fail loudly on a misaligned or unbound surface — no silent-pass branch survives)

**Reuse Log:**
| Existing Asset | Where Reused | Ladder Rung |
|---|---|---|
| `check-parcel-prefix.ps1` registry parser (`:95-103`) | widened and reused for the seed↔live comparison — not duplicated | 2 |
| `check-parcel-prefix.ps1` per-file binding check (`:194-216`) | refactored into a decoupled binding pass covering all 12 files | 2 |
| `check-parcel-prefix.ps1` opencode.json validation (`:222-255`) | reused; the absent/empty-block branch flips SKIP → FAIL | 2 |
| `sync-architecture.ps1` prefix-regeneration step | the pass that already rewrites target prefixes also stamps target `model:` lines | 2 |
| `.devops/templates/*` (already a `portable_dir`) | carries the propagation source; Q1 needs **no new artifact** | 1 |
| `validate.yml` "Parcel prefix integrity" step | hosts the extended check with no new CI step (Q7/Q8) | 2 |
| `sync-architecture.ps1 -SelfTest` | extended to assert propagation instead of a new test harness | 2 |
| `Get-ItemHashes` frontmatter-stripping (`:271-292`) | untouched — Q3 keeps `-Check` hash-only | 4 |

**Spaghetti Triage (See-Name-Route, Do Not Fix):**
- [ ] No smells noticed

| File | Function | Smell Type | Severity | Recommended Action |
|---|---|---|---|---|
| `scripts/check-parcel-prefix.ps1` | whole file | god-script (prefix integrity + binding integrity + JSON config validation in one pass) | medium | See-Name-Route: extract the binding pass into its own function this round; a full 3-way split is a separate parcel only if a fourth concern lands. Route to backlog at Wrap Up. |

### To-Do List
- [x] 1. Widen the registry parser in `scripts/check-parcel-prefix.ps1` to accept `wiki-*` keys, and extract the per-file binding check out of the prefix loop into its own function that resolves binding files from registry keys.
- [x] 2. Add the seed↔live registry comparison (`base-context.template.md` vs the live registry) — missing / blank / differing cells fail.
- [x] 3. Add the seed opencode comparison (`opencode.template.json`) — missing key or `<your provider/model>` fails.
- [x] 4. Flip the live `opencode.json` absent/empty-`agent`-block branch from `SKIP` to `FAIL`, naming the seed.
- [x] 5. Add the symmetric existence checks (registry key with no file, binding file with no row).
- [x] 6. Stamp bindings in `scripts/sync-architecture.ps1`: registry rows → target `base-context.md`, `model:` lines → target agent frontmatter, model values → target `opencode.json`, ordered **before** prefix regeneration.
- [x] 7. Extend `-SelfTest` to assert the stamped target surfaces and the target's clean prefix check.
- [x] 8. Update both registries (live + seed) to **12 rows** and rewrite the "example binding, not a mandate" prose to the clone contract.
- [x] 9. Fill all **12** placeholder model values in `.devops/templates/opencode.template.json`; delete the opt-out paragraph and rewrite the customization note.
- [x] 10. Rewrite `.devops/skills/model-routing/SKILL.md` §1/§3 (registry-canonical, template-side rebind) + bump `version`.
- [x] 11. Mirror the registry paragraph in `.devops/skills/pass-the-parcel/SKILL.md` + bump `version`.
- [x] 12. Update `AGENTS.md` rule 9 **and** its `.devops/templates/AGENTS.template.md` mirror; add the binding contract to `.devops/rules/agents-and-skills.md`.
- [x] 13. Update `HOW-TO.md` (`:134`, `:154`), `README.md:57`, `.devops/templates/SATELLITE-BOOTSTRAP.md` §2 + the v20 migration note + per-satellite CI note.
- [x] 14. Run `check-parcel-prefix.ps1 -Sync` (re-inlines the ORCHESTRATOR-ONLY block into the 3 orchestrator files).
- [x] 15. Amend `.wiki/core/18-knowledge-capture.md`; bump `machinery-version` 38 → 39 and add the `version-history.md` row.
- [x] 16. Run the Test Verification Plan; record proof in Phase 9.

### File-Level Steps
1. `scripts/check-parcel-prefix.ps1` — (a) widen the registry regex's key alternation to include `wiki-[a-z0-9-]+`; (b) new binding-pass function: for each registry key, resolve `.devops/agents/<key>.agent.md` else `<key>.subagent.md`; missing file → failure; each binding file's frontmatter `model:` must equal the registry VS Code column; (c) same function walks the glob-matched agent files and fails on a file with no row; (d) seed registry parse + cell-by-cell compare against the live registry; (e) seed opencode parse: every registry key must be present with the registry's opencode value and not `<your provider/model>`; (f) live opencode: absent or empty `agent` block → failure with the seed pointer; (g) leave the prefix loop's file set **unchanged** (`parcel*.agent.md` + `ptp-*.subagent.md` only).
2. `scripts/sync-architecture.ps1` — new stamping function invoked in the sync path before prefix regeneration: parse the source registry, then (i) rewrite the target registry table's 4 cells for each key, (ii) replace each target agent file's frontmatter `model:` line, (iii) replace each present target `opencode.json` `agent.<key>.model` value. Report `BINDING-SKIP <key>` for keys absent from the target `agent` block; never add or restructure entries. Also extend `-SelfTest` to assert the three stamped surfaces.
3. `.opencode/plans/base-context.md` — 12 registry rows (add `wiki-writer` / `wiki-verifier`); rewrite the paragraph introducing the table from "example binding, not a mandate … each satellite authors its own `base-context.md` and rebinds" to the mandate + propagation contract. **Change only the ORCHESTRATOR-ONLY block** — the shared prefix must stay byte-stable except where the binding rule genuinely belongs.
4. `.devops/templates/base-context.template.md` — the same 12 rows with the same concrete values, and the same introductory-paragraph rewrite.
5. `.devops/templates/opencode.template.json` — replace all 12 `<your provider/model>` values with the concrete provider id; delete the opt-out sentences from the `_comment` array; replace the "REQUIRED CUSTOMIZATION: replace every model value" instruction with the propagation statement; keep the `agent` block and the `permission` blocks exactly as they are.
6. `.devops/skills/model-routing/SKILL.md` — §1 becomes "one registry, two derived runtime surfaces"; §3 becomes the template-side rebind procedure (edit the registry → run `-Sync` → sync stamps the fleet → validate), and it must state that a satellite-side edit is transient; §2 gains the `wiki-*` rows in its class list. Bump `version`.
7. `.devops/skills/pass-the-parcel/SKILL.md` — the Model Registry paragraph (:140) mirrors the new §1 framing. Bump `version`.
8. `AGENTS.md` rule 9 + `.devops/templates/AGENTS.template.md` mirror (preserve the `<!-- MACHINERY: keep verbatim -->` marker) — add the binding contract sentence and the extended-check requirement.
9. `.devops/rules/agents-and-skills.md` § Agent Home + § Sync Protocol + the presets-backtick note — add the binding contract, the `wiki-*` row requirement, and the fact that the check now covers the seed surfaces.
10. `HOW-TO.md` — rewrite `:134` (v20 opt-out migration note) and `:154` (provider/CI prose) to the clone contract; CI is per-satellite.
11. `README.md:57` — bootstrap step 3 no longer says "fill the placeholders"; it states bindings arrive with the seed and are re-propagated by sync.
12. `.devops/templates/SATELLITE-BOOTSTRAP.md` — step 2's `opencode.template.json` row, the v20 migration blockquote (retired), plus a per-satellite CI wiring line (copy the gate set into your own workflow; the check rides in `check-parcel-prefix.ps1`).
13. `.devops/sync-manifest.yaml` + `.devops/logs/version-history.md` — `machinery-version: 39` and its release row.
14. `.wiki/core/18-knowledge-capture.md` — the amended + reversed entries (Phase 4).

### Implementation Instructions (no code snippets except exact string literals: regex, SQL migration, CLI command, config key, error message)
- New exact literals the executor must reproduce byte-for-byte: the failure prefix `model binding mismatch - frontmatter`, the new keys `BINDING-SKIP` (sync report) and `MODEL` / `OC-MODEL` (check output), the placeholder literal `<your provider/model>`, the seed pointer text `.devops/templates/opencode.template.json`, the config paths `.opencode/plans/base-context.md`, `.devops/templates/base-context.template.md`, and the command `powershell -NoProfile -File scripts\check-parcel-prefix.ps1 -Sync`.
- The registry regex must keep requiring a bare (un-backticked) 4-cell row so the backticked `## Orchestrator Presets` keys stay excluded (`:.devops/rules/agents-and-skills.md:49` depends on this).
- **`ponytail:` registry stamping is a naive 4-cell row rewrite keyed on the first cell.** Ceiling: a hand-reordered or 3-cell registry table is not repaired — it fails loudly. Upgrade path: regenerate the table from a machine-readable binding list if a third derived surface ever lands.
- **`ponytail:` sync stamps only keys already present in the target's `agent` block.** Ceiling: a satellite with a missing key is reported (`BINDING-SKIP`) and its validator FAILs — sync never adds or restructures repo-specific JSON (each satellite's `permission` blocks legitimately differ). Upgrade path: a documented seed-block merge if the fleet ever needs unattended convergence.
- **`ponytail:` `-Check` does not compare bindings.** Ceiling: divergence from the source is invisible to sync (Q3). Upgrade path: add a `BINDING-DIVERGED` informational line if the fleet ever tolerates divergence.
- Binding is compared as **string equality on the provider-qualified id**; no credential or model-availability probe is in scope (Phase 3 warning 7).

### Test Verification Plan
| # | Command | Expected |
|---|---|---|
| V1 | `powershell -NoProfile -File scripts\check-parcel-prefix.ps1` | exit 0; 12 `MODEL` lines; 12 `OC-MODEL` lines; no `SKIP` for `wiki-*` |
| V2 | T1 — blank one cell in `.devops/templates/base-context.template.md`, re-run V1, restore | non-zero exit naming the seed file + key + both values; exit 0 after restore |
| V3 | T2 — set one seed opencode model to `<your provider/model>`, re-run V1, restore | non-zero exit naming `.devops/templates/opencode.template.json`; exit 0 after restore |
| V4 | T3 — `powershell -NoProfile -File scripts\sync-architecture.ps1 -Target <temp-dir>` then `powershell -NoProfile -File <temp-dir>\scripts\check-parcel-prefix.ps1` | sync reports the stamps; the target check exits 0 |
| V5 | T4 — edit the throwaway target's registry cell + one frontmatter `model:`, re-run the sync, re-read both | both reverted to the template binding (force, no preservation branch) |
| V6 | T5 — delete the throwaway target's `agent` block, run the target check | non-zero exit; message points at `.devops/templates/opencode.template.json` (no `SKIP`) |
| V7 | T6 — in the throwaway target, edit one `model:` frontmatter line and one registry cell, then `powershell -NoProfile -File scripts\sync-architecture.ps1 -Target <temp-dir> -Check` | exit 0 — binding divergence stays silent to `-Check` by design (Q3); no `BINDING-*` lines in the output |
| V8 | `powershell -NoProfile -File scripts\sync-architecture.ps1 -SelfTest` | exit 0, with the binding assertions included |
| V9 | `powershell -NoProfile -File scripts\check-utf8-agents.ps1` | exit 0 |
| V10 | `python scripts/wiki_lint.py --quiet` + `python scripts/wiki_claims.py check` + `python scripts/wiki_visualize.py --check` | exit 0 (docs touched) |
| V11 | Grep the touched docs for `not a mandate`, `rebinds per its available models`, `delete the` + `agent` block, `<your provider/model>` | zero hits in prose that tells a satellite to bind itself |

### Wiki Core References
- `.wiki/core/18-knowledge-capture.md` -> § Agents & Models + § Sync & Versioning: the decisions this plan amends (Dual-surface agent binding; Farthest-evolved consumer wins) and the reversal it records.
- `.wiki/core/00-system-index.md` -> § 13 Operational Docs: links the changelog + version history this plan bumps; no row change expected (record the rationale if that holds).
- `.wiki/rules/document-structure.md` + `.wiki/rules/frontmatter.md` -> the `status: in-progress` → `stable` discipline for the amended doc.

### Wiki Docs to Add/Edit
- `.wiki/core/18-knowledge-capture.md` — amend one entry, add one (the reversal + clone contract), `status: in-progress`.

---

> **HALT POINT (Gate B — Spec & Plan Review):** Phases 4-5 complete. Present the wiki requirements spec (with acceptance criteria) AND the execution plan together as one decision. Do not proceed to reviews until user approves. Update the **State & Gates** section at the bottom of this file: Status -> `PHASE_5`, Active Persona -> `High-Visionary`. Leave Gate B `OPEN` — the orchestrator records the user's verdict.

---

## 6 Phase 6: Grumpy Architect Spec & Logic Audit
**Skill Executed:** `ptp-grumpy-architect` (`SINGLE`: orchestrator inline)

> **`SINGLE` topology:** no independent reviewer. The orchestrator logs an inline self-review checkpoint here and records **Verdict** `N/A — SINGLE self-review`.

**Verdict:** `PASS` (inline self-review — presented at Gate B; user-accepted absence of an independent reviewer at plan selection)

- **System Contracts Explicit:** All five touched executable surfaces are named with exact paths: `scripts/check-parcel-prefix.ps1` (extended, 7 sub-changes), `scripts/sync-architecture.ps1` (new stamping function + `-SelfTest` assertions), `.opencode/plans/base-context.md` (ORCHESTRATOR-ONLY block only), `.devops/templates/base-context.template.md`, `.devops/templates/opencode.template.json`. `pull-architecture.ps1` and `validate.yml` are deliberately unchanged (thin-wrapper and existing-step reuse).
- **File Boundary & Scope Collisions:** No plan is in flight; `.devops/plans/` holds only the template. `check-parcel-prefix.ps1` / `sync-architecture.ps1` were last rewritten by T1-E3.02 (archived, `depends_on` satisfied) — no live writer conflicts.
- **Dependency Gaps:** None. Every referenced asset exists and was read: registry parser (`:95-103`), binding check (`:194-216`), opencode validation (`:222-255`), self-test harness (`:108-173`), seeds, and the five prose docs (`AGENTS.md`, `.devops/rules/agents-and-skills.md`, `HOW-TO.md`, `README.md`, `SATELLITE-BOOTSTRAP.md`).
- **YAGNI Bloat:** Checked and rejected as out of scope: a separate `check-model-bindings.ps1` (Q7), a new binding manifest (Q1), transporting `validate.yml` (Q8), JSON restructuring/merge of a satellite's `agent` block (Q4 → FAIL instead), provider-credential probes (Phase 3 warning 7), and `BINDING-DIVERGED` reporting (Q3). **No new files are created by this plan.**
- **Paranoid Security (Contract Level):** No secrets involved; bindings are non-secret identifiers. No trust boundary is introduced. The scripts keep failing loudly (`$ErrorActionPreference = 'Stop'` semantics preserved) and gain no silent-pass branch — Q4's change strictly *removes* one silent path.
- **Survivability:** Every new failure path is deterministic and names the file + key + both values. Guarded cases: target without `base-context.md` (stamping must no-op, matching today's prefix-regen SKIP); target without `opencode.json` (unchanged FAIL); target `agent` block without a key (`BINDING-SKIP` + validator FAIL). No network, no async, no timers.
- **Edge Cases:** Enumerated in Phase 4 Spec Notes (empty registry, reordered/3-cell row, file-without-row, row-without-file, extra satellite-local agent files, fresh clone). Two verified-as-cleared items: (i) `-Check`'s frontmatter-stripping branch does **not** need to cover `wiki-*` — after force-stamping those files are byte-identical to the source, so full-file hashing yields `CURRENT`, not phantom DRIFT; (ii) `sync-architecture.ps1 -Verify` already FAILs on a missing `opencode.json`, so Q4 adds no new satellite requirement there.
- **Performance Trade-offs:** Four extra small file parses per check run (two seeds, one registry, 12 frontmatters). No scaling concern — bounded by the registry size.
- **Architectural Anti-Patterns:** The plan removes a duplicate-source-of-truth (three independent binding statements) in favour of one declared source with two projections; it also decouples two concerns that were tangled in one loop. Residual smell — `check-parcel-prefix.ps1` remains a god-script — is See-Name-Routed to the backlog, not fixed here.
- **Endpoint Protection & Rate Limiting:** N/A — no endpoints, no runtime service; scripts run in CI and locally.
- **Wiki Core Compliance:** Every blueprint cites a `.wiki/core/` doc (Phase 5 § Wiki Core References); the single wiki edit is the KC contract entry, held `in-progress` until Wrap Up.
- **Ordering hazard (resolved in-plan):** stamping runs **before** prefix regeneration, otherwise the orchestrator prefixes would inline a stale registry while every check still passed.

**Dead-Code / Orphan Flags (for Wrap Up backlog entry):**
| plan section / file_path:line_number | Reason |
|---|---|
| `scripts/check-parcel-prefix.ps1:1-259` | God-script: prefix integrity + binding integrity + JSON config validation in one pass. Route the 3-way split to `.devops/backlog/` at Wrap Up (See-Name-Route). |
| `scripts/check-parcel-prefix.ps1:199` | The `SKIP … no Model Registry row` branch becomes unreachable once all binding files are glob-matched and a missing row is a failure; delete it or repoint it to a genuinely unknown key. Executor must resolve this, not leave dead output. |

**Required Fixes:**
> [x] [Fix 1 - None] — the inline self-review found no blocking flaw. The two flags above are routed (backlog / executor cleanup), not fixes.

> **Rejection Rule:** If plan does not make the app faster, safer, or easier to modify, do not check boxes. Reject and force rewrite via `PHASE_5_REVISION`.
>
> **Spec-level audit — the plan contains no code, so no code-level scans (DRY/WET, line checks).**

- **System Contracts Explicit:** (function/component names, file paths, interface boundaries)
- **File Boundary & Scope Collisions:** (proposed paths checked against existing code)
- **Dependency Gaps:** (all referenced utils/hooks/schemas/services accounted for)
- **YAGNI Bloat:** (no speculative modules, placeholder files, empty scaffolding)
- **Paranoid Security (Contract Level):** (secrets via env + .gitignore; input typed with Zod/unknown; RLS for DB)
- **Survivability:** (async/timeout/network covered for every flow; Error Boundaries)
- **Edge Cases:** (empty/null inputs; boundary/limit conditions; concurrency/races; state transitions)
- **Performance Trade-offs:** (N+1, unbounded rendering, missing indexes; scaling ceiling + upgrade path)
- **Architectural Anti-Patterns:** (god modules, spaghetti coupling, duplicate sources of truth, dead-end abstractions, feature bleed)
- **Endpoint Protection & Rate Limiting:**
- **Wiki Core Compliance:** (every blueprint cites a .wiki/core/ doc)

**Dead-Code / Orphan Flags (for Wrap Up backlog entry):**
| plan section / file_path:line_number | Reason |
|---|---|

**Required Fixes:**
> [ ] [Fix 1 - or mark "None"]

---

## 7 Phase 7: Smooth Operator Product Review
**Skill Executed:** `ptp-smooth-operator` (`SINGLE`: orchestrator inline)

> **`SINGLE` topology:** no independent reviewer. Record **`N/A — SINGLE self-review`**; the self-review is presented at Gate B.

> **Rejection Rule:** If plan introduces unnecessary complexity or scope expansion, do not check boxes. Reject and force rewrite.

- **Vision & Journey Integrity:** This repo is the template, not an app — the "journey" is the maintainer's: rebind once in the registry, sync, and every satellite lands consistent. The plan shortens that journey (one source, one command) instead of lengthening it; no user-facing surface exists to regress.
- **Scope Containment (No Gold Plating):** Zero new files, zero new dependencies, zero new CI steps. Every rejected expansion is recorded with its reason (Phase 6 YAGNI bullet). The two deliberately *removed* capabilities (silent SKIP for a block-less `opencode.json`; the "not a mandate, rebind yourself" escape hatch) reduce surface area rather than add it.
- **4 Core User States:** N/A as UI states. The four states that actually apply are the script's: **success** (all surfaces agree, exit 0), **empty** (target not yet bootstrapped — stamping no-ops, prefix regen SKIPs as today), **error** (any disagreement fails naming file + key + both values), **partial** (a satellite key missing from the `agent` block → `BINDING-SKIP` + validator FAIL). All four are specified.
- **Guardrails & Permissions:** The plan touches bindings only — never a satellite's `permission` blocks (explicitly out of the merge path). It introduces no new destructive action: the only writes are the three binding surfaces, and each write is value-for-value reversible by editing the registry. This is the one place the plan *removes* a guardrail (the opt-out), and that removal is user-directed (Q4) with the migration path documented in `version-history.md` + `SATELLITE-BOOTSTRAP.md`.
- **Mobile / A11y / Telemetry:** N/A. No UI, no telemetry surface; failure output is human-readable text in CI logs, which is the correct channel for a machinery gate.

**Required Fixes:**
> [x] [Fix 1 - None] — no scope expansion, no unnecessary complexity. Both Phase 6 flags are routed (backlog split; one dead `SKIP` branch for the Executor to resolve), not product-scope items.

---

> **HALT POINT (Gate C — Peer Reviews):** Reviews complete. Present findings and required fixes. Update the **State & Gates** section at the bottom of this file:
> - **PASS:** Phase 6 log clean -> set Status `PHASE_7`, Active Persona `Reviewer`. Do not proceed to execution until user approves Gate C.
> - **FAIL:** Phase 6 or 7 flagged blocking flaws -> set Status `PHASE_5_REVISION`, Active Persona `High-Visionary`. Return to Group B for plan adjustments, then re-run Phases 6-7. **Never advance an unapproved plan to execution.**
> The orchestrator records Gate C -> `APPROVED`/`REJECTED` only after the user's verdict.
>
> **`SINGLE` topology:** Gate C is `N/A` — the plan approval happens once at Gate B (spec + plan + inline self-review). Update the State & Gates `Gate C` row to `N/A (SINGLE)`.

---

## 8 Phase 8: Execute Changes
**Skill Executed:** `ptp-code-surgeon` (`SINGLE`: orchestrator inline)

**Execution Isolation:** Phase 8 triggers ONLY after the plan-approval gate (Gate B in `SINGLE`, Gate C in `MULTI`) is cleared by explicit user input.

**Single-Pass Direct-to-Disk:**
> [x] Implementation written DIRECTLY to source files - no intermediate Markdown code blocks, no drafting files
> [x] Touched only intended lines - no adjacent refactors
> [x] Cleaned up only owned orphans
> [x] Pre-existing dead code left untouched

**Execution trace (22 files written):**
- `scripts/check-parcel-prefix.ps1` — registry regex widened to `wiki-*`; the binding block was removed from the prefix loop and replaced by a decoupled binding pass (registry key → agent file resolution, both directions); seed-registry cell-by-cell compare; seed-opencode compare; live `opencode.json` absent/empty-block `SKIP` → `FAIL`; the now-unreachable `SKIP … no Model Registry row` branch deleted (Phase 6 flag resolved).
- `scripts/sync-architecture.ps1` — new `Get-RegistryBindings` + `Update-TargetModelBindings` (registry rows → frontmatter → opencode model values, targeted textual rewrite, `BINDING-SKIP` report); wired as step 3d **before** prefix regeneration; `-SelfTest` now asserts per-key frontmatter propagation; header docs updated.
- Registry (**live + seed**): 12 rows, mandate prose, binding rule.
- `opencode.template.json` v6 → v7: 12 concrete model values, opt-out text removed.
- Skills: `model-routing` v5 → v6 (§1 registry-canonical triple, §3 template-side procedure, §4 six-surface contract), `pass-the-parcel` v11 → v12 (registry paragraph).
- Docs: `AGENTS.md` + `.devops/templates/AGENTS.template.md` rule 9, `.devops/rules/agents-and-skills.md` (binding files, sync bullet, backtick constraint), `HOW-TO.md` `:134`/`:154`, `README.md:57`, `SATELLITE-BOOTSTRAP.md` v9 → v10.
- State: `machinery-version: 39` + `version-history.md` v0.7.10 row; KC entry amended + `status: in-progress`.
- `check-parcel-prefix.ps1 -Sync` re-inlined the ORCHESTRATOR-ONLY block into the 3 orchestrator agents (×3 `FIXED`).

**Two items outside the numbered steps, recorded as deliberate deviations (not silent):**
1. `.wiki/core/09-design-system.md` + `.wiki/core/12-security-standards.md` — 3 Grounded Claims bind to `AGENTS.md` by content hash and the rule-9 edit invalidated them. `python scripts/wiki_claims.py update` re-stamped them (the documented remedy for a stale hash). Without it the claims gate stays red and V10 cannot pass.
2. `.devops/README.md` and `.wiki/core/00-system-index.md` — reviewed, **no edit needed**: neither carries binding prose, and the operational-docs index rows are generic. Recorded rather than silently skipped.

---

## 9 Phase 9: Verify Changes
**Skill Executed:** `ptp-code-surgeon` (`SINGLE`: orchestrator inline)

**Build & Lint:**
> [x] Compilation check clean (PowerShell parses + runs; no compiler in this repo)
> [x] Lint clean (exit 0) — `check-parcel-prefix.ps1`, `check-utf8-agents.ps1`, wiki lint all exit 0
> [x] Type-generation re-run if schema/API changes — N/A (no schema/API)

**Test Report — every command exits `0`:**

| # | Command | Result |
|---|---|---|
| V1 | `check-parcel-prefix.ps1` (template repo) | **exit 0** — `MODEL` ×12, `SEED-OC-MODEL` ×12, `OC-MODEL` ×12, no `SKIP` for `wiki-*` |
| V2 | T1 — blank one seed registry cell, re-run, restore | **exit 1** — `seed base-context.template.md: registry row 'parcel' has a blank vscode model` (+ `opencode`). Restored → exit 0 |
| V3 | T2 — set a seed opencode value to `<your provider/model>`, re-run, restore | **exit 1** — `seed opencode.template.json: agent 'parcel' still has the placeholder model '<your provider/model>'`. Restored → exit 0 |
| V4 | T3 — sync into a throwaway target, author the seeds, `-Sync`, run the target's check | **exit 0** — OK line; 12 `MODEL` + 12 `OC-MODEL` in the satellite |
| V5 | T4 — rebind registry row + frontmatter + opencode in the target, re-sync | **reverted** — `BINDINGS: stamped 1 registry row(s), 0 frontmatter line(s), 1 opencode model value(s)`; all three back to `DeepSeek V4.1 Flash` / `opencode-go/deepseek-v4.1-flash` (the frontmatter needed 0 stamps because the portable-dir copy already overwrote it — force either way) |
| V6 | T5 — delete the target's `agent` block, run the target's check | **exit 1** — `opencode.json: no 'agent' block - model binding requires it (seed: .devops/templates/opencode.template.json)` — no `SKIP` |
| V7 | T6 — `sync-architecture.ps1 -Target <temp> -Check` after a local binding divergence | **exit 0**, 0 `BINDING` lines — bindings stay invisible to `-Check` by design (Q3) |
| V8 | `sync-architecture.ps1 -SelfTest` | **exit 0** — `SELFTEST OK: 5 dirs, 38 skills, 11 files materialised correctly` (binding assertions included) |
| V9 | `check-utf8-agents.ps1` | **exit 0** — `ALL CLEAN (201 files scanned)` |
| V10 | `wiki_lint.py --quiet` / `wiki_claims.py check` / `wiki_visualize.py --check` / `wiki_coverage_check.py` | **exit 0** — lint 0, claims 0 stale (after the documented `update`), `visualizer OK: 56 docs, 28 edges`, `coverage OK: no src/ tree` |
| V11 | Stale-prose grep across the touched docs (`not a mandate`, `rebinds per its available models`, `VS Code-only satellite`, `fill the placeholders`) | **no hits** — the only `<your provider/model>` occurrence is the `@model-routing` §4 line that declares it fatal |

- [x] Lint pass (exit 0)
- [x] Tests pass (exit 0)
- [x] Build pass (exit 0 — `-SelfTest` is the build)
- [x] Code matches exact plan specifications — verified by re-running the Phase 5 Test Verification Plan commands; every command exits `0`
- [x] No functional gaps identified

**Deviation log (from Phase 8):** 3 claim hashes re-stamped in `.wiki/core/09-design-system.md` + `.wiki/core/12-security-standards.md` (derived from the `AGENTS.md` edit — the documented `wiki_claims.py update` remedy). No other deviation from the approved plan.

---

> **HALT POINT (Gate D — Implementation):** Implementation and verification complete. Present completed work to user. Do not proceed to user review until user signs off. Update the **State & Gates** section at the bottom of this file: Status -> `PHASE_9`, Active Persona -> `Executor`. Leave Gate D `OPEN` — the orchestrator records the user's verdict. On rollback: Status -> `PHASE_8_FAILED`.

---

## 10 Phase 10: User Review & Tweaks
**Skill Executed:** `knowledge-capture`

**Tweak Discipline:**
> Tweaks are surgical, not architectural. `expansion` and `refactor` are HALT conditions.

- [x] Tweak classified: `fix` / `expansion` / `refactor` — **none requested**
- [x] `expansion` and `refactor` routed to new parcel — N/A (no tweaks)
- [x] `fix` touches <= 3 files — N/A
- [x] `fix` introduces no new abstraction absent from original plan — N/A

**Capture Categories:** Recurring / Tribal / One-off / Cosmetic-skip

| Round | Feedback | Action Taken | Capture Flag | Result |
|---|---|---|---|---|
| 1 | Gate D approval, no tweaks requested | none needed | One-off (no capture) | COMPLETE |

**Sign-Off:** 2026-09-13 - User approved.

---

## Completion Note (Wrap Up)
**Skill Executed:** `agent-wrap-up`

> [x] Read `.wiki/core/18-knowledge-capture.md` for existing related decisions
> [x] Themed tweaks synced to knowledge capture doc (none from Phase 10; the plan's own contract recorded instead)
> [x] Wiki docs updated per Phase 10 tweaks (no tweaks; one KC entry amended + one pitfall added)
> [x] Code reconciled against Phase 4 spec — deviations logged
> [x] `status: in-progress` wiki docs promoted to `stable` (or deviation logged)

**Outcome:** all 16 to-dos and all 10 acceptance criteria landed. 24 files written across 8 phases (22 code/doc + this plan + the new parked backlog item). Every gate halt was honoured; no execution rollback, no `PHASE_8_FAILED`.

**Themed Tweaks:** None. Phase 10 closed in one round — Gate D approved without a tweak request.

**Knowledge Capture Entries:**
- **Amended:** `Dual-surface agent binding` → **registry-canonical model binding** (registry = source; frontmatter + `opencode.json` = derived; force-stamped; 12-row coverage; opt-out retired). Records the T1-E1.01 reversal inline.
- **Amended:** `Farthest-evolved consumer wins` gained the binding exception (bindings are template-owned, not ported back).
- **Added (pitfall):** editing a claim source (`AGENTS.md`) invalidates claims in unrelated docs (09/12) — the CI claims gate is the only signal.
- **Amended:** Top-10 rules 3 and 7 now carry the binding qualification.
- Tidy-mode consolidation run: 89 → 91 lines, no cuts, no duplicates, well under the 500-line cap (under the 200-line full-audit trigger, so no audit scheduled).

**Spec Reconciliation:** two recorded deviations, both handled inside the plan rather than silently:
1. Three Grounded Claim hashes re-stamped (`09-design-system.md`, `12-security-standards.md`) — a derived consequence of the `AGENTS.md` rule-9 edit; `wiki_claims.py update` is the documented remedy and V10 requires exit 0.
2. `.devops/README.md` and `.wiki/core/00-system-index.md` were listed in `touches` but needed no content edit (no binding prose; index rows are generic). Recorded in Phase 8 rather than skipped silently.

**In-Progress Promotions:** `.wiki/core/18-knowledge-capture.md` → `stable` (reconciled: the shipped `check-parcel-prefix.ps1` / `sync-architecture.ps1` behaviour matches every clause of the amended entry, proven by V1-V8).

**Dead-Code Backlog Entries (from Phase 6):** `T1-E2.02` — split the `check-parcel-prefix.ps1` god-script (4 unrelated concerns in one pass). Parked at `.devops/backlog/t1-e2.02-check-parcel-prefix-split-backlog.md`, with a row in the T1 theme register's E2 Open table. The other Phase 6 flag (the unreachable `SKIP … no Model Registry row` branch) was **resolved in execution**, not deferred.

**Wiki Updates:** `.wiki/core/18-knowledge-capture.md` (entry amendments + pitfall + promotion), `.wiki/core/00-system-index.md` (`Last Verified` 2026-09-13 for slots 09 + 12, whose claim hashes moved), `.wiki/core/09-design-system.md` + `.wiki/core/12-security-standards.md` (claim re-stamps only).

**Plan Archiving:** Plan archived to `.devops/archive/t1-e1.03-model-binding-propagation-plan.md` (via `git mv`, no stub).

**Backlog Review:** T1 register reviewed — T1-E1.01 (reconcile model registry) is **superseded** by this plan's registry-canonical contract; its Completed-row note still reads "concrete model binding is satellite configuration", which this plan reversed. Annotation added there (2026-09-13). No other open items matched; T2/T3 registers untouched — nothing in this session resolved or invalidated them.

---

## 📍 State & Gates (CACHE-ANCHORED — update ONLY this section at gate transitions)

> **Cache rule:** This is the **last section** in the file. Gate transitions mutate ONLY the rows below — phase content above AND the frozen **Plan Settings** block at the top stay byte-stable to preserve LLM prefix-cache hits. Every "Update Status" instruction in the halt points above means "edit this section".

| Metric | Value |
|---|---|
| **Status** | `COMPLETE` |
| **Version** | `v0.1.0` |
| **Active Persona** | `Lead Context Architect` |
| **Depends On** | `T1-E3.02` (archived 2026-09-13 — satisfied) |
| **Blocks** | none |

> **Settings pointer:** `Mode` and `Agents` live in the frozen **Plan Settings** block at the TOP of this file — read them there before any phase. Never duplicate them here.

> Valid states: `QUEUED`, `CLAIMED`, `PHASE_1`, `PHASE_3`, `PHASE_5`, `PHASE_5_REVISION`, `PHASE_7`, `PHASE_8_FAILED`, `PHASE_9`, `COMPLETE`.

| Gate | Requirement | Status |
|---|---|---|
| A | Scope approved (Phases 1-3, + 3.5 in AUTO) | `APPROVED` |
| B | Spec & plan approved (Phases 4-5) | `APPROVED` |
| C | Peer reviews passed (Phases 6-7) | `N/A (SINGLE)` |
| D | Implementation verified (Phases 8-9) | `APPROVED` |

> Gate flips: rows flip `OPEN` → `APPROVED`/`REJECTED` ONLY after the user's (or AUTO verification's) verdict, recorded by the orchestrator. Executing agents halt with their gate `OPEN`. Rejection routes: A -> `PHASE_1`; B/C -> `PHASE_5_REVISION`; D/rollback -> `PHASE_8_FAILED`. Never edit rows above this section for gate bookkeeping.

> **This section is the LAST section in the file. All gate bookkeeping happens here.**
