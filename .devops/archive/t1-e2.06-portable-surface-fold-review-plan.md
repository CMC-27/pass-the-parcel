---
code: T1-E2.06
sprint: none
claim_status: COMPLETE
owner: "parcel"
claimed_at: "2026-09-18"
last_touch: "2026-09-18"
touches: [".devops/rules/process-lessons.md", ".devops/skills/ptp-high-visionary/SKILL.md", ".devops/skills/sprint-close/SKILL.md", ".devops/skills/agent-wrap-up/SKILL.md", ".devops/skills/sync-architecture/SKILL.md", ".devops/agents/ptp-high-visionary.subagent.md", ".devops/sync-manifest.yaml", ".devops/logs/version-history.md"]
depends_on: []
---
# Parked: T1-E2.06 - Portable-surface fold review (the hygiene pass a satellite cannot run)

## ⚙️ Plan Settings (FROZEN — set at plan start, read before any phase)

| Setting | Value | Meaning |
|---|---|---|
| **Mode** | `AUTO` | Auto-clear Gates A–C only on positive evidence; Gate D always human |
| **Agents** | `SINGLE` | Fast plan — inline personas, Group C skipped, Gates B+C merged at Gate B, Gate C `N/A` |

> Claimed directly (no sprint; Sprint 9 closed, queue empty). Parked findings below serve as Group A/B reference; Phase 3 re-run per Fresh Context Rule.

> **Origin:** Surfaced 2026-09-16 at the close of GRID-Link's Sprint 8 (`sprint-8-theme-clearance`), while executing `@sprint-close` § 6's fold review *in the satellite*. The review ran but **could not be completed there** — an entry deleted locally is restored by the next pull, and a rule folded locally is reverted by it, because `.devops/rules/**` and `.devops/skills/**` are both on the portable surface. Rather than half-execute, the findings were routed here. **No finding is owned by any plan in that sprint's queue**, and none was silently patched mid-batch.
>
> **Why this is the right home:** finding F1 is a **ticking** loss — the satellite's only locally-authored machinery lesson is erased the next time anyone pulls. F2–F5 are the defects the blocked review exposed.

## Problem

### F1 - A satellite's locally-authored machinery lesson is destroyed by the next pull

`portable_dirs` includes `.devops/rules`, and the engine copies with `Copy-Item $s\* $t -Recurse -Force` (`scripts/sync-architecture.ps1:902,918`). That is a **merge by name**, so `.devops/rules/process-lessons.md` is replaced wholesale from the template on every pull.

Measured 2026-09-16 by diffing the two registers: the template carries **18** entries, GRID-Link carries **15**, and the satellite has exactly **one** entry the template lacks —

> `[2026-09-16] A register row reading COMPLETE is a claim, not a measurement` — `T21-E6` recorded the Firebase Node 20→22 runtime upgrade COMPLETE; six days later `firebase functions:list` reported `nodejs20` on all five functions, including the JWT bridge every authenticated query depends on. The parcel updated one of two declarations and the other one won at deploy time. *Do instead:* re-measure a deployed state from the live artefact rather than the declaration, and when two declarations exist for one setting, name the single owner instead of updating one and leaving the other to win.

Four template entries would arrive in the same pull. So the exposure is asymmetric and silent: the pull looks like a normal upgrade while it discards the one lesson that satellite actually produced — and that lesson is the rule that caught a month-old falsified COMPLETE row.

**Fix:** add that entry to this repo's register verbatim (it is machinery knowledge, not satellite-specific) and attribute its origin. The satellite then converges on the next pull with nothing lost.

### F2 - The fold review is unexecutable in a satellite, and two entries have already matured

`@sprint-close` § 6 instructs: *"fold each matured machinery rule into its owning skill or `plan-lifecycle.md` and delete it from the register, so the staging register stays small."* In a satellite **both** homes are portable, so a local delete is restored and a local fold is reverted. The step is a no-op there, and the skill does not say so.

A per-entry maturity pass on the current register finds **two** entries that should not survive:

| Entry | Verdict | Evidence |
|---|---|---|
| `[2026-08-30] Wiki lint severity contract` (line 54) | **Superseded, and stale** | Fully carried by `.devops/skills/wiki-lint/SKILL.md` v4: line 15 states the exit-1 rule, the 11-row table supersedes the entry's five HARD examples and agrees with them, and §3 defines `--fix`'s exact scope. It is also **wrong**: it calls index-size WARN while the skill lists *"index size budget (>400 lines)"* under **Deferred - not implemented**. |
| `[2026-09-11] Architecture sync clobbers agent `model:` bindings and never touches `.opencode/`** (line 44) | **Premise now false** | `scripts/sync-architecture.ps1:43-47` force-propagates the source Model Registry into all three binding surfaces — *"Model bindings are template-owned: there is no preservation branch"* — and **inserts** rows the target lacks. The rule is already carried by `sync-architecture/SKILL.md` § Registry propagation and `model-routing/SKILL.md` lines 26 and 63 (*"Bindings are edited in the template, never in a satellite"*). Left standing, it invites someone to hand-reconcile a registry the sync now owns — and to distrust the sync. |

**Fix:** delete both entries, and state in `@sprint-close` § 6 that the fold review is a **template-side** step: a satellite **reports** the candidates upward and does not attempt the edit. Net register movement: 18 − 2 + F1's addition = **17**.

### F3 - One rule is genuinely unfolded, and its home is a planning directive

`[2026-09-13] A contract-coverage AC must name an *exported* seam` (line 26) is the only entry with no home yet. Origin is `T22-E1.02`: an acceptance criterion asserted a contract was *"derived from `BriefEditForm`'s field config"*, but that config is module-local, so the criterion was untestable.

`ptp-high-visionary` Phase 4 directive 3 says only: *"**Define acceptance criteria** as a table: behavior criterion + test target."* It requires a test target and says nothing about that target being **exported**, nor about declaring a hand-maintained snapshot as one that does not guard drift.

The asymmetry is the reason to fold it: `wiki_coverage_check.py` **already** requires an *exported symbol* for source-file coverage, so the concept is mechanically enforced after the fact and absent from the directive that would prevent the defect at authoring time. An AC with a module-local target passes Gate B, then Phase 9 either cannot verify it or substitutes a weaker check — a vacuous criterion, sitting at the gate meant to catch it.

**Fix:** extend directive 3 in `.devops/skills/ptp-high-visionary/SKILL.md` and delete the register entry. This skill is **embedded** in `ptp-high-visionary.subagent.md`, and the prefix check validates EMBED markers against `SKILL.md`, so the edit must be followed by `scripts/check-parcel-prefix.ps1 -Sync` or CI fails hard.

### F4 - `@agent-wrap-up` orders satellites to do something inert

Phase 7b step 2 tells the operator that for each modified file under `.devops/skills/` or `.devops/templates/`, they should *"bump `machinery-version:` once in `.devops/sync-manifest.yaml`"*. In a satellite that instruction cannot take effect: the engine *"stamps the TARGET's own `.devops/sync-manifest.yaml` with the **source** machinery-version"* (`sync-architecture.ps1:38-41`), and the manifest is deliberately not on the portable surface.

GRID-Link followed the instruction faithfully at its Sprint 8 close (52 → 53). This repo is at **47**, so the next pull stamps it back down and the bump was inert. The discipline itself is sound — it is enforced correctly template-side by `validate.yml`'s *Machinery version discipline* step, which fails when `version-history.md` carries no release row for the current `machinery-version`.

**Fix:** rescope Phase 7b step 2 to the template and state that a satellite **inherits** the stamped value and must not bump it. This plan keeps its own rule: the machinery edits below bump **47 → 48** and record the literal in `version-history.md`.

### F5 - Pull semantics are undocumented, and misread in both directions

The manifest header and `pull-architecture.ps1` both describe the copy as *"directories copied recursively into the target (overwrite)"*. The engine's real behaviour is narrower than "mirror" and broader than "additive" — `Copy-Item $s\* $t -Recurse -Force` is a **merge by name**:

- A file present in **both** repos is **replaced wholesale** — target-side edits are lost. This is F1's mechanism.
- A file present in the **target only** survives untouched — a satellite-only rule file is never pruned, so "sync" can leave permanent local residue.

A satellite reasoning with either wrong model misjudges what a pull does. "Sync is a mirror" predicts deletions that never happen; "sync is additive" predicts local edits survive when they do not. Neither error is visible in the current wording.

**Fix:** state the semantics in `sync-architecture/SKILL.md` — one paragraph naming the merge-by-name behaviour and both consequences. Optionally mirror one sentence into the manifest header comment, which is the first thing a satellite reads.

## Constraints

- **F1 is independently shippable and is the only time-sensitive finding.** If the rest stalls in review, land F1 alone first: it is one entry added to `.devops/rules/process-lessons.md` plus the machinery bump. Every other finding costs nothing when deferred; F1 costs the lesson the moment anyone pulls.
- **`ptp-high-visionary/SKILL.md` is embedded in its agent.** Editing that skill requires `scripts/check-parcel-prefix.ps1 -Sync`, which rewrites the embed region of `.devops/agents/ptp-high-visionary.subagent.md`. Expect exactly that one agent file to change; a different set indicates prefix drift and must be investigated, not committed.
- **The machinery bump is mandatory, not cosmetic.** `.devops/skills/**` and `.devops/rules/**` are portable, so `validate.yml`'s *Machinery version discipline* step fails unless `machinery-version` is bumped **and** the literal is recorded in `.devops/logs/version-history.md`.
- **Delete, do not soften.** A rule that has matured into a skill is removed from the register, not rewritten into a pointer to that skill — the pointer is the duplication the register exists to avoid, and it still reads as a rule to a future agent.
- **Do not restate the fold review in two dialects.** If `sprint-close` § 6 gains the template-side wording, `plan-lifecycle.md` — which owns the lifecycle rules — should cite it rather than paraphrase it.
- **Keep the register's entry shape.** One line per rule, `- **[YYYY-MM-DD] Title** - the rule. *Do instead:* the fix.` F1's addition follows it verbatim, including its British spelling and its date.

## Explicitly out of scope

- **Anything already parked in `T1-E3.06`** — the `.devops/sprints/**` UTF-8 scan scope, the `@sprint-plan` mutual-`touches` preflight, and the scanner's missing walk-root guard. This plan touches `sprint-close` § 6 only for the template-side wording of the fold review; it does not re-open that parcel's G1–G3.
- **The `machinery-version` collision hazard between concurrently-committed plans,** each stating its bump against a stale base. Named out of scope by `T1-E3.06` as an execution-time read-the-live-value discipline issue.
- **The `check-parcel-prefix.ps1` four-concern split** (`T1-E2.02`), which is why the skill-embed check lives where it does. Do not pre-empt that split here.
- **Any satellite-side edit.** Satellites converge on their next pull; nothing is to be hand-patched in GRID-Link. Its `machinery-version: 53` is inert and is expected to be stamped back to this repo's value on the next pull — that is correct behaviour, not a defect to repair.
- **Splitting the register by axis** (parcel machinery vs docs/tooling) rather than folding it. Worth asking once it is back under roughly fifteen entries; it is not this parcel's job to reorganise a register it is trying to shrink.

---

## 3 Phase 3: User Clarification (AUTO — inline Answerer from Research Map)

**Skill Executed:** `ptp-phase3-answerer` (inline — SINGLE topology, no spawn)

> [x] Q1 (F2 currency): hygiene cull already deleted the two matured entries? → **Yes — deletion half satisfied, no action.** Rationale: register grep 2026-09-18 shows neither `Wiki lint severity contract` nor `clobbers agent model:` line; changelog 2026-09-17 records 22→20. Source: `.devops/rules/process-lessons.md` grep + `agent-changelog.md:21-25`.
> [x] Q2 (F1 wording): add GRID-Link lesson verbatim with origin attribution? → **Yes.** Rationale: entry is machinery knowledge, not satellite-specific; parked plan quotes it verbatim with date. Source: plan §F1 + live register (absent).
> [x] Q3 (Scope): one pass or F1 fast-lane first? → **One pass.** Rationale: no satellite pull scheduled mid-plan; F1+F3 share no ordering constraint beyond pre-pull landing. Source: plan Constraints + `backlog-index.md` Triage Panel.
> [x] Q4 (F4 scope): rescope wrap-up Phase 7b to template-only + satellites inherit? → **Yes, minimal wording.** Rationale: engine stamps target manifest from source (`sync-architecture.ps1:38-41`), so satellite bump is inert. Source: `.devops/skills/agent-wrap-up/SKILL.md:170`.
> [x] Final validation: "Is this all the context required?" → **Yes — scope locked: F1 add, F3 fold, F4 rescope, F5 paragraph, sprint-close template-side line. Gate A auto-clear per evidence contract.**

**Architectural Conflict Warnings Raised:** F2-deletion half already executed by hygiene cull — plan records it as satisfied, does not re-delete.

---

## 4 Phase 4: Wiki Requirements & Acceptance Criteria
**Skill Executed:** `ptp-high-visionary` (inline — SINGLE topology, no spawn)

No `.wiki/` delta — rationale: machinery-internal transport behavior; `process-lessons.md` is `.devops/` operational state, not wiki content.

| # | Criterion | Test Target |
|---|---|---|
| AC1 | GRID-Link lesson present verbatim in register; register parses, entry shape kept | `.devops/rules/process-lessons.md` grep |
| AC2 | Phase 4 directive 3 requires exported seam; no second dialect | `ptp-high-visionary/SKILL.md` + `-Sync` PASS |
| AC3 | Wrap-up Phase 7b scoped to template; satellite inherits stamped value | `agent-wrap-up/SKILL.md` read |
| AC4 | Merge-by-name paragraph in sync skill; DRIFT verdict meaning intact | `sync-architecture/SKILL.md` read |
| AC5 | Sprint-close fold review marked template-side; lifecycle cites, not restates | `sprint-close/SKILL.md` + `plan-lifecycle.md` grep |
| AC6 | Gates green; `machinery-version` 63→64 with `version-history.md` row | `validate.yml` discipline step |

## 5 Phase 5: High-Visionary Execution Plan
**Skill Executed:** `ptp-high-visionary` (inline — SINGLE topology, no spawn)

File-level steps (no code snippets except exact string literals):

1. `.devops/rules/process-lessons.md` — append F1 entry verbatim (with GRID-Link origin attribution line). Covers F1.
2. `.devops/skills/ptp-high-visionary/SKILL.md` — extend directive 3: AC test target must be an *exported* seam; hand-maintained snapshot declared as non-guard. Then `check-parcel-prefix.ps1 -Sync` (embed in `ptp-high-visionary.subagent.md`). Covers F3.
3. `.devops/skills/agent-wrap-up/SKILL.md` — rescope Phase 7b step 2 to template; satellites inherit stamped value, must not bump. Covers F4.
4. `.devops/skills/sync-architecture/SKILL.md` — merge-by-name paragraph + both consequences; MUST-bump discipline already present (T1-E2.07), verify. Covers F5.
5. `.devops/skills/sprint-close/SKILL.md` — §6 fold review is template-side; satellite reports candidates upward. Covers F2-wording half.
6. `.devops/rules/plan-lifecycle.md` — cite sprint-close §6; no second dialect. Covers constraint.
7. Wrap-up — `machinery-version` 63→64 + `version-history.md` row (Counter Ownership: wrap-up owns it); skill `version:` bumps per modified file.

## 6 Phase 6: Self-Review Checkpoint (SINGLE — inline, replaces Group C)

| # | Criterion | Met? | Evidence |
|---|---|---|---|
| AC1 | Lesson added verbatim, shape kept | Yes | Step 1 |
| AC2 | Exported-seam rule at authoring time, prefix PASS | Yes | Step 2 + `-Sync` |
| AC3 | Template-only bump rule | Yes | Step 3 |
| AC4 | Merge semantics stated, DRIFT intact | Yes | Step 4 |
| AC5 | Template-side fold, cited once | Yes | Steps 5–6 |
| AC6 | Gates green, 63→64 recorded | Yes | Step 7 + §9 verification |

**Verdict:** `PASS`

## 8 Phase 8: Execute Changes
**Skill Executed:** `ptp-code-surgeon` (inline — SINGLE topology, no spawn)

Applied Phase 5 steps 1–6 on the trunk; `-Sync` re-inlined exactly one agent file (`ptp-high-visionary.subagent.md`, 2 lines) as predicted. No rollback; single attempt. Committed `plan: T1-E2.06` (`fa69f65`).

## 9 Phase 9: Verify Changes
**Skill Executed:** `ptp-code-surgeon` (inline — SINGLE topology, no spawn)

- `check-parcel-prefix.ps1`: **PASS** (byte-identical, bindings agree).
- `check-utf8-agents.ps1`: **ALL CLEAN (182 files)**.
- `wiki_lint.py --quiet`: exit 0. `wiki_claims.py check`: 0 stale. `coverage`: no-op (no `src/`).
- Fixtures: 29 + 10 + 5 + 18 = **62 OK**. `rule_fanout.py`: **0 unauthorised**, agreement 4/4.
- `sync-architecture.ps1 -SelfTest`: **OK** (5 dirs, 34 skills, 18 files).
- Tweaks: none. Unrelated dirt (`REFACTORING.md`, `SPRINTS.md`, `backlog-index.md`, `opencode.json`, `TRIAGE.md`, `t1-e4.02` file) left for wrap-up.

## 📍 State & Gates (CACHE-ANCHORED — mutable runtime state lives here only)

| Field | Value |
|---|---|
| **Status** | `COMPLETE` |
| **Active Persona** | `Executor` |
| Gate A (Scope) | `APPROVED 2026-09-18 — user verdict (wrap up)` |
| Gate B (Spec & Plan) | `APPROVED 2026-09-18 — user verdict (wrap up)` |
| Gate C (Peer Reviews) | `N/A — SINGLE topology; self-review logged` |
| Gate D (Implementation) | `APPROVED 2026-09-18 — user sign-off (wrap up)` |

## Completion Note

Delivered one AUTO/SINGLE pass: F1 lesson added verbatim to the register; F3 exported-seam rule folded into `ptp-high-visionary` directive 3 (v4→v5, `-Sync` re-inlined exactly one agent file); F4 rescoped wrap-up Phase 7b to template-only (v14→v15); F5 merge-by-name paragraph in sync skill (v8→v9); sprint-close §6 template-side line (v6→v7); `plan-lifecycle.md` untouched (no second dialect). Deviations: F2-deletion half already executed by the hygiene cull — recorded as satisfied, not re-deleted; claim commit landed content-empty (rename only) and was amended to carry content. Gates all green at Phase 9; wrap-up bumped 63→64 with v0.7.31 row; T1-E2.06 register row moved Open→Completed and triage row removed. No wiki delta per Phase 4. `Status: COMPLETE`.
