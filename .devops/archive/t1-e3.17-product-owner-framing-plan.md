---
code: T1-E3.17
sprint: sprint-10-owner-loop
claim_status: COMPLETE
owner: parcel-sprint
claimed_at: "2026-09-23"
last_touch: "2026-09-23"
touches: ["OPERATING-PRINCIPLES.md", ".wiki/rules/language/communication-rules.md", ".devops/rules/README.md", ".devops/skills/parcel/SKILL.md", ".opencode/plans/base-context.md"]
depends_on: []
triage: SINGLE
type: backlog
---
# Backlog: T1-E3.17 - Product-owner framing: the user is the product owner, the agents are the dev team

## ⚙️ Plan Settings

| Setting | Value |
|---|---|
| Mode | `AUTO` |
| Agents | `SINGLE` |
| Models | CLI default (inherited) |

*Frozen at plan start (batch preset). `triage: SINGLE` — no MULTI-worthy flag; no batch-risk provenance line required.*

## Theme-Epic: T1 - Parcel Pipeline Machinery, E3 - Concurrency & Sprint Lifecycle

**Discovered:** 2026-09-23, operator direction: "ensure the framing of our parcel development cycle is: the user is the product owner, and the agents are the dev team."

## Findings

| # | Gap | Evidence | Impact |
|---|-----|----------|--------|
| 1 | **The relationship is implied, never declared.** The machinery behaves this way already (gates halt for the user; `communication-rules.md` says "dev to product owner"; Gate D is "the user's verdict"), but no doc states the role model as such, so agents improvise tone and question-address. | `OPERATING-PRINCIPLES.md` (goal statement, no role model); `.wiki/rules/language/communication-rules.md` § User-facing conversation (one line, local to language). | Inconsistent voice across skills; gate questions, reports and asks read sometimes like peer-to-peer dev, sometimes like service to owner. |

## What the fix does (operator-authored intent)

One canonical statement of the role model, everything else cites it:

1. **`OPERATING-PRINCIPLES.md`:** add the role model to the main statement — the operator/user is the **product owner** who holds Gate D and all verdicts; the agent fleet is the **dev team** that plans, builds and verifies.
2. **`communication-rules.md`:** the User-facing section cites the role model and states the consequence: questions addressed to the owner are decision requests, not status filler; reports are written for the owner.
3. **Shared prefix (one line):** the orchestrator/runner personas state the relationship once so every spawned subagent inherits it (single `-Sync` re-inline; puts this parcel on the serial lane).
4. Sweep the highest-traffic user-facing surfaces (sprint-close business report + user testing steps, T1-E3.15/16) so they read as service-to-owner, not engineering log.

## What a fix touches

`OPERATING-PRINCIPLES.md`, `.wiki/rules/language/communication-rules.md`, `.opencode/plans/base-context.md` (+ `-Sync` re-inline across the 9 locked agents), `.devops/rules/README.md` (index line). Per-skill version bumps.

## Open question for Phase 3

Where is the **single home**: `OPERATING-PRINCIPLES.md` (principle-level) or `communication-rules.md` (voice rules)? Prefer principles as home, voice rules citing — but confirm no third restatement site is needed for the subagent prefix to qualify as "cited" under `surface-budget.md`.

## Reference

Operator direction 2026-09-23 (this conversation).

---

## 1 Phase 1: Expansion & Scoping
**Skill Executed:** `ptp-context-hunter` (inline, AUTO + SINGLE)

**Intent:** State the role model once — the user/operator is the **product owner** (holds Gate D and every verdict); the agent fleet is the **dev team** that plans, builds and verifies — in a single canonical home, with every other surface citing it.

**In Scope:**
- `OPERATING-PRINCIPLES.md`: add the role model to the main statement (the single home).
- `.opencode/plans/base-context.md`: one role-model line in the shared prefix; re-inline with `check-parcel-prefix.ps1 -Sync` (reserves the serial lane).
- `.wiki/rules/language/communication-rules.md` § User-facing conversation: cite the home; state the consequence — questions addressed to the owner are decision requests, not status filler; reports are written for the owner.
- `.devops/rules/README.md`: one index line pointing at the framing.

**Out of Scope (Perimeter Notes):**
- **Sprint-close business report + user-testing sweep (fix item 4 as written)** — those files (`.devops/skills/sprint-close/SKILL.md` and friends) are declared `touches` of sibling claims `T1-E3.14`/`T1-E3.15`, both live in `.devops/plans/` at `claim_status: GATE_D_USER_APPROVAL`; the touches-overlap clause (`plan-lifecycle.md` § Claim Protocol) holds them. Narrowed; framing reaches them via the citation the wrap-up already carries.
- **`.devops/skills/pass-the-parcel/SKILL.md`** — the front-matter entry `.devops/skills/parcel/SKILL.md` is a dead path (canonical name is `pass-the-parcel`); that skill is claimed by `T1-E3.14`; no edit. The claim's eligibility was committed by the host on the nominal paths; actual write set is the four files above.
- `.devops/agents/*.md` direct edits — reserved prefix; only `base-context.md` + `-Sync`.
- `AGENTS.md` edits, `.devops/templates/base-context.template.md` seed edits (seed carries the registry, not the prose prefix), `sync-manifest.yaml` machinery-version (batch wrap-up owns it), new fan-out registry rows.
- Any third full restatement site — citations only.

---

## 2 Phase 2: Requirements & Context
**Skill Executed:** `ptp-context-hunter` (inline)

**Forensic Context Inventory (complete — every In-Scope item verified):**
> [x] Wiki core docs read (`OPERATING-PRINCIPLES.md`, `.wiki/rules/language/communication-rules.md`, `.devops/rules/plan-lifecycle.md`, `.devops/rules/README.md`, `.devops/rules/surface-budget.md`, `.opencode/plans/base-context.md`)
> [x] Knowledge capture read (`.wiki/core/18-knowledge-capture.md`)
> [x] Source surfaces verified (the 4 write-set files + the 9 prefix-locked agents + `scripts/check-parcel-prefix.ps1` sync surface list)

**Relevant Existing Decisions (KC):** one canonical home per rule (Managed Simplicity); inherited model routing / no agent declares a model (prefix NOMODEL); one canonical role statement surfaces = `communication-rules.md` § User-facing conversation is already the voice rule that says "dev to product owner".

**Relevant Docs Found:**
| Surface | Current state |
|---|---|
| `OPERATING-PRINCIPLES.md` | goal statement + property table; no role model. Header: "Stated once; every other surface points here." |
| `.wiki/rules/language/communication-rules.md` § User-facing conversation | "Dev to product owner." one line; no citation of a role-model home. |
| `.opencode/plans/base-context.md` | Rule 10 names dev-to-PO voice; no role model; byte-inlined into 9 locked agents by `-Sync`. |
| `.devops/rules/README.md` | Dev-rules index; no framing line. |
| `agents` | 9 locked agents (2 orchestrators + 8 subagent files incl. wiki-*); prefix shared region = content above `<!-- ORCHESTRATOR-ONLY` marker. |

**Not-found notes:** `scripts/check-parcel-prefix.ps1 -Sync` regenerates agent prefixes only from `base-context.md` — the template seed carries the registry, not the prose prefix, so no `.devops/templates/` write is needed.

---

## 3 Phase 3: User Clarification
> [x] Q1: Where is the single home of the role-model statement?
> [x] Q2: Which exact formulation lands in the shared prefix line?
> [x] Q3: Does the sprint-close user-facing sweep (fix item 4) fit this plan's write set?
> [x] Q4: Is a third full restatement site needed for the prefix line to qualify as "cited"?
> [x] Final validation: "Is this all the context required?" -> Yes
**Architectural Conflict Warnings Raised:** fix item 4 conflicts with sibling claims in flight; see Q3 / Out-of-Scope.

### Phase 3.5 Research Map (AUTO)
| Q# | Core Docs | Code Files | KC Entries |
|---|---|---|---|
| Q1 | `OPERATING-PRINCIPLES.md`; `.devops/rules/managed-simplicity.md`; `.devops/rules/surface-budget.md` | `scripts/rule_fanout.py` (report read) | managed-simplicity § "one canonical home per rule" |
| Q2 | `OPERATING-PRINCIPLES.md`; `.devops/rules/plan-lifecycle.md` § Gates | `scripts/check-parcel-prefix.ps1` (sync surface list) | KC rule 7 (never edit PREFIX-LOCKED directly) |
| Q3 | `.devops/rules/plan-lifecycle.md` § Claim Protocol | `.devops/plans/t1-e3.14-*`, `t1-e3.15-*` front-matter (live) | KC "Claim before execution" |
| Q4 | `.devops/rules/surface-budget.md`; `.wiki/rules/language/communication-rules.md` | — | KC fan-out / cascade lessons |

### Auto-Resolutions (Mode: AUTO — resolved inline by `ptp-phase3-answerer`; Skill Executed: ptp-phase3-answerer)

**Q1**
**Auto-Resolution:** `OPERATING-PRINCIPLES.md` is the single home.
**Rationale:** a principle-level fact about who holds authority is part of the operating statement ("stated once; every other surface points here"); Managed Simplicity forbids a second home, and the voice rule already behaves as a pointer surface.
**Source:** `OPERATING-PRINCIPLES.md` (l.3) | `.devops/rules/managed-simplicity.md` § first principle

**Q2**
**Auto-Resolution:** One shared-prefix sentence: the operator/user is the product owner who holds Gate D and every verdict; the agent fleet is the dev team — statement of record: `OPERATING-PRINCIPLES.md`.
**Rationale:** it must restate what is mechanically true ("Gate D always requires the human") and point at the record, in the byte-inlined shared prefix so every spawned subagent inherits it.
**Source:** `.devops/rules/plan-lifecycle.md` § Modes (l.70) | `.opencode/plans/base-context.md` header (byte-for-byte inheritance)

**Q3 — [auto-added] (alters the In-Scope/Out-of-Scope perimeter)**
**Auto-Resolution:** The sprint-close sweep is out of scope for this plan; the framing lands in the four declared surfaces only.
**Rationale:** the sweep's files are `touches` of live sibling claims, and the touches-overlap clause is an independent blocker a satisfied dependency does not clear; the claim front-matter write set is frozen.
**Source:** `.devops/rules/plan-lifecycle.md` § Claim Protocol → Write-Set Overlap Predicate (l.93-103) | KC (2026-09-13) "Claim before execution"

**Q4**
**Auto-Resolution:** No third restatement site — the home plus three citation surfaces (`communication-rules.md`, the shared prefix, the `.devops/rules/README.md` index line) suffice.
**Rationale:** the fan-out rule applies to registered canon rules and this statement is not registered; a citation (path named) not a restatement is what Managed Simplicity demands.
**Source:** `.devops/rules/managed-simplicity.md` § first principle | `.devops/rules/surface-budget.md` § The Metric

### Test Proposals (TDD)
- [x] **T1** — Steps: `Select-String -Path OPERATING-PRINCIPLES.md -Pattern 'product owner'`. Expected: ≥1 match after the edit. **Status:** ACCEPTED **Validation:** Auto-Resolution: ACCEPTED — plain text seam, verifiable.
- [x] **T2** — Steps: `Select-String -Path .wiki\rules\language\communication-rules.md -Pattern 'OPERATING-PRINCIPLES'`. Expected: ≥1 match in § User-facing conversation / By Channel. **Status:** ACCEPTED **Validation:** Auto-Resolution: ACCEPTED — pointer-test-shaped seam.
- [x] **T3** — Steps: `powershell -NoProfile -File scripts/check-parcel-prefix.ps1` then `scripts/check-utf8-agents.ps1`. Expected: both exit 0 (`PASS` ×9 agents, all-clean utf8). **Status:** ACCEPTED **Validation:** Auto-Resolution: ACCEPTED — existing deterministic gates (Gate Invocation Hygiene: one-shot, bounded).

Final validation: **Yes — proceed.** Auto-cleared at Gate A on the evidence above (AUTO Gate Evidence Contract: every Q# carries Auto-Resolution + Rationale + Source; no unreplaced boxes; no blocking verdict).

---

## 4 Phase 4: Wiki Requirements & Acceptance Criteria
**Skill Executed:** `ptp-high-visionary` (inline; wiki-writer discipline: read full doc first, integrate at the semantic section, never append — already read in full during Phase 2)

> Wiki delta applies — condition (c): a `.wiki/rules/**` doc changes content. Spec below; the wiki doc IS the spec.

**Wiki Docs to Write/Update:**
| Doc | Change |
|---|---|
| `.wiki/rules/language/communication-rules.md` | § User-facing conversation gains the consequence bullet citing `OPERATING-PRINCIPLES.md` (stays `status: stable` — a content addition to an existing approved rule, reconciled at wrap-up) |

**Acceptance Criteria:**
| # | Criterion (behavior) | Test Target |
|---|---|---|
| 1 | `OPERATING-PRINCIPLES.md` states the role model once: the user/operator is the product owner who holds Gate D and every verdict; the agent fleet is the dev team that plans, builds and verifies | `Select-String -Path OPERATING-PRINCIPLES.md -Pattern 'product owner'` ≥1 match |
| 2 | `.wiki/rules/language/communication-rules.md` § User-facing conversation cites the home and states the consequence (owner questions are decision requests; reports are written for the owner) | `Select-String -Path .wiki/rules/language/communication-rules.md -Pattern 'OPERATING-PRINCIPLES'` ≥1 match |
| 3 | The shared prefix carries the one-line role model, hence all 9 locked agents after `-Sync`; no agent file hand-edited | `powershell -NoProfile -File scripts/check-parcel-prefix.ps1` exit 0 |
| 4 | `.devops/rules/README.md` indexes the framing with a pointer line | `Select-String -Path .devops/rules/README.md -Pattern 'product owner'` ≥1 match |
| 5 | Repo gates stay green after the `base-context.md` re-inline and the wiki edit | `scripts/check-utf8-agents.ps1` exit 0; `python scripts/wiki_lint.py` exit 0; `python scripts/wiki_claims.py check` exit 0 |

**Spec Notes:**
- Docs consumed (read-first): `OPERATING-PRINCIPLES.md`, `communication-rules.md`, `base-context.md`, `.devops/rules/README.md` — all read in full in Phase 2.
- No data flow / state changes — machinery prose only; no code exports touched.

---

## 5 Phase 5: High-Visionary Standard Implementation Plan
**Skill Executed:** `ptp-high-visionary` (inline)

**Simplicity Gate:**
> [x] Climbed the Simplicity Ladder (rung 5/6 hold: existing surfaces get one line each, no new file, no new dep)
> [x] No speculative abstractions
> [x] Reuse over reimpl — Reuse Log populated
> [x] Deliberate simplifications marked (`ponytail:` below)
> [x] Safety exceptions preserved (none engaged — doc-only)

**Reuse Log:**
| Existing Asset | Where Reused | Ladder Rung |
|---|---|---|
| `check-parcel-prefix.ps1 -Sync` re-inline | prefix propagation to 9 agents | 2 |
| `communication-rules.md` § User-facing conversation | citation surface for the consequence | 2 |
| `.devops/rules/README.md` rule-index table | pointer line | 2 |

**Spaghetti Triage (See-Name-Route, Do Not Fix):**
- [x] No smells noticed

### To-Do List
- [ ] S1: `OPERATING-PRINCIPLES.md` — role-model statement (the home)
- [ ] S2: `base-context.md` — one role-model line in the shared prefix; then `-Sync` re-inline (9 agents)
- [ ] S3: `communication-rules.md` § User-facing conversation — consequence bullet citing the home
- [ ] S4: `.devops/rules/README.md` — index pointer line
- [ ] S5: gates — `wiki_claims.py update` + `check`, `check-parcel-prefix.ps1`, `check-utf8-agents.ps1`

### File-Level Steps
1. `OPERATING-PRINCIPLES.md` — in § The Goal, after the property table, add a short **Role model** paragraph: the user/operator is the **product owner** — they hold Gate D and every verdict; the agent fleet is the **dev team** that plans, builds and verifies for them; every question to the owner is a decision request. 2-3 sentences, no new subsection boilerplate.
2. `.opencode/plans/base-context.md` — immediately after the Managed Simplicity paragraph (still inside the shared prefix, above the ORCHESTRATOR-ONLY block), add one bold line naming the role model and pointing at `OPERATING-PRINCIPLES.md` as the statement of record; then run `powershell -NoProfile -File scripts/check-parcel-prefix.ps1 -Sync` so all 9 locked agents inherit it byte-for-byte.
3. `.wiki/rules/language/communication-rules.md` — in § User-facing conversation, append one bullet: gate questions and reports are addressed to the product owner as decision requests, not status filler — per the role model, see `OPERATING-PRINCIPLES.md`.
4. `.devops/rules/README.md` — in § Core Principles, append one numbered principle stating the role model in one line and citing `OPERATING-PRINCIPLES.md` as its home (index line, not a restatement — one sentence).

### Implementation Instructions (no code; exact string literals only)
- The home sentence must contain the exact strings `product owner` and `dev team` (test seams T1/AC1).
- The prefix line references `OPERATING-PRINCIPLES.md` by path (citation, not quote).
- `ponytail:` the README line repeats the role model in compressed form rather than a bare link — accepted ceiling: two textual appearances of the wording (home + index); upgrade path = drop the paraphrase if fan-out ever registers this statement as a canon rule.

### Wiki Core References
- `.wiki/rules/language/communication-rules.md` -> voice rules derive from this framing.

### Wiki Docs to Add/Edit
- `communication-rules.md` only (per Phase 4 table); zero new docs.

### Test Verification Plan (exact commands, one-shot)
1. `powershell -NoProfile -File scripts/check-parcel-prefix.ps1` -> exit 0
2. `powershell -NoProfile -File scripts/check-utf8-agents.ps1` -> exit 0
3. `python scripts/wiki_lint.py` -> exit 0
4. `python scripts/wiki_claims.py update` then `python scripts/wiki_claims.py check` -> exit 0
5. Seam greps per AC 1/2/4 return matches.

---

## 6 Phase 6: Grumpy Architect Spec & Logic Audit — SINGLE self-review checkpoint
**Skill Executed:** `ptp-grumpy-architect` (inline self-review; `SINGLE`: no independent reviewer — replaces the Gate C checkpoint)

**`SINGLE` self-review (evidence — one row per acceptance criterion):**
| AC # | Criterion met? (`Yes`/`No`) | Evidence / gap |
|---|---|---|
| 1 | Yes | Home statement placed in § The Goal; contains exact strings `product owner` + `dev team`; T1 seam greps ≥1 match (Phase 9 evidence). |
| 2 | Yes | § User-facing conversation consequence bullet names `OPERATING-PRINCIPLES.md` by path; T2 seam greps ≥1 match (Phase 9 evidence). |
| 3 | Yes | Prefix line lives above the ORCHESTRATOR-ONLY marker; `-Sync` rewrote 9 agent files; `check-parcel-prefix.ps1` exit 0 (Phase 9 evidence). |
| 4 | Yes | `.devops/rules/README.md` § Core Principles pointer line; AC4 grep ≥1 match (Phase 9 evidence). |
| 5 | Yes | All four gate commands exit 0 after the re-inline (Phase 9 evidence table). |

**Verdict:** `PASS`

**Spec-level audit (doc-only plan):**
- **System Contracts Explicit:** three prose surfaces + one pointer line; no code contracts touched.
- **File Boundary & Scope Collisions:** none — all four paths are free of sibling-claim overlap after Q3 narrowed the scope; `pass-the-parcel/SKILL.md` deliberately untouched.
- **Dependency Gaps:** prefix repair (`-Sync`) travels with the `base-context.md` edit, per Counter Ownership ("the prefix regenerate stays with the edit").
- **YAGNI Bloat:** no new file, no new registry row, no new section boilerplate; claimed `parcel/SKILL.md` path is dead and excluded.
- **Residual risk:** fan-out treadmill if the wording spreads; citable by path only (`ponytail:` note covers it).

---

## 7 Phase 7: Smooth Operator Product Review
**Skill Executed:** `ptp-smooth-operator` (inline)

> **`SINGLE` topology:** `N/A — SINGLE self-review`; the self-review is presented at Gate B (Phase 6 above, `**Verdict:** PASS`).

- **Vision & Journey Integrity:** framing makes every gate question/report owner-facing — consistent with sprint-close business report + user-testing steps already shipped by E3.12/E3.15.
- **Scope Containment:** out-of-scope recorded crisply; no sweep of files claimed by siblings.
- **4 Core User States:** N/A (no UI).
- **Guardrails & Permissions:** Gate D stays human-owned — explicitly restated in the framing.
- **Mobile / A11y / Telemetry:** N/A.

---

## 📍 State & Gates (CACHE-ANCHORED — update ONLY this section at gate transitions)

| Metric | Value |
|---|---|
| Status | `COMPLETE` |
| **Version** | `v0.1.0` |
| **Active Persona** | `Executor` |
| **Depends On** | none (canonical: claim front-matter `depends_on`) |
| **Blocks** | none |

| Gate | Requirement | Status |
|---|---|---|
| A | Scope approved (Phases 1-3, + 3.5 in AUTO) | `APPROVED` — auto-cleared on Phase 3/3.5 evidence (all `Auto-Resolution:` entries present, final validation recorded) |
| B | Spec & plan approved (Phases 4-5) | `APPROVED` — auto-cleared on positive evidence (≥1 AC row with Test Target; ≥1 file-level step; Phase 6 self-review `**Verdict:** PASS`) |
| C | Peer reviews passed (Phases 6-7) | `N/A (SINGLE)` |
| D | Implementation verified (Phases 8-9) | `APPROVED` — human verdict 2026-09-23 (batch Gate D consolidated) |

---

## 8 Phase 8: Execute Changes
**Skill Executed:** `ptp-code-surgeon` (inline; Phase 8 triggers only after Gate C clearance record — Gate C is `N/A (SINGLE)` / self-review PASS at Gate B)

**Single-Pass Direct-to-Disk:**
> [x] Changes written DIRECTLY to source files — no intermediate markdown blocks
> [x] Touched only intended lines — no adjacent refactors
> [x] Cleaned up only owned orphans
> [x] Pre-existing dead code left untouched

> [x] S1: `OPERATING-PRINCIPLES.md` — role-model paragraph added in § The Goal (after the property table)
> [x] S2: `base-context.md` — role-model line added after the Managed Simplicity paragraph; `-Sync` re-inlined into 9 agent files
> [x] S3: `communication-rules.md` § User-facing conversation — consequence bullet citing the home
> [x] S4: `.devops/rules/README.md` — pointer principle in § Core Principles
> [x] S5: gates re-run (Phase 9)

**machinery-version observed before wrap-up:** `69` in `.devops/sync-manifest.yaml` (recorded as the "before" for the batch wrap-up's single increment; this runner does not bump it).

## 9 Phase 9: Verify Changes
**Skill Executed:** `ptp-code-surgeon` (inline)

**Verification Evidence:**
| # | Command | Expected | Observed | Exit |
|---|---|---|---|---|
| 1 | `powershell -NoProfile -File scripts/check-parcel-prefix.ps1` | exit 0, PASS ×9 + seed checks | PASS ×9 / NOMODEL ×11 + seed clean | 0 |
| 2 | `powershell -NoProfile -File scripts/check-utf8-agents.ps1` | exit 0, all files clean | ALL CLEAN | 0 |
| 3 | `python scripts/wiki_lint.py` | exit 0 | 0 problems | 0 |
| 4 | `python scripts/wiki_claims.py update` then `check` | update stamps, check exit 0 | 0 stale | 0 |
| 5 | `Select-String -Path OPERATING-PRINCIPLES.md -Pattern 'product owner'` (T1/AC1) | ≥1 match | 1 match | 0 |
| 6 | `Select-String -Path .wiki/rules/language/communication-rules.md -Pattern 'OPERATING-PRINCIPLES'` (T2/AC2) | ≥1 match | 1 match | 0 |
| 7 | `Select-String -Path .devops/rules/README.md -Pattern 'product owner'` (AC4) | ≥1 match | 1 match | 0 |

- [x] Compilation check N/A (doc-only diff; no build surface touched)
- [x] Lint clean (wiki_lint exit 0)
- [x] Code matches exact plan specifications — every Phase 5 Test Verification Plan command exits 0
- [x] No functional gaps identified

> **Gate D halt point:** Status -> `PHASE_9`, Gate D `OPEN`, claim_status -> `GATE_D_USER_APPROVAL`. Awaiting the batch's consolidated human verdict.

## Completion Note

**Outcome:** delivered as planned. The role model is canonical in `OPERATING-PRINCIPLES.md` (the user/operator is the product owner holding Gate D and every verdict; the agent fleet is the dev team), cited by `.wiki/rules/language/communication-rules.md`, `.devops/rules/README.md` and the shared prefix (`base-context.md` + `-Sync` re-inline x9). All four declared surfaces edited; prefix + utf8 checks exit 0.

**Deviations:** (1) `sprint-close`'s user-facing sweep (fix item 4) deliberately narrowed out - those files are sibling claims' `touches`; framing reaches them via the wrap-up citation. (2) Front-matter `touches` carries a dead path `.devops/skills/parcel/SKILL.md` (canonical name `pass-the-parcel`) - left as committed; the claim was valid on the nominal paths and the actual write set is the four declared files.

**Batch context:** wave 2 of Sprint 10 via `@sprint-run` (AUTO+SINGLE, CLI default model). Runner spawn died after writing Plan Settings; resumed per the interrupted-run contract (no phases ran; nothing fabricated). Gate D verdict recorded 2026-09-23 (batch-consolidated).
