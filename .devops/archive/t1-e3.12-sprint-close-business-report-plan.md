---
code: T1-E3.12
sprint: sprint-10-owner-loop
claim_status: COMPLETE
owner: parcel-sprint
claimed_at: "2026-09-23"
last_touch: "2026-09-23"
touches: [".devops/skills/sprint-close/SKILL.md", ".devops/templates/sprint.template.md", "HOW-TO.md"]
depends_on: []
triage: SINGLE
type: backlog
---
# Backlog: T1-E3.12 - Sprint-close business report in the canonical skill

## Theme-Epic: T1 - Parcel Pipeline Machinery, E3 - Concurrency & Sprint Lifecycle

## ⚙️ Plan Settings (FROZEN — set at plan start, read before any phase)

| Setting | Value |
|---|---|
| **Mode** | `AUTO` |
| **Agents** | `SINGLE` |
| **Models** | `CLI default` (handed down by the batch host — the operator declined a per-batch override) |

> **Preset provenance:** locked `AUTO` + `SINGLE` batch preset — the `parcel-sprint` host skipped the Mode/Topology questions and spawned `ptp-parcel-fast` with the preset and `Models=CLI default` handed down. `triage: SINGLE` in the claim front-matter, so no MULTI-worthy flag line applies. Recorded once at claim time; frozen thereafter.

---

**Discovered:** 2026-09-21, at the GRID-Link satellite. The operator asked for a business-facing sprint report filed with the sprint archive; the change was authored **directly in the satellite's copy** of `@sprint-close` (v7 → v8: new Step 5 "Write the Business Report", renumbered 6–8, archive/register steps updated). Because `.devops/skills/**` is on the portable surface, the next `@sync-architecture` pull **overwrites the satellite edit wholesale** — so the improvement must land template-side here to stick (the existing "fold candidates upward, do not edit locally" protocol caught exactly this case as designed).

## Findings

| # | Gap | Evidence | Impact |
|---|-----|----------|--------|
| 1 | `@sprint-close` produces only engineering history: retro in `sprint.md`, Kill-List flags, capacity. Nothing speaks to business stakeholders outside the dev team. | Satellite `@sprint-close` before v8 — no stakeholder-facing output step | Each sprint's plain-language story exists only in chat transcripts or is never told |
| 2 | A closed sprint folder holds only `sprint.md`; there is no per-sprint summary for practitioners | `git ls-files .devops/archive/sprints/*` across template + GRID-Link | Stakeholders trawling sprint history have only the heavily-technical retro |

## What the fix does (authored, proven at the satellite)

New **Step 5 — Write the Business Report** in `@sprint-close`, before archiving:

- Writes `business-report.md` into the sprint folder, so Step 6's folder move carries it into `.devops/archive/sprints/sprint-{n}-<slug>/` automatically — no plan-lifecycle rule change needed.
- Audience: business users / practitioners, not developers. Grouped per **outcome/epic/theme**, never per parcel code.
- Reusable template: goal, deliverables by theme, wins, quality paragraph, non-development open items (operator actions), what's next.
- Tone rules: benefit language; only trust-building numbers (delivered totals, test counts, capacity %) — machinery thresholds stay out; machinery vocabulary (parcel, Gate D, Kill List, spaghetti scan) prohibited.
- Operator show-and-tell before archiving; the `SPRINTS.md` register row links the report.

## What a fix touches

`.devops/skills/sprint-close/SKILL.md` (insert Step 5; renumber; extend archive + register steps; frontmatter version bump), `.devops/templates/sprint.template.md` (optional `business-report.md` row in the file table), `HOW-TO.md` (one mention of the sprint-close business report in the cycle walkthrough). Plan-lifecycle needs nothing new — the report is sprint-folder debris, not a plan.

## Open question for Phase 3

Should `sync-architecture`'s `portable_files` handling need any adjustment, or does the skill alone suffice? (`portable_files` already covers `.devops/skills/**` wholesale and `prune_files` handles retirements; a new *file inside a travelling folder* is the ordinary case — verify no allowlist at the file level applies.)

## Reference implementation

GRID-Link 2026-09-21: `sprint-close` v8 diff plus the live first example archived at `.devops/archive/sprints/sprint-12-showcase-slice/business-report.md` (and linked from SPRINTS.md row 12).

---

## 1 Phase 1: Expansion & Scoping
**Skill Executed:** `ptp-context-hunter` (inline, SINGLE)

**Intent:** Land the proven GRID-Link `sprint-close` business-report improvement template-side, so the next `@sync-architecture` pull converges instead of overwriting the satellite's local v8 edit.

**In Scope:**
- `.devops/skills/sprint-close/SKILL.md` — new Step 5 "Write the Business Report"; renumber 5-7 to 6-8; extend the archive step to carry `business-report.md`; extend the registers step to link the report from the SPRINTS.md row; internal step-reference sweep; frontmatter version bump.
- `.devops/templates/sprint.template.md` — header note naming the `business-report.md` artefact (the plan's "optional file-table row" — the template has no file table, so the header comment is the closest structural home; no new schema).
- `HOW-TO.md` — exactly one mention of the sprint-close business report in the lifecycle narrative (§3).

**Out of Scope:**
- `.devops/rules/plan-lifecycle.md` — the report is sprint-folder debris, not a plan; no lifecycle rule changes.
- `.devops/skills/sync-architecture/**` + `.devops/sync-manifest.yaml` — no allowlist adjustment needed (Q1 resolution below).
- Satellite-side edits, SPRINTS.md schema/column changes (the report links from the existing Retro cell), live sprint data wiring (dry-run fidelity is the accepted test per sprint.md).
- `machinery-version` bump + `.devops/logs/version-history.md` row — owned by the batch wrap-up, one writer per batch.

**Perimeter Notes:** Sprint folders gain a second file at close time (`business-report.md`); wrap-up archives *plans* to `.devops/archive/` root and sprint-close archives the *sprint folder* — the two moves never collide.

---

## 2 Phase 2: Requirements & Context
**Skill Executed:** `ptp-context-hunter` (inline, SINGLE)

**Forensic Context Inventory:**
> [x] Machinery canon read in place of app wiki (no `.wiki/` doc governs the sprint lifecycle — machinery truth lives in `.devops/rules/` + skills): `.devops/rules/plan-lifecycle.md` § Claim Protocol + § AUTO Gate Evidence Contract, `.devops/backlog/SPRINTS.md` § Conventions, AGENTS.md rule 5 (changelog review)
> [x] Knowledge capture read — `.devops/logs/agent-changelog.md` last 3 entries (machinery 64→68: no counter drift, no in-flight sprint-close work)
> [x] Source verified — all three touch-set files read in full; sibling plans E3.15/E3.16 read (they also touch `sprint-close/SKILL.md`; neither has executed — file is still v7 at claim time)

**Relevant Existing Decisions:**
- T1-E2.06 (archived): the fold review in `sprint-close` §6 is **template-side** — a satellite reports candidates upward. My renumber moves that wording with its section (§6→§7); the wording itself must survive untouched.
- T1-E3.04 (archived): `sprint-close` §1 must keep treating a surviving `GATE_D_USER_APPROVAL` plan as unfinished — untouched by this fix.
- Version discipline (`sync-architecture` SKILL.md Rule): a skill's `version:` MUST bump with any SKILL.md change, else satellites see DRIFT instead of UPGRADE. Applies to `sprint.template.md` too (templates bump per modified file; `machinery-version` stays with the wrap-up).

**Relevant Docs Found:**

| Path | Relevance |
|---|---|
| `.devops/skills/sprint-close/SKILL.md` | Primary target — v7, steps 1-7 |
| `.devops/templates/sprint.template.md` | v1 seed — header comment is the file-inventory surface |
| `HOW-TO.md` | §3 lifecycle narrative gains the one mention |
| `.devops/skills/sync-architecture/SKILL.md` + `scripts/sync-architecture.ps1` | Q1 source: skills copy per-folder recursively; no file-level allowlist |
| `.devops/backlog/SPRINTS.md` | Register shape: Retro cell holds links (report links there, no schema change) |
| `.devops/backlog/REFACTORING.md`, `.devops/backlog/TRIAGE.md` | Live `@sprint-close` § 2 cross-refs — step 2 is below the insertion point, unaffected |

**Relevant Code Found:** No application code exists in this template repo; the product surface is machinery docs/scripts. Renumber-drift scan (the sprint's named historical failure mode): live cross-references to sprint-close step numbers are `§ 2` (scan — unaffected), `§ 1` (completion check — unaffected), `§6` (fold review — **shifts to §7**, one live citation in `sync-architecture/SKILL.md:83`), ## 3 Phase 3: User Clarification
**Skill Executed:** `ptp-context-hunter` (inline, SINGLE) — questions auto-resolved in Phase 3.5 (`AUTO` mode)

> [x] Q1 — Does `sync-architecture`'s `portable_files` handling need any adjustment, or does the skill alone suffice? (the plan's authored open question)
> [x] Q2 — Does inserting Step 5 and renumbering 5-7 to 6-8 break live cross-references to sprint-close step numbers?
> [x] Q3 — Where does the "optional `business-report.md` row in the file table" land in `sprint.template.md`?
> [x] Q4 — Where does the SPRINTS.md register link to the report live?
> [x] Final validation: "Is this all the context required?" -> Yes

**Architectural Conflict Warnings Raised:** None blocking. (The §6-to-§7 fold-review citation drift in `sync-architecture/SKILL.md:83` is real but non-blocking — see Q2; it is outside this plan's declared touch set, so it is recorded as a follow-up, not edited here.)

---

## 3.5 Phase 3.5: AUTO Auto-Resolution (`ptp-phase3-answerer`, inline)

**Q1 — sync-architecture adjustment needed?**
> Auto-Resolution: No — the skill alone suffices. `sync-architecture.ps1` copies each portable skill **folder recursively** (all files in the folder travel; no file-level allowlist exists), and `.devops/templates` is a wholesale `portable_dirs` root, so a new file inside either travelling surface is the ordinary case. No manifest key changes; `prune_files` is for retirements only and none occur.
> Rationale: The pull engine's transport unit for skills is the folder, not the file list — verified in the engine itself, not just the manifest prose; `excluded_skills: []` means every skill folder travels.
> Source: `scripts/sync-architecture.ps1:494-503` ("Portable skills (derived: all skills minus excluded_skills; copy each folder)"), `.devops/sync-manifest.yaml` (`portable_dirs: .devops/templates`, `excluded_skills: []`), `.devops/skills/sync-architecture/SKILL.md:83` (merge-by-name semantics).

**Q2 — renumbering fanout?**
> Auto-Resolution: Proceed with the renumber. Live cross-references below the insertion point: only the fold-review citation `sync-architecture/SKILL.md:83` ("the fold review in `@sprint-close` §6") drifts to §7, and one internal `§ 6's` self-reference inside sprint-close itself (fixed in-file as part of the edit). The `§ 2` scan references (REFACTORING.md x4, TRIAGE.md) and `§ 1` completion-check references sit above the insertion point and are unaffected.
> Rationale: The drifted file is outside this plan's declared `touches` (claim discipline: the declared write set equals the actual one), so the one-line citation repair is recorded as a follow-up finding in Phase 9 and NOTES, not edited here.
> Source: grep sweep `sprint-close.{0,40}(§|Step|#)\s*[0-9]` across `.devops/` — hits: `REFACTORING.md:23,83,149,151` (all § 2), `sync-architecture/SKILL.md:83` (§6), archived plans (historical, untouched), `sprint-close/SKILL.md` internal ("Step 3's promotion — and § 6's").

**Q3 — template row placement?**
> Auto-Resolution: Add the `business-report.md` note to `sprint.template.md`'s header comment (the file-inventory surface: it currently states "One file, open -> close -> retro. There is no separate retro.md."). The plan's "file table" does not exist in the template; inventing one for a single future row would be YAGNI. Bump template `version: 1 -> 2` + `updated:` per version discipline.
> Rationale: The header comment is where the template already enumerates its files; a note there reaches every `@sprint-plan` author at generation time without new structure.
> Source: `.devops/templates/sprint.template.md:1-8` (header comment), `.devops/skills/sync-architecture/SKILL.md:86-89` (version discipline).

**Q4 — register link placement?**
> Auto-Resolution: Link the report from the sprint row's existing **Retro** cell in `SPRINTS.md` (next to the `sprint.md#retro` link), not a new column. sprint-close §7 (registers) gains the instruction; SPRINTS.md and its seed need no schema change.
> Rationale: The Retro cell already holds arbitrary links; a new column would ripple into `SPRINTS.template.md`, which is outside the touch set and carries no new information.
> Source: `.devops/backlog/SPRINTS.md:83-87` (row shape), plan body "Operator show-and-tell before archiving; the SPRINTS.md register row links the report".

**Final validation verdict recorded:** "Is this all the context required?" -> **Yes** — no `Unresolvable:` entries.

---

> **GATE A (Scope) — AUTO clearance evidence:** Phases 1-3 (+3.5) complete. Positive, presence-based evidence per `.devops/rules/plan-lifecycle.md` § AUTO Gate Evidence Contract: every Q# carries an `Auto-Resolution:` with `Rationale:` + `Source:`; the final-validation verdict line is recorded; no unchecked boxes and no `TBD`/`TODO`/`FIXME` tokens in this evidence block; no blocking verdict lines. **Gate A: APPROVED (auto).** Status -> `PHASE_3`, Active Persona -> `Scoper`.

---

## 4 Phase 4: Wiki Requirements & Acceptance Criteria
**Skill Executed:** `ptp-high-visionary` (spec-first directives) — inline, SINGLE

> **No wiki delta — rationale:** the parcel's surface is machinery (`.devops/skills/`, `.devops/templates/`, `HOW-TO.md`), and the `.wiki/` library documents the app-facing pattern, which does not consume sprint-close behavior. Nothing in `.wiki/` changes shape, contract, or grounding, so no doc is drafted as `in-progress` and none will be promoted at wrap-up. The machinery canon for this behavior lives in the skill itself (single source of truth — no second dialect).

**Acceptance Criteria:**

| # | Criterion (behavior) | Test Target |
|---|---|---|
| 1 | `sprint-close/SKILL.md` frontmatter bumped `version: 7 -> 8`, `updated: 2026-09-23` | read frontmatter |
| 2 | New `## 5. Write the Business Report` section sits between the retro step and the archive step, with the reusable outline (goal / deliverables by theme / wins / quality / non-development open items / what's next), audience + tone rules, and the operator show-and-tell | grep headings + section body |
| 3 | Steps renumbered to 8 with no duplicate numbers; the archive step is `## 6`, registers `## 7`, hand-off `## 8`; the internal `§ 6's` self-reference in the lane-opt-in paragraph now reads `§ 7's` | grep numbered headings + grep `§ 6's` (0 hits) / `§ 7's` (1 hit) |
| 4 | §6 archive step explicitly moves `business-report.md` alongside `sprint.md`, with a never-silent fallback if the report was skipped | grep `business-report.md` in §6 |
| 5 | §7 registers step instructs linking the report from the SPRINTS.md row's Retro cell | grep `business report` in §7 |
| 6 | `sprint.template.md` header comment names the `business-report.md` artefact and when it appears; template `version: 1 -> 2` + `updated:` | grep + frontmatter read |
| 7 | `HOW-TO.md` gains exactly one mention of the sprint-close business report in the §3 lifecycle narrative | grep count == 1 |
| 8 | Unaffected live cross-refs stay valid: `@sprint-close` § 2 references in REFACTORING.md (x4) still point at the scan step | grep `@sprint-close` + `§ 2` vs heading `## 2.` |
| 9 | Encoding intact: all three touched files carry no mojibake/U+FFFD after edits | `check-utf8-agents.ps1` exit 0 |
| 10 | Repo gates stay green after the edits (wiki lint, claims check, prefix freshness, machinery scan) | commands recorded in Phase 9 |

**Spec Notes:**
- Data flow: `business-report.md` is written into the sprint folder BEFORE the archive move, so one `git mv` carries it into `.devops/archive/sprints/sprint-{n}-<slug>/` — sprint-folder debris, no plan-lifecycle involvement.
- Edge cases: report skipped or operator absent -> recorded in the retro, never silent; batch path never runs sprint-close (wrap-up owns batch retirement), so show-and-tell always has an operator present.
- Docs consumed by this feature (read-first list): `plan-lifecycle.md` § AUTO Gate Evidence Contract, `sync-architecture/SKILL.md` (version discipline + merge-by-name), `SPRINTS.md` § Conventions, archived T1-E2.06/T1-E3.04 wording in sprint-close §1/§6.

---

## 5 Phase 5: High-Visionary Standard Implementation Plan
**Skill Executed:** `ptp-high-visionary` — inline, SINGLE

**Simplicity Gate:**
> [x] Climbed the Simplicity Ladder (7 rungs) — the report is a step in the existing close ritual and a file in the existing folder; no new template file, no new register column, no new lifecycle rule
> [x] No speculative abstractions — no `business-report.template.md` seed file, no schema, no automation; the outline lives in the skill like the retro's does
> [x] Reuse over reimpl — Reuse Log populated below
> [x] Deliberate simplifications marked with `ponytail:` comments (one, below)
> [x] Safety exceptions preserved — the never-silent skip fallback; Gate D human sign-off unaffected

**Reuse Log:**
| Existing Asset | Where Reused | Ladder Rung |
|---|---|---|
| Retro-template pattern (fenced outline inside `sprint-close` §4) | §5's reusable report outline | 2 (reuse the pattern) |
| Sprint folder + archive `git mv` | report travels with the folder; no new move rule | 2 |
| SPRINTS.md Retro cell | report link | 2 |

**Spaghetti Triage (See-Name-Route, Do Not Fix):**
- [x] No new smells introduced. Pre-existing note: `sprint-close/SKILL.md` is the sprint lifecycle's busiest prose surface (E3.15/E3.16 also land here) — already on record in sprint.md § Risks; routed, not fixed.

### To-Do List
- [x] Step 1: `.devops/skills/sprint-close/SKILL.md` — all edits (1(a)-1(f) below)
- [x] Step 2: `.devops/templates/sprint.template.md` — header note + version bump
- [x] Step 3: `HOW-TO.md` — one mention in §3
- [x] Phase 9 verification commands + evidence table

### File-Level Steps
1. `.devops/skills/sprint-close/SKILL.md` — (a) frontmatter `version: 7 -> 8`, `updated: 2026-09-23`; (b) insert new `## 5. Write the Business Report` between §4 and the archive step: when/why (business stakeholders, not the dev team), where (into the sprint folder, before archiving, so §6's move carries it), the reusable outline (goal; deliverables by theme — grouped per outcome/epic/theme, never per parcel code; wins; quality paragraph; non-development open items = operator actions; what's next), tone rules (benefit language; only trust-building numbers: delivered totals, test counts, capacity % — machinery thresholds out; machinery vocabulary — parcel, Gate D, Kill List, spaghetti scan — prohibited), operator show-and-tell before archiving; (c) renumber `## 5. Archive...` -> `## 6.`, `## 6. Update the Registers` -> `## 7.`, `## 7. Hand Off` -> `## 8.`; (d) §6 archive list gains: move `business-report.md` alongside `sprint.md` (+ recorded-skip fallback); (e) §7 registers gains: link the report from the SPRINTS.md row's Retro cell; (f) fix the internal cross-ref in the lane-opt-in paragraph (`§ 6's` -> `§ 7's`).
2. `.devops/templates/sprint.template.md` — header comment gains one line naming `business-report.md` (written by `@sprint-close` at close, so it is never present while the sprint is open and archives with the folder); frontmatter `version: 1 -> 2`, `updated: 2026-09-23`.
3. `HOW-TO.md` — §3 lifecycle list gains one bullet naming `@sprint-close` and its plain-language business report filed with the archived sprint record. ASCII-only insertions everywhere (mojibake guard); existing non-ASCII bytes untouched.

**Implementation discipline:** no code snippets beyond exact literals (`business-report.md`, frontmatter keys, heading strings); edits land direct-to-disk in Phase 8; every inserted line keeps the file's UTF-8 encoding clean (the repo's CP437 mojibake history is the reason for the ASCII-only rule).

`ponytail:` the show-and-tell is recorded as an operator-facing instruction in §5 prose rather than a checklist — one file, one instruction, no new state to track.

### Wiki Core References
- Machinery-domain parcel: no `.wiki/core/` blueprint derives from sprint-close behavior (see Phase 4's no-wiki-delta rationale).

### Wiki Docs to Add/Edit
- None (no wiki delta).

---

> **GATE B (Spec & Plan) — AUTO clearance evidence (SINGLE: Gates B+C merge here):** Phase 4 carries ≥1 acceptance-criterion row with non-empty criterion and Test Target (10 rows), plus the recorded no-wiki-delta line with rationale. Phase 5 carries ≥3 file-level steps naming paths. The inline Phase 6 self-review below fills one row per AC with evidence, and its `**Verdict:**` line reads `PASS` (checked after writing it — the contract's ordering for the SINGLE checkpoint). No unresolved placeholders in the evidence blocks; no blocking verdict lines. **Gate B: APPROVED (auto). Gate C: N/A (SINGLE).** Status -> `PHASE_5`, Active Persona -> `High-Visionary`.

---

## 6 Phase 6: Grumpy Architect Spec & Logic Audit
**Skill Executed:** `ptp-grumpy-architect` (`SINGLE`: orchestrator inline)

**`SINGLE` self-review (evidence — one row per acceptance criterion):**

| AC # | Criterion met? (`Yes`/`No`) | Evidence / gap |
|---|---|---|
| 1 | Yes | File-Level Step 1(a) bumps `version: 7 -> 8` + `updated:` per version discipline (`sync-architecture/SKILL.md:86-89`) |
| 2 | Yes | Step 1(b) enumerates the full §5 content: audience, location/timing, six-part outline, tone rules, show-and-tell — mirroring the plan body's proven satellite spec verbatim |
| 3 | Yes | Step 1(c)+(f): renumber 5->6, 6->7, 7->8 and the internal `§ 6's` -> `§ 7's` fix; Phase 9 grep proves no duplicate numbers and 0 stale hits |
| 4 | Yes | Step 1(d): §6 gains the `business-report.md` move + recorded-skip fallback (never silent) |
| 5 | Yes | Step 1(e): §7 gains the Retro-cell link instruction; Q4 resolution rules out a schema change |
| 6 | Yes | Step 2: header-comment note + template `version: 1 -> 2`; Q3 resolution records why no file table is invented |
| 7 | Yes | Step 3: exactly one bullet in HOW-TO.md §3; Phase 9 grep count == 1 proves no drift |
| 8 | Yes | Phase 2 renumber-drift scan: all `§ 2` refs sit above the insertion point; Phase 9 re-greps to confirm unchanged |
| 9 | Yes | ASCII-only insertion discipline (Step 3) + `check-utf8-agents.ps1` in Phase 9 (exit 0 required) |
| 10 | Yes | Phase 9 evidence table runs the full gate set; every command must exit 0 |

**Verdict:** `PASS`

- **System Contracts Explicit:** three files, all named; the only new contract surface is the `business-report.md` file convention (name + location + timing), owned by sprint-close §5.
- **File Boundary & Scope Collisions:** E3.15/E3.16 also edit `sprint-close/SKILL.md` but have not claimed it (file still v7 at this claim; claim protocol serializes). No other plan in `.devops/plans/` holds any touch-set file.
- **Dependency Gaps:** none — no new utilities, schemas, or scripts; the step reuses the existing archive move and register cell.
- **YAGNI Bloat:** rejected: separate template seed file, new SPRINTS.md column, plan-lifecycle rule, business-report automation. The outline lives in the skill beside the retro's.
- **Paranoid Security (Contract Level):** N/A — no code, no secrets, no inputs; tone rules keep machinery internals out of a stakeholder-facing file (information-hygiene guard, documented in §5).
- **Survivability:** N/A — prose ritual; the recorded-skip fallback covers the "operator absent / report skipped" path.
- **Edge Cases:** skipped report (fallback recorded in retro); satellite without the sprint (skill travels via folder copy — Q1); sibling plans landing later must renumber again (on record in sprint.md § Risks; their plans already name the linkage).
- **Performance Trade-offs:** none — static prose; SKILL.md grows ~40 lines, far under any ceiling.
- **Architectural Anti-Patterns:** no duplicate source of truth (the skill owns the ritual; the template only names the artefact; HOW-TO only mentions it — one home each).
- **Endpoint Protection & Rate Limiting:** N/A.
- **Wiki Core Compliance:** no `.wiki/` surface touched; machinery canon stays in `.devops/` (Phase 4 rationale).

**Dead-Code / Orphan Flags (for Wrap Up backlog entry):**

| plan section / file_path:line_number | Reason |
|---|---|
| None | — |

**Required Fixes:**
> [x] None

---

## 7 Phase 7: Smooth Operator Product Review
**Skill Executed:** `ptp-smooth-operator` (`SINGLE`: orchestrator inline)

> **`SINGLE` topology:** no independent reviewer. Recorded **`N/A — SINGLE self-review`**; the self-review is presented at Gate B.

- **Vision & Journey Integrity:** the close ritual gains a stakeholder-facing artefact exactly where the journey ends (report before archive) — matches the satellite-proven order.
- **Scope Containment (No Gold Plating):** three files, no new schemas/templates/rules; the plan's authored scope is unchanged.
- **4 Core User States:** N/A (no UI); the operator's journey: show-and-tell before archiving, link visible in the register after.
- **Guardrails & Permissions:** never-silent skip fallback; version discipline keeps satellites on UPGRADE, not DRIFT.
- **Mobile / A11y / Telemetry:** N/A — markdown artefact.

**Required Fixes:**
> [x] None

---

> **GATE C (Peer Reviews) — `SINGLE` topology:** recorded **`N/A (SINGLE)`** — plan approval happened once at Gate B with the inline self-review. No review files spawned.

---

## 8 Phase 8: Execute Changes
**Skill Executed:** `ptp-code-surgeon` (inline, SINGLE)

**Execution Isolation:** Phase 8 triggers after Gate B cleared with the SINGLE self-review `PASS` (Gate C `N/A`).

**Single-Pass Direct-to-Disk:**
> [x] Implementation written DIRECTLY to source files - no intermediate Markdown code blocks, no drafting files
> [x] Touched only intended lines - no adjacent refactors (one em-dash degraded to a hyphen in §6 item 3 during the archive-step edit was caught in diff review and restored byte-identically)
> [x] Cleaned up only owned orphans
> [x] Pre-existing dead code left untouched

> [x] Step 1: `.devops/skills/sprint-close/SKILL.md` — edits 1(a)-1(f) per Phase 5: frontmatter `version: 7 -> 8` + `updated: 2026-09-23`; new `## 5. Write the Business Report` (where/when, audience+grouping, reusable outline with six sections, tone rules, show-and-tell + recorded-skip fallback); renumbered Archive -> `## 6` (gains the `business-report.md` move as item 2), Registers -> `## 7` (gains the Retro-cell report link), Hand Off -> `## 8`; internal `§ 6's` -> `§ 7's` in the lane-opt-in paragraph
> [x] Step 2: `.devops/templates/sprint.template.md` — header comment names `business-report.md` (written at close, never present while open, archives with the folder); `version: 1 -> 2`, `updated: 2026-09-23`
> [x] Step 3: `HOW-TO.md` — one §3 bullet naming `@sprint-close` and the business report filed with the archived sprint record

---

## 9 Phase 9: Verify Changes
**Skill Executed:** `ptp-code-surgeon` (inline, SINGLE)

**Build & Lint:** N/A — no compilation surface in this template repo (machinery docs only). The verification set is the deterministic script gates.

**Test Report:**
- [x] Lint pass (exit 0) — wiki lint
- [x] Tests pass (exit 0) — wiki claims check + rule fanout
- [x] Build pass (exit 0) — N/A (no build in this template repo); the machinery gate set below substitutes
- [x] Code matches exact plan specifications — AC test targets re-run; every command exits 0
- [x] No functional gaps identified

**Verification Evidence (required — the checkbox list alone is not evidence):**
| # | Command | Expected | Observed | Exit |
|---|---|---|---|---|
| 1 | `python scripts/wiki_lint.py` | 0 broken links, frontmatter valid | OK: wiki healthy - 0 broken links | 0 |
| 2 | `python scripts/wiki_claims.py check` | 0 stale claims | claims OK: 0 stale, all grounded sources present | 0 |
| 3 | `powershell scripts/check-utf8-agents.ps1` | ALL CLEAN, no mojibake | ALL CLEAN (188 files scanned) | 0 |
| 4 | `powershell scripts/check-parcel-prefix.ps1` | prefix PASS, NOMODEL OK | byte-identical prefixes, no binding surface declares a model | 0 |
| 5 | `node scripts/spaghetti-monster-scan.cjs` | scan completes, no crash | machinery roots scanned; top-risk row unchanged (scanner self-flag, pre-existing) | 0 |
| 6 | `python scripts/rule_fanout.py` | 0 unauthorized | GATE_D_USER_APPROVAL / MULTI / SINGLE all AGREE 3/3 | 0 |
| 7 | `powershell scripts/sync-architecture.ps1 -SelfTest` | transport smoke OK | SELFTEST OK: 5 dirs, 34 skills, 18 files | 0 |
| 8 | AC greps (python probe): heading sequence 1-8 unique; stale `§ 6's` == 0; `§ 7's` == 1; §6 carries `business-report.md` + never-silent fallback; §7 carries the Retro-cell link; §5 outline probes (stakeholders / outcome-epic-theme / six outline headings / tone / show-and-tell / machinery-vocab prohibition) all true; template note + `version: 2`; HOW-TO `@sprint-close` count == 1; REFACTORING `§ 2` refs == 4 and `## 2. Run the Spaghetti Scan` unchanged | all pass | all pass | 0 |

**AC-by-AC disposition (Phase 4 table -> Phase 9):**
| AC | Disposition |
|---|---|
| 1 | PASS — frontmatter `version: 8`, `updated: 2026-09-23` |
| 2 | PASS — §5 present with all six outline sections + tone + show-and-tell probes true |
| 3 | PASS — headings 1-8 unique, no duplicates; stale `§ 6's` 0 hits, `§ 7's` 1 hit |
| 4 | PASS — §6 item 2 moves `business-report.md` with the recorded-skip fallback |
| 5 | PASS — §7 links the report from the row's Retro cell |
| 6 | PASS — template header note + `version: 2` / `updated: 2026-09-23` |
| 7 | PASS — exactly one `@sprint-close` mention in HOW-TO.md §3 |
| 8 | PASS — 4 `§ 2` refs intact; `## 2. Run the Spaghetti Scan` unchanged |
| 9 | PASS — `check-utf8-agents.ps1` ALL CLEAN (188 files) |
| 10 | PASS — rows 1-7 of the evidence table all exit 0 |

**Findings recorded for the wrap-up / follow-up (outside this plan's touch set — not edited here):**
1. `sync-architecture/SKILL.md:83` cites "the fold review in `@sprint-close` §6"; after this plan's renumber the fold review lives in §7. One-line citation repair needed in a follow-up (the file is outside `touches`, so the declared write set stays equal to the actual one).
2. `machinery-version` before-value observed at Phase 9: **68** (`.devops/sync-manifest.yaml`) — the batch wrap-up's single increment reads the live value at its own moment; per-file skill bumps landed with the edits (`sprint-close` 7->8, `sprint.template.md` 1->2).

---

> **HALT POINT (Gate D — Implementation):** Implementation and verification complete. Batch path: this plan terminates at `PHASE_9` with Gate D `OPEN`; the batch host presents the consolidated report and the human records the verdict. Never archived here; never flipped to APPROVED by the runner.

---

## 10 Phase 10: User Review & Tweaks
**Skill Executed:** `knowledge-capture` (post-Gate-D — owned by the wrap-up path, not this runner)

*Empty until the Gate D verdict lands; tweaks then follow the surgical-fix discipline.*

---

## Completion Note (Wrap Up)
**Skill Executed:** `agent-wrap-up` (post-Gate-D — per-plan or batch scope; never this runner)

*To be completed by the wrap-up: themed tweaks, knowledge capture, spec reconciliation, promotions, archiving (`git mv` to `.devops/archive/`), backlog review. The batch wrap-up owns the single `machinery-version` increment (live before-value observed by this runner: **68**).*

---

## 📍 State & Gates (CACHE-ANCHORED — update ONLY this section at gate transitions)

> **Cache rule:** This is the **last section** in the file. Gate transitions mutate ONLY the rows below — phase content above AND the frozen **Plan Settings** block at the top stay byte-stable to preserve LLM prefix-cache hits. Every "Update Status" instruction in the halt points above means "edit this section".
>
> **This section records state, never a handoff instruction** — a plan is never a baton between sessions. The orchestrator advances group to group in the session it started in; see `@pass-the-parcel` § Review Gates.

| Metric | Value |
|---|---|
| **Status** | `COMPLETE` |
| **Version** | `v0.1.0` |
| **Active Persona** | `Executor` |
| **Depends On** | none (canonical: claim front-matter `depends_on`) |
| **Blocks** | none |

> **Settings pointer:** `Mode` and `Agents` live in the frozen **Plan Settings** block at the TOP of this file — read them there before any phase. Never duplicate them here.

> Valid states: `QUEUED`, `CLAIMED`, `GATE_D_USER_APPROVAL`, `PHASE_1`, `PHASE_3`, `PHASE_5`, `PHASE_5_REVISION`, `PHASE_7`, `PHASE_8_FAILED`, `PHASE_9`, `COMPLETE`.
>
> `GATE_D_USER_APPROVAL` is a **claim-status** value (top front-matter), not a pipeline phase: a plan carrying it has executed through Phase 9 and sits at bottom `Status: PHASE_9` with Gate D `OPEN`, awaiting the human verdict. Valid `claim_status` enum: `QUEUED | CLAIMED | GATE_D_USER_APPROVAL | COMPLETE`.

| Gate | Requirement | Status |
|---|---|---|
| A | Scope approved (Phases 1-3, + 3.5 in AUTO) | `APPROVED` (auto — evidence in Phase 3.5 block) |
| B | Spec & plan approved (Phases 4-5) | `APPROVED` (auto — evidence at Gate B block; SINGLE merge) |
| C | Peer reviews passed (Phases 6-7) | `N/A (SINGLE)` |
| D | Implementation verified (Phases 8-9) | `APPROVED` — human verdict 2026-09-23 (batch Gate D consolidated) |

> Gate flips: rows flip `OPEN` -> `APPROVED`/`REJECTED` ONLY after the user's (or AUTO verification's) verdict, recorded by the orchestrator. Executing agents halt with their gate `OPEN`. Rejection routes: A -> `PHASE_1`; B/C -> `PHASE_5_REVISION`; D/rollback -> `PHASE_8_FAILED`. Never edit rows above this section for gate bookkeeping.
>
> **This section is the LAST section in the file. All gate bookkeeping happens here.**

## Completion Note

**Outcome:** delivered as planned. `@sprint-close` v8 carries the new Step 5 "Write the Business Report" (stakeholder-facing, grouped per outcome/epic/theme, six-section outline, tone rules, operator show-and-tell); the archive step carries `business-report.md` into the closed sprint folder and the SPRINTS.md register row links it; `sprint.template.md` v2 names the artefact in its header comment; HOW-TO §3 mentions it once. No plan-lifecycle change was needed — the report is sprint-folder debris, not plan state.

**Deviations:** (1) the plan's "optional file-table row" in `sprint.template.md` became a header-comment note — the template has no file table; no new schema (Phase 3 auto-resolution). (2) The runner verified `sync-architecture` needs no adjustment: skill folders copy recursively and `.devops/templates` is a wholesale `portable_dirs` root, so the template-side fix propagates to satellites unchanged.

**Follow-up finding (outside this plan's write set, deliberately untouched):** `sync-architecture/SKILL.md:83` cites the fold review at "`@sprint-close` §6" — the fold review now lives in §7. One-line repair candidate for a future parcel.

**Batch context:** executed via `@sprint-run` (ptp-parcel-fast, AUTO+SINGLE, model = CLI default). Gate D verdict recorded 2026-09-23 (batch-consolidated). Terminal verification: 8-command battery, all exit 0 (see Phase 9 table).
