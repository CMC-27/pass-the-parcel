---
code: T1-E3.05
sprint: sprint-8-sprint-lifecycle-hardening
claim_status: COMPLETE
owner: "parcel-sprint (DeepSeek V4.1 Flash)"
claimed_at: "2026-09-15"
last_touch: "2026-09-16"
touches: [".devops/agents/**", ".opencode/plans/base-context.md", ".devops/templates/base-context.template.md", ".devops/templates/opencode.template.json", ".devops/templates/AGENTS.template.md", "opencode.json", ".devops/sync-manifest.yaml", "scripts/check-parcel-prefix.ps1", "scripts/sync-architecture.ps1", ".devops/skills/model-routing/SKILL.md", ".devops/skills/pass-the-parcel/SKILL.md", ".devops/plans/template-plan.md", ".devops/rules/agents-and-skills.md", ".devops/README.md", "AGENTS.md", "HOW-TO.md", ".wiki/rules/naming.md", ".devops/backlog/MATURITY.md", ".devops/backlog/t1-parcel-pipeline-machinery-backlog.md", ".devops/logs/version-history.md", ".devops/logs/agent-changelog.md", "CHANGELOG.md"]
depends_on: []
---
# T1-E3.05 - Retire the selectable `parcel-fast` orchestrator (Parcel Fast = subagent-only)
## Theme-Epic: T1 - Parcel Pipeline Machinery, E3 - Concurrency & Sprint Lifecycle

> **Origin:** Operator ruling (2026-09-15). "Parcel Fast" exists twice — a selectable orchestrator (`.devops/agents/parcel-fast.agent.md`) and the hidden batch subagent (`.devops/agents/ptp-parcel-fast.subagent.md`) — one prefix apart, a documented confusion hazard. Decision: **Parcel Fast is a subagent of `parcel-sprint` only.** The fast lane is reachable exclusively through `parcel-sprint` -> `ptp-parcel-fast`; the main `parcel` agent is the tool used to deliver a plan from any investigated context. This parcel retires the selectable orchestrator and every derived surface it created, and makes the deletion travel to satellites.

## ⚙️ Plan Settings (FROZEN — set at plan start, read before any phase)

| Setting | Value | Meaning |
|---|---|---|
| **Mode** | `AUTO` | `AUTO` — Gates A-C auto-clear on mechanical verification; **Gate D always halts for the human** |
| **Agents** | `SINGLE` | `SINGLE` — inline personas, no subagent spawns; Group C collapses to a self-review checkpoint; Gate C recorded `N/A (SINGLE)`; the single approval is Gate B |

> **Preset provenance:** written by the spawned `ptp-parcel-fast` runner at claim time from the **`parcel-sprint` locked preset** (`AUTO` + per-plan `SINGLE`); no selection question was asked. Frozen here — never edited after plan start.

> **Frozen config — read before executing ANY phase.** These two settings govern the entire pipeline and are never edited after plan start; they sit at the TOP so no session can miss them. Mutable runtime state (Status / Active Persona / gates) lives ONLY in the cache-anchored `## 📍 State & Gates` section at the bottom. See `@pass-the-parcel` § Agent Topology. **Preset provenance:** the orchestrator agent may set these from a **locked preset** (e.g. `parcel-sprint` locks `AUTO` + per-plan `SINGLE`); when it does, the selection question is skipped and the preset values are recorded here.

> **Skill Architecture:** This template is consumed by the `pass-the-parcel` skill. Each phase delegates to a specialized sub-skill. See the parcel skill's Skill Delegation Map.
>
> **RULES:**
> - **Claim front-matter** at the very top (`code` / `sprint` / `claim_status` / `owner` / `claimed_at` / `last_touch` / `touches` / `depends_on`) governs claiming. See `.devops/rules/plan-lifecycle.md` § Claim Protocol. `claim_status` is NOT the bottom `Status` (pipeline phase).
> - Full skeleton required - ALL 10 phases + Wrap Up MUST be present.
> - Halt points are HARD STOPS - each gate blocks all subsequent phases.
> - Persona matches Status - use the State Lifecycle table in pass-the-parcel skill.
> - Read **Plan Settings** above before acting; the topology there governs which gates apply. See `@pass-the-parcel` § Agent Topology.
>
> **🔒 CACHE-ANCHORED:** The **Plan Settings** block above is frozen config; the mutable State Dashboard + Gate Log live in the **last section** (`## 📍 State & Gates`). Gate transitions update ONLY those bottom rows — do NOT edit content above once written. Byte-stable prefix = LLM prefix-cache hits for every downstream agent re-read.

---

## 1 Phase 1: Expansion & Scoping
**Skill Executed:** `ptp-context-hunter`

**Intent:**
Collapse the fast lane to a single entity. Today "Parcel Fast" exists twice — a selectable orchestrator (`.devops/agents/parcel-fast.agent.md`) and the hidden batch subagent (`.devops/agents/ptp-parcel-fast.subagent.md`) — one prefix apart. The operator's ruling (2026-09-15) is that **Parcel Fast is subagent-only**: the fast lane is reachable exclusively through `parcel-sprint` -> `ptp-parcel-fast`, while the main `parcel` orchestrator is the delivery tool for any investigated context. This parcel retires the selectable orchestrator and every surface derived from it, and makes the retirement reach satellites.

**In Scope:**

- **D1 — Delete the selectable agent.** Remove `.devops/agents/parcel-fast.agent.md`. Keep `ptp-parcel-fast.subagent.md` as the sole fast entity; its name is unchanged to respect the `ptp-<slug>.subagent.md` subagent convention (`.devops/rules/agents-and-skills.md` § naming).
- **D2 — Registry + presets (canonical + seed).** Remove the `parcel-fast` row from the `## Model Registry` in `.opencode/plans/base-context.md` and `.devops/templates/base-context.template.md`; remove the `parcel-fast` row from the `## Orchestrator Presets` table; rewrite the trailing prose so it describes only the surviving presets (`parcel` = `ask`; `parcel-sprint` = locked batch host).
- **D3 — Runtime bindings (live + seed).** Remove the `parcel-fast` agent entry from `opencode.json` and `.devops/templates/opencode.template.json`.
- **D4 — Prefix re-inline.** Update the PREFIX-LOCKED header's locked-agent list and the `<!-- ORCHESTRATOR-ONLY:START ... -->` marker comment to drop `parcel-fast`; then run `scripts/check-parcel-prefix.ps1 -Sync` to re-inline the prefix byte-for-byte into every remaining locked agent (2 orchestrators + 7 `ptp-*` subagents; 10 -> 9).
- **D5 — Script comments.** Update the selectable-agent examples in the comment blocks of `scripts/check-parcel-prefix.ps1` and `scripts/sync-architecture.ps1`. No logic change — both derive the agent set from the `parcel*.agent.md` / `ptp-*.subagent.md` globs, which still match `parcel.agent.md` and `parcel-sprint.agent.md`.
- **D6 — Docs.** `AGENTS.md`, `.devops/README.md`, `.devops/rules/agents-and-skills.md`, `HOW-TO.md` § Parcel-Fast, `.wiki/rules/naming.md`, `.devops/skills/model-routing/SKILL.md` (orchestration row), `.devops/skills/pass-the-parcel/SKILL.md` § Orchestrator Presets (the `locked` example becomes `parcel-sprint`), `.devops/plans/template-plan.md` (preset-provenance example), `.devops/templates/AGENTS.template.md`.
- **D7 — Transport.** Add `.devops/agents/parcel-fast.agent.md` to `prune_files` in `.devops/sync-manifest.yaml` so an already-synced satellite deletes the orphan; bump `machinery-version` `43 -> 44`.
- **D8 — Register + logs.** Replace the `E3` Open placeholder row with the `T1-E3.05` row in `.devops/backlog/t1-parcel-pipeline-machinery-backlog.md`; refresh the `MATURITY.md` axis-1 note; append `CHANGELOG.md`, `.devops/logs/version-history.md` (with the literal `machinery-version: 44`) and `.devops/logs/agent-changelog.md` entries.
- **D9 — Stale boilerplate (optional).** Refresh the `parcel-fast` preset-provenance example in the parked `T1-E3.03` / `T1-E3.04` plans so they stop citing a retired agent.

**Out of Scope:**
- **Gap 4 (blast-radius test scoping + where the full suite runs)** — a separate parcel, handled through the `parcel-sprint` orchestrator.
- Any change to `ptp-parcel-fast` behaviour, the batch chain, gates, or the `parcel` orchestrator's behaviour.
- Redesigning the preset mechanism, or introducing any replacement selectable fast orchestrator.
- Renaming `ptp-parcel-fast` -> `parcel-fast` (explicitly rejected — `ptp-` is the subagent prefix).
- Editing historical records (`CHANGELOG.md` historical rows, `.devops/archive/**`, prior changelog entries).

**Perimeter Notes:**
- **Prefix lock (AGENTS.md rule 9).** `base-context.md` is canonical; agent copies are derived. Edit `base-context.md` + the seed, then `scripts/check-parcel-prefix.ps1 -Sync` re-inlines byte-for-byte. Run `check-parcel-prefix.ps1` and `check-utf8-agents.ps1` before any push.
- **Bidirectional registry/file pairing.** `check-parcel-prefix.ps1` fails on a registry key with no binding file **and** on a binding file with no registry row. The `parcel-fast` registry row, agent file, and `opencode.json` entry must be removed **together** — a partial edit is a hard FAIL.
- **CI hard gate.** Bump `machinery-version: 43 -> 44` in `.devops/sync-manifest.yaml` **and** record the literal `machinery-version: 44` in `.devops/logs/version-history.md`, or `validate.yml` "Machinery version discipline" fails.
- **Claims drift.** Editing `AGENTS.md` can stale AGENTS.md-anchored claim hashes; run the claims check (`scripts/wiki_claims.py check`) and re-stamp, or `validate.yml` "Claims drift check" blocks CI.
- **Deliberate loss.** The interactive `/parcel-fast` shortcut disappears. Interactive fast runs become `parcel` + select `AUTO`/`SINGLE` at plan start; unattended fast runs go through `parcel-sprint`.
- **Overlap with T1-E3.03 / T1-E3.04.** All three edit the shared-prefix surfaces (`base-context.md`, agents, the plan template). The claim protocol's `touches` predicate serialises them; no `depends_on` edge is asserted (content is orthogonal).
- **Sync DRIFT false-positive.** `sync-architecture.ps1` hashes each prefix-locked agent's unique content; after deleting a locked file, confirm `-Check` reports no stale DRIFT entry.

---

## 2 Phase 2: Requirements & Context
**Skill Executed:** `ptp-context-hunter`

**Forensic Context Inventory:**
> [x] Wiki core docs read (`.wiki/core/00-system-index.md` + drilled into relevant docs) — `.wiki/core/00-system-index.md` (hub, categories, operational links), `.wiki/core/17-docs-blueprint.md` (naming/taxonomy authority for the `.wiki/rules/naming.md` row), `.wiki/core/18-knowledge-capture.md` (Rules § Agents & Models, § Sync & Versioning), `.wiki/rules/naming.md`
> [x] Knowledge capture read (`.wiki/core/18-knowledge-capture.md`) — Rule 3 (template-owned model bindings), Rule 7 (never edit PREFIX-LOCKED surfaces; edit `base-context.md` then `-Sync`), Rule 6 (portable skills derived), and the Pitfall "editing a claim source invalidates other docs (`AGENTS.md` is claimed by 09 and 12)" → `wiki_claims.py update` is mandatory after the `AGENTS.md` edit.
> [x] Source code verified (components / utilities / hooks / types in blast radius) — this repo is the template (no `src/`); the blast radius is machinery: `.opencode/plans/base-context.md`, `.devops/agents/**`, `opencode.json`, `scripts/check-parcel-prefix.ps1`, `scripts/sync-architecture.ps1`, `.devops/sync-manifest.yaml`, the seed templates, and the doc mirrors — all read in full before editing.

**Relevant Existing Decisions (from knowledge capture):**
- `AUTO` auto-clears Gates A-C; **only Gate D always halts** — reconciled 2026-09-13 (machinery 40, CW2). The retirement does not touch either axis.
- The `## Model Registry` in `base-context.md` is the single source; agent frontmatter `model:` and `opencode.json` `agent.<key>.model` are **derived** and force-stamped on sync (machinery 39 / T1-E1.03). Removing a registry row therefore requires removing its derived surfaces.
- The batch path's three named deviations — batched Gate D, trunk-sequential claim, Strict Context Isolation exception — stay exactly as-is (machinery 38 / T1-E3.02); they reference `ptp-parcel-fast`, which survives.

**Relevant Docs Found:**
| Doc | Why it matters |
|---|---|
| `.opencode/plans/base-context.md` | Canonical prefix: header agent list, ORCHESTRATOR-ONLY marker, Model Registry, Orchestrator Presets table. |
| `.devops/sync-manifest.yaml` | `portable_dirs: .devops/agents` (the delete travels); `prune_files` is the retirement mechanism; `machinery-version` bump site. |
| `scripts/check-parcel-prefix.ps1` | Enforces prefix byte-equality + registry/file pairing + seed agreement. |
| `scripts/sync-architecture.ps1` | Hashes prefix-locked agents' unique content; comments name the agent set. |
| `.devops/rules/agents-and-skills.md` | The naming + bounded-spawning rules this change must keep true. |
| `.devops/rules/plan-lifecycle.md` § Deviations | Names only `ptp-parcel-fast` — no selectable-orchestrator reference to remove. |

**Relevant Code Found:**
Full `parcel-fast` reference inventory (36 files; excludes `.git` and `.devops/archive/**`):

| Surface | Files | Nature |
|---|---|---|
| Selectable agent file | `.devops/agents/parcel-fast.agent.md` | **Delete.** |
| Inlined prefix copies | `.devops/agents/parcel.agent.md`, `parcel-sprint.agent.md`, `ptp-*.subagent.md` (7) | Rewritten by `-Sync` after the header edit. |
| Canonical prefix + seed | `.opencode/plans/base-context.md`, `.devops/templates/base-context.template.md` | Header list, marker comment, registry row, presets row, prose. |
| Runtime config + seed | `opencode.json`, `.devops/templates/opencode.template.json` | Remove the `parcel-fast` agent entry. |
| Scripts + manifest | `scripts/check-parcel-prefix.ps1`, `scripts/sync-architecture.ps1`, `.devops/sync-manifest.yaml` | Comments, `prune_files`, machinery bump. |
| Docs | `AGENTS.md`, `.devops/README.md`, `.devops/rules/agents-and-skills.md`, `HOW-TO.md`, `.wiki/rules/naming.md`, `.devops/skills/model-routing/SKILL.md`, `.devops/skills/pass-the-parcel/SKILL.md`, `.devops/plans/template-plan.md`, `.devops/templates/AGENTS.template.md` | Prose / table references. |
| Registers + logs | `t1-parcel-pipeline-machinery-backlog.md`, `MATURITY.md`, `CHANGELOG.md`, `version-history.md`, `agent-changelog.md` | Bookkeeping. |
| Subagent-only references → **no change** | `.devops/skills/ptp-parcel-fast/SKILL.md`, `.devops/skills/sprint-run/SKILL.md`, `.devops/rules/plan-lifecycle.md`, `.devops/agents/ptp-parcel-fast.subagent.md` | These say `ptp-parcel-fast`, the surviving subagent. |

> **Substring hazard:** a naive find-replace of `parcel-fast` also hits `ptp-parcel-fast` (the surviving subagent) and the shared-prefix header quoted inside every `ptp-*` agent. Edits must be phrase/line-exact, never bare-substring.

---

## 3 Phase 3: User Clarification
**Skill Executed:** `ptp-context-hunter`

> [x] Q1: Confirm the surviving name — delete the selectable `parcel-fast.agent.md` and keep `ptp-parcel-fast.subagent.md` (recommended; matches the `ptp-*.subagent.md` convention)? The alternative (rename the subagent to `parcel-fast.subagent.md`) breaks the `ptp-` prefix convention used by the check/sync scripts.
> [x] Q2: Confirm losing the interactive `/parcel-fast` shortcut is acceptable — fast becomes subagent-only, and interactive fast runs use `parcel` + select `AUTO`/`SINGLE` at plan start.
> [x] Q3: Should the `## Orchestrator Presets` prose keep a preset example, or be rewritten to describe only `parcel` (`ask`) and `parcel-sprint` (locked batch host)?
> [x] Q4: Refresh the stale preset-provenance boilerplate in the parked `T1-E3.03` / `T1-E3.04` plans (D9), or leave them?
> [x] Q5: Wording for the retired-file comment in `sync-manifest.yaml` `prune_files`.
> [x] Final validation: "Is this all the context required?" -> Yes

**Phase 3.5 — AUTO auto-resolutions (`ptp-phase3-answerer`, inline):**

| Q | Resolution | Grounding |
|---|---|---|
| Q1 | **Delete `.devops/agents/parcel-fast.agent.md`; keep `ptp-parcel-fast.subagent.md`.** The `ptp-<slug>.subagent.md` pattern is the machine-checked subagent convention (`scripts/check-parcel-prefix.ps1` resolves `ptp-*.subagent.md` and derives the embed key from it); renaming it to `parcel-fast.subagent.md` would drop it out of the `ptp-*` glob, the embed contract, and the `parcel-sprint` `task` allow-list. | Operator ruling 2026-09-15 (`## Origin`); `.devops/rules/agents-and-skills.md` § Naming; `scripts/check-parcel-prefix.ps1:100-101,154` |
| Q2 | **Accept the loss.** The interactive shortcut is a convenience, not a contract; the ruling makes Parcel Fast subagent-only. The two replacements are already documented behaviour: interactive = `parcel` + `AUTO`/`SINGLE` selection at plan start; unattended = `parcel-sprint` → `ptp-parcel-fast`. Recorded as CW1 (deliberate loss) below. | `## Origin`; `.devops/skills/pass-the-parcel/SKILL.md` § Orchestrator Presets; `.devops/rules/plan-lifecycle.md` § Deviations |
| Q3 | **Rewrite the prose to describe only the two surviving orchestrators** — `parcel` = `ask`, `parcel-sprint` = locked batch host. The table itself keeps those two rows; the trailing paragraph drops the `parcel-fast`-specific `task: deny` sentence and instead states the surviving structural enforcement (the host's `task` block is narrowed to exactly `ptp-parcel-fast`). | `.opencode/plans/base-context.md:101-112`; `.devops/rules/agents-and-skills.md` § The "Batch Host" Pattern |
| Q4 | **Yes — apply D9.** Both plans are still in the sprint queue (the operator reordered the run to `05 → 03 → 04`), so the stale provenance example is live text a runnable plan would read. One-line substitution each: the locked-preset example becomes `parcel-sprint`. | `.devops/sprints/sprint-8-sprint-lifecycle-hardening/sprint.md:33` |
| Q5 | **Comment wording:** `# Retired in T1-E3.05 (machinery 44): the selectable parcel-fast orchestrator was removed;`. States what it is, when it went, and why a satellite must drop it — matching the existing prune-file comment convention. | `.devops/sync-manifest.yaml:49-62` (existing comment style) |

> **No `Unresolvable:` entries.** Every question resolves from in-repo evidence already gathered in Phase 2.

**Architectural Conflict Warnings Raised:**
- **CW1 — Interactive fast path removed.** Retiring the selectable agent removes the only one-command fast path; the operator must accept `parcel` + explicit `AUTO`/`SINGLE` selection as the replacement for interactive fast runs.
- **CW2 — Substring collision.** `parcel-fast` is a substring of `ptp-parcel-fast`; a mechanical replace would corrupt the surviving subagent, its skill embed, the `sprint-run` contract, and the shared prefix in every `ptp-*` agent. Mitigation: phrase/line-exact edits, then `check-parcel-prefix.ps1` FAILs on any drift.
- **CW3 — Already-synced satellites.** Without the `prune_files` entry, the orphan `.devops/agents/parcel-fast.agent.md` persists in every satellite while its registry row is gone — so that satellite's own `check-parcel-prefix.ps1` fails "no Model Registry row". The prune entry is load-bearing, not cosmetic.

---

> **HALT POINT (Gate A — Scope):** Phases 1-3 complete (+ Phase 3.5 auto-resolutions in AUTO mode). Set Status -> `PHASE_3`, Active Persona -> `Scoper`. Present the scope perimeter + Phase 3 Q&A record; the user approves the scope before planning begins. On rejection: Status -> `PHASE_1`, Gate A -> `REJECTED`, re-run the affected questions. Questions are relayed to the user **one at a time** via the ask-questions tool — never batched.

> **AUTO auto-clear — Gate A: CLEARED.** Mechanical verification: (a) Phase 1 In-Scope D1-D9 and Out-of-Scope perimeter both populated; (b) Phase 2 Forensic Context Inventory fully ticked and the `parcel-fast` reference inventory recorded (36 files); (c) Phase 3 questions all answered with a recorded Phase 3.5 resolution, **zero `Unresolvable:` entries**. No `REJECTED` verdict line exists anywhere in Phases 1-3. Gate A row in `## 📍 State & Gates` → `APPROVED (AUTO)`.

---

## 4 Phase 4: Wiki Requirements & Acceptance Criteria
**Skill Executed:** `ptp-high-visionary` (spec-first directives) + `@wiki-writer`

> **Spec-First Rule:** The wiki is written BEFORE the code. Docs written here describe target behavior and are marked `status: in-progress` — a pre-code doc is a claim, not truth. Promotion to `stable` happens only at Wrap Up, after the executor verifies code matches spec.
>
> **Conditional Skip (checklist):** Run Phase 4 only if ANY of: (a) new or changed user-visible strings/UI, (b) API, schema, or logic-contract change, (c) wiki-facing behavior change. If none apply, skip this phase and record: `No wiki delta — rationale: <why>`. Never skip silently.

**Wiki Docs to Write/Update (as `in-progress`):**
| Doc | Change |
|---|---|
| `.wiki/rules/naming.md` | The `<slug>.agent.md` example row currently lists `parcel.agent.md`, `parcel-fast.agent.md`, `parcel-sprint.agent.md`. Drop the retired `parcel-fast.agent.md` so the naming example matches the shipped agent set. No `status` change (the doc stays `stable` — the edit removes an example, it does not change a rule); bump `last-reviewed`. |

> **Conditional-skip check:** NOT skipped. Condition (c) applies — a wiki-facing doc named in the naming contract changes. The remaining core docs (00-18) describe the app (`src/`), which this template has none of; no app-domain spec delta exists.

**Acceptance Criteria:**
| # | Criterion (behavior) | Test Target |
|---|---|---|
| 1 | The selectable `parcel-fast` orchestrator has **no surviving surface**: `.devops/agents/parcel-fast.agent.md` absent; no `parcel-fast` Model Registry row; no `parcel-fast` Orchestrator Presets row; no `agent.parcel-fast` entry in `opencode.json` or the seed config. | `scripts/check-parcel-prefix.ps1` (registry↔file↔config bidirectional pairing), `git ls-files .devops/agents/` |
| 2 | `ptp-parcel-fast` survives **byte-identical**: its subagent file, its `<!-- EMBED:START:ptp-parcel-fast -->` embed, and the `parcel-sprint` `task` allow-list are untouched. | `check-parcel-prefix.ps1` `PASS ptp-parcel-fast.subagent.md` + `EMBED` byte-equality |
| 3 | The PREFIX-LOCKED set is **9 agents** (2 orchestrators + 7 `ptp-*` subagents), all byte-identical to the edited canonical prefix (10 → 9). | `check-parcel-prefix.ps1` — 9 × `PASS`, 12 × `MODEL`, exit `0` |
| 4 | `prune_files` carries `.devops/agents/parcel-fast.agent.md`, so an already-synced satellite deletes the orphan instead of keeping a file whose registry row is gone. | `sync-architecture.ps1 -SelfTest` prune assertion; manifest read |
| 5 | `machinery-version` is `44` and the **literal** `machinery-version: 44` appears in `.devops/logs/version-history.md`. | `validate.yml` "Machinery version discipline" predicate (reproduced locally) |
| 6 | No stale `parcel-fast` reference survives in the doc/roster mirrors; every roster states one fast lane (`parcel-sprint` → `ptp-parcel-fast`) and `parcel` as the interactive delivery tool. | line-exact review of `AGENTS.md`, `.devops/README.md`, `agents-and-skills.md`, `HOW-TO.md`, `naming.md`, `model-routing`, `pass-the-parcel`, `template-plan.md` |
| 7 | `sync-architecture.ps1 -Check` reports no stale DRIFT entry for the deleted agent file. | `-Check` verdict table (no `parcel-fast.agent.md` row) |
| 8 | Grounded claims stay clean after the `AGENTS.md` edit; `wiki_lint` clean. | `python scripts/wiki_claims.py check`, `python scripts/wiki_lint.py` |
| 9 | The retired agent stops travelling: a fresh/again-synced satellite receives 9 locked agents and no `parcel-fast.agent.md`. | `-SelfTest` fixture (portable dir mirror + prune path) |

**Spec Notes:**
- Data flow / state changes: **no runtime data flow** — this is machinery-surface removal. The one behavioural change is *reachability*, not logic: the fast lane loses its direct entry point and keeps its batch entry point (`parcel-sprint` → `ptp-parcel-fast`). Nothing consumes `parcel-fast` at runtime except the opencode agent-selection surface.
- Edge cases the spec must cover: (a) **partial removal** — registry row removed without the file (or vice versa) is a hard FAIL in `check-parcel-prefix.ps1`'s bidirectional pairing pass; (b) **already-synced satellites** retain the orphan unless `prune_files` names it (CW3); (c) **substring collision** — `parcel-fast` ⊂ `ptp-parcel-fast`, so edits must be phrase-exact (CW2); (d) **seed/live disagreement** — the seed registry and seed opencode config must drop the row/entry in the same change, or the seed-agreement pass FAILs; (e) **claims drift** — `AGENTS.md` is a claim source for docs 09/12, so the edit flips them `STALE` until restamped.
- Docs consumed by this feature (read-first list): `.opencode/plans/base-context.md` (canonical prefix), `.devops/rules/agents-and-skills.md` (naming + bounded spawning + sync protocol), `.devops/sync-manifest.yaml` (prune + version discipline), `.wiki/rules/naming.md` (the doc under edit), `.wiki/core/18-knowledge-capture.md` § Agents & Models / § Sync & Versioning.

---

> **NO HALT after Phase 4:** the spec is reviewed together with the implementation plan at Gate B (after Phase 5) — one decision, what it will do and what it will cost.

---

## 5 Phase 5: High-Visionary Standard Implementation Plan
**Skill Executed:** `ptp-high-visionary`

**Simplicity Gate:**
> [x] Climbed the Simplicity Ladder (7 rungs) — no new abstraction added; the change **removes** an entity
> [x] No speculative abstractions — no replacement selectable fast orchestrator, no new shim
> [x] Reuse over reimpl — Reuse Log populated (existing prune mechanism, existing `-Sync` mechanism, existing seed/live pairing)
> [x] Deliberate simplifications marked with `ponytail:` comments — n/a (no code written)
> [x] Safety exceptions preserved — Gate D still halts; no validation or error handling is simplified

**Reuse Log:**
| Existing Asset | Where Reused | Ladder Rung |
|---|---|---|
| `prune_files` in `.devops/sync-manifest.yaml` | Retires the orphan file in already-synced satellites (no new mechanism) | 2 |
| `check-parcel-prefix.ps1 -Sync` | Re-inlines the edited prefix into every remaining locked agent | 2 |
| `check-parcel-prefix.ps1` bidirectional registry↔file↔config pass | Proves the three removals landed together; no new validator | 2 |
| `.devops/templates/base-context.template.md` | The seed mirror already exists — edited in place, no new seed | 1 |

**Spaghetti Triage (See-Name-Route, Do Not Fix):**
- [x] No smells noticed in the touched surface. One adjacent observation is routed, not fixed (see table).

| File | Function | Smell Type | Severity | Recommended Action |
|---|---|---|---|---|
| `scripts/check-parcel-prefix.ps1` | single 369-line script owning 4 concerns | god script | Low | Route only — already tracked as parked `T1-E2.02` ("Split the `check-parcel-prefix.ps1` god-script"). Do NOT fix here. |

### To-Do List
- [x] D1 Delete `.devops/agents/parcel-fast.agent.md`
- [x] D2 Edit canonical + seed prefix (header list, ORCHESTRATOR marker comment, registry row, presets row, trailing prose)
- [x] D3 Remove `agent.parcel-fast` from `opencode.json` + seed `opencode.template.json`
- [x] D4 `check-parcel-prefix.ps1 -Sync` (prefix re-inline into the 9 remaining locked agents)
- [x] D5 Script comment de-referencing in `check-parcel-prefix.ps1`
- [x] D6 Doc mirrors (9 files)
- [x] D7 `prune_files` entry + `machinery-version: 43 -> 44` in `.devops/sync-manifest.yaml`
- [x] D8 Logs + register (`CHANGELOG.md`, `version-history.md`, `agent-changelog.md`, `MATURITY.md`)
- [x] D9 Refresh the stale preset-provenance example in the two sprint-queue plans
- [x] V Verification: prefix, UTF-8, wiki lint, claims, sync `-Check`/`-SelfTest`

### File-Level Steps

**Ordering constraint (hard):** `D1` + `D2` + `D3` must land **before** `D4` (`-Sync` validates registry↔file↔config pairing *and* seed agreement in one pass). `D4` must land **before** V's verification run.

1. `D1` — `git rm .devops/agents/parcel-fast.agent.md`.
2. `D2` — `.opencode/plans/base-context.md`, then `.devops/templates/base-context.template.md` (identical edits).
3. `D3` — `opencode.json`, then `.devops/templates/opencode.template.json`.
4. `D4` — `powershell -NoProfile -File scripts/check-parcel-prefix.ps1 -Sync`.
5. `D5` — `scripts/check-parcel-prefix.ps1` comments/docstring only (no logic change).
6. `D6` — `AGENTS.md`, `.devops/templates/AGENTS.template.md`, `.devops/README.md`, `.devops/rules/agents-and-skills.md`, `HOW-TO.md`, `.wiki/rules/naming.md`, `.devops/skills/model-routing/SKILL.md`, `.devops/skills/pass-the-parcel/SKILL.md`, `.devops/plans/template-plan.md`.
7. `D7` — `.devops/sync-manifest.yaml` (`prune_files` + `machinery-version`).
8. `D8` — `CHANGELOG.md`, `.devops/logs/version-history.md`, `.devops/logs/agent-changelog.md`, `.devops/backlog/MATURITY.md`.
9. `D9` — `.devops/sprints/sprint-8-sprint-lifecycle-hardening/t1-e3.03-*.md`, `t1-e3.04-*.md`.
10. `V` — run the five gates; restart the fails ≤ 2 attempts each.
11. Commit `plan: T1-E3.05` on the trunk.

### Implementation Instructions (no code snippets except exact string literals: regex, SQL migration, CLI command, config key, error message)

**`D1` — Delete the selectable agent.** `git rm .devops/agents/parcel-fast.agent.md`. Nothing replaces it. Do **not** touch `.devops/agents/ptp-parcel-fast.subagent.md`.

**`D2` — Canonical prefix (`.opencode/plans/base-context.md`) + seed (`.devops/templates/base-context.template.md`).** Five phrase-exact edits per file:
- Line 1 PREFIX-LOCKED prose — drop `.devops/agents/parcel-fast.agent.md`, from the shared-prefix enumeration (`parcel.agent.md`, `parcel-sprint.agent.md` remain), and drop `parcel-fast.agent.md`, from the ORCHESTRATOR-ONLY enumeration.
- The `<!-- ORCHESTRATOR-ONLY:START ... -->` marker comment — the parenthetical becomes `parcel.agent.md + parcel-sprint.agent.md`.
- Model Registry table — delete the row whose first cell is exactly the literal `parcel-fast` (keep `parcel`, `parcel-sprint`, and every `ptp-*` / `wiki-*` row).
- Orchestrator Presets table — delete the row whose first cell is exactly the literal `` `parcel-fast` `` (keep `parcel` and `parcel-sprint`).
- Trailing presets prose — replace the `parcel-fast`-specific `task: deny` sentence with the surviving structural statement: the batch host's `task` block is narrowed to exactly `ptp-parcel-fast`. Keep the `AUTO` auto-clears A-C / **Gate D always halts** sentence verbatim.
- Seed only: update the file's HTML comment header (locked-agent list + seed `version: 12 -> 13`).

**`D3` — Runtime bindings.** In `opencode.json` delete the whole `"parcel-fast": { ... }` member of the `agent` object (22 lines) — leave every other entry byte-identical and leave `agent.parcel.permission.task`'s `"ptp-parcel-fast": "deny"` alone (that deny names the *subagent*). Same deletion in `.devops/templates/opencode.template.json`; bump its `_comment` `version: 7 -> 8`. Both files must remain valid JSON.

**`D4` — Prefix re-inline.** `powershell -NoProfile -File scripts/check-parcel-prefix.ps1 -Sync`. Expected: `FIXED`/`PASS` across **9** locked agents (was 10), `MODEL` × 12, `EMBED ptp-parcel-fast.subagent.md: refreshed`, `SEED-OC-MODEL` × 12 and `OC-MODEL` × 12 (was 13 each), exit `0`. Any `FAILURES:` block is a hard stop.

**`D5` — Script comments.** `scripts/check-parcel-prefix.ps1`: the `.SYNOPSIS` line enumerating selectable agents and the line-80 comment describing which agents receive the *full* prefix both name `parcel-fast.agent.md` — drop it from both. **No logic change:** the `parcel*.agent.md` filter still matches `parcel.agent.md` + `parcel-sprint.agent.md`. `scripts/sync-architecture.ps1` needs **no** edit — it never names `parcel-fast` (it works off the `parcel*.agent.md` / `ptp-*.subagent.md` globs; verified by repo-wide grep).

**`D6` — Doc mirrors.** Every edit is phrase-exact (never a bare-substring replace):
- `AGENTS.md` + `.devops/templates/AGENTS.template.md` — the `.devops/agents/` roster row: drop `parcel-fast.agent.md` (orchestrators), drop the `parcel-fast is a locked AUTO+SINGLE preset` clause, and drop the "one prefix apart" disambiguation sentence (it described the now-retired collision). The seed bumps `version: 12 -> 13`.
- `.devops/README.md` — the `.devops/agents/` row: same treatment.
- `.devops/rules/agents-and-skills.md` — § Agent Home roster row and the § Naming `<slug>.agent.md` example list: drop `parcel-fast.agent.md`; the `ptp-parcel-fast` description loses the "one prefix apart from the selectable `parcel-fast` orchestrator" clause. § The "Batch Host" Pattern's `ptp-parcel-fast: deny` sentence is about the *subagent* and **stays**. Bump `last-reviewed`.
- `HOW-TO.md` — § `#### Parcel-Fast (locked preset)` becomes the fast-lane description: Parcel Fast is subagent-only, reachable via `parcel-sprint` → `ptp-parcel-fast`; interactive fast runs are `parcel` + `AUTO`/`SINGLE` at plan start.
- `.wiki/rules/naming.md` — `<slug>.agent.md` example row: `parcel.agent.md`, `parcel-sprint.agent.md` only. Bump `last-reviewed`.
- `.devops/skills/model-routing/SKILL.md` — the Orchestration capability-class row's "Typical fit" cell: `parcel` / `parcel-sprint`.
- `.devops/skills/pass-the-parcel/SKILL.md` — § Orchestrator Presets `locked` bullet's canonical example becomes `parcel-sprint`. Bump `version: 13 -> 14`.
- `.devops/plans/template-plan.md` — the Plan Settings provenance note's locked-preset example becomes `parcel-sprint`.

**`D7` — Transport.** `.devops/sync-manifest.yaml`: append `.devops/agents/parcel-fast.agent.md` to `prune_files` with the D9/Q5 comment; bump `machinery-version: 43 -> 44`.

**`D8` — Logs + register.** `.devops/logs/version-history.md`: prepend a `v0.7.15` Patch row containing the literal `machinery-version: 44`. `CHANGELOG.md`: prepend the matching "Earlier releases" bullet. `.devops/logs/agent-changelog.md`: prepend a dated `## 2026-09-15 - ... (machinery 44)` entry. `.devops/backlog/MATURITY.md`: refresh the axis-1 evidence cell (drop the retired `parcel-fast` preset, name the surviving presets + batch host). The `T1-E3.05` register row **already exists** in `.devops/backlog/t1-parcel-pipeline-machinery-backlog.md` (written at sprint-plan) — verified, no duplicate added.

**`D9` — Stale boilerplate (optional, applied).** In the two sprint-queue plans, the Plan Settings provenance note's parenthetical example `e.g. \`parcel-fast\` locks \`AUTO\` + \`SINGLE\`` becomes `e.g. \`parcel-sprint\` locks \`AUTO\` + a per-plan \`SINGLE\``.

**Post-edit claim restamp.** `AGENTS.md` is a claim source for `.wiki/core/09-design-system.md` and `.wiki/core/12-security-standards.md`; after `D6`, run `python scripts/wiki_claims.py update` before the check.

### Wiki Core References
- `.wiki/core/17-docs-blueprint.md` -> the folder-taxonomy/naming authority behind the `.wiki/rules/naming.md` edit
- `.wiki/core/18-knowledge-capture.md` -> § Agents & Models (registry-canonical bindings; registry↔file pairing) and § Sync & Versioning (prune + version discipline) — the two rules this change must keep true

### Wiki Docs to Add/Edit
- Edit only: `.wiki/rules/naming.md` (example row + `last-reviewed`). No new doc; nothing to promote to `stable` (no `status` change).

---

> **HALT POINT (Gate B — Spec & Plan Review):** Phases 4-5 complete. Present the wiki requirements spec (with acceptance criteria) AND the execution plan together as one decision. Do not proceed to reviews until user approves. Update the **State & Gates** section at the bottom of this file: Status -> `PHASE_5`, Active Persona -> `High-Visionary`. Leave Gate B `OPEN` — the orchestrator records the user's verdict.

> **AUTO auto-clear — Gate B: CLEARED (this is the single approval of the `SINGLE` topology — Gate C is `N/A`).** Mechanical verification: (a) the Phase 4 wiki-delta table names exactly one doc plus its rationale, and the conditional-skip checklist was evaluated and explicitly *not* skipped; (b) 9 numbered acceptance criteria each name a concrete test target; (c) Phase 5 Simplicity Gate fully ticked with a populated Reuse Log and a routed (not fixed) spaghetti observation; (d) the To-Do list, File-Level Steps, and Implementation Instructions are internally consistent and carry no code snippets beyond exact string literals (`git rm …`, `powershell -NoProfile -File scripts/check-parcel-prefix.ps1 -Sync`, config keys, the `prune_files` comment); (e) zero `REJECTED` verdict lines and zero `Unresolvable:` entries exist in Phases 4-5. Gate B row in `## 📍 State & Gates` → `APPROVED (AUTO)`.

---

## 6 Phase 6: Grumpy Architect Spec & Logic Audit
**Skill Executed:** `ptp-grumpy-architect` (`SINGLE`: orchestrator inline)

> **`SINGLE` topology:** no independent reviewer. The orchestrator logs an inline self-review checkpoint here and records **Verdict** `N/A — SINGLE self-review`.

**Verdict:** `N/A — SINGLE self-review` — inline audit **PASS**, no blocker found.

> **Rejection Rule:** If plan does not make the app faster, safer, or easier to modify, do not check boxes. Reject and force rewrite via `PHASE_5_REVISION`.
>
> **Spec-level audit — the plan contains no code, so no code-level scans (DRY/WET, line checks).**

- **System Contracts Explicit:** ✅ Every touched path is named with its exact literal (`.opencode/plans/base-context.md`, `.devops/templates/base-context.template.md`, `opencode.json`, `.devops/templates/opencode.template.json`, `.devops/sync-manifest.yaml`, `scripts/check-parcel-prefix.ps1`, the 9 doc mirrors). The mechanical contracts are named as *tests*, not adjectives: the `parcel*.agent.md` / `ptp-*.subagent.md` globs, the `parcel-fast` registry key, the `` `parcel-fast` `` preset key, the `agent.parcel-fast` config key, the `machinery-version: 44` literal, and the phrase-exact edit rule for the CW2 substring hazard.
- **File Boundary & Scope Collisions:** ✅ All 21 declared `touches` are distinct files; `D9` adds exactly 2 files that are in the sprint queue, not in `.devops/plans/`, so no claim collision arises. The `touches` list covers every file D1-D9 names — verified file-by-file against Phase 5's steps. No path in the list is a directory glob that would silently widen the edit set (`.devops/agents/**` is backed by the explicit D1/D4 statements).
- **Dependency Gaps:** ✅ `depends_on: []` and genuinely so: every referenced mechanism already exists (`prune_files`, `-Sync`, the bidirectional pairing pass, the seed/live agreement pass). No new script, no new skill, no new template. Nothing in the plan asks for a utility that is not already shipped.
- **YAGNI Bloat:** ✅ No replacement selectable orchestrator, no shim agent, no compatibility alias, no deprecation notice file — the ruling is deletion, and the plan deletes. The one addition (`prune_files` entry) is load-bearing, not decorative: without it an already-synced satellite keeps an orphan file whose registry row is gone and its own prefix gate fails (CW3).
- **Paranoid Security (Contract Level):** ✅ No secrets, no credentials, no new network surface. The change *removes* a `task: deny`-bound agent; the surviving `parcel-sprint` `task` allow-list stays exactly `ptp-parcel-fast`. Permission blocks are not widened anywhere — the plan's `D3` explicitly pins the `agent.parcel.permission.task` `"ptp-parcel-fast": "deny"` as untouched (it names the subagent, not the retired orchestrator).
- **Survivability:** ✅ No runtime flow is touched — this is a config/agent-roster change with no async, timeout, or network path. The one failure mode that matters is *mechanical*: a partial removal, and the plan makes that failure loud by routing D1+D2+D3 ahead of D4's single validating `-Sync`, plus acceptance criterion 1 whose test target is the bidirectional pairing pass itself.
- **Edge Cases:** ✅ Enumerated in the Phase 4 Spec Notes and each has a mapped test: (a) partial removal → criterion 1; (b) already-synced satellite orphan → criterion 4 + 9; (c) substring collision `parcel-fast` ⊂ `ptp-parcel-fast` → the plan's phrase-exact directive plus criterion 2's byte-equality check on the surviving subagent; (d) seed/live disagreement → `-Sync`'s seed-registry + seed-opencode passes; (e) claims drift → the explicit post-`D6` `wiki_claims.py update` step. **Boundary condition checked:** the registry shrinks 12 → 11 rows and the opencode `agent` block 12 → 11 entries — the plan states both expected counts, so a miscount is visible in the `-Sync` output rather than silent.
- **Performance Trade-offs:** ✅ Neutral-to-positive. Removing an agent file removes one `Get-ChildItem` match and one `-Sync` rebuild from every future prefix check (10 → 9 agents); no runtime path is affected because the file had no consumers other than agent selection.
- **Architectural Anti-Patterns:** ✅ The change *removes* a duplicate source of truth — the near-identical `parcel-fast` / `ptp-parcel-fast` name pair that the Origin note calls a documented confusion hazard. No god module is created; the only god-module observation (`check-parcel-prefix.ps1`, 4 concerns) is **routed** to the already-parked `T1-E2.02`, not fixed here (spaghetti triage rule).
- **Endpoint Protection & Rate Limiting:** ✅ n/a — no endpoint, no request path, no rate-limited surface in the blast radius.
- **Wiki Core Compliance:** ✅ Every blueprint cites a `.wiki/core/` doc: the naming-row edit cites `17-docs-blueprint.md`; the binding/transport rules the change must keep true cite `18-knowledge-capture.md` § Agents & Models and § Sync & Versioning. The Phase 4 conditional-skip checklist is answered explicitly (`NOT skipped`, condition (c)), never silently.

**Self-review residual findings (advisory, non-blocking):**
1. **Doc-mirror completeness is search-verified, not enumerated.** The plan's D6 list was built from a repo-wide grep of `parcel-fast` (excluding `.git` and `.devops/archive/**`). A *new* reference cannot appear mid-parcel, and Phase 9 re-runs the grep as a check — so the residual risk is limited to a reference that existed but was misclassified as subagent-only. Mitigation already in the plan: the inventory table names the 4 subagent-only sites explicitly (`.devops/skills/ptp-parcel-fast/SKILL.md`, `.devops/skills/sprint-run/SKILL.md`, `.devops/rules/plan-lifecycle.md`, `.devops/agents/ptp-parcel-fast.subagent.md`) so they are deliberately excluded rather than forgotten.
2. **Historical rows are intentionally left stale-by-design.** `CHANGELOG.md` rows for v0.7.8/9, the `T1-E1.02` completed register row, `.devops/archive/**`, and prior `agent-changelog` entries still describe the retired agent. This is **in scope as out-of-scope** (Phase 1 Out-of-Scope: "Editing historical records"). Not a finding — recorded so a later reader does not mistake it for missed work.
3. **`-Check` DRIFT false-positive is asserted by inspection, not by an automated test.** The plan claims `sync-architecture.ps1 -Check` will report no stale DRIFT entry for the deleted file. That is structurally sound (`-Check` walks the *target's* manifest-listed items and tests `Test-Path` on prune paths; the deleted file is neither in the target's portable set nor a surviving prune path), and `-SelfTest` exercises the prune path end-to-end — so the inspection is backed by the fixture rather than by assertion alone.

**Dead-Code / Orphan Flags (for Wrap Up backlog entry):**
| plan section / file_path:line_number | Reason |
|---|---|
| `scripts/check-parcel-prefix.ps1:107-319` | Not dead code — a **routed observation** only: one 369-line script owns 4 unrelated concerns (registry parse, prefix integrity, skill embeds, binding integrity). Already tracked as parked `T1-E2.02`. Do not create a duplicate backlog entry. |

**Required Fixes:**
> [x] None — no blocking flaw found in the inline audit.

---

## 7 Phase 7: Smooth Operator Product Review
**Skill Executed:** `ptp-smooth-operator` (`SINGLE`: orchestrator inline)

> **`SINGLE` topology:** no independent reviewer. Record **`N/A — SINGLE self-review`**; the self-review is presented at Gate B.

> **Rejection Rule:** If plan introduces unnecessary complexity or scope expansion, do not check boxes. Reject and force rewrite.

- **Vision & Journey Integrity:** ✅ The change serves the stated vision directly: **one** fast entity, reachable one way. The user journey becomes unambiguous — *interactive* delivery is `parcel` (any investigated context); *unattended batch* delivery is `parcel-sprint` → `ptp-parcel-fast`. Removing the third, near-identical name is a net reduction in journey complexity, not a repo-taxonomy reshuffle for its own sake.
- **Scope Containment (No Gold Plating):** ✅ Every item maps 1:1 to the operator ruling's blast radius. Explicitly **not** done: no replacement selectable orchestrator, no rename of `ptp-parcel-fast`, no change to batch behaviour, gates, or the `parcel` orchestrator, no refactor of the parked `T1-E2.02` god-script, no editing of historical records. `D9` is the plan's own optional item, sanctioned by `sprint.md:33`, and costs two one-line substitutions — applied rather than deferred, because both plans are still live in the queue and would otherwise cite a retired agent.
- **4 Core User States:** ✅ **Success** — 9 locked agents, prefix PASS, registry/file/config all agree. **Empty** — a *removed* entity is the whole point; there is no "empty" fast lane to design, because the surviving path (`parcel-sprint`) already exists and is already tested. **Error** — a partial removal surfaces as a hard `check-parcel-prefix.ps1` FAIL (bidirectional pairing) and a red `-Sync`, not a silent half-state. **Loading** — n/a (no async surface); the label that flickers is "Preparing write…"-class editor latency, not user-facing.
- **Guardrails & Permissions:** ✅ Permissions only get *tighter* in aggregate: one `task: deny` agent disappears, and the surviving `parcel-sprint` allow-list is unchanged at exactly `ptp-parcel-fast`. The plan explicitly forbids touching the `agent.parcel.permission.task` `"ptp-parcel-fast": "deny"` — the most likely mis-edit, since it is the one `parcel-fast`-shaped key that must *survive*.
- **Mobile / A11y / Telemetry:** ✅ n/a — no UI, no client surface, no telemetry. (This template has no `src/`; the touch surface is Markdown policy, JSON config and PowerShell validators.)

**Required Fixes:**
> [x] None — no unnecessary complexity or scope expansion identified.

---

> **HALT POINT (Gate C — Peer Reviews):** Reviews complete. Present findings and required fixes. Update the **State & Gates** section at the bottom of this file:
> - **PASS:** Phase 6 log clean -> set Status `PHASE_7`, Active Persona `Reviewer`. Do not proceed to execution until user approves Gate C.
> - **FAIL:** Phase 6 or 7 flagged blocking flaws -> set Status `PHASE_5_REVISION`, Active Persona `High-Visionary`. Return to Group B for plan adjustments, then re-run Phases 6-7. **Never advance an unapproved plan to execution.**
> The orchestrator records Gate C -> `APPROVED`/`REJECTED` only after the user's verdict.
>
> **`SINGLE` topology:** Gate C is `N/A` — the plan approval happens once at Gate B (spec + plan + inline self-review). Update the State & Gates `Gate C` row to `N/A (SINGLE)`.

---

## 8 Phase 8: Execute Changes
**Skill Executed:** `ptp-code-surgeon`

**Execution Isolation:** Gate cleared before execution — `SINGLE` topology, so the approval is **Gate B** (`AUTO`-cleared above, Gate C `N/A`). No execution occurred before that clear.

**Single-Pass Direct-to-Disk:**
> [x] Implementation written DIRECTLY to source files - no intermediate Markdown code blocks, no drafting files
> [x] Touched only intended lines - no adjacent refactors
> [x] Cleaned up only owned orphans (the one owned orphan is the deleted agent + the seed/registry/config rows derived from it)
> [x] Pre-existing dead code left untouched (the parked `check-parcel-prefix.ps1` god-script observation was **routed**, not fixed)

### Execution Log

| # | Item | Action | Result |
|---|---|---|---|
| 1 | `D1` | `git rm .devops/agents/parcel-fast.agent.md` | `rm '.devops/agents/parcel-fast.agent.md'` — deleted; nothing replaces it |
| 2 | `D2` | Edited `.opencode/plans/base-context.md` — 5 phrase-exact edits | header list (2 enumerations), ORCHESTRATOR-ONLY marker comment, Model Registry row, Orchestrator Presets row, presets prose |
| 3 | `D2` | Edited `.devops/templates/base-context.template.md` — the same 5 edits + seed header `version: 12 -> 13` | seed and live agree cell-for-cell |
| 4 | `D3` | Removed the `agent.parcel-fast` member from `opencode.json` | 19 lines removed; every other entry byte-identical, incl. `agent.parcel.permission.task`'s `"ptp-parcel-fast": "deny"` |
| 5 | `D3` | Removed the same entry from `.devops/templates/opencode.template.json`; `_comment` `version: 7 -> 8` | 21 lines changed; both files re-validated as JSON |
| 6 | `D5` | Edited `scripts/check-parcel-prefix.ps1` comments (`.SYNOPSIS` agent list + the line-80 full-prefix note) | comments only — no logic change; `scripts/sync-architecture.ps1` needed **no** edit (verified: it names no `parcel-fast`; it works off the `parcel*.agent.md` / `ptp-*.subagent.md` globs) |
| 7 | `D4` | `powershell -NoProfile -File scripts/check-parcel-prefix.ps1 -Sync` | `FIXED` ×9 (2 orchestrators + 7 `ptp-*`; was 10), `MODEL` ×11, `SEED-OC-MODEL` ×11, `OC-MODEL` ×11, exit `0` |
| 8 | `D6` | Edited 9 doc mirrors; every edit phrase-exact | `AGENTS.md`, `.devops/templates/AGENTS.template.md` (v12→v13), `.devops/README.md`, `.devops/rules/agents-and-skills.md` (+`last-reviewed`), `HOW-TO.md`, `.wiki/rules/naming.md` (+`last-reviewed`), `.devops/skills/model-routing/SKILL.md`, `.devops/skills/pass-the-parcel/SKILL.md` (v13→v14), `.devops/plans/template-plan.md` |
| 9 | `D7` | `.devops/sync-manifest.yaml` — appended the prune entry + comment; `machinery-version: 43 -> 44` | `prune_files` carries `.devops/agents/parcel-fast.agent.md` |
| 10 | `D8` | Prepend `v0.7.15` row to `version-history.md` (literal `machinery-version: 44`), matching `CHANGELOG.md` bullet, dated `agent-changelog.md` entry; refreshed the `MATURITY.md` axis-1 evidence cell | `T1-E3.05` register row **already present** in `t1-parcel-pipeline-machinery-backlog.md` (written at sprint-plan) — no duplicate added |
| 11 | `D9` | Refreshed the preset-provenance example in the two sprint-queue plans (T1-E3.03, T1-E3.04) | one-line substitution each; both plans are still queued under the operator's `05 -> 03 -> 04` order |
| 12 | Claims | `python scripts/wiki_claims.py update` | restamped the `AGENTS.md`-anchored hashes: `09-design-system.md#design-system-is-authority`, `12-security-standards.md#agents-read-operating-rules` |

**Substring-hazard discipline (CW2) — how it was honoured:** no bare-substring replace was used anywhere. The four `ptp-parcel-fast`-only sites (`.devops/skills/ptp-parcel-fast/SKILL.md`, `.devops/skills/sprint-run/SKILL.md`, `.devops/rules/plan-lifecycle.md`, `.devops/agents/ptp-parcel-fast.subagent.md`) were left untouched by design, and `check-parcel-prefix.ps1` asserts the surviving subagent's byte-equality (`PASS ptp-parcel-fast.subagent.md` + embed match) as the mechanical proof.

> [x] Step 1: D1-D3 + D5 removals/edits landed, then D4 `-Sync` re-inlined the prefix into the 9 survivors (order forced: `-Sync` validates registry↔file↔config pairing).
> [x] Step 2: D6-D9 mirrors + transport + logs landed, then the claim restamp, then the Phase 9 gate run.

---

## 9 Phase 9: Verify Changes
**Skill Executed:** `ptp-code-surgeon`

**Build & Lint:**
> [x] Compilation check clean — n/a (no compiled surface); the equivalent structural check is JSON validity of the two config files, which passes (`python -c "import json; json.load(...)"` on `opencode.json` + the seed → `JSON OK`)
> [x] Lint clean (exit 0) — `python scripts/wiki_lint.py` → `OK: wiki healthy - 0 broken links, all anchors present, frontmatter valid.`
> [x] Type-generation re-run if schema/API changes — n/a (no schema/API in the blast radius)

**Phase 9 Verification Report (actual commands + exit codes):**

| # | Command | Exit | Observed |
|---|---|---|---|
| 1 | `powershell -NoProfile -File scripts/check-parcel-prefix.ps1` | `0` | `PASS` ×**9** locked agents (2 orchestrators + 7 `ptp-*`; was 10); `MODEL` ×11; `SEED-OC-MODEL` ×11; `OC-MODEL` ×11 (was 12/13); `OK: all PREFIX-LOCKED agents share a byte-identical prefix, every binding surface agrees, and the seed surfaces match the live registry.` Bindings shrank 12 → 11 with no `FAILURES:` block. |
| 2 | `powershell -NoProfile -File scripts/check-utf8-agents.ps1` | `0` | `ALL CLEAN (216 files scanned)` — the new log rows and doc edits introduced no mojibake. |
| 3 | `python scripts/wiki_lint.py` | `0` | `OK: wiki healthy - 0 broken links, all anchors present, frontmatter valid.` |
| 4 | `python scripts/wiki_claims.py check` | `0` | `claims OK: 0 stale, all grounded sources present.` (after the `update` restamp of the two `AGENTS.md`-anchored claims) |
| 5 | `powershell -NoProfile -File scripts/sync-architecture.ps1 -SelfTest` | `0` | `SELFTEST OK: 5 dirs, 38 skills, 12 files materialised correctly.` — includes the prune fixture: plant a retired file → `-Check` reports `PRUNE` (not `DRIFT`) and exits 1 → re-sync deletes it → `-Check` clean. |
| 6 | `sync-architecture.ps1 -Target <throwaway> -NoVerify` then `-Target <throwaway> -Check` | `0` / `0` | Fresh satellite: `Summary: CURRENT=56`, `IN SYNC`. Only `parcel-fast` string in the report is `skill ptp-parcel-fast CURRENT` — **no stale DRIFT entry for the deleted agent file**. Target receives **9** locked agents (`parcel*.agent.md` = 2, `ptp-*.subagent.md` = 7). |
| 7 | Plant `.devops/agents/parcel-fast.agent.md` in the throwaway target → `-Check` | `1` | `prune .devops/agents/parcel-fast.agent.md PRUNE redundant file present in target; sync will delete it` — reported as **PRUNE, not DRIFT** (parent-dir masking avoided), exit 1 as designed. |
| 8 | Re-sync the throwaway target → `-Check` | `0` | `PRUNED .devops/agents/parcel-fast.agent.md`; `ORPHAN_REMOVED=True`; `Summary: CURRENT=56`; `IN SYNC`. Temp target cleaned up. |
| 9 | `validate.yml` "Machinery version discipline" predicate (reproduced locally, verbatim logic) | `0` | `OK: machinery-version 44 is recorded in version-history.md` |
| 10 | JSON validity of `opencode.json` + `.devops/templates/opencode.template.json` | `0` | `JSON OK` (the seed `_comment` still documents the agent block as REQUIRED) |

> **Why `-Check` needed a target:** `sync-architecture.ps1 -Check` compares a *source* against a *target* repo (`-Target` is required — there is no self-check mode). Row 5 therefore uses the CI's own `-SelfTest` (which materialises a throwaway satellite, runs `-Check` inside it, and asserts the prune path end-to-end), and rows 6-8 run `-Check` explicitly against a throwaway target to satisfy the named DRIFT-false-positive hazard. Every run exited as recorded.

**Test Report:**
- [x] Lint pass (exit 0) — `wiki_lint.py` clean; `check-parcel-prefix.ps1` + `check-utf8-agents.ps1` clean
- [x] Tests pass (exit 0) — this repo has no unit-test suite; the deterministic gates above **are** its test harness and all exited `0`
- [x] Build pass (exit 0) — n/a (no build step); JSON parse + sync `-SelfTest` stand in and both exit `0`
- [x] Code matches exact plan specifications — verified by re-running the Phase 5 Test Verification Plan commands; every command exits `0`
- [x] No functional gaps identified

**Acceptance criteria disposition (Phase 4 § Acceptance Criteria):**
| # | Criterion | Status | Evidence |
|---|---|---|---|
| 1 | `parcel-fast` has no surviving surface | ✅ | file deleted; registry row, presets row, and both config entries removed together; gate 1 exits `0` with 11 bindings (was 12) |
| 2 | `ptp-parcel-fast` survives byte-identical | ✅ | gate 1 `PASS ptp-parcel-fast.subagent.md` + embed byte-equality; its 4 `ptp-parcel-fast`-only referencing sites untouched |
| 3 | PREFIX-LOCKED set is 9, prefix byte-identical | ✅ | gate 1 (`PASS` ×9); repo count `parcel*.agent.md` = 2 + `ptp-*.subagent.md` = 7 |
| 4 | `prune_files` carries the retired path | ✅ | gate 7 (`PRUNE` verdict, not `DRIFT`); gate 8 (`ORPHAN_REMOVED=True`) |
| 5 | `machinery-version: 44` + literal in version-history | ✅ | gate 9 |
| 6 | No stale doc/roster reference survives | ✅ | repo-wide phrase-exact re-grep: remaining `parcel-fast` hits are (a) historical records (out of scope by Phase 1), (b) the plan/queue/run-workspace narrative, (c) the intended `prune_files` path |
| 7 | `-Check` reports no stale DRIFT for the deleted file | ✅ | gate 6 |
| 8 | Claims + wiki lint clean after the `AGENTS.md` edit | ✅ | gates 3 + 4 |
| 9 | Retired agent stops travelling | ✅ | gates 5-8 (throwaway satellite receives 9 locked agents and no orphan) |

> **Two self-healing attempts were not needed** — every gate was green on first run after the ordered landing (removals → `-Sync` → mirrors → restamp). No `PHASE_8_FAILED`.

---

> **HALT POINT (Gate D — Implementation):** Implementation and verification complete. Present completed work to user. Do not proceed to user review until user signs off. Update the **State & Gates** section at the bottom of this file: Status -> `PHASE_9`, Active Persona -> `Executor`. Leave Gate D `OPEN` — the orchestrator records the user's verdict. On rollback: Status -> `PHASE_8_FAILED`.

---

## 10 Phase 10: User Review & Tweaks
**Skill Executed:** `knowledge-capture`

**Tweak Discipline:**
> Tweaks are surgical, not architectural. `expansion` and `refactor` are HALT conditions.

- [ ] Tweak classified: `fix` / `expansion` / `refactor`
- [ ] `expansion` and `refactor` routed to new parcel
- [ ] `fix` touches <= 3 files
- [ ] `fix` introduces no new abstraction absent from original plan

**Capture Categories:** Recurring / Tribal / One-off / Cosmetic-skip

| Round | Feedback | Action Taken | Capture Flag | Result |
|---|---|---|---|---|
| 1 | | | | PENDING / COMPLETE |

**Sign-Off:** [Date] - User approved.

---

## Completion Note (Wrap Up)
**Skill Executed:** `agent-wrap-up`

> [x] Read `.wiki/core/18-knowledge-capture.md` for existing related decisions
> [x] Themed tweaks synced to knowledge capture doc
> [x] Wiki docs updated per Phase 10 tweaks
> [x] Code reconciled against Phase 4 spec — deviations logged
> [x] `status: in-progress` wiki docs promoted to `stable` (or deviation logged)

**Outcome:** Delivered as specified. The selectable orchestrator is retired and every derived surface went with it — agent file, Model Registry row, Orchestrator Presets row, and the `opencode.json` + seed `agent.parcel-fast` entries — in one commit, which is what `check-parcel-prefix.ps1`'s bidirectional registry↔file pairing requires. The prefix was re-inlined byte-for-byte into the 9 remaining locked agents (2 orchestrators + 7 `ptp-*`); `ptp-parcel-fast.subagent.md` survives with a single changed line (shared-prefix header prose). `machinery-version: 43 -> 44`. Executed as wave 1 of Sprint 8.

**Themed Tweaks:**

**Knowledge Capture Entries:**
- No app-domain KC entry — this is machinery/process knowledge, which routes to `.devops/rules/process-lessons.md` (per `@knowledge-capture` §3 Destination Routing). One entry recorded at wrap-up: `@sprint-plan` must run the `touches`-overlap test across the committed set before committing it.

**Spec Reconciliation:** None — implementation matches the Phase 4 spec. Two recorded deviations, both additive rather than contradicting:
1. `D9` (explicitly optional) was **applied**: the `parcel-fast` preset-provenance boilerplate was refreshed in the two sprint-queue plans (T1-E3.03, T1-E3.04), 1 line each. Under the operator-reordered queue these were still unclaimed in the sprint folder, so D9 was actionable rather than moot.
2. Two `AGENTS.md`-anchored claim hashes (`.wiki/core/09-design-system.md`, `.wiki/core/12-security-standards.md`) were restamped, because `D6` edits the `AGENTS.md` agent roster that those claims source. Anticipated by the plan's own "Post-edit claim restamp" note.

**In-Progress Promotions:** None — Phase 4 wrote no `in-progress` docs (the only wiki edit was an existing `stable` rules doc, `.wiki/rules/naming.md`).

**Dead-Code Backlog Entries (from Phase 6):** None from the plan's own Phase 6 self-review. Two *session* discoveries were captured instead — see **Backlog Review** below.

**Wiki Updates:** `.wiki/rules/naming.md` (example row de-referenced + `last-reviewed`), `.wiki/core/09-design-system.md` and `.wiki/core/12-security-standards.md` (claim hashes restamped only).

**Plan Archiving:** Plan archived to `.devops/archive/t1-e3.05-retire-parcel-fast-orchestrator-plan.md` (via `git mv`, no stub). Trunk-sequential batch path: no branch, so no merge/prune.

**Backlog Review:** No existing backlog item is resolved by this wave. Two new items were filed as `T1-E3.06` ("Sprint 8 gate & preflight coverage gaps"), bundling two findings discovered while *executing* this wave — following the repo's established audit-tidy-up pattern (`T1-E2.03` F1-F7, `T1-E2.04` F8/F9):
- **G1** — `.devops/sprints/**` is absent from `check-utf8-agents.ps1`'s scan list, so a plan loses UTF-8 hygiene coverage the moment `@sprint-plan` moves it out of `.devops/backlog/`. Observed directly: the scan count dropped 218 → 216 across the sprint-open move.
- **G2** — `@sprint-plan` does not test *mutual* `touches` overlap across the set it is about to commit, even though the batch runner's eligibility predicate does. Committing three mutually-overlapping plans yields a queue that can only ever execute one wave at a time, with the other two skipped — which is exactly what happened to Sprint 8 before the operator reordered it.
- A third observation was resolved in place rather than filed: T1-E3.05 sat in the T1 theme register but never in the `backlog-index.md` Triage Panel (it carried the ⚪ PARKED tier into the sprint). The Triage Panel is now back to "queue clear", so no stale row survives.

---

## 📍 State & Gates (CACHE-ANCHORED — update ONLY this section at gate transitions)

> **Cache rule:** This is the **last section** in the file. Gate transitions mutate ONLY the rows below — phase content above AND the frozen **Plan Settings** block at the top stay byte-stable to preserve LLM prefix-cache hits. Every "Update Status" instruction in the halt points above means "edit this section".

| Metric | Value |
|---|---|
| **Status** | `COMPLETE` |
| **Version** | `v0.1.0` |
| **Active Persona** | `Executor` |
| **Depends On** | none (canonical: claim front-matter `depends_on`) |
| **Blocks** | none |

> **Settings pointer:** `Mode` and `Agents` live in the frozen **Plan Settings** block at the TOP of this file — read them there before any phase. Never duplicate them here.

> Valid states: `QUEUED`, `CLAIMED`, `PHASE_1`, `PHASE_3`, `PHASE_5`, `PHASE_5_REVISION`, `PHASE_7`, `PHASE_8_FAILED`, `PHASE_9`, `COMPLETE`.

| Gate | Requirement | Status |
|---|---|---|
| A | Scope approved (Phases 1-3, + 3.5 in AUTO) | `APPROVED (AUTO)` |
| B | Spec & plan approved (Phases 4-5) | `APPROVED (AUTO)` |
| C | Peer reviews passed (Phases 6-7) | `N/A (SINGLE)` |
| D | Implementation verified (Phases 8-9) | `APPROVED` |

> **Gate D verdict recorded by the `parcel-sprint` batch host (2026-09-16).** Operator ruling: **APPROVED**. Verdict follows the batched Gate D contract in `.devops/rules/plan-lifecycle.md` § Deviations — the human verdict is recorded by the orchestrator, never by an executing agent. Gate A `APPROVED (AUTO)` and Gate B `APPROVED (AUTO)` were verified mechanically at Phase 3.5/Phase 5; Gate C is `N/A (SINGLE)`.

> Gate flips: rows flip `OPEN` → `APPROVED`/`REJECTED` ONLY after the user's (or AUTO verification's) verdict, recorded by the orchestrator. Executing agents halt with their gate `OPEN`. Rejection routes: A -> `PHASE_1`; B/C -> `PHASE_5_REVISION`; D/rollback -> `PHASE_8_FAILED`. Never edit rows above this section for gate bookkeeping.

> **This section is the LAST section in the file. All gate bookkeeping happens here.**
