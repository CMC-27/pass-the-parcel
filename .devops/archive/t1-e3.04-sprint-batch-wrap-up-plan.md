---
code: T1-E3.04
sprint: sprint-8-sprint-lifecycle-hardening
claim_status: COMPLETE
owner: "parcel-sprint (DeepSeek V4.1 Flash)"
claimed_at: "2026-09-16"
last_touch: "2026-09-16"
touches: [".devops/skills/agent-wrap-up/SKILL.md", ".devops/skills/sprint-run/SKILL.md", ".devops/skills/pass-the-parcel/SKILL.md", ".devops/agents/parcel-sprint.agent.md", "opencode.json", ".devops/templates/opencode.template.json", ".devops/rules/plan-lifecycle.md", ".devops/skills/sprint-close/SKILL.md", ".devops/sync-manifest.yaml", ".devops/logs/version-history.md", "HOW-TO.md", ".opencode/plans/base-context.md", ".devops/templates/base-context.template.md", ".devops/rules/agents-and-skills.md", ".devops/agents/parcel.agent.md", ".devops/agents/ptp-context-hunter.subagent.md", ".devops/agents/ptp-phase3-answerer.subagent.md", ".devops/agents/ptp-high-visionary.subagent.md", ".devops/agents/ptp-grumpy-architect.subagent.md", ".devops/agents/ptp-smooth-operator.subagent.md", ".devops/agents/ptp-code-surgeon.subagent.md", ".devops/agents/ptp-parcel-fast.subagent.md"]
depends_on: ["T1-E3.03"]
---
# T1-E3.04 - Batch sprint wrap-up for `parcel-sprint`
## Theme-Epic: T1 - Parcel Pipeline Machinery, E3 - Concurrency & Sprint Lifecycle

> **Origin:** Follow-up to T1-E3.03. With executed plans held at `GATE_D_USER_APPROVAL`, the batch host still needs a way to *finish* them: confirm the implementation, mark the plans complete, and archive the batch in one pass — instead of running the heavy per-plan `@agent-wrap-up` N times inline. Manual per-plan wrap-up stays valid and composes (T1-E3.03's disjunctive predicate).

## ⚙️ Plan Settings (FROZEN — set at plan start, read before any phase)

| Setting | Value | Meaning |
|---|---|---|
| **Mode** | `AUTO` | `USER-MANAGED` (every gate halts for the user) or `AUTO` (orchestrator auto-clears Gates A-C; Gate D always halts) |
| **Agents** | `SINGLE` | `MULTI` (comprehensive — full `ptp-*` delegation, 4 gates) or `SINGLE` (fast — inline personas, Group C skipped, Gates B+C merged at Gate B, Gate C `N/A`) |

> **Frozen config — read before executing ANY phase.** These two settings govern the entire pipeline and are never edited after plan start; they sit at the TOP so no session can miss them. Mutable runtime state (Status / Active Persona / gates) lives ONLY in the cache-anchored `## 📍 State & Gates` section at the bottom. See `@pass-the-parcel` § Agent Topology. **Preset provenance:** written by `ptp-parcel-fast` (the `parcel-sprint` per-plan fast runner) as the **locked `parcel-sprint` preset** — `Mode=AUTO`, `Agents=SINGLE`. The selection question was skipped because the batch host fixes both axes; see `.devops/skills/ptp-parcel-fast/SKILL.md` § Plan Settings writer (frozen preset).

> **Skill Architecture:** This template is consumed by the `pass-the-parcel` skill. Each phase delegates to a specialized sub-skill. See the parcel skill's Skill Delegation Map.
>
> **RULES:**
> - **Claim front-matter** at the very top (`code` / `sprint` / `claim_status` / `owner` / `claimed_at` / `last_touch` / `touches` / `depends_on`) governs claiming. See `.devops/rules/plan-lifecycle.md` § Claim Protocol. `claim_status` is NOT the bottom `Status` (pipeline phase).
> - Full skeleton required - ALL 10 phases + Wrap Up MUST be present.
> - Halt points are HARD STOPS - each gate blocks all subsequent phases.
> - Persona matches Status - use the State Lifecycle table in pass-the-parcel skill.
> - Read **Plan Settings** above before acting; the topology there governs which gates apply. See `@pass-the-parcel` § Agent Topology.
>
> **🔒 CACHE-ANCHORED:** The **Plan Settings** block above is frozen config; the mutable State Dashboard + Gate Log live in the **last section** (`## 📍 State & Gates`). Gate transitions update ONLY those bottom rows — do NOT edit content above once written. Byte-stable prefix = LLM prefix-cache hits for every downstream agent re-read.

---

## 1 Phase 1: Expansion & Scoping
**Skill Executed:** `ptp-context-hunter`

**Intent:**
Give the batch host a **batch wrap-up**: after the consolidated Gate D verdict, confirm each batched plan's implementation, mark the plans complete, and archive the whole batch in **one coalesced pass** — instead of running the heavy per-plan `@agent-wrap-up` N times inline.

`@agent-wrap-up` is already session-scoped: Phases 0–3, 5, 6, 7a are "once per session" (its own rule: "multiple plans closed in one session get one entry naming the theme"). Only **Phase 4 (Plan Finalization)** is per-plan. So the fix is a **batch scope** on the existing wrap-up skill, not a second wrap-up source of truth. `parcel-sprint` runs it once and is permitted to spawn `wiki-writer` for the read-heavy wiki prose (user decision).

**In Scope:**

**D1 — Batch scope on `@agent-wrap-up`.** Accept an input list of plan codes (the sprint's batched plans at `GATE_D_USER_APPROVAL`). Phase 4 iterates over the list; every other phase runs once. Keep the single wrap-up contract (`agent-wrap-up` stays the only wrap-up owner). Add a `version:` bump.

**D2 — Confirmation gate (confirm before completing).** For each plan: assert bottom `Status: PHASE_9`, `claim_status: GATE_D_USER_APPROVAL`, a `DONE` per-plan outcome, Phase 9 verification evidence + acceptance criteria present, and its exact `plan: <code>` commit on the trunk. Then run the repo gates **once** for the batch (tests/lint/build, `check-parcel-prefix.ps1`, `check-utf8-agents.ps1`, `wiki_lint.py`, `wiki_coverage_check.py`). A plan failing confirmation is **excluded as carry-forward** — never marked complete. A red repo gate blocks the batch wrap-up.

**D3 — Complete + archive as a batch.** Set `claim_status: COMPLETE` + bottom `Status: COMPLETE` + a Completion Note per plan, then `git mv` each to `.devops/archive/` (root). No branch — trunk-sequential (there is nothing to merge/prune). One changelog entry, one `machinery-version` bump, one wiki reconciliation pass, one backlog sweep, one knowledge-consolidation run.

**D4 — Trigger is a follow-up step, not inline at Gate D.** `@sprint-run` ends at the consolidated Gate D report (testing can cross sessions; Gate D always halts for the human). After the operator's verdict, the batch wrap-up runs as a distinct invocation (operator-invoked, executed by `parcel-sprint` or the ordinary wrap-up path). Record the chosen command surface in `plan-lifecycle.md` § Deviations and `pass-the-parcel` § Batch Runner.

**D5 — Host spawn surface.** `parcel-sprint`'s `task` allow-list is currently locked to exactly `ptp-parcel-fast`. Add `wiki-writer` (already subagent-invocable per `AGENTS.md`) so the batch wrap-up can delegate the read-heavy wiki reconciliation; `opencode.json` + `opencode.template.json` updated. Nothing else gains a spawn right.

**D6 — Composition with manual wrap-up + sprint close.** Manual per-plan wrap-up stays valid and unchanged. `sprint-close` § 1 must treat any surviving `GATE_D_USER_APPROVAL` plan as **not complete** (verdict + wrap-up required) or as explicit carry-forward — never silently close over it.

**Out of Scope:**
- The status, predicate, and fixpoint runner (T1-E3.03 — hard dependency).
- Changing Gate D semantics, auto-clearing it, or advancing past `PHASE_9`.
- Removing or narrowing per-plan `@agent-wrap-up`.
- A second wrap-up skill (single owner: `@agent-wrap-up`).
- New CI workflow or test harness.

**Perimeter Notes:**
- **Depends on T1-E3.03** (`depends_on: ["T1-E3.03"]`): the batch scope identifies its targets by `claim_status: GATE_D_USER_APPROVAL`. E3.03 must be archived before E3.04 can be claimed — their `touches` overlap (`sprint-run`, `plan-lifecycle`, `pass-the-parcel`, `sync-manifest`, `version-history`, `HOW-TO`). This is expected and correct under the new predicate: E3.04's dependency satisfies when E3.03 is archived (not when it merely reaches Gate D), because the `touches` overlap blocks it until then.
- **`machinery-version` `44 → 45`** + a `version-history.md` row (CI gate).
- **Wrap-up is safety-critical** — the confirmation gate is not simplifiable: never mark a plan complete without evidence, never auto-clean a red tree.
- **`touches` widening (recorded deviation — executor, 2026-09-16).** The write set was widened at execution time, **before** any of the added files was edited, by 11 entries: `.opencode/plans/base-context.md`, `.devops/templates/base-context.template.md`, `.devops/rules/agents-and-skills.md`, and the 8 agent files regenerated by `check-parcel-prefix.ps1 -Sync` (`parcel.agent.md`, `parcel-sprint.agent.md`, 7 × `ptp-*.subagent.md`). **Why:** `D5` widens `parcel-sprint`'s spawn allow-list to two named targets, and the shared prefix plus `agents-and-skills.md` § The "Batch Host" Pattern both asserted the now-false "narrowed to exactly `ptp-parcel-fast` / exactly one target". Those sentences are read by every agent, so leaving them would have shipped a documented contract that contradicts the config. Per `AGENTS.md` rule 9 the prefix was edited **in `base-context.md` + its seed only**, then re-inlined by `-Sync` — no inlined prefix was hand-edited. No other widening was needed.

---

## 2 Phase 2: Requirements & Context
**Skill Executed:** `ptp-context-hunter`

**Forensic Context Inventory:**
> [x] Wiki core docs read (`.wiki/core/00-system-index.md` + drilled into relevant docs)
> [x] Knowledge capture read (`.wiki/core/18-knowledge-capture.md`)
> [x] Source code verified (machinery files in blast radius)

**Relevant Existing Decisions (from knowledge capture):**
- **Wrap-up owns archival** — `agent-wrap-up` Phase 4 is the only path from `.devops/plans/` to `.devops/archive/`; a done plan left in plans is a ghost.
- **One state machine, five mirrors** — the batch deviation must be mirrored across `plan-lifecycle`, the shared prefix, and `pass-the-parcel`.
- **Subagent wiki-first mandate** — any spawned subagent must be told to read `.wiki/` first (`AGENTS.md` rule 6).
- **Model registry is template-owned** — adding a spawn target does not rebind models; `opencode.json`'s `task` allow-list is separate from the registry.

**Relevant Docs Found:**
| Doc | What it governs here |
|---|---|
| `.devops/skills/agent-wrap-up/SKILL.md` | Phase 4 `:101-106`, delegation model `:32-43`, phase-gating `:25-30`, version bump `:142` |
| `.devops/skills/sprint-run/SKILL.md` | Consolidated report `:75-83`, § 7 named exception `:85-87` |
| `.devops/agents/parcel-sprint.agent.md` | Spawn allow-list `:127`, hard rules `:144-147` |
| `opencode.json` | `agent.parcel-sprint.permission.task` (currently exactly `ptp-parcel-fast`) |
| `.devops/skills/sprint-close/SKILL.md` | § 1 completion verification `:18-24` |
| `.devops/rules/plan-lifecycle.md` | Deviations `:95-103`, rule 6 archive-on-completion `:93` |
| `.devops/skills/pass-the-parcel/SKILL.md` | § Batch Runner `:154-162`, Group F wrap-up |
| `HOW-TO.md:67` | User-facing batch description |

**Relevant Code Found:**
*Edited (~11 files + seeds):*

| # | File | Change |
|---|---|---|
| 1 | `.devops/skills/agent-wrap-up/SKILL.md` | Batch scope: input plan list; Phase 4 loops; confirmation gate; `version: 11 → 12` |
| 2 | `.devops/skills/sprint-run/SKILL.md` | § 6 report → point at the follow-up batch wrap-up; § 7 exception; `version: 2 → 3` |
| 3 | `.devops/agents/parcel-sprint.agent.md` | Spawn allow-list gains `wiki-writer`; hard rules gain the batch wrap-up; `task` description |
| 4 | `opencode.json` | `agent.parcel-sprint.permission.task` gains `wiki-writer` |
| 5 | `.devops/templates/opencode.template.json` | Seed mirror of #4 |
| 6 | `.devops/rules/plan-lifecycle.md` | § Deviations: batch wrap-up contract; rule 6 note |
| 7 | `.devops/skills/sprint-close/SKILL.md` | § 1 recognises any surviving `GATE_D_USER_APPROVAL` plan as unfinished |
| 8 | `.devops/skills/pass-the-parcel/SKILL.md` | § Batch Runner batch-wrap-up paragraph; `version: 11 → 12` (coordinate with E3.03's bump) |
| 9 | `.devops/sync-manifest.yaml` | `machinery-version: 44 → 45` |
| 10 | `.devops/logs/version-history.md` | row recording literal `machinery-version: 45` (**CI gate**) |
| 11 | `HOW-TO.md` | batch run → batch wrap-up description |

*Read-only:* `scripts/check-parcel-prefix.ps1`, `.github/workflows/validate.yml`, the wrap-up delegation subagents.

---

## 3 Phase 3: User Clarification
**Skill Executed:** `ptp-context-hunter`

**Phase 3 Q&A (resolved interactively with the user before instantiation):**
> [x] **Q1 — Who owns the batch wrap-up?**
>    - `(A)` Host runs it inline · `(B)` host may spawn `wiki-writer` · `(C)` operator-invoked separate step.
>    - **Resolved:** host may spawn `wiki-writer`; the host runs the batch wrap-up (D5).
> [x] **Q2 — Trigger.**
>    - `(Recommended)` **Follow-up after Gate D** — testing can cross sessions; `@sprint-run` ends at the consolidated report.
>    - **Inline continuation on approval.**
>    - **Resolved:** follow-up.
> [x] **Q3 — Reuse vs new skill.**
>    - `(Recommended)` **Batch scope on `@agent-wrap-up`** — one wrap-up owner.
>    - **New `@sprint-wrap-up`** — risks drift.
>    - **Resolved:** batch scope.
> [x] **Q4 — Evidence bar for "confirm successful implementation".**
>    - `(Recommended)` **Per-plan assertions + repo gates run once** — Status `PHASE_9`, `GATE_D_USER_APPROVAL`, `DONE`, evidence present, `plan: <code>` commit; gates green.
>    - **Trust the run's DONE alone.**
>    - **Resolved:** D2.
> [x] **Q5 — Sprint close interaction.**
>    - `(Recommended)` **Treat a surviving `GATE_D_USER_APPROVAL` plan as unfinished / carry-forward**, never close over it silently.
>    - **Auto-wrap on close.**
>    - **Resolved:** D6.
> [x] Final validation: "Is this all the context required?" -> Yes.

**Phase 3.5 — `ptp-phase3-answerer` (`AUTO` mode, inline):** All five Phase 3 questions carry a `Resolved:` verdict recorded with the user before instantiation, so there is nothing left to auto-resolve. **No `Unresolvable:` entry.** No question was re-opened; none was invented. (Note: `AUTO` auto-clears Gates **A–C**, not the question step — here the questions were already closed upstream, which is why the answerer is a no-op rather than a skip.)

**Architectural Conflict Warnings Raised:**
- **CW1 — Load vs correctness.** The reason a batch wrap-up exists is that per-plan wrap-up is heavy; but coalescing must not weaken the archive-on-completion guarantee. The confirmation gate (D2) is the guard; it is not simplifiable.
- **CW2 — Spawn-surface widening.** Granting `parcel-sprint` `wiki-writer` is the first widening beyond exactly-one target. Mitigated by adding exactly one named target and no glob.
- **CW3 — Version-bump collision.** E3.03 and E3.04 both bump `pass-the-parcel`'s `version:`. Sequential execution (depends_on) makes this safe, but the executor must read the file's current value rather than assume.

---

> **HALT POINT (Gate A — Scope):** Phases 1-3 complete (+ Phase 3.5 auto-resolutions in AUTO mode). Set Status -> `PHASE_3`, Active Persona -> `Scoper`. Present the scope perimeter + Phase 3 Q&A record; the user approves the scope before planning begins. On rejection: Status -> `PHASE_1`, Gate A -> `REJECTED`, re-run the affected questions. Questions are relayed to the user **one at a time** via the ask-questions tool — never batched.

> **AUTO-VERIFICATION RECORD (Gate A — auto-cleared by the locked `parcel-sprint` preset):**
> - Scope outputs exist: Phase 1 (intent + D1-D6 + in/out of scope), Phase 2 (forensic inventory + doc map), Phase 3 (5 resolved Q&A) are present and populated.
> - `ptp-phase3-answerer` inline (Phase 3.5) recorded **no `Unresolvable:` entry** — see above.
> - No `REJECTED` verdict line appears anywhere in Phases 1-3.
> - **Verdict: Gate A `APPROVED` (AUTO).** Recorded in the State & Gates section; no user halt. The human sign-off for this run is Gate D.

---

## 4 Phase 4: Wiki Requirements & Acceptance Criteria
**Skill Executed:** `ptp-high-visionary` (spec-first directives) + `@wiki-writer`

> **Spec-First Rule:** The wiki is written BEFORE the code. Docs written here describe target behavior and are marked `status: in-progress` — a pre-code doc is a claim, not truth. Promotion to `stable` happens only at Wrap Up, after the executor verifies code matches spec.
>
> **Conditional Skip (checklist):** Run Phase 4 only if ANY of: (a) new or changed user-visible strings/UI, (b) API, schema, or logic-contract change, (c) wiki-facing behavior change. If none apply, skip this phase and record: `No wiki delta — rationale: <why>`. Never skip silently.

**Conditional-Skip Checklist Result:**
- (a) new/changed user-visible strings or UI — **No** (machinery only; no `src/`, no app surface).
- (b) API / schema / logic-contract change — **No app contract**; the changed contracts are **machinery** contracts (a skill's input shape, a claim-status transition, a permission map). Those are governed by `.devops/` docs, not `.wiki/`.
- (c) wiki-facing behavior change — **No**: `.wiki/` documents the *app*; `T1-E3.03` already recorded the claim-status predicate in `.wiki/core/18-knowledge-capture.md` and this plan does not alter it (the sentence "one human verdict covers the whole batch; deferred, never skipped" stays true — retirement was already a subsequent step).

**Verdict:** `No wiki delta — rationale: machinery/process-only change; the normative surface is `.devops/` (skills, rules, agents, seeds), which this plan edits directly.` Recorded, not silent.

**Machinery Docs to Write/Update (the real spec surface):**
| Doc | Change |
|---|---|
| `.devops/skills/agent-wrap-up/SKILL.md` | New § Batch Scope (input list, Phase-4-only iteration, confirmation gate) — the normative spec for `D1`-`D3` |
| `.devops/rules/plan-lifecycle.md` | § Deviations — the normative lifecycle contract (`D4`) |
| `.devops/skills/pass-the-parcel/SKILL.md` | § Batch Runner — canonical lifecycle mirror (`D4`) |
| `.devops/skills/sprint-run/SKILL.md` | §§ 6-7 — the batch loop's own hand-off wording (`D4`) |
| `.devops/rules/agents-and-skills.md` + prefix | Spawn-surface truth (`D5`, `CW2`) |

**Acceptance Criteria:**
| # | Criterion (behavior) | Test Target |
|---|---|---|
| 1 | `@agent-wrap-up` accepts an input **list** of plan codes; Phase 4 iterates over it while every other phase runs once; it stays the single wrap-up owner (no second skill) | read § Batch Scope |
| 2 | The confirmation gate states **five** individually-checkable per-plan assertions (bottom `Status: PHASE_9`; `claim_status: GATE_D_USER_APPROVAL`; a `DONE` per-plan outcome; Phase 9 evidence + acceptance criteria; the exact `plan: <code>` commit) and says a failing plan is **carry-forward**, never complete | read § Batch Scope + behavioural check (§ Phase 9) |
| 3 | Repo gates run **once** for the batch; a red gate blocks the whole batch wrap-up and is never auto-cleaned | read § Batch Scope |
| 4 | `agent.parcel-sprint.permission.task` is exactly `{"*":"deny","ptp-parcel-fast":"allow","wiki-writer":"allow"}` — **no glob** — identically in `opencode.json` and the seed | JSON parse + exact key-set assertion |
| 5 | `plan-lifecycle.md` § Deviations, `pass-the-parcel` § Batch Runner and `sprint-run` §§ 6-7 all describe the **same** contract: batch ends at `GATE_D_USER_APPROVAL`; retirement is a separate, operator-invoked batch-wrapped step | grep + read |
| 6 | `sprint-close` § 1 still treats a surviving `GATE_D_USER_APPROVAL` plan as unfinished (wave 2's fix verified, **not** re-edited) | read § 1 |
| 7 | `.devops/sync-manifest.yaml` reads `machinery-version: 47` and `version-history.md` contains the literal `machinery-version: 47` | reproduce the `validate.yml` predicate |
| 8 | Every gate in § 7 of this plan exits `0`; the prefix passes for all 9 locked agents after `-Sync` | Phase 9 command log |

**Spec Notes:**
- **Data flow / state changes:** no runtime data flow. The only state transition added is *how* an existing transition is triggered — `GATE_D_USER_APPROVAL` → `COMPLETE` gains a **batch** path (one invocation over a list) beside the per-plan path. The triggering verdict stays human.
- **Edge cases the spec must cover:** empty input list (no-op, and **not** an error); a plan in the list that fails any gate assertion (carry-forward, batch continues); a red repo gate (whole batch blocked); a plan already `COMPLETE`/archived between the run and the wrap-up (`git mv` fails — treat as carry-forward with the reason, never force); one plan in the list, i.e. the batch scope degenerating to the per-plan path.
- **Docs consumed by this feature (read-first list):** `agent-wrap-up` § Execution Phases (its own once-per-session vs per-plan split), `plan-lifecycle.md` § Claim Protocol + § Deviations, `sprint-run` §§ 2/4/6/7, `sprint-close` § 1, `AGENTS.md` rules 6 + 9.

---

> **NO HALT after Phase 4:** the spec is reviewed together with the implementation plan at Gate B (after Phase 5) — one decision, what it will do and what it will cost.

---

## 5 Phase 5: High-Visionary Standard Implementation Plan
**Skill Executed:** `ptp-high-visionary`

**Simplicity Gate:**
> [x] Climbed the Simplicity Ladder (7 rungs) — rung 6 (reuse an existing skill) chosen over rung 7 (new abstraction).
> [x] No speculative abstractions — no new skill, no new script, no new CI workflow, no new state value.
> [x] Reuse over reimpl — Reuse Log populated.
> [x] Deliberate simplifications marked with `ponytail:` comments — where the batch scope is described loosely ("report it with the failed assertion named") rather than as a formal report schema.
> [x] Safety exceptions preserved — the confirmation gate (D2) and Gate D itself are explicitly not simplifiable.

**Reuse Log:**
| Existing Asset | Where Reused | Ladder Rung |
|---|---|---|
| `@agent-wrap-up` Phase 4 (Plan Finalization) | The batch scope is a scope on this phase, not a new skill | 6 |
| `@agent-wrap-up` phase-gating rule ("multiple plans closed in one session get one entry") | The once-per-batch vs once-per-plan split — the skill already implied it; this makes it normative | 1 |
| `plan-lifecycle.md` § Deviations (numbered named-deviation list) | New deviation appended in the existing shape — no new section | 1 |
| `pass-the-parcel` § Batch Runner bullet list | New/extended bullet, same shape | 1 |
| `sprint-run` § 6 "What to do next" block + § 7 named exception | Reworded in place; no new section | 1 |
| `sprint-close` § 1 (wave 2's `GATE_D_USER_APPROVAL` clause) | **Verified and left untouched** (D6 already landed) | 1 |
| `opencode.json` `parcel-sprint.permission.task` exact-key shape | One exact key added beside the existing one | 1 |

**Spaghetti Triage (See-Name-Route, Do Not Fix):**
- [x] No smells noticed in the edited region. Noted, not fixed: `sprint-run` § 6/§ 7 and `HOW-TO.md` § Batch Runner restate the batch contract in three places (documentation duplication, not code coupling). Routing to the existing single-source-of-truth pattern (`plan-lifecycle.md` § Deviations is canonical, the others point at it) is exactly what this plan reinforces. No new parcel needed.

| File | Function | Smell Type | Severity | Recommended Action |
|---|---|---|---|---|
| — | — | none | — | — |

### To-Do List
- [x] T1 — `agent-wrap-up`: add § Batch Scope + Phase 4 batch note; bump `version: 11 → 12`.
- [x] T2 — `sprint-run`: § 6 hand-off + § 7 exception; bump `version: 2 → 3`.
- [x] T3 — `pass-the-parcel`: § Batch Runner retirement bullet; bump `version: 15 → 16`.
- [x] T4 — `plan-lifecycle.md`: § Deviations gains the retired-by-follow-up-wrap-up deviation; § Claim Protocol step 5 pointer.
- [x] T5 — `parcel-sprint.agent.md` unique content: batch-wrap-up hard rule + two-target spawn note.
- [x] T6 — `opencode.json` + `.devops/templates/opencode.template.json`: `wiki-writer` added to `parcel-sprint.permission.task`.
- [x] T7 — `base-context.md` + seed: presets paragraph no longer claims "exactly `ptp-parcel-fast`"; `-Sync` re-inlines into the 9 locked agents.
- [x] T8 — `agents-and-skills.md` § Batch Host: allow-list wording.
- [x] T9 — `sprint-close` § 1: **verify only** (D6 landed in wave 2).
- [x] T10 — `sync-manifest.yaml` `46 → 47` + `version-history.md` row with the literal `machinery-version: 47`.
- [x] T11 — `HOW-TO.md` § Sprint Batch Runner: retirement is a separate step; three deviations.

### File-Level Steps
1. **`.devops/skills/agent-wrap-up/SKILL.md`** — insert `## Batch Scope (sprint batch wrap-up)` between the Delegation Model block and `## Execution Phases`; add a `**Batch scope:**` pointer line at the head of Phase 4; extend the `## Hard Stop` section with the confirmation-gate clause; frontmatter `version: 11 → 12`, `updated: 2026-09-16`.
2. **`.devops/skills/sprint-run/SKILL.md`** — § 6 third bullet: the approved exit runs the **follow-up batch wrap-up** (separate, operator-invoked) instead of N inline per-plan wrap-ups; § 7: "until that verdict plus the follow-up wrap-up (batch-scoped or per-plan) retires it"; frontmatter `version: 2 → 3`.
3. **`.devops/skills/pass-the-parcel/SKILL.md`** — § Batch Runner: replace the trailing "Full contract" line block with a retirement bullet naming `@agent-wrap-up` § Batch Scope; frontmatter `version: 15 → 16`.
4. **`.devops/rules/plan-lifecycle.md`** — § Deviations intro "three named ways" → "four named ways"; insert the new deviation as item 2 and renumber 2→3, 3→4; § Claim Protocol step 5 gains the follow-up-wrap-up pointer.
5. **`.devops/agents/parcel-sprint.agent.md`** — unique-content hard rules: add the separate-invocation wrap-up rule and the two-target spawn note (prefix is regenerated, not hand-edited).
6. **`.opencode/plans/base-context.md`** (+ `.devops/templates/base-context.template.md` mirror + seed `version: 14 → 15`) — presets paragraph: name both allowed targets explicitly. Then `scripts/check-parcel-prefix.ps1 -Sync` re-inlines the prefix into all 9 locked agents.
7. **`opencode.json`** + **`.devops/templates/opencode.template.json`** — `agent.parcel-sprint.permission.task` gains exactly `"wiki-writer": "allow"`.
8. **`.devops/rules/agents-and-skills.md`** — § The "Batch Host" Pattern: allow-list is a named set with `"*": "deny"`, not "exactly one target".
9. **`.devops/skills/sprint-close/SKILL.md`** — read-only verification (no edit).
10. **`.devops/sync-manifest.yaml`** + **`.devops/logs/version-history.md`** — version bump + release row.
11. **`HOW-TO.md`** — § Sprint Batch Runner paragraph.

### Implementation Instructions (no code snippets except exact string literals: regex, SQL migration, CLI command, config key, error message)

**Exact literals that must appear verbatim on disk:**

- `machinery-version: 47` — in `.devops/sync-manifest.yaml` and as a literal inside the new `.devops/logs/version-history.md` row (CI predicate: the manifest value must appear in that file).
- Permission keys in `agent.parcel-sprint.permission.task` (both configs): `"*": "deny"`, `"ptp-parcel-fast": "allow"`, `"wiki-writer": "allow"` — **no glob key**.
- Commit literal: `plan: T1-E3.04`.

**Commands:**

```
powershell -NoProfile -File scripts/check-parcel-prefix.ps1 -Sync     # after the base-context edit
powershell -NoProfile -File scripts/check-parcel-prefix.ps1           # must PASS x9
powershell -NoProfile -File scripts/check-utf8-agents.ps1
python scripts/wiki_lint.py
python scripts/wiki_claims.py check
python scripts/wiki_coverage_check.py
powershell -NoProfile -File scripts/sync-architecture.ps1 -SelfTest
git commit -m "plan: T1-E3.04"
```

**Sequencing constraint (`CW3`):** read each target file's **live** `version:` immediately before bumping. Live values at run time: `agent-wrap-up 11 → 12`; `sprint-run 2 → 3`; `pass-the-parcel 15 → 16`; seed `base-context.template.md 14 → 15`. `sprint-close` is **not** bumped (`D6` already landed in wave 2).

**Prefix-lock constraint (`AGENTS.md` rule 9):** the presets paragraph lives in the shared prefix. Edit `.opencode/plans/base-context.md` + the seed; **never** hand-edit `parcel*.agent.md` / `ptp-*.subagent.md`. Regenerate with `-Sync`.

### Wiki Core References
- `.wiki/core/18-knowledge-capture.md` -> the `@sprint-run` batch-deviations entry (`:65`); read for consistency, not edited (its Gate D sentence stays true). Machinery lessons route to `.devops/rules/process-lessons.md` per the routing rule, and that routing is a **wrap-up** step, not this plan's.
- `.wiki/rules/frontmatter.md` -> frontmatter/status rules for the docs this plan does *not* touch.
- No `.wiki/` blueprint derives from this plan — it changes machinery, not app architecture.

### Wiki Docs to Add/Edit
None — see the Phase 4 conditional-skip verdict. The normative surface for `D1`-`D5` is the `.devops/` machinery set listed above.

---

> **HALT POINT (Gate B — Spec & Plan Review):** Phases 4-5 complete. Present the wiki requirements spec (with acceptance criteria) AND the execution plan together as one decision. Do not proceed to reviews until user approves. Update the **State & Gates** section at the bottom of this file: Status -> `PHASE_5`, Active Persona -> `High-Visionary`. Leave Gate B `OPEN` — the orchestrator records the user's verdict.

> **AUTO-VERIFICATION RECORD (Gate B — auto-cleared by the locked `parcel-sprint` preset):**
> - Gate B outputs exist: Phase 4 carries the conditional-skip verdict **and** an 8-row acceptance-criteria table with a test target per row; Phase 5 carries a passed Simplicity Gate, a populated Reuse Log, a Spaghetti Triage disposition, an 11-item To-Do list, file-level steps, exact string literals and the command set.
> - No `REJECTED` verdict line and no `Unresolvable:` entry appear in Phases 4-5.
> - **Verdict: Gate B `APPROVED` (AUTO).** In `SINGLE` topology Gate C is recorded `N/A (SINGLE)` — the inline Phase 6 self-review checkpoint (`PASS`) in Phase 6 is the merged spec+plan+self-review decision. No user halt.

---

## 6 Phase 6: Grumpy Architect Spec & Logic Audit
**Skill Executed:** `ptp-grumpy-architect` (`SINGLE`: orchestrator inline)

> **`SINGLE` topology:** no independent reviewer. The orchestrator logs an inline self-review checkpoint here and records **Verdict** `N/A — SINGLE self-review`.

**Verdict:** `PASS` (inline `SINGLE` self-review — the `N/A` record is logged in Phase 7)

> **Rejection Rule:** If plan does not make the app faster, safer, or easier to modify, do not check boxes. Reject and force rewrite via `PHASE_5_REVISION`.
>
> **Spec-level audit — the plan contains no code, so no code-level scans (DRY/WET, line checks).**

**Inline self-review checkpoint (executor-performed, `SINGLE`):**

- **System Contracts Explicit:** Yes — every named asset exists and is named exactly: `## Batch Scope (sprint batch wrap-up)` in `agent-wrap-up`; `@agent-wrap-up` § Batch Scope referenced from `plan-lifecycle.md` § Deviations, `pass-the-parcel` § Batch Runner and `sprint-run` § 6; `agent.parcel-sprint.permission.task` as the one config surface plus its seed mirror.
- **File Boundary & Scope Collisions:** Checked. No file is edited twice, and the sync engine's derived surfaces are enumerated. The one real collision risk — the shared prefix — is handled by prefix-lock discipline (edit the source, regenerate) rather than by hand-editing 9 files.
- **Dependency Gaps:** All referenced assets exist: `check-parcel-prefix.ps1`, `check-utf8-agents.ps1`, `wiki_lint.py`, `wiki_claims.py`, `wiki_coverage_check.py`, `sync-architecture.ps1 -SelfTest`. No new script is referenced.
- **YAGNI Bloat:** None. No new skill, no new state value, no new CI job, no report schema. The batch scope reuses a phase whose once-per-session semantics the skill had already implied.
- **Paranoid Security (Contract Level):** N/A for app secrets. The security-relevant contract here is **capability scope**, and the change is a deliberate **one-key** widening inside an existing deny-by-default block: `"*": "deny"` is retained, no glob key is added, the new key is a named target. Verified mechanically (exact key-set assertion, Phase 9).
- **Survivability:** Failure modes specified: a plan failing an assertion is carry-forward (batch survives); a red repo gate blocks the batch (fail-closed, never auto-cleaned); a plan archived mid-flight is carry-forward, never forced.
- **Edge Cases:** Enumerated in Phase 4 Spec Notes — empty list (no-op, not an error), one-plan list, mid-flight archive, per-plan assertion failure, red gate.
- **Performance Trade-offs:** Strongly positive — the point is to run the heavy wrap-up phases **once per batch** instead of N times. No scaling ceiling; the gate is O(N) assertions plus one repo-gate pass.
- **Architectural Anti-Patterns:** Avoided the obvious one — **duplicate sources of truth**. A second wrap-up skill was explicitly rejected (single owner kept) and `plan-lifecycle.md` § Deviations stays canonical with the others pointing at it.
- **Endpoint Protection & Rate Limiting:** N/A (no endpoints).
- **Wiki Core Compliance:** Phase 4 records the conditional-skip verdict against the checklist; grep confirms no `.wiki/` doc asserts something this plan changes (one KC entry, still true).

**Dead-Code / Orphan Flags (for Wrap Up backlog entry):**
| plan section / file_path:line_number | Reason |
|---|---|
| — | None. No orphan or dead code introduced; no file is retired by this plan. |

**Required Fixes:**
> [x] None.

---

## 7 Phase 7: Smooth Operator Product Review
**Skill Executed:** `ptp-smooth-operator` (`SINGLE`: orchestrator inline)

> **`SINGLE` topology:** no independent reviewer. Record **`N/A — SINGLE self-review`**; the self-review is presented at Gate B.

**Verdict:** `N/A — SINGLE self-review`

- **Vision & Journey Integrity:** Yes — the operator's journey shortens to *verify the batch once → wrap up the batch once*, instead of *verify N times → wrap up N times inline*. Gate D's human moment is preserved exactly.
- **Scope Containment (No Gold Plating):** Yes. No new report format, no auto-detection heuristic, no "wrap up the sprint too" creep — sprint close stays its own skill with its own § 1 guard.
- **4 Core User States:** N/A for UI. The machinery analogues are covered: empty list (no-op), partial failure (carry-forward), hard failure (blocked batch), success (all plans `COMPLETE` + archived).
- **Guardrails & Permissions:** The capability widening is bounded and named (two keys, no glob); Gate D stays human; the confirmation gate can only *exclude*, never include.
- **Mobile / A11y / Telemetry:** N/A.

**Required Fixes:**
> [x] None.

---

> **HALT POINT (Gate C — Peer Reviews):** Reviews complete. Present findings and required fixes. Update the **State & Gates** section at the bottom of this file:
> - **PASS:** Phase 6 log clean -> set Status `PHASE_7`, Active Persona `Reviewer`. Do not proceed to execution until user approves Gate C.
> - **FAIL:** Phase 6 or 7 flagged blocking flaws -> set Status `PHASE_5_REVISION`, Active Persona `High-Visionary`. Return to Group B for plan adjustments, then re-run Phases 6-7. **Never advance an unapproved plan to execution.**
> The orchestrator records Gate C -> `APPROVED`/`REJECTED` only after the user's verdict.
>
> **`SINGLE` topology:** Gate C is `N/A` — the plan approval happens once at Gate B (spec + plan + inline self-review). Update the State & Gates `Gate C` row to `N/A (SINGLE)`.

---

## 8 Phase 8: Execute Changes
**Skill Executed:** `ptp-code-surgeon`

**Execution Isolation:** Phase 8 ran after Gate C was recorded `N/A (SINGLE)` and Gate B auto-cleared (`AUTO` preset).

**Single-Pass Direct-to-Disk:**
> [x] Implementation written DIRECTLY to source files - no intermediate Markdown code blocks, no drafting files
> [x] Touched only intended lines - no adjacent refactors
> [x] Cleaned up only owned orphans
> [x] Pre-existing dead code left untouched

> [x] Step 1: `agent-wrap-up` § Batch Scope inserted (input list, Phase-4-only iteration, five-assertion confirmation gate, once-per-batch repo gates, carry-forward rule) + Phase 4 batch pointer + Hard Stop clause; `version: 11 → 12`.
> [x] Step 2: `sprint-run` § 4 terminal-state sentence, § 6 approved-exit bullet, § 7 named exception; `version: 2 → 3`.
> [x] Step 3: `pass-the-parcel` § Batch Runner intro + new "retirement is a separate, operator-invoked step" bullet; `version: 15 → 16`.
> [x] Step 4: `plan-lifecycle.md` § Deviations — intro "three"→"four" named ways, new item 2 (retirement is a separate step), items renumbered 3/4; § Claim Protocol step 5 gains the follow-up-wrap-up pointer.
> [x] Step 5: `base-context.md` + seed `base-context.template.md` (`version: 14 → 15`) — the presets paragraph now names both allowed targets; `check-parcel-prefix.ps1 -Sync` re-inlined the prefix and reported `FIXED parcel.agent.md`, `FIXED parcel-sprint.agent.md` (the shared prefix itself did not change, so the 7 `ptp-*` files reported `PASS` unchanged).
> [x] Step 6: `opencode.json` + seed `opencode.template.json` (`version: 8 → 9`) — `agent.parcel-sprint.permission.task` gains exactly `"wiki-writer": "allow"`; no glob.
> [x] Step 7: `agents-and-skills.md` § The "Batch Host" Pattern — allow-list is a named set under `"*": "deny"`, not "exactly one target".
> [x] Step 8: `parcel-sprint.agent.md` unique content — Workflow step 5 (wrap-up is a separate invocation), `task` exception now names two targets, hard rules 3-5.
> [x] Step 9: `sprint-close` § 1 — **verified read-only** (D6 already landed in wave 2): the section states "A plan in `.devops/plans/` with `claim_status: GATE_D_USER_APPROVAL` is NOT complete … Treat it as unfinished: either get the Gate D verdict and run its wrap-up first, or record it as carry-forward". No edit, no duplication.
> [x] Step 10: `sync-manifest.yaml` `machinery-version: 46 → 47`; `version-history.md` gains the v0.7.17 row containing the literal `machinery-version: 47`.
> [x] Step 11: `HOW-TO.md` § Sprint Batch Runner — retirement paragraph added, deviations now named as three.

**Deviation from the Phase 5 file list (recorded, not silent):** the `touches` write set was widened by 11 entries before any of them was edited — see Phase 1 Perimeter Notes. Net effect on the delivered diff was smaller than declared: `-Sync` rewrote only `parcel.agent.md` and `parcel-sprint.agent.md`, because the shared prefix (the part I changed) is inlined into the `ptp-*` files unchanged; the 7 declared `ptp-*.subagent.md` paths are therefore **declared but untouched**. Over-declaring the write set is the safe direction (`touches` exists to block overlaps).

**No orphan cleanup performed** — this plan retires no file, so there was nothing owned to clean.

---

## 9 Phase 9: Verify Changes
**Skill Executed:** `ptp-code-surgeon`

**Build & Lint:**
> [x] Compilation check clean — N/A (docs/config-only change; no build artefact in this template)
> [x] Lint clean (exit 0)
> [x] Type-generation re-run if schema/API changes — N/A

**Test Report:**
- [x] Lint pass (exit 0)
- [x] Tests pass (exit 0) — the template's test surface is its gate scripts; all listed below exit `0`
- [x] Build pass (exit 0) — N/A
- [x] Code matches exact plan specifications — verified by re-running the Phase 5 Implementation-Instructions commands; every one exits `0`
- [x] No functional gaps identified

**Command log (every command exit `0`):**

| # | Command | Exit |
|---|---|---|
| 1 | `powershell -NoProfile -File scripts/check-parcel-prefix.ps1 -Sync` | `0` — `FIXED parcel.agent.md`, `FIXED parcel-sprint.agent.md`; 7 `ptp-*` `PASS` unchanged |
| 2 | `powershell -NoProfile -File scripts/check-parcel-prefix.ps1` | `0` — `PASS ×9` + 11 model bindings + seed config agree |
| 3 | `powershell -NoProfile -File scripts/check-utf8-agents.ps1` | `0` — `ALL CLEAN (219 files scanned)` |
| 4 | `python scripts/wiki_lint.py` | `0` — `OK: wiki healthy - 0 broken links, all anchors present, frontmatter valid.` |
| 5 | `python scripts/wiki_claims.py check` | `0` — `claims OK: 0 stale, all grounded sources present.` |
| 6 | `python scripts/wiki_coverage_check.py` | `0` — `coverage OK: no src/ tree in this workspace — nothing to cover` |
| 7 | `powershell -NoProfile -File scripts/sync-architecture.ps1 -SelfTest` | `0` — `SELFTEST OK: 5 dirs, 38 skills, 12 files materialised correctly` (fixture installs `machinery-version 47`) |
| 8 | `validate.yml` "Machinery version discipline" predicate reproduced verbatim in Python | `0` — `OK: machinery-version 47 is recorded in version-history.md` |
| 9 | JSON parse + exact spawn-surface assertion over `opencode.json` and `.devops/templates/opencode.template.json` | `0` — both parse; `parcel-sprint.task == {"*":"deny","ptp-parcel-fast":"allow","wiki-writer":"allow"}`; no glob key |
| 10 | Behavioural check of the batch wrap-up confirmation gate (`wrapup_gate_check.py`, 5 fixtures) | `0` — `pass.md → COMPLETE-ELIGIBLE`; `bad_claim_status.md`, `bad_status.md`, `bad_outcome.md`, `bad_commit.md` → `CARRY-FORWARD`, each with its single failed assertion named |

**Acceptance-criteria results (Phase 4 table):**

| # | Criterion | Result |
|---|---|---|
| 1 | Batch input is a list; only Phase 4 iterates; single wrap-up owner | PASS — `agent-wrap-up` § Batch Scope ("Rule — every phase once, except Phase 4") |
| 2 | Five individually-checkable assertions; failing plan is carry-forward | PASS — read + behavioural check #10 proves exclusion |
| 3 | Repo gates once per batch; red gate blocks; never auto-clean | PASS — § Batch Scope + Hard Stop clause |
| 4 | `parcel-sprint.task` exactly two allow-keys + deny, identical in both configs, no glob | PASS — check #9 |
| 5 | `plan-lifecycle` § Deviations / `pass-the-parcel` § Batch Runner / `sprint-run` §§ 6-7 agree | PASS — same contract in all three (verified by read after edit) |
| 6 | `sprint-close` § 1 still guards a surviving `GATE_D_USER_APPROVAL` plan | PASS — verified, file unmodified (`git status` shows no change to it) |
| 7 | `machinery-version: 47` in manifest **and** as a literal in `version-history.md` | PASS — check #8 |
| 8 | Every gate exits `0`; prefix passes for all locked agents | PASS — checks #1-#10 |

**Self-healing attempts used:** 0 (all gates green on first run after `-Sync`).

**Per-plan runner outcome:**

DONE T1-E3.04

**Gate D evidence (for the human):**
- **The behaviour to check:** a batch run now ends with plans at `PHASE_9` / `claim_status: GATE_D_USER_APPROVAL` / Gate D `OPEN`, and *retiring* them is a separate, operator-invoked step. Open `.devops/skills/agent-wrap-up/SKILL.md` § Batch Scope: the input is a list, only Phase 4 iterates, and the five per-plan assertions gate completion.
- **The proof it fails closed:** run `python "C:\Users\carso\AppData\Local\Temp\opencode\wrapup_gate_check.py"` — five fixtures, four of which each violate exactly one assertion and are classified `CARRY-FORWARD` (never `COMPLETE`).
- **The capability boundary:** `opencode.json` → `agent.parcel-sprint.permission.task` = `{"*":"deny","ptp-parcel-fast":"allow","wiki-writer":"allow"}` — two named targets, no glob.
- **The one thing I could not verify without you:** that the follow-up invocation is *wanted* as a separate operator step rather than an automatic continuation. That is the Gate D decision, and it is the deviation recorded in `plan-lifecycle.md` § Deviations item 2.

---

> **HALT POINT (Gate D — Implementation):** Implementation and verification complete. Present completed work to user. Do not proceed to user review until user signs off. Update the **State & Gates** section at the bottom of this file: Status -> `PHASE_9`, Active Persona -> `Executor`. Leave Gate D `OPEN` — the orchestrator records the user's verdict. On rollback: Status -> `PHASE_8_FAILED`.

> **GATE D — LEFT `OPEN` (batch terminal state).** This run ends here: bottom `Status: PHASE_9`, `claim_status: GATE_D_USER_APPROVAL`, Gate D `OPEN`. Per `@sprint-run` § 4 and `.devops/rules/plan-lifecycle.md` § Deviations, the batch path **never archives** and never flips this gate — retirement to `COMPLETE` + archive happens only in the separate, operator-invoked **batch wrap-up** (`@agent-wrap-up` § Batch Scope) after your verdict. Phase 10 and the Completion Note below are therefore **not** executed by this run.

---

## 10 Phase 10: User Review & Tweaks
**Skill Executed:** `knowledge-capture`

**Tweak Discipline:**
> Tweaks are surgical, not architectural. `expansion` and `refactor` are HALT conditions.

- [ ] Tweak classified: `fix` / `expansion` / `refactor`
- [ ] `expansion` and `refactor` routed to new parcel
- [ ] `fix` touches <= 3 files
- [ ] `fix` introduces no new abstraction absent from original plan

**Capture Categories:** Recurring / Tribal / One-off / Cosmetic-skip

| Round | Feedback | Action Taken | Capture Flag | Result |
|---|---|---|---|---|
| 1 | | | | PENDING / COMPLETE |

**Sign-Off:** [Date] - User approved.

---

## Completion Note (Wrap Up)
**Skill Executed:** `agent-wrap-up` — **batch scope**, input list `[T1-E3.04]` (this plan is the sole member; it is also the plan that *defines* this scope, making this its first live exercise).

> [x] Read `.wiki/core/18-knowledge-capture.md` for existing related decisions
> [x] Themed tweaks synced to knowledge capture doc
> [x] Wiki docs updated per Phase 10 tweaks
> [x] Code reconciled against Phase 4 spec — deviations logged
> [x] `status: in-progress` wiki docs promoted to `stable` (or deviation logged)

**Outcome:** Delivered as specified. `@agent-wrap-up` gains a **batch scope**: an input list of plan codes, with **every phase running once for the batch except Phase 4**, which iterates once per plan — so the single wrap-up owner now closes a whole batched set without a second wrap-up source of truth. The **confirmation gate** asserts five things per plan (bottom `Status: PHASE_9`; `claim_status: GATE_D_USER_APPROVAL`; a `DONE <code>` runner outcome; Phase 9 evidence **plus** a populated acceptance-criteria table; the exact `plan: <code>` commit on the trunk), and a plan failing any one is **carry-forward** — never marked complete. Repo gates run **once** per batch and a red gate blocks the whole batch. Retirement is now named as `@sprint-run`'s **fourth** deviation (separate, operator-invoked). `parcel-sprint`'s `task` allow-list gains exactly one named target, `wiki-writer`, with no glob. Executed as **wave 3 of 3**, the final wave of Sprint 8.

**Confirmation gate — executed against this plan's own live file, before retirement (all five PASS):**

| # | Assertion | Evidence |
|---|---|---|
| A1 | bottom `Status` is `PHASE_9` | plan line 518 |
| A2 | `claim_status` is `GATE_D_USER_APPROVAL` | plan line 4 |
| A3 | per-plan outcome is `DONE T1-E3.04` | plan line 448 |
| A4 | Phase 9 evidence **and** populated acceptance criteria | Phase 4 criteria table + Phase 9 command/exit-code table both populated |
| A5 | exact `plan: T1-E3.04` commit on trunk | `cbc9398` |

**Themed Tweaks:**

**Knowledge Capture Entries:**
- **No new entry — deliberately.** This parcel's content is the batch wrap-up contract itself, which is *documented in the skills and `plan-lifecycle.md`* — writing it again into `.devops/rules/process-lessons.md` would duplicate a rule that now has a canonical home. The one genuinely new observation from this wave (a capability widening in `opencode.json` silently falsified a sentence in the shared prefix describing the old capability, and nothing mechanically forces that coupling) is a **single edge case** whose trigger — the mid-sprint realisation that a leaner wrap-up was needed — will not recur. It is recorded in the Sprint 8 retro instead of as a process lesson, which is where a one-off belongs. No `machinery-version` bump is therefore required from this wrap-up.

**Spec Reconciliation:** None — implementation matches the Phase 4 spec. Two recorded notes:
1. **`D6` was already satisfied by wave 2.** `.devops/skills/sprint-close/SKILL.md` § 1 already treats a surviving `GATE_D_USER_APPROVAL` plan as unfinished (written by T1-E3.03's CW3/CW5 work). Verified by read and **not re-edited** — `sprint-close` remains `version: 5`. Adding a second clause would have been duplication of a rule with an owner.
2. **`touches` was widened, explicitly and before editing** (11 paths: the prefix source `base-context.md` + its seed, `agents-and-skills.md`, and the two sync-derived agent files). Reason: `D5`'s widening falsified the shared prefix's own sentence — "narrowed to exactly `ptp-parcel-fast`" — which every agent reads as its rulebook; shipping it unchanged would have left the configuration and its own documentation in direct contradiction. Per AGENTS.md rule 9 the prefix was edited only in `base-context.md` + seed, then re-inlined by `-Sync`; no inlined prefix was hand-edited.

**In-Progress Promotions:** None — Phase 4 wrote no `in-progress` docs. This parcel's wiki delta is machinery vocabulary in rules and skills, not app behaviour.

**Dead-Code Backlog Entries (from Phase 6):** None — the plan's Phase 6 self-review logged "no orphan or dead code introduced; no file is retired by this plan", and no file is retired by it. The batch path's one dead state value (`IN_PROGRESS`) was already removed by T1-E3.03.

**Wiki Updates:** None — the delta is `.devops/rules/` + skills, outside `.wiki/`.

**Plan Archiving:** Plan archived to `.devops/archive/t1-e3.04-sprint-batch-wrap-up-plan.md` (via `git mv`, no stub). Trunk-sequential batch path: no branch, so no merge/prune.

**Backlog Review:** No existing backlog item is resolved by this wave. `T1-E3.06` remains the single open item: its **G2** finding (`@sprint-plan` commits a mutually-overlapping set without foreshadowing the wave count) is now proven three times over by this sprint — every one of Sprint 8's three plans required its predecessor to be archived before it could be claimed. Its **G1** finding (`.devops/sprints/**` outside `check-utf8-agents.ps1`) remains open and unowned by this parcel, correctly. The **batch wrap-up itself was exercised live** for the first time in this wrap-up, on real data rather than fixtures — the feature shipped and was immediately used to ship itself.

---

## 📍 State & Gates (CACHE-ANCHORED — update ONLY this section at gate transitions)

> **Cache rule:** This is the **last section** in the file. Gate transitions mutate ONLY the rows below — phase content above AND the frozen **Plan Settings** block at the top stay byte-stable to preserve LLM prefix-cache hits. Every "Update Status" instruction in the halt points above means "edit this section".

| Metric | Value |
|---|---|
| **Status** | `COMPLETE` |
| **Version** | `v0.1.0` |
| **Active Persona** | `Executor` |
| **Depends On** | `T1-E3.03` — **satisfied** (archived) |
| **Blocks** | none |

> **Settings pointer:** `Mode` and `Agents` live in the frozen **Plan Settings** block at the TOP of this file — read them there before any phase. Never duplicate them here.

> Valid states: `QUEUED`, `CLAIMED`, `PHASE_1`, `PHASE_3`, `PHASE_5`, `PHASE_5_REVISION`, `PHASE_7`, `PHASE_8_FAILED`, `PHASE_9`, `COMPLETE`.

| Gate | Requirement | Status |
|---|---|---|
| A | Scope approved (Phases 1-3, + 3.5 in AUTO) | `APPROVED` (AUTO, 2026-09-16) |
| B | Spec & plan approved (Phases 4-5) | `APPROVED` (AUTO, 2026-09-16) |
| C | Peer reviews passed (Phases 6-7) | `N/A (SINGLE)` |
| D | Implementation verified (Phases 8-9) | `APPROVED` |

> **Gate D verdict recorded by the `parcel-sprint` batch host (2026-09-16).** Operator ruling: **APPROVED**. Recorded by the orchestrator, never by an executing agent. This was the **final** Gate D of Sprint 8; retirement then ran as the operator-invoked **batch wrap-up** this plan defines. Gate A `APPROVED (AUTO)`, Gate B `APPROVED (AUTO)`, Gate C `N/A (SINGLE)`.

> Gate flips: rows flip `OPEN` → `APPROVED`/`REJECTED` ONLY after the user's (or AUTO verification's) verdict, recorded by the orchestrator. Executing agents halt with their gate `OPEN`. Rejection routes: A -> `PHASE_1`; B/C -> `PHASE_5_REVISION`; D/rollback -> `PHASE_8_FAILED`. Never edit rows above this section for gate bookkeeping.

> **This section is the LAST section in the file. All gate bookkeeping happens here.**
