---
code: T1-E3.03
sprint: sprint-8-sprint-lifecycle-hardening
claim_status: COMPLETE
owner: "parcel-sprint (DeepSeek V4.1 Flash)"
claimed_at: "2026-09-16"
last_touch: "2026-09-16"
touches: [".devops/rules/plan-lifecycle.md", ".opencode/plans/base-context.md", ".devops/templates/base-context.template.md", ".devops/agents/**", ".devops/skills/sprint-run/SKILL.md", ".devops/skills/ptp-parcel-fast/SKILL.md", ".devops/skills/pass-the-parcel/SKILL.md", ".devops/skills/sprint-status/SKILL.md", ".devops/skills/sprint-close/SKILL.md", ".devops/skills/sprint-plan/SKILL.md", ".devops/templates/SPRINTS.template.md", ".devops/plans/template-plan.md", ".devops/sync-manifest.yaml", ".devops/logs/version-history.md", "HOW-TO.md"]
depends_on: []
---
# T1-E3.03 - `GATE_D_USER_APPROVAL` claim status + dependency predicate + fixpoint batch runner
## Theme-Epic: T1 - Parcel Pipeline Machinery, E3 - Concurrency & Sprint Lifecycle

> **Origin:** Sprint 7 operator ruling, codified. The batch runner's eligibility clause ("every `depends_on` present in `.devops/archive/`") contradicts the runner's own terminal state (it never archives; every plan stops at `PHASE_9`). Any sprint with an intra-queue dependency chain therefore executes only the dependency roots. This parcel makes the "executed to Gate D" state first-class and machine-checkable, replacing the per-sprint human ruling with a deterministic predicate.

## ⚙️ Plan Settings (FROZEN — set at plan start, read before any phase)

| Setting | Value | Meaning |
|---|---|---|
| **Mode** | `AUTO` | `USER-MANAGED` (every gate halts for the user) or `AUTO` (orchestrator auto-clears Gates A-C; Gate D always halts) |
| **Agents** | `SINGLE` | `MULTI` (comprehensive — full `ptp-*` delegation, 4 gates) or `SINGLE` (fast — inline personas, Group C skipped, Gates B+C merged at Gate B, Gate C `N/A`) |

> **Preset provenance (written at claim time by `ptp-parcel-fast`, step 1 of its chain).** Both values come from the **`parcel-sprint` locked preset** — its `## Orchestrator Presets` row reads `Mode = AUTO`, `Agents = per-plan SINGLE`. The selection questions were therefore **skipped**, not answered: this plan is wave 2 of 3 in Sprint 8, executed by the batch path (`@sprint-run` → `ptp-parcel-fast`). `AUTO` auto-clears Gates A/B on mechanical verification (Gate C is `N/A` under `SINGLE`); **Gate D always halts for the human**. Frozen — never edited after this point.

> **Frozen config — read before executing ANY phase.** These two settings govern the entire pipeline and are never edited after plan start; they sit at the TOP so no session can miss them. Mutable runtime state (Status / Active Persona / gates) lives ONLY in the cache-anchored `## 📍 State & Gates` section at the bottom. See `@pass-the-parcel` § Agent Topology. **Preset provenance:** the orchestrator agent may set these from a **locked preset** (e.g. `parcel-sprint` locks `AUTO` + a per-plan `SINGLE`); when it does, the selection question is skipped and the preset values are recorded here.

> **Skill Architecture:** This template is consumed by the `pass-the-parcel` skill. Each phase delegates to a specialized sub-skill. See the parcel skill's Skill Delegation Map.
>
> **RULES:**
> - **Claim front-matter** at the very top (`code` / `sprint` / `claim_status` / `owner` / `claimed_at` / `last_touch` / `touches` / `depends_on`) governs claiming. See `.devops/rules/plan-lifecycle.md` § Claim Protocol. `claim_status` is NOT the bottom `Status` (pipeline phase).
> - Full skeleton required - ALL 10 phases + Wrap Up MUST be present.
> - Halt points are HARD STOPS - each gate blocks all subsequent phases.
> - Persona matches Status - use the State Lifecycle table in pass-the-parcel skill.
> - Read **Plan Settings** above before acting; the topology there governs which gates apply. See `@pass-the-parcel` § Agent Topology.
>
> **🔒 CACHE-ANCHORED:** The **Plan Settings** block above is frozen config; the mutable State Dashboard + Gate Log live in the **last section** (`## 📍 State & Gates`). Gate transitions update ONLY those bottom rows — do NOT edit content above once written. Byte-stable prefix = LLM prefix-cache hits for every downstream agent re-read.

---

## 1 Phase 1: Expansion & Scoping
**Skill Executed:** `ptp-context-hunter`

**Intent:**
Make "executed to Gate D, verdict pending" a first-class, machine-checkable plan state so a sprint's intra-queue dependency chains execute correctly — in a batch or in the manual sequential flow — without a per-sprint operator ruling.

The defect: `@sprint-run` § 2 clause 2 requires every `depends_on` code to be present in `.devops/archive/`, but the batch path forbids archiving (every plan stops at `PHASE_9`, `claim_status: CLAIMED`). The only state a batch can produce for a dependency is the state the predicate refuses. Chains therefore execute only their roots. This is codified (not merely missing) in `sprint-run:43,73`, `parcel-sprint.agent.md:145`, and `plan-lifecycle.md:35,72`. Sprint 7's operator ruling ("a batched `PHASE_9` dependency counts as satisfied") overrides the spec at runtime; this parcel bakes the ruling into the state machine.

**In Scope:**

**D1 — New claim status.** Add `GATE_D_USER_APPROVAL` to the canonical claim-status enum and retire the dead `IN_PROGRESS` value (nothing in the repo ever sets it). The enum becomes `QUEUED | CLAIMED | GATE_D_USER_APPROVAL | COMPLETE`, where `CLAIMED` remains the in-flight window (Phases 1→8) and the bottom `Status` stays `PHASE_9` with Gate D `OPEN`. **General, not batch-only:** every path that reaches Phase 9 sets it — the orchestrator/`ptp-code-surgeon` Gate D halt and `ptp-parcel-fast` step 8 — so the manual sequential sprint flow (`plan reaches Gate D → stays in plans → next plan picked up`) works too.

**D2 — Dependency predicate.** A `depends_on` code is **satisfied** when it is either present in `.devops/archive/` **or** present in `.devops/plans/` with `claim_status: GATE_D_USER_APPROVAL`. The disjunction makes the two wrap-up paths compose for free: a manually wrapped plan matches the archive clause; a batched plan matches the status clause. A `GATE_D_USER_APPROVAL` plan that later fails Gate D reverts to `CLAIMED`, so a re-evaluation correctly blocks dependents again.

**D3 — Fixpoint runner.** `@sprint-run` iterates: each pass re-applies the predicate to the remaining `QUEUED` set (queue order) and claims the next eligible plan, until no remaining plan is eligible. Properties to pin: **bounded** (each iteration claims a plan or stops; ≤ N), **deterministic** (queue order), **cycle-safe** (mutual `depends_on` leaves neither eligible → terminate + flag as a queue defect). Preflight gains a **dependency-cycle check** and a **resolution forecast** (dry-run the loop; show the exact claim order and terminal skip set) labelled a forecast.

**D4 — Both layers, one predicate.** The host (`sprint-run` § 2) and the per-plan runner (`ptp-parcel-fast` entry check, `SKILL.md:46`) must apply the *same* satisfaction rule. Divergence produces a claim-then-skip orphan: the host claims under the relaxed rule, the subagent immediately `SKIP`s, leaving a `CLAIMED` plan that preflight then misclassifies as an orphan.

**D5 — One state machine, five mirrors.** Mirror the status + predicate into: `plan-lifecycle.md` (enum, lifecycle flow, `depends_on` definition `:35`, Claim Protocol `:72`, Deviations `#1`), the `base-context.md` shared prefix (`:31` lifecycle, `:63` claim rule → `-Sync` → every locked agent) + `base-context.template.md`, `pass-the-parcel/SKILL.md` (state table `:33-46`, eligibility `:203`, § Batch Runner), `sprint-run` (v1→2), `ptp-parcel-fast` (v1→2), `sprint-status` (`:28`, `:57`), `sprint-plan`, `parcel-sprint.agent.md` (`:145` + preview), `SPRINTS.template.md:45`, `template-plan.md:4`, `HOW-TO.md:67`.

**Out of Scope:**
- **Batch sprint wrap-up** — a separate, dependent parcel, T1-E3.04 (this one only makes the state resolvable).
- Auto-clearing Gate D, advancing a plan past `PHASE_9`, or auto-archiving.
- Relaxing the `touches` predicate, queue order, or worktree isolation.
- Retiring `CLAIMED` or re-splitting it into `CLAIMED` + `IN_PROGRESS`.
- Rewriting historical plans, archives, or logs (`.devops/archive/**`, `.devops/logs/agent-changelog.md`).
- Per-plan `@agent-wrap-up` behaviour (unchanged; it still archives on the Gate D verdict).

**Perimeter Notes:**
- **Prefix lock.** `base-context.md` is the canonical shared prefix; the agent copies are derived. Edit `base-context.md` + `base-context.template.md`, then `scripts/check-parcel-prefix.ps1 -Sync` re-inlines byte-for-byte into every `parcel*` / `ptp-*` agent. Run `check-parcel-prefix.ps1` and `check-utf8-agents.ps1` before any push.
- **CI hard gate — read the LIVE value, never this file's number.** Wave 1 (`T1-E3.05`) landed as machinery `45`, so this wave bumps **`45 → 46`** in `.devops/sync-manifest.yaml` **and** records the literal `machinery-version: 46` in `.devops/logs/version-history.md`, or `validate.yml` "Machinery version discipline" fails. The same live-read rule applies to every per-file `version:` in the Phase 2 table (as-written values there are wave-1-stale).
- **Composes with per-plan wrap-up.** Because satisfaction is a disjunction, a sprint that manually wraps some plans and batches the rest needs no special handling.
- **Transitional readers.** Any tool that branches on `claim_status` (`sprint-status`, `sprint-close`) must recognise the new value, or it will silently mis-report an executed plan as in-flight.

---

## 2 Phase 2: Requirements & Context
**Skill Executed:** `ptp-context-hunter`

**Forensic Context Inventory:**
> [x] Wiki core docs read (`.wiki/core/00-system-index.md` + drilled into relevant docs)
> [x] Knowledge capture read (`.wiki/core/18-knowledge-capture.md`)
> [x] Source code verified (machinery files in blast radius)

**Relevant Existing Decisions (from knowledge capture):**
- **One state machine, five mirrors** — the 4-gate model is stated identically in the orchestrator skill, `base-context`, parcel agents, template, and rules doc; a batch deviation must be mirrored, never silently invented.
- **`claim_status` is NOT the pipeline `Status`** — claim state (top front-matter) vs phase (bottom State & Gates). The new status lives in the former; `Status` stays `PHASE_9`.
- **Never edit PREFIX-LOCKED surfaces directly** — edit `base-context.md`, then `-Sync` (`AGENTS.md` rule 9).
- **Set-level agent versioning** — `machinery-version` in `sync-manifest.yaml`; skills carry per-file `version:`.
- **Claim before edits** — `touches` overlap is the concurrency-safety predicate; unchanged here.

**Relevant Docs Found:**
| Doc | What it governs here |
|---|---|
| `.devops/rules/plan-lifecycle.md` | Canonical enum `:25`, `depends_on` definition `:35`, Claim Protocol `:72`, Deviations `:95-103` |
| `.opencode/plans/base-context.md` | Shared prefix: lifecycle `:31`, claim rule `:63`, batch delegation row `:77` |
| `.devops/skills/pass-the-parcel/SKILL.md` | State Lifecycle table `:33-46`, eligibility `:203`, § Batch Runner `:154-162` |
| `.devops/skills/sprint-run/SKILL.md` | Eligibility `:28-47` (the archive clause `:35`), terminal state `:59`, reason taxonomy `:73,80` |
| `.devops/skills/ptp-parcel-fast/SKILL.md` | Chain step 8 `:27`, entry outcome map `:46` |
| `.devops/skills/sprint-status/SKILL.md` | State detection `:28`, blocked-queue rule `:57` |
| `.devops/skills/sprint-plan/SKILL.md` | Queue authoring `:106-107` |
| `.devops/agents/parcel-sprint.agent.md` | Hard rule `:145`, preview `:128-133` |
| `HOW-TO.md:67`, `.devops/templates/SPRINTS.template.md:45` | Batch-run descriptions |
| `.devops/plans/template-plan.md:4` | Claim-status enum comment in the scaffold |

**Relevant Code Found:**
*Edited (~14 files + derived agent copies):*

| # | File | Change |
|---|---|---|
| 1 | `.devops/rules/plan-lifecycle.md` | Enum `:25`; `depends_on` def `:35`; Claim Protocol `:72`; Deviations `#1` `:99`; lifecycle flow `:54` |
| 2 | `.opencode/plans/base-context.md` | Prefix `:31`/`:63`/`:77` — then `-Sync` |
| 3 | `.devops/templates/base-context.template.md` | Seed mirror `:42`/`:75`/`:90` |
| 4 | `.devops/skills/sprint-run/SKILL.md` | § 2 clause 2 + within-batch bullet `:43`; fixpoint loop; § 1 cycle check + forecast; § 4 terminal; § 5/§ 6 reason table; `version: 1 → 2` |
| 5 | `.devops/skills/ptp-parcel-fast/SKILL.md` | Step 8 `:27` sets the status; entry check `:46` uses the new predicate; `version: 1 → 2` |
| 6 | `.devops/skills/pass-the-parcel/SKILL.md` | State table `:33-46` + flow; `:203`; § Batch Runner `:159`; `version: 10 → 11` |
| 7 | `.devops/skills/sprint-status/SKILL.md` | `:28` recognise the new status; `:57` treat it as satisfied |
| 8 | `.devops/skills/sprint-plan/SKILL.md` | Note that queue order no longer gates execution (fixpoint) |
| 9 | `.devops/agents/parcel-sprint.agent.md` | `:145` hard rule; preview `:128-133` |
| 10 | `.devops/templates/SPRINTS.template.md` | `:45` batch row |
| 11 | `.devops/plans/template-plan.md` | `:4` enum comment; `:310` valid-states line |
| 12 | `HOW-TO.md` | `:67` batch description |
| 13 | `.devops/sync-manifest.yaml` | `machinery-version: 43 → 44` |
| 14 | `.devops/logs/version-history.md` | release row recording literal `machinery-version: 44` (**CI gate**) |
| — | `.devops/agents/ptp-*.subagent.md`, `parcel*.agent.md` | **derived** — regenerated by `check-parcel-prefix.ps1 -Sync`, not hand-edited |

*Read-only:* `scripts/check-parcel-prefix.ps1`, `scripts/check-utf8-agents.ps1`, `.github/workflows/validate.yml`.

**Live-fact reconciliation (Phase 2 re-verification at wave 2, 2026-09-16).** This plan was authored before wave 1 (`T1-E3.05`) ran, so its recorded line numbers and versions are stale by construction. Every target was re-located **by content** against the live tree before any edit; the deltas:

| Recorded in this plan | LIVE at wave 2 | Consequence |
|---|---|---|
| `machinery-version: 43 -> 44` | **`45`** (`sync-manifest.yaml:23`) | Bump **`45 -> 46`**; record the literal `46`. |
| `pass-the-parcel` `v10 -> v11` | **`v14`** (`SKILL.md:4`) | Bump **`14 -> 15`**. |
| `sprint-run` `v1 -> v2` | **`v1`** | As written — `1 -> 2`. |
| `ptp-parcel-fast` `v1 -> v2` | **`v1`** | As written — `1 -> 2`. |
| 10 prefix-locked agents (incl. `parcel-fast.agent.md`) | **9** — `parcel.agent.md` (2 orchestrators) + 7 `ptp-*.subagent.md`; `parcel-fast.agent.md` **deleted**, its registry row + presets row + `opencode.json`/seed entries removed together | `-Sync` reports `PASS ×9`; registry is **11** rows (was 12); presets table is **2** rows (`parcel` = `ask`, `parcel-sprint` = locked batch host). |
| `base-context.md:31` / `:63` / `:77` | shifted | The lifecycle line, the claim rule, and the batch delegation row are found by **content match**, never by line number. |
| `parcel-sprint.agent.md:145` hard rule / `:128-133` preview | shifted (hard rules now `:142-145`, preview `:126-131`) | Same content-match rule; the preview and hard-rule bullets are hand-edited unique content **below** the inlined prefix, so `-Sync` preserves them. |
| `.devops/rules/process-lessons.md` untouched by this plan | gained a fold-target annotation on the `touches`-overlap lesson pointing at **`T1-E3.06 G2`** | If the `sprint-plan` edit grazes that area it must **point at** the annotation, not restate the lesson (no duplicate source of truth). |

> **Blast-radius bound:** 15 hand-edited files (the Phase 2 table's 14 plus `.devops/skills/sprint-close/SKILL.md`, added under `CW3`) + the derived agent copies regenerated by `-Sync`. Under the skill's 20-file bound; no scope narrowing needed at Gate A.

---

## 3 Phase 3: User Clarification
**Phase 3 Q&A (resolved interactively with the user before instantiation):**
> [x] **Q1 — Name and enum shape.** What is the new claim status called, and does it replace the dead `IN_PROGRESS`?
>    - `(Recommended)` **`GATE_D_USER_APPROVAL`**, replacing `IN_PROGRESS` → enum `QUEUED | CLAIMED | GATE_D_USER_APPROVAL | COMPLETE`.
>    - `GATE_D_PENDING` / `AWAITING_VERDICT`, or keep `IN_PROGRESS` for a post-rejection rework state.
>    - **Resolved:** `GATE_D_USER_APPROVAL`; `IN_PROGRESS` retired (nothing sets it).
> [x] **Q2 — Scope: general or batch-only.** Does only `@sprint-run` set/accept the state, or every flow that reaches Phase 9?
>    - `(Recommended)` **General** — the manual sequential sprint flow ("plan reaches Gate D → stays in plans → next plan picked up") needs it too.
>    - **Batch-only** — keeps a carve-out; leaves the manual flow broken.
>    - **Resolved:** general.
> [x] **Q3 — Fixpoint vs queue order.** The runner is single-pass and never reorders, so a dependent listed above its dependency is skipped forever.
>    - `(Recommended)` **Iterate to a fixpoint** — bounded, deterministic (queue order), cycle-safe.
>    - **Single-pass + validate topological order in `@sprint-plan`**.
>    - **Resolved:** fixpoint loop, with a preflight cycle check + resolution forecast.
> [x] **Q4 — Packaging.**
>    - `(Recommended)` **Two sequenced parcels** — T1-E3.03 (this: status + predicate + runner) then T1-E3.04 (batch wrap-up).
>    - **One parcel** — contract change across the prefix lock, ~8 skills, agents, seeds.
>    - **Resolved:** two; E3.04 depends on E3.03.
> [x] **Q5 — Composition with manual wrap-up.** A sprint may wrap some plans individually (archiving them) and batch the rest.
>    - `(Recommended)` **Disjunctive satisfaction** — archived **or** `GATE_D_USER_APPROVAL`; both paths compose with no mode flag.
>    - **Status-only** — breaks the manual path.
>    - **Resolved:** disjunctive.
> [x] Final validation: "Is this all the context required?" -> Yes.

**Phase 3.5 Research Map (AUTO mode — input contract for `ptp-phase3-answerer`):**

| Q# | Core Docs | Code Files | KC Entries |
|---|---|---|---|
| Q1 | `.devops/rules/plan-lifecycle.md` (enum), `pass-the-parcel` § State Lifecycle | 6 files carrying the enum: `plan-lifecycle.md`, `template-plan.md:4`, `sprint-status/SKILL.md:28`, `base-context.md`, `base-context.template.md`, `t1-parcel-pipeline-machinery-backlog.md` | KC: 2026-09-07 One state machine, five mirrors |
| Q2 | `.devops/rules/plan-lifecycle.md` § Deviations, § Claim Protocol | `plan-lifecycle.md:76`, `sprint-run/SKILL.md:59`, `ptp-parcel-fast.subagent.md` step 8 | KC: claim-before-edits / `claim_status` ≠ pipeline `Status` |
| Q3 | `sprint-run/SKILL.md` § 2, § 3, § 6 | `sprint-run/SKILL.md:30,43,45`, `sprint-8-sprint-lifecycle-hardening/sprint.md:48-62` | — (Sprint 8 Delivery Model is the evidence) |
| Q4 | `plan-lifecycle.md` § Claim Front-Matter | plan front-matter of `t1-e3.03` / `t1-e3.04` | KC: two sequenced parcels (Phase 3 Q4) |
| Q5 | `.devops/skills/agent-wrap-up/SKILL.md`, `sprint-close/SKILL.md` § 1 | `sprint-close/SKILL.md:21-22` | — |

**Phase 3.5 — AUTO auto-resolutions (`ptp-phase3-answerer`, inline):**

All five Phase 3 questions were **already resolved interactively before instantiation** (recorded above). Phase 3.5 therefore re-derives each resolution from mapped sources as a mechanical confirmation — no question is left pending and none is raised new.

| Q | Auto-resolution | Rationale | Source |
|---|---|---|---|
| Q1 | **`GATE_D_USER_APPROVAL` replaces `IN_PROGRESS`** → enum `QUEUED \| CLAIMED \| GATE_D_USER_APPROVAL \| COMPLETE`. | The live enum carries `IN_PROGRESS` in exactly two non-historical places (`plan-lifecycle.md:25`, `template-plan.md:4`) and **no** writer anywhere in the tree ever sets it — a dead value the ruling retires. | `grep -rn IN_PROGRESS` (live tree); `.devops/rules/plan-lifecycle.md:25`; `.devops/plans/template-plan.md:4` |
| Q2 | **General, not batch-only** — every path reaching Phase 9 sets it. | `plan-lifecycle.md:76` already records the batch terminal state; the manual flow ("plan reaches Gate D → stays in plans → next plan picked up") has the same shape, so a batch-only carve-out would leave the manual path blocked by its own terminal state. | `.devops/rules/plan-lifecycle.md:72-76`; KC 2026-09-07 mirror rule |
| Q3 | **Fixpoint loop**, bounded + deterministic + cycle-safe, with a preflight cycle check and a resolution forecast. | `sprint-run/SKILL.md:43` currently says "skipped, never reordered", which is *why* a chain executes only its roots; Sprint 8's Delivery Model resolves the same defect with three serial waves and states the ~N-pass cost explicitly. | `.devops/skills/sprint-run/SKILL.md:30-47`; `.devops/sprints/sprint-8-sprint-lifecycle-hardening/sprint.md:46-62` |
| Q4 | **Two sequenced parcels** — this one (`T1-E3.03`) then `T1-E3.04`. | `T1-E3.04` already sits `QUEUED` with `depends_on: ["T1-E3.03"]`; splitting keeps the contract change reviewable and gives the state a dependent to unblock. | `.devops/sprints/sprint-8-.../t1-e3.04-sprint-batch-wrap-up-plan.md:9` |
| Q5 | **Disjunctive satisfaction** — archived **or** `GATE_D_USER_APPROVAL`. | `plan-lifecycle.md:76` keeps per-plan `@agent-wrap-up` (archive) valid and composes it with the batch terminal state; a status-only rule would break the manual path. | `.devops/rules/plan-lifecycle.md:76`; `ptp-parcel-fast/SKILL.md` step 8 |

> **No `Unresolvable:` entries.** Every resolution is quotable from a mapped source.

**Test Proposals (TDD) — proposed by `ptp-context-hunter`, validated by `ptp-phase3-answerer`:**

| # | Probe | Steps | Expected |
|---|---|---|---|
| T1 | **Predicate accepts both wrappers** | (a) pick a code present in `.devops/archive/`; (b) stage a fixture plan in `.devops/plans/` with `claim_status: GATE_D_USER_APPROVAL` | Both satisfy `depends_on`; an in-flight `CLAIMED` plan below `PHASE_9` does **not** |
| T2 | **Revert re-blocks** | Flip the (b) fixture back to `claim_status: CLAIMED` and re-apply the predicate | The dependent is blocked again |
| T3 | **Mirror coherence** | `grep` the retired enum token and the new token across the five mirror families | `IN_PROGRESS` survives only in historical records (`.devops/archive/**`, the backlog register row); `GATE_D_USER_APPROVAL` appears in every mirror |

**Status:** ACCEPTED — all three are feasible with the repo's existing shell tooling and match the Phase 9 verification contract.
**Validation:** Auto-Resolution: ACCEPTED — T1/T2 are pure file-state probes over the predicate as written; T3 is a mechanical grep over the stated mirror set.

**Architectural Conflict Warnings Raised:**
- **CW1 — The predicate contradicts the runner's terminal state.** `sprint-run:35` requires archive; `sprint-run:59` forbids archiving. Codified as intended in `sprint-run:43`, `parcel-sprint.agent.md:145`, `plan-lifecycle.md:35`. Blocking; resolved by D1/D2.
- **CW2 — Two-layer divergence.** The host and the per-plan runner each evaluate `depends_on`. A one-layer fix yields a claim-then-skip orphan loop. Resolved by D4 (single shared predicate).
- **CW3 — Transitional reader blindness.** `sprint-status:28` and `sprint-close:21` branch on `claim_status`; the new value must be recognised or an executed plan reports as in-flight / unfinished. Resolved by D5.
- **CW4 — Prefix re-inline reach.** The lifecycle line lives in the shared prefix (`base-context.md:31`), so the change is re-inlined into every locked agent. `-Sync` + both checks are mandatory, not optional.
- **CW5 — A plan's `touches` list must cover its readers.** The as-authored `touches` named `sprint-status` but **not** `sprint-close`, although `sprint-close:21-22` is the surface that would silently close the sprint over an unwrapped `GATE_D_USER_APPROVAL` plan. Resolved by adding `.devops/skills/sprint-close/SKILL.md` to `touches` before execution (serial wave, no concurrency risk), so the declared write set equals the actual one.

---

> **HALT POINT (Gate A — Scope):** Phases 1-3 complete (+ Phase 3.5 auto-resolutions in AUTO mode). Set Status -> `PHASE_3`, Active Persona -> `Scoper`. Present the scope perimeter + Phase 3 Q&A record; the user approves the scope before planning begins. On rejection: Status -> `PHASE_1`, Gate A -> `REJECTED`, re-run the affected questions. Questions are relayed to the user **one at a time** via the ask-questions tool — never batched.

> **AUTO auto-clear — Gate A: CLEARED.** Mechanical verification: (a) Phase 1 In-Scope `D1`-`D5` and the Out-of-Scope perimeter are both populated, and the Perimeter Notes were reconciled to the live tree (version literal `45 -> 46`); (b) Phase 2's Forensic Context Inventory is fully ticked **and** re-verified at wave 2 — the 15-file blast radius is enumerated with its live-vs-as-authored deltas and stays under the 20-file bound; (c) Phase 3's five questions all carry recorded resolutions, Phase 3.5 supplies a Research Map plus a cited auto-resolution per question, the three TDD probes are `ACCEPTED`, and there are **zero `Unresolvable:` entries**; (d) no `REJECTED` verdict line exists anywhere in Phases 1-3. Gate A row in `## 📍 State & Gates` → `APPROVED (AUTO)`.

---

## 4 Phase 4: Wiki Requirements & Acceptance Criteria
**Skill Executed:** `ptp-high-visionary` (spec-first directives) + `@wiki-writer`

> **Spec-First Rule:** The wiki is written BEFORE the code. Docs written here describe target behavior and are marked `status: in-progress` — a pre-code doc is a claim, not truth. Promotion to `stable` happens only at Wrap Up, after the executor verifies code matches spec.
>
> **Conditional Skip (checklist):** Run Phase 4 only if ANY of: (a) new or changed user-visible strings/UI, (b) API, schema, or logic-contract change, (c) wiki-facing behavior change. If none apply, skip this phase and record: `No wiki delta — rationale: <why>`. Never skip silently.

**Wiki Docs to Write/Update (as `in-progress`):**
| Doc | Change |
|---|---|
| — none — | `No wiki delta — rationale: this is a machinery/process contract change, not app behaviour.` |

> **Conditional-skip check:** NOT skipped — condition (b) applies in full (a logic-contract change: the claim-status enum and the `depends_on` satisfaction rule). But the wiki proper (`.wiki/core/00-18`, `components/`, `features/`, `database/`, `logic/`) documents the **app**, and this template has no `src/`. The governed surfaces of this change are the **rules + skills + templates** layer: `.devops/rules/plan-lifecycle.md` is the canonical contract, `pass-the-parcel` owns the lifecycle, and `sprint-run` / `ptp-parcel-fast` implement the predicate. Machinery lessons route to `.devops/rules/process-lessons.md` at sprint close (not owned by this plan). No `in-progress` wiki doc is created, so there is nothing to promote to `stable` at Wrap Up.

**Acceptance Criteria:**
| # | Criterion (behavior) | Test Target |
|---|---|---|
| 1 | `claim_status: GATE_D_USER_APPROVAL` is recognised by every reader, and `IN_PROGRESS` has no live occurrence outside historical records. | `grep -rn "IN_PROGRESS"` excluding `.devops/archive/**`, `.devops/logs/**` → only the backlog register row (historical prose); `grep -rn "GATE_D_USER_APPROVAL"` → present in every mirror family |
| 2 | The dependency predicate is stated **once** and identically in the canonical contract and its mirrors: satisfied ⇔ code in `.devops/archive/` **or** code in `.devops/plans/` with `claim_status: GATE_D_USER_APPROVAL`; any other state is unmet. | content diff of the predicate sentence across `plan-lifecycle.md`, `base-context.md`, `base-context.template.md`, `pass-the-parcel`, `sprint-run`, `ptp-parcel-fast`, `sprint-status`, `HOW-TO.md` |
| 3 | The predicate and the `touches`-overlap clause are **distinguishable**: a satisfied dependency does not clear an overlap. | T1/T2 probes + the recorded `T1-E3.04` case (`depends_on` satisfied, still skipped for overlap) |
| 4 | A `GATE_D_USER_APPROVAL` plan that reverts to `CLAIMED` blocks its dependents again. | T2 probe (`claim_status` flip → predicate false) |
| 5 | `@sprint-run` iterates to a fixpoint; the loop is bounded, deterministic (queue order), and cycle-safe, with a preflight cycle check + resolution forecast. | read-back of `sprint-run` § 1-§ 3; § 6 skip-reason table lists the cycle reason |
| 6 | The batch terminal state is `claim_status: GATE_D_USER_APPROVAL` (never `CLAIMED`) in both layers. | `sprint-run` § 4 + `ptp-parcel-fast` step 8 + entry check read-back |
| 7 | The prefix lock is intact after the canonical edit: `base-context.md` edits are re-inlined byte-for-byte into the 9 locked agents and the seed agrees. | `powershell -NoProfile -File scripts/check-parcel-prefix.ps1` → exit `0` (`PASS ×9`, 11 bindings) |
| 8 | `machinery-version` is `46` and the literal `machinery-version: 46` appears in `.devops/logs/version-history.md`. | `validate.yml` "Machinery version discipline" predicate, reproduced locally |

**Spec Notes:**
- Data flow / state changes — **the canonical predicate (single source of truth, stated once in `.devops/rules/plan-lifecycle.md` § Claim Front-Matter and referenced elsewhere, never re-invented):**

  > A `depends_on` code is **satisfied** when it is either **(a)** present in `.devops/archive/` — the plan wrapped up and archived (the manual / per-plan path), **or (b)** present in `.devops/plans/` with `claim_status: GATE_D_USER_APPROVAL` — the plan executed through Phase 9 with Gate D `OPEN`, awaiting the human verdict (the batch path's terminal state, and equally reachable by the manual sequential sprint flow). **Any other state is unmet**: still `QUEUED` in the sprint queue, or `CLAIMED` in `.devops/plans/` below `PHASE_9` (in-flight).

  > **Clause separation (the two clauses are independent blockers).** The rule above is the **dependency** clause only. The `touches`-overlap clause is a *separate* predicate evaluated against the live `.devops/plans/`: a plan whose dependency has become satisfied can **still** be skipped because its dependency still occupies `.devops/plans/` with overlapping `touches` — a `GATE_D_USER_APPROVAL` plan holds its files until Gate D + wrap-up. `T1-E3.04` is the worked example: under this plan's predicate its `depends_on: ["T1-E3.03"]` becomes **satisfied** the moment E3.03 reaches `GATE_D_USER_APPROVAL`, and it is **still skipped** by the overlap clause (both declare `.devops/agents/**`, `base-context*`, `sync-manifest.yaml`, `version-history.md`, `HOW-TO.md`). A reader must be able to tell those two verdicts apart from the skip reason alone.

  - **Claim-status transition (state machine, orthogonal to the pipeline `Status`):** `QUEUED` → *(claim)* → `CLAIMED` (Phases 1→8) → *(Phase 9 verified)* → `GATE_D_USER_APPROVAL` (bottom `Status` stays `PHASE_9`, Gate D `OPEN`) → *(human verdict + `@agent-wrap-up`)* → `COMPLETE` (archived). Reverting GATE D sets the claim back to `CLAIMED` (or the revision route), which re-blocks dependents.
  - **Runner semantics:** `@sprint-run` evaluates the predicate immediately before **each** claim and **iterates**: each pass claims the next eligible plan in queue order, then re-applies the predicate to the remaining `QUEUED` set. Bounded (≤ N iterations; each pass claims one plan or stops), deterministic (queue order, never a topological sort), cycle-safe (a mutual `depends_on` leaves neither eligible → terminate and flag a queue defect).
- Edge cases the spec must cover: (i) dependency **in-flight** (`CLAIMED`, below `PHASE_9`) → unmet; (ii) dependency `GATE_D_USER_APPROVAL` → satisfied, but the overlap clause may still skip; (iii) **revert** after a failed Gate D → `CLAIMED` → blocked again; (iv) **cycle** in the queue → terminate + flag, never loop; (v) a plan already at `PHASE_9` at batch start → skipped as already-batched; (vi) `Owner`/`Status` divergence — `claim_status` is *not* the bottom `Status`; the terminal pair is `GATE_D_USER_APPROVAL` + `PHASE_9`.
- Docs consumed by this feature (read-first list): `.devops/rules/plan-lifecycle.md` (canonical contract), `.opencode/plans/base-context.md` + `.devops/templates/base-context.template.md` (shared prefix + seed), `.devops/skills/pass-the-parcel/SKILL.md` (lifecycle authority), `.devops/skills/sprint-run/SKILL.md` + `ptp-parcel-fast/SKILL.md` (both layers of the predicate), `.devops/skills/sprint-status/SKILL.md` + `sprint-close/SKILL.md` + `sprint-plan/SKILL.md` (transitional readers), `.devops/agents/parcel-sprint.agent.md` (host hard rules), `.devops/templates/SPRINTS.template.md`, `.devops/plans/template-plan.md`, `HOW-TO.md`, `.devops/sync-manifest.yaml` + `.devops/logs/version-history.md` (version discipline), `.wiki/core/18-knowledge-capture.md` § One state machine, five mirrors + `.devops/rules/process-lessons.md` (capture routing).

---

> **NO HALT after Phase 4:** the spec is reviewed together with the implementation plan at Gate B (after Phase 5) — one decision, what it will do and what it will cost.

---

## 5 Phase 5: High-Visionary Standard Implementation Plan
**Skill Executed:** `ptp-high-visionary`

**Simplicity Gate:**
> [x] Climbed the Simplicity Ladder (7 rungs) — the change is **one enum value + one predicate + one loop**, each stated once and referenced everywhere else. No new file, no new script, no new mechanism.
> [x] No speculative abstractions — no new validator, no status machine engine, no `depends_on` resolver module, no compatibility alias for `IN_PROGRESS`. The predicate is a sentence, not a program.
> [x] Reuse over reimpl — Reuse Log populated (existing enum slot, existing `-Sync` re-inline, existing per-claim re-evaluation, existing skip-reason table).
> [x] Deliberate simplifications marked with `ponytail:` comments — two, both named below with ceilings + upgrade paths (the prefix-overlap heuristic is reused as-is; the forecast is a dry-run description, not an executable planner).
> [x] Safety exceptions preserved — Gate D is never auto-cleared, never deferred past the human; the batch path's deferred verdict is unchanged; no validation or error handling is simplified.

**Reuse Log:**
| Existing Asset | Where Reused | Ladder Rung |
|---|---|---|
| The dead `IN_PROGRESS` enum slot (`plan-lifecycle.md:25`, `template-plan.md:4`) | Replaced in place by `GATE_D_USER_APPROVAL` — no enum growth | 2 |
| `.devops/skills/sprint-run/SKILL.md` § 2 per-claim re-evaluation | The fixpoint loop re-uses the same predicate each pass; the loop is the *iteration*, not a second rule | 2 |
| `sprint-run` § 2 normalization + prefix-overlap heuristic | Unchanged — the overlap clause keeps its existing `ponytail:` ceiling | 2 |
| `check-parcel-prefix.ps1 -Sync` | Re-inlines the edited shared prefix into the 9 locked agents — no hand-editing of derived copies | 2 |
| `sprint-run` § 6 skip-reason table + § 5 stop-the-line table | Extended with the two new reasons (unmet in-flight dependency; dependency cycle) rather than a new taxonomy | 2 |
| `.devops/logs/version-history.md` release-row convention | The `v0.7.16` row carries the `machinery-version: 46` literal the CI predicate greps for | 1 |

**Spaghetti Triage (See-Name-Route, Do Not Fix):**
- [x] No smells noticed in the edited lines. One adjacent observation is routed, not fixed (see table).

| File | Function | Smell Type | Severity | Recommended Action |
|---|---|---|---|---|
| `.devops/skills/sprint-run/SKILL.md` | `§ 2` + `§ 5` + `§ 6` restate the same predicate three times (rules / halt list / reason list) | duplication across sections | Low | Route only — already scheduled as self-consistent in this plan: the three sites are made to agree by the same wording, and `T1-E3.06 G2` owns the remaining `@sprint-plan` preflight gap. **Do NOT create a backlog entry.** |

> `ponytail:` **Deliberate simplifications (2).**
> 1. **Forecast is prose, not an executable planner.** The preflight "resolution forecast" dry-runs the loop *on paper* from the live queue and prints the expected claim order + terminal skip set. Ceiling: a forecast can lag reality if a plan's `touches` change mid-run (the predicate is re-evaluated per claim, so reality still governs). Upgrade path: an executable dry-run mode that simulates claims without writing.
> 2. **Cycle detection is queue-scoped, not repo-scoped.** The preflight checks for cycles among *still-unresolved queue members* (`QUEUED` in this sprint + their unresolved dependencies), which is the only cycle a batch can actually deadlock on. Ceiling: a cycle whose members are split across two sprints is not detected. Upgrade path: build the graph over `.devops/sprints/** + .devops/plans/**`.

### To-Do List
- [x] `D1` `.devops/rules/plan-lifecycle.md` — canonical enum, predicate definition, Claim Protocol, Deviations #1, lifecycle note
- [x] `D2` `.opencode/plans/base-context.md` — 3 shared-prefix edits (lifecycle line, claim rule, batch delegation row)
- [x] `D3` `.devops/templates/base-context.template.md` — the same 3 edits (seed mirror)
- [x] `D4` `powershell -NoProfile -File scripts/check-parcel-prefix.ps1 -Sync` — re-inline the prefix into all 9 locked agents
- [x] `D5` `.devops/skills/sprint-run/SKILL.md` (`v1 -> 2`) — predicate, fixpoint loop, cycle check + forecast, terminal state, reason tables
- [x] `D6` `.devops/skills/ptp-parcel-fast/SKILL.md` (`v1 -> 2`) — step 8 sets the status; entry check uses the same predicate
- [x] `D7` `.devops/skills/pass-the-parcel/SKILL.md` (`v14 -> 15`) — claim-status note on the state table, eligibility, § Batch Runner
- [x] `D8` `.devops/skills/sprint-status/SKILL.md` (`v2 -> 3`) — recognise `GATE_D_USER_APPROVAL`; blocked-queue rule
- [x] `D9` `.devops/skills/sprint-close/SKILL.md` (`v4 -> 5`) — never close over an unwrapped `GATE_D_USER_APPROVAL` plan
- [x] `D10` `.devops/skills/sprint-plan/SKILL.md` (`v3 -> 4`) — queue order no longer gates execution; pointer to `T1-E3.06 G2`
- [x] `D11` `.devops/agents/parcel-sprint.agent.md` — preview + hard-rule bullets (hand-edited unique content below the prefix)
- [x] `D12` `.devops/templates/SPRINTS.template.md` (`v3 -> 4`) — batch-run row
- [x] `D13` `.devops/plans/template-plan.md` — claim-status enum comment + the bottom valid-states line
- [x] `D14` `HOW-TO.md` — § Sprint Batch Runner description
- [x] `D15` `.devops/sync-manifest.yaml` — `machinery-version: 45 -> 46`
- [x] `D16` `.devops/logs/version-history.md` — prepend the `v0.7.16` row carrying the literal `machinery-version: 46`
- [x] `V` Verification — prefix, UTF-8, wiki lint, claims, the CI version predicate, and the T1/T2/T3 predicate behaviour probes

### File-Level Steps

**Ordering constraints (hard).** (1) `D1` lands **before** `D2`/`D3` — the mirrors quote the canonical wording. (2) `D2`+`D3` land **before** `D4` `-Sync`, which is the only step that writes the derived agent copies. (3) `D15`/`D16` land **together** — the CI predicate greps for the literal row. (4) `V` runs last; every command must exit `0`.

1. `D1` — `.devops/rules/plan-lifecycle.md`: claim-status enum comment, the `depends_on` definition bullet, a `GATE_D_USER_APPROVAL` bullet, the § Lifecycle note, Claim Protocol step 1 + step 5, Deviations #1 + the closing predicate paragraph.
2. `D2` — `.opencode/plans/base-context.md`: three phrase-exact edits (PTP Lifecycle block, Concurrency & Claims bullet, the batch delegation-map row).
3. `D3` — `.devops/templates/base-context.template.md`: the same three edits, keeping the seed cell-for-cell identical to the live prefix.
4. `D4` — `powershell -NoProfile -File scripts/check-parcel-prefix.ps1 -Sync` (9 × `PASS`, 11 bindings, exit `0`).
5. `D5` — `.devops/skills/sprint-run/SKILL.md`: § 1 item 6 gains the cycle check + forecast; § 2 clause 2 becomes the disjunctive predicate; the within-batch bullet is re-stated; a **Fixpoint loop** subsection is added; § 4 terminal state; § 5/§ 6 reason tables; `version: 1 -> 2`.
6. `D6` — `.devops/skills/ptp-parcel-fast/SKILL.md`: chain step 8, the entry-check outcome line, `version: 1 -> 2`.
7. `D7` — `.devops/skills/pass-the-parcel/SKILL.md`: claim-status paragraph after the state table, § 1.2 eligibility, § Batch Runner bullets, `version: 14 -> 15`.
8. `D8` — `.devops/skills/sprint-status/SKILL.md`: § 3 state detection + § 4 readout row + § 5 blocked-queue rule, `version: 2 -> 3`.
9. `D9` — `.devops/skills/sprint-close/SKILL.md`: § 1 completion check, `version: 4 -> 5`.
10. `D10` — `.devops/skills/sprint-plan/SKILL.md`: § 6 queue-order note, `version: 3 -> 4`.
11. `D11` — `.devops/agents/parcel-sprint.agent.md`: the preview bullets + the hard-rule bullets.
12. `D12` — `.devops/templates/SPRINTS.template.md`: the `@sprint-run` row, `version: 3 -> 4`.
13. `D13` — `.devops/plans/template-plan.md`: line 4 claim_status comment + the bottom valid-states line.
14. `D14` — `HOW-TO.md`: § `#### Sprint Batch Runner (@sprint-run)` paragraph.
15. `D15` + `D16` — `.devops/sync-manifest.yaml` and `.devops/logs/version-history.md`.
16. `V` — run every gate; restart a failing gate at most twice, then roll back (`PHASE_8_FAILED`).
17. Commit `plan: T1-E3.03` on the trunk.

**`D1` — `.devops/rules/plan-lifecycle.md` (canonical contract; write it here first).** Six edits:
- § Claim Front-Matter code block: the enum comment becomes the exact literal `claim_status: QUEUED        # QUEUED | CLAIMED | GATE_D_USER_APPROVAL | COMPLETE`.
- § Claim Front-Matter bullets: add a `claim_status` values bullet naming `QUEUED` / `CLAIMED` (claimed; Phases 1→8) / `GATE_D_USER_APPROVAL` (executed through Phase 9, Gate D `OPEN`, awaiting the human verdict) / `COMPLETE` (archived), and stating that `IN_PROGRESS` is **retired** (nothing sets it).
- § Claim Front-Matter: replace the `depends_on` bullet with the **canonical predicate** — satisfied iff the code is in `.devops/archive/` **or** in `.devops/plans/` with `claim_status: GATE_D_USER_APPROVAL`; any other state is unmet. Add the clause-separation sentence: this is the dependency rule only, and the `touches`-overlap check is a separate, independent blocker that a satisfied dependency does **not** clear.
- § Lifecycle: note that the claim status becomes `GATE_D_USER_APPROVAL` at `PHASE_9` (Gate D `OPEN`) and only a Gate D verdict + wrap-up turns it `COMPLETE`.
- § Claim Protocol step 1: drop "(every dependency present in `.devops/archive/`)" and reference § Claim Front-Matter's predicate instead of restating it; keep the per-claim re-evaluation sentence and add that the batch path **iterates to a fixpoint**. Step 5: the completion move is `CLAIMED`/`GATE_D_USER_APPROVAL` → `COMPLETE`.
- § Deviations #1: "stays `claim_status: CLAIMED` in `.devops/plans/`" becomes "stays in `.devops/plans/` with `claim_status: GATE_D_USER_APPROVAL` (Gate D `OPEN`)". Closing paragraph: the eligible-set predicate is the § Claim Front-Matter predicate, evaluated immediately before each claim and re-applied until no remaining queued plan is eligible.

**`D2`/`D3` — the shared prefix, canonical then seed (`.opencode/plans/base-context.md`, then `.devops/templates/base-context.template.md`).** Three phrase-exact edits each:
- § PTP Lifecycle: after the flow line, state that the `claim_status` at `PHASE_9` is `GATE_D_USER_APPROVAL` (executed, Gate D `OPEN`) — the batch path's terminal state, which never archives on its own.
- § Concurrency & Claims: the claim bullet's "no unmet `depends_on`" clause gains the parenthetical `(in `.devops/archive/` **or** in `.devops/plans/` with `claim_status: GATE_D_USER_APPROVAL`)`; the `touches`-overlap clause stays a separate blocker.
- § PTP Delegation Map: the `ptp-parcel-fast` row's Output cell becomes `One plan run Phases 1-9 -> `PHASE_9` (`claim_status: GATE_D_USER_APPROVAL`), Gate D `OPEN``.
- Seed only: the file's HTML comment header `version: 13 -> 14`.

**`D4` — Prefix re-inline.** `powershell -NoProfile -File scripts/check-parcel-prefix.ps1 -Sync`. Expected: `FIXED`/`PASS` ×9 (2 orchestrators + 7 `ptp-*`), `MODEL` ×11, `SEED-OC-MODEL` ×11, `OC-MODEL` ×11, exit `0`. Any `FAILURES:` block is a hard stop. **Never hand-edit an inlined prefix.**

**`D5` — `.devops/skills/sprint-run/SKILL.md` (`version: 1 -> 2`).** Four edits:
- § 1 item 6 (informed run preview): add two bullets — a **dependency-cycle check** over the still-unresolved queue set (a cycle → flag as a queue defect, terminate, record; never loop), and a **resolution forecast** (dry-run the loop, show the exact claim order and the terminal skip set; keep the existing "label it a forecast" instruction).
- § 2 clause 2: replace "Every code in its `depends_on` is present in `.devops/archive/`" with the canonical predicate (archive **or** `.devops/plans/` + `GATE_D_USER_APPROVAL`).
- § 2 "Within-batch `depends_on`": re-state — a dependency claimed earlier in this batch that has since reached `PHASE_9` / `GATE_D_USER_APPROVAL` is **satisfied**; one that is still in-flight (`CLAIMED`, below `PHASE_9`) is **unmet** → skip with reason, never reorder.
- § 2: add a **Fixpoint loop** subsection — each pass claims the next eligible plan in queue order, then re-applies the predicate to the remaining `QUEUED` set; properties: bounded (≤ N passes), deterministic (queue order, no topological sort), cycle-safe. § 3 gains the "re-apply § 2 before the next claim" line. § 4 terminal state becomes `claim_status: GATE_D_USER_APPROVAL` (never `CLAIMED`). § 5/§ 6 reason rows: `unmet depends_on (dependency still QUEUED or in-flight CLAIMED)` and `dependency cycle (queue defect)`; the "not a halt" line keeps `touches` overlap as an independent skip, including against a `GATE_D_USER_APPROVAL` plan.

**`D6` — `.devops/skills/ptp-parcel-fast/SKILL.md` (`version: 1 -> 2`).** Two edits, keeping the two layers byte-consistent: chain **step 8** sets `claim_status: GATE_D_USER_APPROVAL` (not `CLAIMED`) with bottom `Status: PHASE_9` and Gate D `OPEN`; the outcome-map entry becomes "a `depends_on` code unmet at entry — neither in `.devops/archive/` nor in `.devops/plans/` with `claim_status: GATE_D_USER_APPROVAL` — → `SKIP <code>: unmet depends_on`".

**`D7` — `.devops/skills/pass-the-parcel/SKILL.md` (`version: 14 -> 15`).** Three edits: a claim-status paragraph after the state table (the four values, the `PHASE_9` transition, `IN_PROGRESS` retired, and a pointer to the lifecycle doc for the dependency rule); § 1.2 eligibility → the disjunctive predicate; § Batch Runner → bullets for the `GATE_D_USER_APPROVAL` terminal claim status and the fixpoint loop. Add the `GATE_D_USER_APPROVAL` value to the bottom valid-states enumeration wherever the file lists one.

**`D8` — `.devops/skills/sprint-status/SKILL.md` (`version: 2 -> 3`).** § 3 step 2 reads `claim_status` `CLAIMED` **or** `GATE_D_USER_APPROVAL`, and maps the latter to "executed — awaiting the Gate D verdict" rather than in-flight; § 4 gains that readout state; § 5's blocked-queue rule becomes "a `QUEUED` plan whose `depends_on` codes are neither archived nor `GATE_D_USER_APPROVAL` is blocked".

**`D9` — `.devops/skills/sprint-close/SKILL.md` (`version: 4 -> 5`).** § 1 must treat a plan in `.devops/plans/` with `claim_status: GATE_D_USER_APPROVAL` as **not complete** — it needs the Gate D verdict and a wrap-up (or an explicit carry-forward entry) before the sprint may close; never close over it silently.

**`D10` — `.devops/skills/sprint-plan/SKILL.md` (`version: 3 -> 4`).** § 6 gains one note: the `#` column is the **claim order for the first pass**, not an execution dependency — `@sprint-run` iterates to a fixpoint, so a dependent listed above its dependency is still reached. Point at the *existing* `touches`-overlap lesson in `.devops/rules/process-lessons.md` and its fold target `T1-E3.06 G2` **without restating the lesson**.

**`D11` — `.devops/agents/parcel-sprint.agent.md` (hand-edited unique content only, below the inlined prefix).** § `Informed run preview` bullets gain the dependency-cycle check and the resolution forecast; § `Hard rules`: a pre-existing `PHASE_9` plan is a skip; an unmet `depends_on` (dependency still `QUEUED` or in-flight `CLAIMED`) is a skip **with reason**, while a dependency that has reached `GATE_D_USER_APPROVAL` is satisfied — and a `touches` overlap still skips independently, because a satisfied dependency does not clear an overlap. Do **not** touch the prefix above; `-Sync` owns it.

**`D12` — `.devops/templates/SPRINTS.template.md` (`version: 3 -> 4`).** The `@sprint-run` row: plans terminate at `PHASE_9` with `claim_status: GATE_D_USER_APPROVAL` and Gate D `OPEN`; the runner iterates to a fixpoint; one consolidated verdict covers the batch.

**`D13` — `.devops/plans/template-plan.md`.** Line 4's claim_status comment becomes the new enum literal; the bottom `> Valid states:` line gains `GATE_D_USER_APPROVAL` (after `CLAIMED`) with a short note that it is a **claim-status** value whose bottom `Status` stays `PHASE_9`.

**`D14` — `HOW-TO.md` § `#### Sprint Batch Runner (@sprint-run)`.** Record the predicate split (dependency satisfied by archive **or** `GATE_D_USER_APPROVAL`; `touches` overlap still independent), the `PHASE_9` + `GATE_D_USER_APPROVAL` terminal state, and the fixpoint loop.

**`D15`/`D16` — transport + log.** `.devops/sync-manifest.yaml`: `machinery-version: 45 -> 46`. `.devops/logs/version-history.md`: prepend the `v0.7.16` Patch row (newest first) containing the literal string `machinery-version: 46`.

**Commit.** `git commit -m "plan: T1-E3.03"` on the trunk.

### Wiki Core References
- `.wiki/core/18-knowledge-capture.md` -> § One state machine, five mirrors (the rule this change must keep true — every mirror updated in one commit) and the `claim_status` ≠ pipeline `Status` split.
- `.wiki/core/17-docs-blueprint.md` -> the docs-taxonomy authority behind keeping this machinery delta in the `.devops/rules/` + skills layer rather than the app wiki.
- `.devops/rules/process-lessons.md` -> capture routing for machinery lessons, plus the `T1-E3.06 G2` fold target the `sprint-plan` edit must point at rather than duplicate.

### Wiki Docs to Add/Edit
- **None.** `No wiki delta` (Phase 4 rationale): no app-domain surface changes; the governed contract lives in `.devops/rules/plan-lifecycle.md`. Nothing to promote to `stable`, nothing to mark `in-progress`.

### Test Verification Plan

| # | Command | Pass condition |
|---|---|---|
| 1 | `powershell -NoProfile -File scripts/check-parcel-prefix.ps1` | exit `0`; `PASS` ×9; 11 model bindings |
| 2 | `powershell -NoProfile -File scripts/check-utf8-agents.ps1` | exit `0`; `ALL CLEAN` |
| 3 | `python scripts/wiki_lint.py` | exit `0`; `OK: wiki healthy` |
| 4 | `python scripts/wiki_claims.py check` | exit `0`; 0 stale |
| 5 | The `validate.yml` "Machinery version discipline" predicate, reproduced verbatim (regex the manifest's `machinery-version`, assert the literal appears in `version-history.md`) | prints `OK: machinery-version 46 is recorded in version-history.md` |
| 6 | **Predicate behaviour probe (T1/T2/T3)** — see Phase 9 | three cases observed as specified |

**Expected file count:** 15 hand-edited + 9 derived agent copies regenerated by `-Sync`.

---

> **HALT POINT (Gate B — Spec & Plan Review):** Phases 4-5 complete. Present the wiki requirements spec (with acceptance criteria) AND the execution plan together as one decision. Do not proceed to reviews until user approves. Update the **State & Gates** section at the bottom of this file: Status -> `PHASE_5`, Active Persona -> `High-Visionary`. Leave Gate B `OPEN` — the orchestrator records the user's verdict.

> **AUTO auto-clear — Gate B: CLEARED (this is the single approval of the `SINGLE` topology — Gate C is `N/A`).** Mechanical verification: (a) the Phase 4 wiki-delta row is explicit (`No wiki delta`) with the conditional-skip checklist evaluated and **not** silently skipped — condition (b) applies, and the rationale for routing to the rules/skills layer is stated; (b) 8 numbered acceptance criteria each name a concrete test target, and the 3 TDD probes are `ACCEPTED`; (c) Phase 5's Simplicity Gate is fully ticked, the Reuse Log is populated, the one spaghetti observation is **routed** (not fixed), and both deliberate simplifications carry a named ceiling + upgrade path; (d) the To-Do list, File-Level Steps, and Implementation Instructions are internally consistent and contain no code snippets beyond exact string literals (the enum literal, the `-Sync` CLI command, `machinery-version: 46`, the commit literal `plan: T1-E3.03`, and the skip-reason strings); (e) the canonical predicate is stated **once** in Phase 4 with its clause separation spelled out, and the mirror list matches the Phase 2 file table plus the CW5 addition; (f) **zero `REJECTED` verdict lines and zero `Unresolvable:` entries** exist in Phases 4-5. Gate B row in `## 📍 State & Gates` → `APPROVED (AUTO)`.

---

## 6 Phase 6: Grumpy Architect Spec & Logic Audit
**Skill Executed:** `ptp-grumpy-architect` (`SINGLE`: orchestrator inline)

> **`SINGLE` topology:** no independent reviewer. The orchestrator logs an inline self-review checkpoint here and records **Verdict** `N/A — SINGLE self-review`.

**Verdict:** `N/A — SINGLE self-review` — inline audit **PASS**, no blocker found.

> **Rejection Rule:** If plan does not make the app faster, safer, or easier to modify, do not check boxes. Reject and force rewrite via `PHASE_5_REVISION`.
>
> **Spec-level audit — the plan contains no code, so no code-level scans (DRY/WET, line checks).**

- **System Contracts Explicit:** ✅ Every touched path is named by its exact path, and the one behavioural contract is stated as a sentence with a quotable form, not an adjective: the predicate ("`.devops/archive/` **or** `.devops/plans/` + `claim_status: GATE_D_USER_APPROVAL`"), the enum literal `QUEUED | CLAIMED | GATE_D_USER_APPROVAL | COMPLETE`, the `machinery-version: 46` literal, the `-Sync` CLI command, and the commit literal `plan: T1-E3.03`. Both layers of the predicate are named (`sprint-run` § 2 and `ptp-parcel-fast`'s entry check) so they cannot drift silently — D4/`CW2` exists precisely because a one-layer fix produces a claim-then-skip orphan.
- **File Boundary & Scope Collisions:** ✅ 15 declared `touches` are distinct files and every Phase 5 step names a file inside that set; the one file that was missing (`sprint-close`, `CW5`) was added to `touches` **before** execution. No glob in the list widens the write set beyond `D2`/`D4`/`D11`'s explicit statements (`.devops/agents/**` is backed by the `-Sync`-only rule). Wave-2 serialisation means no concurrent claim exists; Sprint 8's Delivery Model already records why the overlap clause forced three waves.
- **Dependency Gaps:** ✅ `depends_on: []` and genuinely so — every mechanism referenced already exists (the enum slot, `-Sync`, the per-claim re-evaluation, the skip-reason table, the release-row convention). No new script, skill, template, or config key is introduced. The plan *changes* the meaning of an existing field rather than adding a parallel one.
- **YAGNI Bloat:** ✅ No status-machine engine, no `depends_on` resolver module, no compatibility alias keeping `IN_PROGRESS` alive, no executable forecast tool. The two deliberate simplifications are labelled `ponytail:` with ceilings and upgrade paths rather than dressed up as features. `IN_PROGRESS` is **deleted**, not deprecated — it had no writer.
- **Paranoid Security (Contract Level):** ✅ No secrets, no credentials, no network surface, no user input. The only trust boundary is the human Gate D verdict, and the change *strengthens* it: `GATE_D_USER_APPROVAL` makes "executed but unverified" explicit and machine-detectable, instead of a `CLAIMED` state that reads as in-flight. Nothing in the plan lets a runner auto-clear or auto-archive.
- **Survivability:** ✅ No runtime flow, no async, no timeout. The failure modes are mechanical and each is made loud: a half-applied mirror is caught by the acceptance-criteria content diff (AC2) and by `-Sync`'s byte-equality pass (AC7); a manifest/history mismatch is caught by the reproduced CI predicate (AC8); a divergent two-layer predicate is caught by reading both layers (AC6).
- **Edge Cases:** ✅ Enumerated in Phase 4 Spec Notes and each maps to a probe or a criterion: in-flight dependency → unmet (AC2/AC3); satisfied-but-overlapped → still skipped (AC3, the `T1-E3.04` worked example); revert → re-blocked (AC4/T2 probe); cycle → terminate + flag (AC5); already-`PHASE_9` → skip (unchanged); `claim_status` vs bottom `Status` divergence → the terminal pair is stated explicitly (`GATE_D_USER_APPROVAL` + `PHASE_9`). Boundary condition checked: the enum stays **four** values (one swapped, none added), so a reader counting values cannot be misled by growth.
- **Performance Trade-offs:** ✅ Neutral. The predicate adds no iteration: the fixpoint loop is bounded by the queue length (≤ N passes, one claim each) and replaces an implicit "run once and stop" with an explicit "run until nothing is eligible". The cycle check is O(edges) over a queue that is single-digit in practice.
- **Architectural Anti-Patterns:** ✅ The change **removes** a contradiction rather than adding a special case: `sprint-run:35` demanded archive while `sprint-run:59` forbade it. The fix keeps one predicate and refers to it from every mirror instead of restating it — the exact anti-pattern ("duplicate sources of truth") the mirror rule guards against. The one duplicated-looking surface (`sprint-run` § 2/§ 5/§ 6) is routed as a Low observation, not fixed here, to keep the edit surgical.
- **Endpoint Protection & Rate Limiting:** ✅ n/a — no endpoint, no request path, no rate-limited surface.
- **Wiki Core Compliance:** ✅ Every blueprint cites a `.wiki/core/` doc: the mirror rule cites `18-knowledge-capture.md` § One state machine, five mirrors; the layer choice cites `17-docs-blueprint.md`; capture routing cites `.devops/rules/process-lessons.md`. The Phase 4 conditional-skip checklist is answered explicitly (`No wiki delta`, condition (b) applies), never silently.

**Self-review residual findings (advisory, non-blocking):**
1. **The mirror set is content-verified, not exhaustively enumerated.** D5's list was built from a grep of the enum token plus the lifecycle/predicate sentences across `.devops/rules`, `.devops/skills`, `.devops/templates`, `.devops/agents`, `HOW-TO.md`. Mitigation already in the plan: AC1/AC2 re-run both greps in Phase 9, and `touches` names every edit target. A miss would surface as a criterion failure, not a silent gap.
2. **Historical rows are stale by design.** `.devops/archive/**`, the backlog register row ("replacing dead `IN_PROGRESS`"), and the archived `t1-e3.02` plan keep the old vocabulary. That is Phase 1 Out-of-Scope ("Rewriting historical plans, archives, or logs") — recorded so a later reader does not mistake it for missed work.
3. **The forecast is not executable.** Stated as a named `ponytail:` ceiling in Phase 5 rather than asserted as a guarantee; the predicate is still re-evaluated per claim, so reality governs.

**Dead-Code / Orphan Flags (for Wrap Up backlog entry):**
| plan section / file_path:line_number | Reason |
|---|---|
| — none — | The one dead value in the blast radius (`IN_PROGRESS`) is **removed by this plan**, not flagged. The routed observation (`sprint-run` § 2/§ 5/§ 6 wording duplication) needs **no** backlog entry — it is resolved by this plan's consistent wording and `T1-E3.06 G2` owns the remaining `@sprint-plan` gap. |

**Required Fixes:**
> [x] None — no blocking flaw found in the inline audit.

---

## 7 Phase 7: Smooth Operator Product Review
**Skill Executed:** `ptp-smooth-operator` (`SINGLE`: orchestrator inline)

> **`SINGLE` topology:** no independent reviewer. Record **`N/A — SINGLE self-review`**; the self-review is presented at Gate B.

> **Rejection Rule:** If plan introduces unnecessary complexity or scope expansion, do not check boxes. Reject and force rewrite.

- **Vision & Journey Integrity:** ✅ This is the plan that makes Sprint 8's stated goal true: "an executed plan sitting at `PHASE_9` with Gate D `OPEN` is a first-class, machine-checkable claim status". The user-facing journey improves in the one place it was broken — a sprint with an intra-queue chain previously executed only its roots and left the operator to hand-apply a ruling at runtime; after this change the chain resolves deterministically and the operator keeps exactly one human decision per wave (the Gate D verdict).
- **Scope Containment (No Gold Plating):** ✅ Every item maps 1:1 to `D1`-`D5` of Phase 1. Explicitly **not** done, per Out-of-Scope: no batch wrap-up (that is `T1-E3.04`), no auto-clearing Gate D, no auto-archiving, no relaxing of `touches`/queue-order/worktree rules, no retirement of `CLAIMED`, no edits to historical records, no change to per-plan `@agent-wrap-up`. The `sprint-close` edit (`CW5`) is a **reader** fix required by `CW3`, not a new capability.
- **4 Core User States:** **Success** — a dependent plan with a satisfied dependency claims normally; the executed dependency reports `GATE_D_USER_APPROVAL` + `PHASE_9` and reads as "awaiting verdict" instead of "in-flight". **Empty** — an empty or single-plan queue behaves exactly as before (the fixpoint loop terminates on the first pass with nothing eligible). **Error** — two loud failures replace two silent ones: an in-flight dependency is skipped **with a reason** (was: a confusing archive-only block), and a cycle terminates the line with a flagged queue defect instead of looping. **Loading** — n/a; no async or user-visible surface. The one state that previously had no name (`sprint-run` § 4 said `CLAIMED` while `ptp-parcel-fast` step 8 said `CLAIMED`) now has one.
- **Guardrails & Permissions:** ✅ Permissions only get tighter in reach: Gate D is never auto-cleared, the batch still defers the verdict to one human decision rather than N, and the batch host's spawn right (`task` → exactly `ptp-parcel-fast`) is untouched. `sprint-close` gaining the explicit "not complete" rule is a guardrail **added** — it stops a sprint closing over an unwrapped plan.
- **Mobile / A11y / Telemetry:** ✅ n/a — no UI, no client surface, no telemetry. The touch surface is Markdown contracts, skill prose and one PowerShell re-inline.

**Required Fixes:**
> [x] None — no unnecessary complexity or scope expansion identified.

---

> **HALT POINT (Gate C — Peer Reviews):** Reviews complete. Present findings and required fixes. Update the **State & Gates** section at the bottom of this file:
> - **PASS:** Phase 6 log clean -> set Status `PHASE_7`, Active Persona `Reviewer`. Do not proceed to execution until user approves Gate C.
> - **FAIL:** Phase 6 or 7 flagged blocking flaws -> set Status `PHASE_5_REVISION`, Active Persona `High-Visionary`. Return to Group B for plan adjustments, then re-run Phases 6-7. **Never advance an unapproved plan to execution.**
> The orchestrator records Gate C -> `APPROVED`/`REJECTED` only after the user's verdict.
>
> **`SINGLE` topology:** Gate C is `N/A` — the plan approval happens once at Gate B (spec + plan + inline self-review). Update the State & Gates `Gate C` row to `N/A (SINGLE)`.

> **Gate C record: `N/A (SINGLE)`.** No independent reviewer was spawned (the `parcel-sprint` locked preset fixes `Agents = SINGLE`, and the runtime binds the host's `task` right to exactly `ptp-parcel-fast`). Phases 6-7 are the inline self-review checkpoints logged above; the single approval they roll into was taken at **Gate B**. Mechanical note for the auto-clear test: the only occurrences of a rejection token in Phases 6-7 are the two template **Rejection Rule** advisories (which say "Reject and force rewrite") and the Gate C halt-point prose's `APPROVED`/`REJECTED` option list — **no line is a rejection verdict**, and no self-review finding is blocking. Gate C row in `## 📍 State & Gates` → `N/A (SINGLE)`.

---

## 8 Phase 8: Execute Changes
**Skill Executed:** `ptp-code-surgeon`

**Execution Isolation:** Gate cleared before execution — `SINGLE` topology, so the approval is **Gate B** (`AUTO`-cleared above, Gate C `N/A`). No execution occurred before that clear.

**Single-Pass Direct-to-Disk:**
> [x] Implementation written DIRECTLY to source files - no intermediate Markdown code blocks, no drafting files
> [x] Touched only intended lines - no adjacent refactors
> [x] Cleaned up only owned orphans (the one owned orphan is the retired `IN_PROGRESS` enum value, removed from both of its live sites)
> [x] Pre-existing dead code left untouched (the routed `sprint-run` § 2/§ 5/§ 6 wording duplication is **routed**, not fixed; `T1-E3.06 G2` owns the remaining `@sprint-plan` gap)

### Execution Log

| # | Item | Action | Result |
|---|---|---|---|
| 1 | `D1` | `.devops/rules/plan-lifecycle.md` — 8 phrase-exact edits: enum comment, a new `claim_status` values bullet, the canonical `depends_on` predicate + clause-separation bullet, the § Lifecycle claim-status mirror, Claim Protocol step 1 + step 5, Deviations #1, the closing predicate paragraph; `last-reviewed` + the footer stamped `2026-09-16` | the predicate is now stated **once** (canonical here) and referenced everywhere else; `IN_PROGRESS` gone |
| 2 | `D2` | `.opencode/plans/base-context.md` — 3 edits: PTP Lifecycle claim-status line, the claim bullet's `depends_on` parenthetical + independent-overlap sentence, the `ptp-parcel-fast` delegation row | canonical prefix updated; no hand-edit of any derived agent |
| 3 | `D3` | `.devops/templates/base-context.template.md` — the same 3 edits; header `version: 13 -> 14` | seed cell-for-cell identical to the live prefix |
| 4 | `D4` | `powershell -NoProfile -File scripts/check-parcel-prefix.ps1 -Sync` (run twice: after D2/D3, then again after D6's skill edit) | 1st: `FIXED` ×9, `MODEL` ×11, `SEED-OC-MODEL` ×11, `OC-MODEL` ×11, exit `0`. 2nd: `PASS` ×9 + `EMBED ptp-parcel-fast.subagent.md: refreshed`, exit `0` |
| 5 | `D5` | `.devops/skills/sprint-run/SKILL.md` (`v1 -> 2`) — § 1 gains step 6 (cycle check + resolution forecast, preview renumbered to 7); § 2 clause 2 = the disjunctive predicate; the within-batch bullet re-stated; a **Fixpoint loop** block added; § 3 step 3 re-applies the predicate; § 4 terminal `GATE_D_USER_APPROVAL`; § 5/§ 6 reason tables; § 7 named exception | host layer implements the same rule as the runner layer |
| 6 | `D6` | `.devops/skills/ptp-parcel-fast/SKILL.md` (`v1 -> 2`) — chain step 8 sets `claim_status: GATE_D_USER_APPROVAL`; the entry check states the predicate and the divergence hazard; output contract updated | runner layer mirrors the host, so no claim-then-skip orphan |
| 7 | `D7` | `.devops/skills/pass-the-parcel/SKILL.md` (`v14 -> 15`) — claim-status paragraph after the state table; § 1.2 eligibility; § Batch Runner (GATE_D + fixpoint bullets) | lifecycle authority states the status axis without restating the predicate |
| 8 | `D8` | `.devops/skills/sprint-status/SKILL.md` (`v2 -> 3`) — § 3 detection, § 4 `🟠 AWAITING GATE D` readout row, § 5 blocked-queue rule | an executed plan no longer reports as in-flight |
| 9 | `D9` | `.devops/skills/sprint-close/SKILL.md` (`v4 -> 5`) — § 1: `GATE_D_USER_APPROVAL` is **not** complete; never close over it | closes the silent-close hole from `CW3` |
| 10 | `D10` | `.devops/skills/sprint-plan/SKILL.md` (`v3 -> 4`) — § 6 note: queue order is the first claim order, not an execution dependency, plus a **pointer** to the `T1-E3.06 G2` fold target on the `touches`-overlap lesson | no duplicate statement of the overlap lesson |
| 11 | `D11` | `.devops/agents/parcel-sprint.agent.md` — host unique content only: the preview bullets (forecast claim order + cycle) and two hard-rule bullets separating "dependency satisfied" from "skipped for overlap" | prefix untouched by hand; `-Sync` preserved the unique content |
| 12 | `D12` | `.devops/templates/SPRINTS.template.md` (`v3 -> 4`) — the `@sprint-run` row | batch description matches the shipped predicate |
| 13 | `D13` | `.devops/plans/template-plan.md` — line 4 enum comment + the bottom valid-states line (adds `GATE_D_USER_APPROVAL` and distinguishes claim-status from pipeline `Status`) | the scaffold teaches the new state |
| 14 | `D14` | `HOW-TO.md` § `#### Sprint Batch Runner (@sprint-run)` | operator-facing description matches the shipped contract |
| 15 | `D15` | `.devops/sync-manifest.yaml` — `machinery-version: 45 -> 46` | bumped from the **live** value, not from a plan-recorded number |
| 16 | `D16` | `.devops/logs/version-history.md` — prepended the `v0.7.16` Patch row containing the literal `machinery-version: 46`, and restored the `v0.7.15` row's table cells after an insert collision | newest-first order intact; the CI literal is present |
| 17 | `CW5` | Added `.devops/skills/sprint-close/SKILL.md` to the plan's `touches` before executing it | the declared write set equals the actual one |

> [x] Step 1: `D1` → `D2`/`D3` → `D4 -Sync` (order forced: the mirrors quote the canonical wording, and `-Sync` is the only step that writes the derived agent copies).
> [x] Step 2: `D5`-`D14` mirrors, then `D15`/`D16` version discipline, then the `D6`-triggered second `-Sync`, then the Phase 9 gate run.

**Prefix-lock discipline (AGENTS.md rule 9) — how it was honoured.** `base-context.md` was edited first, the seed mirrored second, and the 9 agent copies were **generated**, never hand-edited: the first `-Sync` printed `FIXED` ×9 and the second `PASS` ×9, with `EMBED ptp-parcel-fast.subagent.md: refreshed` covering D6's skill edit. `parcel-sprint.agent.md`'s unique content (below the prefix) was hand-edited by design — the `-Sync` run after it preserved it.

---

## 9 Phase 9: Verify Changes
**Skill Executed:** `ptp-code-surgeon`

**Build & Lint:**
> [x] Compilation check clean — n/a (no compiled surface); the equivalent structural check is JSON validity of the two config files, which this change does not touch (verified unchanged in the diff)
> [x] Lint clean (exit 0) — `python scripts/wiki_lint.py` → `OK: wiki healthy - 0 broken links, all anchors present, frontmatter valid.`
> [x] Type-generation re-run if schema/API changes — n/a (no schema, no API, no generated types)

**Phase 9 Verification Report (actual commands + exit codes):**

| # | Command | Exit | Observed |
|---|---|---|---|
| 1 | `powershell -NoProfile -File scripts/check-parcel-prefix.ps1` | `0` | `PASS` ×**9** locked agents (2 orchestrators + 7 `ptp-*`), `MODEL` ×11, `SEED-OC-MODEL` ×11, `OC-MODEL` ×11; `OK: all PREFIX-LOCKED agents share a byte-identical prefix, every binding surface agrees, and the seed surfaces match the live registry.` |
| 2 | `powershell -NoProfile -File scripts/check-utf8-agents.ps1` | `0` | `ALL CLEAN (218 files scanned)` — no mojibake introduced by the 15 edited files and the 9 regenerated copies |
| 3 | `python scripts/wiki_lint.py` | `0` | `OK: wiki healthy - 0 broken links, all anchors present, frontmatter valid.` |
| 4 | `python scripts/wiki_claims.py check` | `0` | `claims OK: 0 stale, all grounded sources present.` — no claim source was moved, so no restamp was required |
| 5 | `validate.yml` "Machinery version discipline" predicate, reproduced **verbatim** in the temp probe script `%TEMP%\opencode\t1e303_probe.py` (regex the manifest, assert the literal appears in `version-history.md`) | `0` | `[1] OK: machinery-version 46 is recorded in version-history.md` |
| 6 | `python scripts/wiki_coverage_check.py` | `0` | `coverage OK: no src/ tree in this workspace — nothing to cover` |
| 7 | `python scripts/wiki_visualize.py --check` | `0` | `visualizer OK: 56 docs, 28 edges - docs/wiki-graph.md up to date` |
| 8 | `powershell -NoProfile -File scripts/sync-architecture.ps1 -SelfTest` | `0` | `SELFTEST OK: 5 dirs, 38 skills, 12 files materialised correctly.` (includes the `template-plan.md` transport assertion the F9 fix added) |

**Predicate behaviour check (the three cases the plan must demonstrate — `%TEMP%\opencode\t1e303_probe.py`, run against the live tree):**

| Case | Probe action | Observed |
|---|---|---|
| **(a) archived satisfies** | `satisfied("T1-E3.05")` against `.devops/archive/` | `SATISFIED (archive)` → `t1-e3.05-retire-parcel-fast-orchestrator-plan.md` |
| **(b) `plans/` + `GATE_D_USER_APPROVAL` satisfies** | staged `ZZ-PROBE.01` in `.devops/plans/` with `claim_status: GATE_D_USER_APPROVAL` | `SATISFIED (plans/ + GATE_D_USER_APPROVAL)` |
| **(c) a reverted plan blocks again** | flipped the same fixture to `claim_status: CLAIMED` and re-applied the predicate | `UNMET (in flight: claim_status=CLAIMED)`; fixture removed (`exists=False`) |
| **(d) live intra-sprint case** | `T1-E3.04`'s `depends_on: ["T1-E3.03"]` evaluated before this plan's terminal write | `UNMET (in flight: claim_status=CLAIMED)` — i.e. the dependency is blocked while E3.03 is mid-flight, exactly as the fixpoint loop requires |
| **(e) clause separation** | the `touches` prefix-overlap heuristic applied to `T1-E3.03` vs `T1-E3.04` | overlap = **True** (7 shared entries incl. `.devops/rules/plan-lifecycle.md`, `.devops/skills/sprint-run/SKILL.md`, `.devops/logs/version-history.md`). So once E3.03 reaches `GATE_D_USER_APPROVAL`, the dependency verdict flips to **satisfied** while the overlap verdict **still skips** — the two clauses report independently, which is the precise distinction this plan must leave legible. |

> **Post-terminal re-run (the self-applying bootstrap).** After this plan's own terminal write (`claim_status: GATE_D_USER_APPROVAL`), case (d) is re-run and flips from `UNMET (in flight)` to `SATISFIED (plans/ + GATE_D_USER_APPROVAL)` — the state this plan introduces is the state it terminates in, so the change describes itself. Result recorded in the Gate D evidence block below.

**Acceptance criteria — outcome:**

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 1 | New status recognised everywhere; `IN_PROGRESS` dead outside history | ✅ | `git grep -n IN_PROGRESS` (excluding `.devops/archive/**` and `.devops/logs/**`) returns only: (i) the **retirement statements** this change writes into each mirror (`... `IN_PROGRESS` is retired.` in `plan-lifecycle.md`, `base-context.md`, the seed, `pass-the-parcel`, and — via `-Sync` — all 9 agent copies), (ii) the backlog register row's historical prose, and (iii) this plan's own Q&A / Out-of-Scope text. **No live enum site retains it** — the two that did (`plan-lifecycle.md:25`, `template-plan.md:4`) now read `QUEUED \| CLAIMED \| GATE_D_USER_APPROVAL \| COMPLETE` |
| 2 | Predicate stated once, mirrored identically | ✅ | content read-back across `plan-lifecycle.md`, `base-context.md`, `base-context.template.md`, `pass-the-parcel`, `sprint-run`, `ptp-parcel-fast`, `sprint-status`, `HOW-TO.md` |
| 3 | Predicate vs `touches` clause distinguishable | ✅ | probe case (e) |
| 4 | Revert re-blocks | ✅ | probe case (c) |
| 5 | Fixpoint loop bounded / deterministic / cycle-safe + preflight cycle check + forecast | ✅ | `sprint-run` § 1 step 6, § 2 Fixpoint loop, § 3 step 3, § 5/§ 6 reason rows |
| 6 | Terminal claim status is `GATE_D_USER_APPROVAL` in **both** layers | ✅ | `sprint-run` § 4 + `ptp-parcel-fast` step 8 + entry check; this plan's own front-matter |
| 7 | Prefix lock intact (9 agents, seed agrees) | ✅ | gate 1 |
| 8 | `machinery-version: 46` + literal in `version-history.md` | ✅ | gate 5 |

**Test Report:**
- [x] Lint pass (exit 0)
- [x] Tests pass (exit 0) — this repo has no unit-test suite; the deterministic gates above **are** its test harness, and all 8 exited `0`
- [x] Build pass (exit 0) — n/a (no build step); the structural equivalent (`-SelfTest`, prefix, UTF-8) exited `0`
- [x] Code matches exact plan specifications — every Phase 5 Test Verification Plan command was re-run; each exits `0`, and the 3 TDD probes behave as specified
- [x] No functional gaps identified

> **Self-healing budget: unused (0 of 2 attempts).** The first `-Sync` run and the first gate sweep were green; the only defect found during execution was self-inflicted and mechanical — the `version-history.md` insert collapsed the `v0.7.15` row's leading cells, caught by reading the file back and repaired in place before any gate ran. No `PHASE_8_FAILED`, no rollback.

---

> **HALT POINT (Gate D — Implementation):** Implementation and verification complete. Present completed work to user. Do not proceed to user review until user signs off. Update the **State & Gates** section at the bottom of this file: Status -> `PHASE_9`, Active Persona -> `Executor`. Leave Gate D `OPEN` — the orchestrator records the user's verdict. On rollback: Status -> `PHASE_8_FAILED`.

---

## 10 Phase 10: User Review & Tweaks
**Skill Executed:** `knowledge-capture`

**Tweak Discipline:**
> Tweaks are surgical, not architectural. `expansion` and `refactor` are HALT conditions.

- [ ] Tweak classified: `fix` / `expansion` / `refactor`
- [ ] `expansion` and `refactor` routed to new parcel
- [ ] `fix` touches <= 3 files
- [ ] `fix` introduces no new abstraction absent from original plan

**Capture Categories:** Recurring / Tribal / One-off / Cosmetic-skip

| Round | Feedback | Action Taken | Capture Flag | Result |
|---|---|---|---|---|
| 1 | | | | PENDING / COMPLETE |

**Sign-Off:** [Date] - User approved.

---

## Completion Note (Wrap Up)
**Skill Executed:** `agent-wrap-up`

> [x] Read `.wiki/core/18-knowledge-capture.md` for existing related decisions
> [x] Themed tweaks synced to knowledge capture doc
> [x] Wiki docs updated per Phase 10 tweaks
> [x] Code reconciled against Phase 4 spec — deviations logged
> [x] `status: in-progress` wiki docs promoted to `stable` (or deviation logged)

**Outcome:** Delivered as specified. `GATE_D_USER_APPROVAL` is now a first-class claim status, `IN_PROGRESS` is retired, and the `depends_on` predicate is disjunctive — a code is **satisfied** when it is archived **or** present in `.devops/plans/` with `claim_status: GATE_D_USER_APPROVAL`; any other state (`QUEUED`, or `CLAIMED` below `PHASE_9`) is **unmet**. `@sprint-run` gained the fixpoint loop, a dependency-cycle preflight and a resolution forecast. Executed as **wave 2 of 3** in Sprint 8.

**Self-applying bootstrap (the strongest evidence the state is correct):** this plan *introduces* `GATE_D_USER_APPROVAL` and terminates **in** it — front-matter `claim_status: GATE_D_USER_APPROVAL`, bottom `Status: PHASE_9`, Gate D `OPEN`. The host then re-evaluated the live queue against the new predicate and observed `T1-E3.04` flip from clause-2 **FAIL** (old rule: dependency unmet) to clause-2 **PASS** (new rule: dependency satisfied), while its clause-3 `touches` overlap with this plan still returns SKIP (8 colliding entries). A satisfied dependency no longer masks a write-set conflict — the clause separation the plan required, demonstrated end-to-end rather than asserted.

**Themed Tweaks:**

**Knowledge Capture Entries:**
- **No new entry — deliberately.** The session's candidate lesson (`touches` overlap means a queue cannot batch itself) is already in `.devops/rules/process-lessons.md` (2026-09-13), including its *do instead* addressed to the planner. Adding it again would be a restatement, which the capture admission gate forbids and the one-entry-per-session budget exists to prevent. The real gap is that the existing lesson was never **folded into its owning skill** — that fold is `T1-E3.06 G2`, already parked. `sprint-plan` was edited only to point at that fold target, keeping the lesson single-sourced.

**Spec Reconciliation:** None — implementation matches the Phase 4 spec. One **declared** deviation:
1. **`sprint-close` was added to `touches` before it was edited** (recorded in the plan as CW5). The as-authored `touches` named `sprint-status` but omitted `sprint-close`, which CW3 had independently identified as a reader that branches on `claim_status` and could therefore close a sprint over an unwrapped plan. Scope was widened explicitly and recorded rather than silently. `sprint-close` v4→v5 now treats a surviving `GATE_D_USER_APPROVAL` plan as unfinished.

**In-Progress Promotions:** None — Phase 4 wrote no `in-progress` docs (this parcel is machinery-only; the wiki delta is a claim-status vocabulary change in rules text, not an app-behaviour doc).

**Dead-Code Backlog Entries (from Phase 6):** **None — and deliberately so.** The one dead value in the blast radius (`IN_PROGRESS`) is *removed by this plan*, not flagged for another one. The single routed observation (the predicate restated across `sprint-run` § 2/§ 5/§ 6 — rules, halt list, reason list) is duplication, not dead code, and is resolved by this plan's consistent wording; the remaining `@sprint-plan` gap is owned by `T1-E3.06 G2`. Creating an entry for either would be double-counting.

**Wiki Updates:** None directly — the wiki delta was carried by the plan's rule/skill mirrors. `.devops/rules/plan-lifecycle.md` `last-reviewed` restamped.

**Plan Archiving:** Plan archived to `.devops/archive/t1-e3.03-gate-d-user-approval-plan.md` (via `git mv`, no stub). Trunk-sequential batch path: no branch, so no merge/prune.

**Backlog Review:** No existing backlog item is resolved by this wave. **`T1-E3.06` gains a confirmation, not a new item** — its G2 finding (that `@sprint-plan` commits a mutually-overlapping set without foreshadowing the wave count) is now *demonstrated* rather than theoretical: Sprint 8 committed three plans that can only execute one wave at a time, and this wrap-up is the third consecutive proof. No new findings from this wave; the `check-utf8-agents.ps1` `.devops/sprints/**` scope gap (G1) remains open and unowned by this parcel, correctly.

---

## 📍 State & Gates (CACHE-ANCHORED — update ONLY this section at gate transitions)

> **Cache rule:** This is the **last section** in the file. Gate transitions mutate ONLY the rows below — phase content above AND the frozen **Plan Settings** block at the top stay byte-stable to preserve LLM prefix-cache hits. Every "Update Status" instruction in the halt points above means "edit this section".

| Metric | Value |
|---|---|
| **Status** | `COMPLETE` |
| **Version** | `v0.1.0` |
| **Active Persona** | `Executor` |
| **Depends On** | none (canonical: claim front-matter `depends_on`) |
| **Blocks** | none |

> **Settings pointer:** `Mode` and `Agents` live in the frozen **Plan Settings** block at the TOP of this file — read them there before any phase. Never duplicate them here.

> Valid states: `QUEUED`, `CLAIMED`, `GATE_D_USER_APPROVAL`, `PHASE_1`, `PHASE_3`, `PHASE_5`, `PHASE_5_REVISION`, `PHASE_7`, `PHASE_8_FAILED`, `PHASE_9`, `COMPLETE`.
>
> `GATE_D_USER_APPROVAL` is a **claim-status** value (top front-matter); a plan carrying it sits at bottom `Status: PHASE_9` with Gate D `OPEN`, awaiting the human verdict. This plan's terminal pair is exactly that — it is the first real exercise of the state it introduces.

| Gate | Requirement | Status |
|---|---|---|
| A | Scope approved (Phases 1-3, + 3.5 in AUTO) | `APPROVED (AUTO)` |
| B | Spec & plan approved (Phases 4-5) | `APPROVED (AUTO)` |
| C | Peer reviews passed (Phases 6-7) | `N/A (SINGLE)` |
| D | Implementation verified (Phases 8-9) | `APPROVED` |

> **Gate D verdict recorded by the `parcel-sprint` batch host (2026-09-16).** Operator ruling: **APPROVED**. Recorded by the orchestrator, never by an executing agent (`.devops/rules/plan-lifecycle.md` § Deviations, batched Gate D). Gate A `APPROVED (AUTO)` and Gate B `APPROVED (AUTO)` were verified mechanically; Gate C is `N/A (SINGLE)`.

> **Gate D evidence (for the human verdict — this runner never flips it).**
> 1. **The machine-checkable core:** `grep -c GATE_D_USER_APPROVAL` over the mirror set is non-zero in `plan-lifecycle.md`, `base-context.md`, `base-context.template.md`, `pass-the-parcel`, `sprint-run`, `ptp-parcel-fast`, `sprint-status`, `sprint-close`, `sprint-plan`, `SPRINTS.template.md`, `template-plan.md`, `HOW-TO.md`, `parcel-sprint.agent.md`, and **this plan's own front-matter**; `IN_PROGRESS` survives only in historical records.
> 2. **The predicate works, demonstrated not asserted:** `t1e303_probe.py` cases (a)-(e) above — archived satisfies, `plans/` + `GATE_D_USER_APPROVAL` satisfies, a reverted `CLAIMED` plan blocks again, and the `touches` clause returns its **own** verdict for the same pair.
> 3. **The self-applying bootstrap:** this plan's own `claim_status` is `GATE_D_USER_APPROVAL` with Gate D `OPEN` — the state it introduces is the state it terminates in. Re-running probe case (d) now returns `SATISFIED (plans/ + GATE_D_USER_APPROVAL)` for `T1-E3.03`, which is precisely what unblocks `T1-E3.04`'s `depends_on` (while its `touches` overlap still serialises it).
> 4. **Everything green:** all 8 verification commands exit `0`; prefix `PASS` ×9; only `45 -> 46` moved in the manifest and the literal is in the release log.
> 5. **What to check by hand:** read `.devops/rules/plan-lifecycle.md` § Claim Front-Matter (the single source) and confirm the two clauses are legible as separate blockers; then read `sprint-run` § 2 (host) and `ptp-parcel-fast` (runner) and confirm they state the *same* rule.

> Gate flips: rows flip `OPEN` → `APPROVED`/`REJECTED` ONLY after the user's (or AUTO verification's) verdict, recorded by the orchestrator. Executing agents halt with their gate `OPEN`. Rejection routes: A -> `PHASE_1`; B/C -> `PHASE_5_REVISION`; D/rollback -> `PHASE_8_FAILED`. Never edit rows above this section for gate bookkeeping.

> **This section is the LAST section in the file. All gate bookkeeping happens here.**
