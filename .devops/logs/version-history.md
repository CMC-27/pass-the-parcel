---
type: "core"
name: "Version History"
status: "stable"
description: "Formal release log and 3-level versioning strategy for the application."
---

# Version History & Policy

This log records the formal releases, deployments, and the 3-level versioning strategy enforced across the application.

## 📌 Versioning Strategy (3-Level System)

All versioning follows the semantic hierarchy configured within the `/Test-and-Deploy` pipeline:

1. **Level 1 (Major)**: User-directed primary versions (e.g., `1.02.003` -> `2.00.000`). Triggered for fundamental architectural updates, paradigm shifts, or major product milestones. Resets both minor and patch levels to double/triple zero padding.
2. **Level 2 (Minor)**: New feature versions (e.g., `1.02.003` -> `1.03.003`). Adds `.01` to Level 2 versioning (allowing up to 99 level 2 versions), while preserving the patch level as-is. Prompted and confirmed when introducing discrete new features, capabilities, or major screen flows.
3. **Level 3 (Patch)**: Automated deployment versions (e.g., `1.02.003` -> `1.02.004`). Adds `.001` to Level 3 versioning (allowing up to 999 level 3 versions), while preserving the minor level as-is. Automatically bumped on every routine code deployment or patch push if no major/minor bump is specified.

---

## 📈 Release Log

| **Version** | **Date** | **Level** | **Deployer** | **Key Release Highlights / Milestones** |
| **v0.7.30** | 2026-09-18 | Patch | opencode (DeepSeek V4.1 Flash) | **Retirement transport gaps** (T1-E2.07 — GRID-Link 54→59 pull findings). Additions travelled but retirements did not; four fixes so the next pull self-heals. **F1** — `sync-bindings.ps1` gains a registry delete pass symmetric with the insert pass (4-cell row shape only, prose untouched): a retired Model Registry row is pruned from an already-synced satellite instead of failing its prefix check. **F2** — the culled `wiki-bootstrap/references/qa-14-testing-standards.md` joins `prune_files`, and the parent-dir mask extends to portable skills, so a retired skill-internal file reports `PRUNE`, never a false skill `DRIFT`. **F3** — the locked-preset host's `permission.task` allow-list is structural and stamped from the seed (`parcel-sprint` regains `wiki-writer`); all other authored permissions stay untouched. **F4** — version discipline stated in the `sync-architecture` skill (`version` MUST bump with any body/reference change); `model-routing` v8→v9, `sync-architecture` v7→v8. `-SelfTest` gains a T1-E2.07 fixture (orphan row + stale skill file + aged task list → converged, idempotent, `IN SYNC`). machinery-version: 63. |
|---|---|---|---|---|
| **v0.7.29** | 2026-09-17 | Patch | opencode (DeepSeek V4.1 Flash) | **Template hygiene cull** (direct user direction, no theme/epic). Five dead or self-erasing surfaces removed and two live docs corrected to truth. **Deletions.** `.devops/skills/wiki-bootstrap/references/qa-14-testing-standards.md` — an orphan: the skill's own Question Set table routes slot 14 to the testing subtree (`—`), no surface referenced the file, and its `references:` frontmatter claim drops **19 → 18 question sets**. `.wiki/hooks/README.md` — unreferenced, and it contradicted `hooks-index.md` (the linked spoke) about what the folder is for. `.github/CODEOWNERS` — template-owner-specific (`@CMC-27`), inherited by every fork that is not the owner. The **`docs/` directory itself** — after T1-E4.01 its only content was a note about a retired export, so the tombstone stopped paying for itself; `@Managed Simplicity`. **Truth corrections.** `.devops/rules/process-lessons.md` drops the two entries a prior maturity pass proved superseded or wrong — *Wiki lint severity contract* (fully carried by `@wiki-lint` v4, and wrong about index-size severity) and *Architecture sync clobbers agent `model:` bindings* (premise false since sync force-stamps the registry) — register **22 → 20**. `.wiki/core/18-knowledge-capture.md` records the `docs/` directory removal beside the existing visualizer note and past-tenses the retired `wiki_okf.py import` action so the entry no longer reads as current. **Registry/manifest follow-through.** Structure manifest anchor 14 (`docs/`) is pruned and the canonical-authority rows renumbered 15-22 → 14-21; `README.md`, `.gitattributes`, `.wiki/core/17-docs-blueprint.md`, `.devops/rules/surface-budget.md` and the `check-utf8-agents.ps1` scan list all lose their `docs/` reference. `.devops/archive/` is deliberately **kept** (its traceability is load-bearing for 27 live links). No Grounded Claim source was touched; `wiki_lint`, `wiki_claims`, the prefix check and the encoding guard all stay green. **`machinery-version: 59`.** |
| **v0.7.28** | 2026-09-17 | Patch | opencode (DeepSeek V4.1 Flash) | **Scripts scale with satellite size** (direct user direction: "review all our scripts to ensure they can run as fast as possible in large satellites"). A profiling pass over every portable script, then three behaviour-preserving workstreams. **W1 — Python wiki tooling.** `Path.resolve()` was **48% of `wiki_lint`'s runtime** (one `GetFinalPathNameByHandle` syscall per link per check); `wiki_lint_core` gains a syscall-free `path_key` (`normcase`+`normpath`+`abspath`) and a memoised `resolve_link`, and `wiki_lint_checks` gets a per-run `LintContext` cache — the `.wiki` corpus is enumerated **once** and every file read **at most once** (previously ~6 enumerations and 4-5 reads per doc), with `os.walk` pruning so the unbounded `.devops/archive|logs|plans` trees are never descended. `wiki_claims` shares one read per doc and one read+hash per unique source between `digest` and `symbol_resolves`, and no longer resolves each frontmatter link twice. **W2 — PowerShell gates.** `check-utf8-agents.ps1` collects targets with `Directory.GetFiles`/`EnumerateFiles` instead of `Get-ChildItem -Recurse` (the provider materialises a `FileInfo` per entry on PS 5.1); `sync-manifest.ps1` hashes with one `BitConverter.ToString` call instead of a per-byte `ForEach-Object` pipeline and enumerates with .NET. **W3 — Node scanner.** `spaghetti-monster-scan.cjs` fills a `WALKED` set from its single walk and answers sibling test-file candidates from it, and accumulates the recursive walk instead of spreading arrays. **Measured** (synthetic 2100-doc satellite, old vs new, byte-identical findings): `wiki_lint` **5,979 → 900 ms (6.6×)**; `check-utf8-agents` **1,206 → 615 ms (2.0×)**; template-repo `wiki_lint` **604 → 203 ms**; `spaghetti-monster-scan` **305 → 94 ms**. `wiki_claims` improves modestly (its cost is dominated by the wiki walk, not the claim reads). All four fixture suites green (29+10+5+18), every gate byte-identical except the scanner's own self-measurement, `claims_coverage`/`rule_fanout`/`sprint_eligible`/`prefix` unchanged, PREFIX-LOCKED bytes untouched. Two claims hashing `scripts/wiki_claims.py` re-stamped. **`machinery-version: 58`.** |
| **v0.7.27** | 2026-09-17 | Patch | opencode (DeepSeek V4.1 Flash) | **Operating Principles codified: goal → principal instrument → supporting instruments** (direct user direction, no theme/epic). The template states *why it exists* once, in the new root `OPERATING-PRINCIPLES.md`: **The Goal — Pass the Parcel** (stateless, independently reviewed, gated, deterministic) → **The Principal Instrument — the agent-managed wiki** (canonical truth + stewardship) → **The Supporting Instruments** (cache-first context, Managed Simplicity, deterministic guardrails). This replaces README's "Two jobs" peer framing and adds a **The Goal** block to `AGENTS.md`; the doc is **template-only** — not synced and not in the prefix, so the PREFIX-LOCKED bytes stay untouched (no re-inline, no cache invalidation). Satellites keep their own principles and detail in `.wiki/core/`. Two surface-budget rows are registered (`operating-principles`, `cache-first`; 3 sites / 0 unauthorised each), and `rule_fanout.py` gains an optional **report-only `agreement:`** section that flags a declared token (`PHASE_9`, `GATE_D_USER_APPROVAL`, `MULTI`, `SINGLE`) missing from any surface that must carry it — all four report `AGREE 3/3`. `test_rule_fanout.py` grows 13 → 18 cases. No new gate; `validate.yml` unchanged; the two pre-existing `auto-clear` unauthorised sites were reworded to cite the contract (`parcel.agent.md`, `ptp-parcel-fast.subagent.md`), taking the report to **0 unauthorised across all eight registered rules**. Two Grounded Claims that hash `AGENTS.md` re-stamped. **`machinery-version: 57`.** |
| **v0.7.26** | 2026-09-17 | Patch | opencode (DeepSeek V4.1 Flash) | **Machinery surface budget & Managed Simplicity** (T1-E4.01 - all six waves, one trunk-sequential pass). The template gains the counterweight it was missing. **W0** adopts **Managed Simplicity** as the first principle (`.devops/rules/managed-simplicity.md`, full statement) with a compact form plus a link in `AGENTS.md` (a *section*, so rules 1-10 do not renumber), its seed, and the shared prefix + seed. **W1** adds the **surface-budget report**: `.devops/rules/surface-budget.md` carries a machine-readable registry (walk roots, one row per rule, its canonical home, its allowed citation surfaces) parsed in-file by the new stdlib-only `scripts/rule_fanout.py`, which prints, per registered rule, the raw restatement count **and** the count that restates without naming the canonical home - report-only, always exits 0, never in `validate.yml`. **W2** consolidates the five worst offenders (`claim_status`, `GATE_D_USER_APPROVAL`, `auto-clear`, `context-isolated`, `Chunked Write Discipline`): each gets one canonical home and every legitimate surface cites it instead of restating it - fan-out on the declared metric fell **59 -> 2** unauthorised sites, with no `unlinked-allowed-surface`. **W6** decomposes both Kill List god-scripts, behaviour-preserving: `sync-architecture.ps1` **995 -> 485** into five dot-sourced `scripts/lib/` modules (all in `portable_files`; the `-SelfTest` body stays entry-scoped), and `wiki_lint.py` **485 -> 60** into `wiki_lint_core.py` + `wiki_lint_checks.py` over an explicit `LintContext`, with the eleven re-exported names kept literal so five Grounded Claims keep resolving. Four CLI modes are line-for-line against pre-split goldens after three declared volatile classes and a seven-entry manifest-growth delta, and the `--fix` scenario matches in stdout **and** mutated bytes. **W7** rewrites the encoding gate: one Latin-1 decode plus one compiled alternation of markers built from code points - **18.41 s -> 0.57 s**, no literal glyph in the guard's own source, and the declared delta that the old loop bound never scanned the final two bytes. `.devops/archive` leaves the recurring scan; `-All` reproduces the whole-tree scan on demand and is unscheduled. **W8** retires four skills, two scripts and the generated `docs/wiki-graph.md`; the coverage gate **moves, never dies** (now `python scripts/wiki_claims.py coverage`, with `--root`); the OKF CI step is replaced **in place** by a dependency-free `.wiki/**` frontmatter-YAML step so that invariant survives the tooling (exactly two assertions are lost with their subjects and both are named in the dated `MATURITY.md` row); `@test-and-deploy` absorbs the app-facing hardening sweep; and the new **`prune_dirs`** manifest key deletes a retired *directory* recursively - the only mechanism that removes a retired skill folder from an already-synced satellite, since portable skills are derived and `prune_files` cannot remove a directory shell. Evidence includes a live pre-retirement target: 7 `PRUNE` verdicts -> deleted -> `IN SYNC`. `MATURITY.md` gains **Axis 7 (machinery surface budget, B)** with earlier history rows backfilled, re-scores Axis 2 `A+ -> A` and Axis 5 `B- -> C` (their cited evidence was retired), and `REFACTORING.md` moves three Kill List rows to Completed. **`machinery-version: 56`.** |
| **v0.7.25** | 2026-09-16 | Patch | opencode (DeepSeek V4.1 Flash) | **Two-lane classification + one `machinery-version` writer per batch** (T1-E3.11 - wave 6 of Sprint 9, the sprint's final wave). Two changes and one refusal. **Predicate:** `scripts/sprint_eligible.py`'s overlap matcher goes **segment-wise** — `*` matches one segment, `**` matches zero-or-more, and a literal segment still matches only itself (`src/a` does not swallow `src/ab`) — which **closes the mid-path-wildcard ceiling** that both `plan-lifecycle.md` and the module docstring had recorded as a `ponytail:` ceiling naming this exact upgrade path; the fixture that pinned the old *miss* is inverted into its positive form, and strictness is one-directional (the matcher is a superset, so it can only ever skip more). **Lanes:** the payload gains an advisory `lanes` (`serial`/`parallel` per queued plan) plus the echoed `reserved_surfaces`, defined once in `plan-lifecycle.md` § Claim Protocol → *Reserved Surfaces & the Lane Model*: the surfaces whose write is **global rather than file-local** (the counter and its release row, the prefix lock, the changelog, the sprint/backlog registers) plus the **prefix-embed cascade** — a `ptp-*` `SKILL.md` edit forces a `-Sync` that rewrites a reserved agent file, so a classifier without that rule would call this template's most common edit lane-B and be wrong. `parallel_groups` becomes lane-aware (a serial-lane plan is never packed). **Counter:** `machinery-version` gets exactly **one writer per batch** — the follow-up batch wrap-up, reading the value live at that moment — canonically in the new § Claim Protocol → *Counter Ownership* and enforced across `agent-wrap-up` § Batch Scope / Phase 7b, `sprint-run` §§ 3/6, `ptp-parcel-fast`, `pass-the-parcel` § Batch Runner, `parcel-sprint` (unique content) and `HOW-TO.md`. **Refused, with reasons recorded in the plan:** the plan's parallel **worktree** lanes and its "one `-Sync` per batch" both contradicted live machinery and were removed rather than papered over — deviation 3 (`trunk-sequential`) is **unchanged** and § Deviations stays at **four**; and a deferred prefix regenerate would leave `check-parcel-prefix.ps1` red in exactly the window the next claim's green-baseline preflight inspects, so the repair stays with the edit while the host owns only the invariant. Lanes are therefore a **classification, not a concurrency**: the batch still executes serially, and an empty lane B is the honest normal case here. **This run obeyed the legacy per-plan model** — the batch wrap-up that now owns the increment is created by this very change, so the live `54` was read and bumped once to `55` here; from the next batch onward the wrap-up owns it (a redundant earlier bump is harmless: the contract is strictly-increasing recorded values, not a count). `sprint-run` v7→v8, `ptp-parcel-fast` v5→v6 (agent embed re-inlined), `agent-wrap-up` v12→v13, `pass-the-parcel` v21→v22, `SPRINTS.template.md` v4→v5; the predicate's fixture suite grows 24→29 cases. machinery-version: 55. |
| **v0.7.24** | 2026-09-16 | Patch | opencode (DeepSeek V4.1 Flash) | **MULTI-worthy triage flag + batch pause/yield** (T1-E3.10 - wave 5 of Sprint 9). The batch preset is locked `AUTO` + `SINGLE`, so a cross-cutting plan committed to a sprint runs with **no independent reviewer** - and nothing said so: Sprint 9's own `Agents: MULTI` values were scaffold boilerplate no surface ever consulted. The flag is therefore a **union**, which is what makes it survive that failure mode. **Declared:** `@sprint-plan` § 4c scores each candidate against the canonical five signals (`@pass-the-parcel` Agent Topology, cited not restated) and writes `triage: MULTI|SINGLE` into the plan's **claim front-matter** - the commit-time *recommendation*, deliberately distinct from the frozen `Plan Settings` `Agents` config (the field this sprint proved untrustworthy when scaffolded), with the signal names recorded in `sprint.md` Delivery Model's new `Flag` column. **Mechanical backstop:** `scripts/sprint_eligible.py` gains the advisory `complexity` key (`{triage, signals, multi_worthy, blocks}` per queued plan; the `parallel_groups` precedent - data, not a predicate clause): `blast-radius` fires when a plan declares more than 3 `touches` (the triage table's own "<= 3 files, one domain" low bound), an unrecognised `triage` value fails **open** to the backstop rather than halting a batch on a typo, and `blocks` is the transitive closure of still-queued dependents a deferral would strand. **Fork + yield:** the host's existing single-yes/no preview (`sprint-run` § 1 step 7 - whose step 6 now *runs* the script instead of dry-running the loop "on paper") presents every flagged plan for **accept batch risk** or **defer to manual**; a deferral is a **pure pause at that plan's slot** - the loop never claims past the hole - and the report carries a `DEFERRED-MANUAL` row with the topology needed, the stranded dependents and the resume path. It is a **halt, never a fifth deviation**: `plan-lifecycle.md` § Deviations keeps its four and gains a sentence saying so, and a new § Claim Protocol subsection *Multi-worthy Yield* holds the semantics. `ptp-parcel-fast` records the flag in its claim-time Plan Settings provenance line, so a flagged plan that ran `SINGLE` says so at Gate D instead of leaving the risk in a transcript. Fixtures 18->24, including a **counterfactual pin**: all six Sprint 9 waves' declared `touches` counts (10-15) flag, and a live read-only audit over copies of the six real plans reports `flagged = 6 of 6` with `blocks` = `T1-E3.09 -> {T1-E3.10, T1-E3.11}`. Surfaced across `sprint-plan` v5->v6, `sprint-run` v6->v7, `pass-the-parcel` v20->v21, `ptp-parcel-fast` v4->v5, `parcel-sprint.agent.md`, `plan-lifecycle.md`, `template-plan.md`, the prefix + seed (9-agent `-Sync`, exit 0) and `HOW-TO.md`. machinery-version: 54. |
| **v0.7.23** | 2026-09-16 | Patch | opencode (DeepSeek V4.1 Flash) | **Batch eligibility becomes an executable, fixture-tested predicate** (T1-E3.09 — wave 4 of Sprint 9, the batch's hard checkpoint). The predicate (dependency satisfaction + `touches` overlap + queue-order fixpoint) lived only as prose restated across `plan-lifecycle.md`, the `sprint-run`/`ptp-parcel-fast` skills, `pass-the-parcel` § Batch Runner, `HOW-TO.md` and the `parcel-sprint` host — the class of gap that cost Sprint 8 its single-verdict batch. New **`scripts/sprint_eligible.py`** (stdlib-only; imports the repo's single flat-front-matter reader trio from `wiki_lint.py` rather than forking a second dialect) resolves the ACTIVE sprint from `SPRINTS.md` (or an explicit `--sprint-dir`), reads the queue + `.devops/plans/` + `.devops/archive/`, and prints **one JSON object**: `sprint`, `queue`, `eligible`, `claim_order`, `parallel_groups` (advisory layers, data only — `T1-E3.11` owns any scheduler), `skipped` (each entry a **`reasons` list**, so the dependency verdict and the write-set verdict stay separately reportable), `in_flight`, `orphans`, `already_phased`, `counts`. **Exit `0` = computed, exit `1` = error with a stderr cause and NO JSON** — and the host is now **authoritative on that output**: `parcel-sprint` runs the script, acts only on its JSON, and HALTS on a non-zero exit; falling back to prose is explicitly forbidden, because that would re-install the untested dialect exactly when the tested one fails. **`scripts/tests/test_sprint_eligible.py`** (stdlib `unittest`, 18 cases) drives the real CLI over temp trees — dependency chain, mutual cycle, overlap skip, satisfied-via-`GATE_D_USER_APPROVAL`, in-flight dependency, `already PHASE_9`, template exclusion (proved by an unchanged payload), archive code fallback, normalisation, the documented mid-path-wildcard **miss**, parallel-group layering, determinism, and five halt paths (no ACTIVE row, `--root` missing, no front-matter, failed reader import, `--sprint-dir` override) — and `validate.yml` gains one step running it (fixtures only: the live zero-arg run exits `1` whenever no sprint is ACTIVE and must never gate CI). Mirrored across `plan-lifecycle.md` § Claim Protocol (one bullet naming the script the predicate's *executable embodiment* — the definition stays canonical) and § Deviations (`Executed by …`), `sprint-run` v5→v6 (§ 2 *computed, not reasoned* + § 5 script-error stop-the-line), `ptp-parcel-fast` v3→v4 and `pass-the-parcel` v19→v20 (cite, never restate), `parcel-sprint.agent.md` (§ Workflow step 1 + § Hard halts), and `HOW-TO.md` § Sprint Batch Runner. The script joins `portable_files` (the portable host names its path); the fixture test stays local because `validate.yml` is not portable. Self-applying evidence: the live run reports `T1-E3.10` **dependency-satisfied** by this plan's terminal `GATE_D_USER_APPROVAL` while still returning a `touches overlap:` skip — the clause separation the design depends on, demonstrated rather than asserted. machinery-version: 53. |
| **v0.7.22** | 2026-09-16 | Patch | opencode (DeepSeek V4.1 Flash) | **Wave-2 residual closed: the retired negative `AUTO` test removed from three live surfaces** (Sprint 9 wave 3 wrap-up). `T1-E3.07` replaced the negative auto-clear test with the positive-evidence contract and grepped only two surfaces for the old wording, so three live copies survived its "mirror gone" claim — `.devops/skills/pass-the-parcel/SKILL.md` § Review Gates, `.devops/rules/plan-lifecycle.md` § Modes/Agents, and `.devops/agents/parcel.agent.md` rule 14, the last of which still quoted the retired test **verbatim** ("outputs present, no `REJECTED` verdict, no `Unresolvable:` entries") in the main orchestrator's own gate rulebook. All three now clear `AUTO` Gates A-C only on positive evidence and **cite** `plan-lifecycle.md` § AUTO Gate Evidence Contract instead of restating a test. `pass-the-parcel` v18→v19. `.devops/rules/process-lessons.md` gains the generalisable lesson: verify a rule's retirement by grepping its distinctive phrase across the **whole** machinery surface and counting live copies before claiming retirement — and have Gate D evidence name the surfaces swept, not just the match count. Retiring a rule and proving it retired are separate jobs. machinery-version: 52. |
| **v0.7.21** | 2026-09-16 | Patch | opencode (DeepSeek V4.1 Flash) | **Batched Phase 3 questionnaire** (T1-E3.08 — wave 3 of Sprint 9). Phase 3's "one question at a time (non-negotiable)" becomes a **mode**: the whole decision surface is relayed in one batched questionnaire where the ask tool accepts a question **array** (`question`), and one call per question where it accepts a single question (`vscode_askQuestions`) — the tool's own signature is the capability test, no probe and no hard-coded surface list, and the final `"Is this all the context required?"` confirm is always asked **on its own**, never as a questionnaire row. The 5-8 budget and the Phase 10 overflow route are unchanged, and the `AUTO`/batch path still asks nothing (`ptp-parcel-fast` never calls an ask tool; `ptp-phase3-answerer` resolves from the Research Map) — the rule is scoped to the interactive relay. `ptp-context-hunter` v5→v6 **deletes** its two duplicate statements of the rule (the non-negotiable bullet and the `Interactive Interrogation` bullet) in favour of one authoritative bullet, and § 5 gains the standalone-confirm clause; `pass-the-parcel` v17→v18, `ptp-phase3-answerer` v2→v3, `template-plan.md`'s Gate A halt note, the ORCHESTRATOR-ONLY delegation-map row + its seed mirror, `parcel.agent.md` steps 7-8 and `ptp-context-hunter.subagent.md`'s relay hard rule are swept in the same commit — the rule lived in **seven** live places, and a partial flip is indistinguishable from drift. The reversal rationale is staged in `.devops/rules/process-lessons.md`; the wiki's live rule row (KC Quick Reference 8) is corrected and `00-system-index.md` slot 18 re-stamped. Prefix re-inlined over the 4 affected agents (byte-identical, `PASS`); encoding ALL CLEAN. machinery-version: 51. |
| **v0.7.20** | 2026-09-16 | Patch | opencode (DeepSeek V4.1 Flash) | **Batch wrap-up: `touches`-staleness lesson + two documentation drifts corrected** (Sprint 9 wave 2 wrap-up). `.devops/rules/process-lessons.md` gains the lesson both Sprint 9 waves proved the hard way: in a *self-referential* sprint a plan's `touches` goes stale as its siblings land, because an earlier wave can move a later plan's canonical home (E3.06 relocated the AUTO clause into `plan-lifecycle.md`, which E3.07 then had to declare) — so `touches` is re-verified against the plan's body **and** the live canonical-home convention immediately before claiming. Two pre-existing drifts observed by waves 1 and 2 and left untouched by both are corrected here: `HOW-TO.md` claimed **three** `@sprint-run` deviations where `plan-lifecycle.md` lists **four** (retirement-as-a-separate-step joined in `T1-E3.04`), and `.devops/backlog/backlog-index.md` linked a `TRIAGE.md` this repo has never had — now stated as not-yet-adopted, with the seed path named. No behaviour change; documentation and register truth only. machinery-version: 50. |
| **v0.7.19** | 2026-09-16 | Patch | opencode (DeepSeek V4.1 Flash) | **Positive-evidence AUTO gates** (T1-E3.07 — wave 2 of Sprint 9). `AUTO` cleared Gates A-C on the **absence** of a rejection ("no `REJECTED` line, no `Unresolvable:`"), so an empty or vague self-review passed by omission — and in the batch path, where `SINGLE` records Gate C `N/A` and no adversarial reviewer runs, that self-review was the only check before Gate D. The negative test is replaced by one canonical **AUTO Gate Evidence Contract** in `.devops/rules/plan-lifecycle.md` § AUTO Gate Evidence Contract — stated once, cited by `@pass-the-parcel` § Review Gates, `ptp-parcel-fast` § Auto-clear test and `@sprint-run` § 5 (the same cite-never-restate shape T1-E3.06 gave the write-set predicate). Evidence is **presence-based**: **P1** (after stripping fenced code blocks and code spans, no line-leading unchecked box and no bare `TBD`/`TODO`/`FIXME` in the gate's evidence blocks — the strip step is what makes the test quotation-safe, and forward work lists are the named exemption) plus **P2** (no line-leading `**REJECTED:**`/`Unresolvable:`, and no Phase 6 `**Verdict:**` other than `PASS`), plus a per-gate row: A needs an answer per `Q#` plus the final-validation verdict; B needs an acceptance-criterion row with a criterion **and** `Test Target` (or the recorded "no wiki delta" line) plus ≥1 file-level step; the `SINGLE` Phase 6 self-review needs ≥1 row naming an AC `#` with **both** cells filled. An unproven gate is now a **stop-the-line** — never a `REJECTED` verdict (that routes to `PHASE_5_REVISION`), never a silent skip: `@sprint-run` § 5 gains the cause and `ptp-parcel-fast` returns `HALT <code>: unproven <gate> — <missing artifact>`. The contract is deliberately scoped away from Phases 8-9 — Gate D stays the human's. Three flaws surfaced by this parcel's own inline Phase 6 audit were corrected **before** the verdict, not papered over: the unchecked-box arm would have failed every plan at Gate B because its own `### To-Do List` is `[ ]` by design; P2 missed a rejection recorded as a `**Verdict:**` value; and a naive substring test matched the contract's own quotation of the very tokens it forbids. Prefix cascade: `base-context.md` + its seed (shared-prefix **and** orchestrator-only clauses) re-inlined byte-for-byte by `-Sync` into **9** locked agents (`PASS ×9`, 11 model bindings, `check-utf8-agents.ps1` ALL CLEAN at 228 files); `template-plan.md` gains the Phase 6 acceptance-criterion-keyed self-review table and the Phase 9 command + exit-code evidence table; `HOW-TO.md`'s three operator-facing `AUTO` clauses align. Skill versions `pass-the-parcel` v16→v17, `ptp-parcel-fast` v2→v3, `sprint-run` v4→v5. machinery-version: 49. |
| **v0.7.18** | 2026-09-16 | Patch | opencode (DeepSeek V4.1 Flash) | **Sprint 8 gate & preflight coverage gaps** (T1-E3.06 — wave 1 of Sprint 9). Three deterministic gaps, each found while *executing* Sprint 8 rather than while planning it, closed as one parcel. **G1** — `.devops/sprints/**` joins `check-utf8-agents.ps1`'s scan list: sprint-queue plans were the only plan-state artefact with no encoding coverage (the gap went live at Sprint 8 planning, when the scan count fell 218→216 as three plans moved out of `.devops/backlog/`), so a queue plan could carry mojibake or a stray BOM straight into the batch runner's context. **G2** — `@sprint-plan` § 4 gains a **commit-time wave preflight** that runs the write-set predicate over the candidate set *against itself* and records the predicted wave decomposition in `sprint.md` § Delivery Model, with the accepted cost stated explicitly (**N waves = N Gate D verdicts and N wrap-ups**, not one consolidated verdict, because an executed plan holds its files until its wrap-up archives it). The predicate is now stated **once**, canonically, in `plan-lifecycle.md` § Claim Protocol → *Write-Set Overlap Predicate* (normalize / entry- and plan-level overlap / the `GATE_D_USER_APPROVAL`-still-blocks consequence / the "never relax it to make a queue batch in one pass" clause + the mid-path-wildcard ceiling); `sprint-run` § 2 cites it instead of restating a second dialect, and the matured `[2026-09-13] touches-overlap` lesson is folded into its owning surfaces and **deleted** from `process-lessons.md` (its recorded fold target was this plan). **G3** — `spaghetti-monster-scan.cjs` no longer hard-codes `src/`: a missing root prints a one-line skip and the run continues at exit 0 (no more `ENOENT … \src` crash at sprint close), and the machinery roots `scripts/`, `.devops/skills/`, `.devops/agents/`, `.devops/templates/` join the scan under the same thresholds — the template's actual product surface is finally policed instead of invisible. Non-ECMAScript rows are ranked on line count only (their `CCN`/`imp`/`fn` columns print `-`, meaning "not measured", never "clean"); CommonJS `require()` now counts toward the CBO proxy; a missing sibling test only penalises files under `src/`. G3's second half — **the refactoring lane had no live register** — is resolved by adopting `.devops/backlog/REFACTORING.md` from its seed (as `SPRINTS.md` was adopted at Sprint 8 planning), seeded with the first machinery scan and its two caveats, plus a pointer row from `backlog-index.md`; the **opt-in fallback** is stated in `@sprint-close` § 2/§ 6 and `@sprint-plan` § 3 (a satellite without the register records scan findings in the retro, explicitly, rather than promoting them or creating it implicitly). Skill versions: `sprint-plan` v4→v5, `sprint-run` v3→v4, `sprint-close` v5→v6. machinery-version: 48. |
| **v0.7.17** | 2026-09-16 | Patch | opencode (DeepSeek V4.1 Flash) | **Batch sprint wrap-up** (T1-E3.04). Wave 3 of Sprint 8 closes the loop `T1-E3.03` opened: a batch whose plans stop at `GATE_D_USER_APPROVAL` had no way to *finish* them except running the heavy per-plan wrap-up N times inline. `@agent-wrap-up` v11→v12 gains a **batch scope** — input is a list of plan codes (default: every `.devops/plans/` plan carrying `GATE_D_USER_APPROVAL`), **Phase 4 iterates once per plan while every other phase runs once per batch** (one diff, one changelog entry naming the theme, one wiki reconciliation, one backlog sweep, one consolidation, one gate pass). `@agent-wrap-up` stays the **only** wrap-up owner — batch scope is a scope, not a second skill. Its **confirmation gate** is safety-critical and not simplifiable: per plan it asserts bottom `Status: PHASE_9`, `claim_status: GATE_D_USER_APPROVAL`, a `DONE` per-plan outcome, Phase 9 evidence plus acceptance criteria, and the exact `plan: <code>` commit on the trunk; then the repo gates run **once** for the set. A plan failing **any** assertion is **carry-forward**, never marked complete, and a red repo gate blocks the whole batch (a red tree is never auto-cleaned). **Retirement is a separate, operator-invoked step** — the batch loop never archives — recorded as a **fourth** named deviation in `plan-lifecycle.md` § Deviations (intro "three"→"four"; § Claim Protocol step 5 pointer) and mirrored in `pass-the-parcel` v15→v16 § Batch Runner, `sprint-run` v2→v3 §§ 4/6/7 (the approved exit now runs the follow-up batch wrap-up instead of N per-plan wrap-ups; § 6's "What to do next" is the only hand-off), `HOW-TO.md` § Sprint Batch Runner (three deviations named, was two), and the `parcel-sprint` agent's unique content (Workflow step 5 + hard rules: wrap-up is never inline). **Spawn surface** (`D5`, `CW2`): `agent.parcel-sprint.permission.task` gains exactly one **named** key, `"wiki-writer": "allow"` (the wrap-up's read-heavy wiki prose), beside `"ptp-parcel-fast": "allow"` under a retained `"*": "deny"` — **no glob**, because a `wiki-*` glob would hand the host a class of targets and undo the structural batch preset; mirrored into the seed `opencode.template.json` v8→v9 and the batch-host prose in `.devops/rules/agents-and-skills.md` § The "Batch Host" Pattern. That widening falsified the shared prefix's own claim ("narrowed to exactly `ptp-parcel-fast`"), so the sentence was corrected **in `base-context.md` + its seed** (`base-context.template.md` v14→v15) and re-inlined by `check-parcel-prefix.ps1 -Sync` — never hand-edited in an agent file. `D6` (never close a sprint over a surviving `GATE_D_USER_APPROVAL` plan) was **already landed** by wave 2 in `sprint-close` v5 § 1; verified and left untouched. machinery-version: 47. |
| **v0.7.16** | 2026-09-16 | Patch | opencode (DeepSeek V4.1 Flash) | **Executed-to-Gate-D becomes a first-class claim status** (T1-E3.03). The batch runner's eligibility clause ("every `depends_on` present in `.devops/archive/`") contradicted its own terminal state (it never archives; every plan stops at `PHASE_9`), so any sprint with an intra-queue chain executed only its dependency roots — Sprint 7's operator ruling worked around it at runtime. `GATE_D_USER_APPROVAL` is now the terminal claim status of every path reaching Phase 9, and `depends_on` satisfaction is a **disjunction**: a code is satisfied by archive **or** by `.devops/plans/` + `GATE_D_USER_APPROVAL`, so the manual wrap-up and batch paths compose with no mode flag. The dead `IN_PROGRESS` value is retired (nothing ever set it) — enum `QUEUED \| CLAIMED \| GATE_D_USER_APPROVAL \| COMPLETE`. `@sprint-run` gains a **fixpoint loop** (each pass claims at most one plan, then re-applies the predicate to the live `.devops/plans/`; bounded by queue length, deterministic in queue order, cycle-safe) plus a preflight **dependency-cycle check** and **resolution forecast**. The predicate is stated **once** in `plan-lifecycle.md` § Claim Front-Matter and referenced everywhere — mirrored into the `base-context.md` shared prefix (re-inlined by `-Sync` into **9** locked agents, `PASS ×9`, 11 bindings), the seed, `pass-the-parcel` v14→v15, `sprint-run` v1→v2, `ptp-parcel-fast` v1→v2 (the same rule, both layers — a one-layer fix orphans a claim-then-skip), and the transitional readers `sprint-status` v2→v3 (recognise the state, treat it as satisfied) and `sprint-close` v4→v5 (never close over an unwrapped plan); `sprint-plan` v3→v4 records that queue order is the first claim order, not an execution dependency. Docs/seeds: `HOW-TO.md`, `SPRINTS.template.md` v3→v4, `base-context.template.md` v13→v14, `template-plan.md`, `parcel-sprint.agent.md` (unique content). machinery-version: 46. |
| **v0.7.15** | 2026-09-15 | Patch | opencode (DeepSeek V4.1 Flash) | **Parcel Fast becomes subagent-only** (T1-E3.05). "Parcel Fast" existed twice — a selectable orchestrator (`.devops/agents/parcel-fast.agent.md`) and the hidden batch subagent (`ptp-parcel-fast.subagent.md`) — one prefix apart, a documented confusion hazard. The selectable one is retired and every derived surface removed **together** (the prefix check's bidirectional registry↔file↔config pairing makes a partial edit a hard FAIL): the agent file, its `## Model Registry` row, its `## Orchestrator Presets` row, and its `opencode.json` + seed `agent.parcel-fast` entries. The prefix header, the `ORCHESTRATOR-ONLY:START` marker comment and the presets prose now name only the survivors (`parcel` = `ask`; `parcel-sprint` = locked batch host); `check-parcel-prefix.ps1 -Sync` re-inlined the prefix byte-for-byte into the **9** remaining locked agents (2 orchestrators + 7 `ptp-*`; 10 → 9, PASS ×9, 11 model bindings). `prune_files` gains `.devops/agents/parcel-fast.agent.md` — load-bearing, because a satellite that already synced would otherwise keep an orphan file whose registry row is gone and fail its own prefix check. Docs/seeds mirrored: `AGENTS.md` + `AGENTS.template.md` v12→v13, `.devops/README.md`, `agents-and-skills.md`, `HOW-TO.md` (§ Parcel-Fast rewritten as the subagent-only fast lane), `.wiki/rules/naming.md`, `model-routing`, `pass-the-parcel` v13→v14 (its `locked` example is now `parcel-sprint`), `template-plan.md`, `base-context.template.md` v12→v13, `opencode.template.json` v7→v8. The interactive `/parcel-fast` shortcut is a deliberate loss: interactive fast runs are `parcel` + `AUTO`/`SINGLE` at plan start; unattended runs go through `parcel-sprint`. machinery-version: 45. *(44 at plan close; the wrap-up then folded a fold-target annotation into `.devops/rules/process-lessons.md` — a synced rules file — so the release lands at 45.)* |
| **v0.7.14** | 2026-09-14 | Patch | opencode (DeepSeek V4.1 Flash) | **Knowledge Capture gets a second destination** — machinery/process/tooling lessons now have a home of their own. The wiki documents the *app*, so a process rule captured in `.wiki/core/18-knowledge-capture.md` (KC) could never graduate out of it: KC was a one-way ratchet, and every sprint's machinery lessons accumulated permanently. Capture and consolidation now **route by subject** — app-domain → KC; machinery/process/tooling (parcel/sprint pipeline, dev toolchain, scripts, docs tooling) → the new `.devops/rules/process-lessons.md` staging register, which folds a matured rule into its owning skill or `plan-lifecycle.md` and is reviewed at sprint close (`sprint-close` v3→v4 gains that step, so the register's own maintenance promise is not dead). `knowledge-capture` v6→v7 adds §3 Destination Routing plus a **one-entry-per-session budget**; `knowledge-consolidation` v7→v8 adds Phase 6b (`promote-machinery`) and **retires the line-based size metric in favour of an entry count** — tidy ceiling 25 entries, full audit fires above 25 and fails above 40 — because hard-wrapping inflated physical lines without adding a rule, so the old trigger fired on formatting rather than growth. That same shift had orphaned the `#hard 500-line ceiling` claim anchor, leaving `.wiki/core/18-knowledge-capture.md` asserting a contract its own skill no longer stated; the doc's scope note and Knowledge System rule are reconciled and their claims restamped. `.devops/rules/` is a `portable_dir`, so the register rides to satellites with the skills. machinery-version: 43. |
| **v0.7.13** | 2026-09-14 | Patch | opencode (DeepSeek V4.1 Flash) | **Chunked Write Discipline** — a universal large-file authoring rule, born from satellites whose plan/sprint-writing subagents stalled on OpenCode's `write` tool ("Preparing write…"). The tool takes the whole file as one JSON `content` string and runs a synchronous diff over it before the permission prompt, so a single oversized write blocks the UI and looks hung; because `write` **overwrites** and cannot append, the fix is skeleton-then-fill: one small `write` (frontmatter + section headings, each with a unique placeholder) then one small `edit` per section replacing its placeholder, capped at ~60–100 lines, resuming from `read` after a stall instead of re-sending the payload. Landed as rule 9 in the `base-context.md` shared prefix (re-inlined into all 10 locked agents, prefix PASS), rule 10 in `AGENTS.md`, a `<!-- MACHINERY -->` rule 10 in seed `AGENTS.template.md` v11→v12 (subsequent rules renumbered: 11 sprint, 12 customize), and rule 4 in seed `base-context.template.md` v11→v12; `sprint-plan` v2→v3 gains a mandatory chunked-write step in §5 (the immediate offender — large `sprint.md`). Scope note: the skill + seeds transport automatically, but a satellite's `base-context.md` is repo-specific, so an already-bootstrapped satellite must add the rule to its own prefix (or re-seed) to carry it into its agents. machinery-version: 42. |
| **v0.7.12** | 2026-09-13 | Patch | opencode (DeepSeek V4.1 Flash) | Sync transport completeness (T1-E2.04), closing the two findings the sync audit left open (F8/F9, continuing T1-E2.03's F1–F7). **F8** — `sync-architecture.ps1` `Update-TargetModelBindings` step 3 now **inserts** a missing `agent.<key>` entry into a satellite's `opencode.json`, sourced verbatim from the target's own synced `.devops/templates/opencode.template.json` (new `Find-MatchingBrace` / `Get-SeedAgentEntry` / `Add-TargetAgentEntry`; anchored before the agent object's closing brace, EOL-preserving, idempotent, `-DryRun`-clean). A registry key the satellite had **never authored** previously landed as an agent *file* the runtime never mounted, reported `BINDING-SKIP`, and failed the satellite's own `check-parcel-prefix.ps1` and the whole pull (exit 1) — reproduced on a throwaway target. Entries the satellite already authored are never restructured (permissions/order/formatting preserved; only the `model` value is stamped), which retires the recorded `ponytail:` ceiling in `t1-e1.03` exactly as its own named upgrade path prescribed. **F9** — `.devops/plans/template-plan.md` joins `portable_files`: the canonical scaffold was named by the shared prefix (`base-context.md:53`) and by five shipped skills but never travelled, so a fresh satellite got "instantiate from the canonical template" with no template. `-SelfTest` gains a satellite-shaped fixture that plants the seed config minus one registry key before the first sync and asserts insert + model + sibling byte-equality + idempotence, plus an F9 assertion that reads the *reference* out of the prefix rather than the manifest (the old assertions were manifest-fenced, so a missing entry was structurally invisible). Mirrored across `sync-architecture` v6→v7, `model-routing` v7→v8, `agents-and-skills.md`, `SATELLITE-BOOTSTRAP.md` v11→v12 (new-agent-key migration note), `.devops/README.md`, and the KC entry (§ Sync & Versioning transport-completeness rule + § Agents & Models clause). machinery-version: 41. |
| **v0.7.11** | 2026-09-13 | Patch | opencode (DeepSeek V4.1 Flash) | Pre-satellite-sync audit tidy-up (T1-E2.03) — seven findings from a pre-sync architecture audit closed. **F1** — `sync-architecture.ps1` now **inserts** Model Registry rows the target's `base-context.md` lacks (after the last existing row), so v39's registry growth (10 → 12 rows) reaches an already-bootstrapped satellite; previously the target's own `check-parcel-prefix.ps1` failed `no Model Registry row for 'wiki-writer'` and the sync exited 1 with no automated repair (reproduced on a throwaway target). `Get-RegistryBindings` gains the capability-class cell so inserted rows are well-formed. **F2** — `prune_files` gains `.devops/agents/parcel-compactor.md` (retired 2026-08-22 while `.devops/agents` was already portable; omitted from the v0.3.3 prune list), and the comment is corrected. **F3** — `check-utf8-agents.ps1` now scans `.devops/rules/**` + `.devops/templates/**` (both portable and agent-read). **F4** — the T1 register is backfilled with the completed `T1-E1.03` row (archived + changelogged but never registered). **F5** — `MATURITY.md` reassessed for machinery 38–40. **F6** — root `CHANGELOG.md` "Earlier releases" backfilled (v0.7.6/8/9/10/11). **F7** — the Gate-A-in-`AUTO` contradiction recorded as CW2 by T1-E3.02 is reconciled: `AUTO` auto-clears Gates A–C; **only Gate D always halts** — fixed in `base-context.md` (+ seed, re-inlined into all 10 locked agents, prefix PASS) and mirrored in `pass-the-parcel` v12→v13, `plan-lifecycle.md`, `HOW-TO.md`, `parcel.agent.md` (unique content), `parcel-fast.agent.md` (unique content). Docs/seeds: `SATELLITE-BOOTSTRAP.md` v10→v11 (registry-row migration note), `model-routing` v6→v7, `sync-architecture` v5→v6, `agents-and-skills.md`, `base-context.template.md` v10→v11. machinery-version: 40. |
| **v0.7.10** | 2026-09-13 | Patch | opencode (DeepSeek V4.1 Flash) | Model bindings become **registry-canonical and template-owned** (T1-E1.03). The `## Model Registry` table in `base-context.md` is now the single source; agent frontmatter `model:` and `opencode.json` `agent.<key>.model` are **derived** surfaces that `sync-architecture.ps1` **force-stamps** into every satellite on each sync (no preservation branch, before PREFIX-LOCKED regeneration) — a satellite-side rebind is transient. The pre-v20 VS Code-only opt-out is **retired**: a missing/empty `opencode.json` `agent` block is now a FAIL, not a SKIP. `check-parcel-prefix.ps1` additionally validates the seed registry and seed opencode config cell-for-cell against the live table, fails on a registry key with no agent file and on a binding file with no row, and treats `<your provider/model>` as fatal — the binding pass is **decoupled** from the prefix pass so the two `wiki-*` agents (new registry rows: `wiki-writer` → deep planning/authoring, `wiki-verifier` → independent audit) can be validated without entering it. Registry grows 10 → **12 rows**; seeds ship the concrete binding; `-SelfTest` asserts propagation. Mirrored across `model-routing` v5→v6, `pass-the-parcel` v11→v12, `base-context.template.md` v9→v10, `opencode.template.json` v6→v7, `SATELLITE-BOOTSTRAP.md` v9→v10, `AGENTS.md` + `AGENTS.template.md` rule 9, `agents-and-skills.md`, `HOW-TO.md`, `README.md`, and the KC entry (Dual-surface → registry-canonical triple + the T1-E1.01 reversal). machinery-version: 39. |
| **v0.7.9** | 2026-09-13 | Patch | opencode (DeepSeek V4.1 Flash) | Sprint batch runner (T1-E3.02). New selectable host `parcel-sprint` + `@sprint-run` skill walk the committed sprint queue and run each eligible plan Phases 1→9 unattended, one at a time in a fresh context, by spawning one hidden `ptp-parcel-fast` subagent per plan (locked `AUTO`+`SINGLE`). Plans are claimed on the trunk (`git mv` + `claim: <code>`) and terminate at `PHASE_9` with Gate D `OPEN`; one consolidated report and one human verdict cover the whole batch — Gate D is deferred, never skipped. Three named deviations: **batched Gate D**, **trunk-sequential** claim (drops `git worktree add`), and the **Strict Context Isolation** exception (one plan's 1→9 in a single context), mirrored into the `base-context.md` shared prefix (→ `-Sync` → every locked agent), `plan-lifecycle.md` § Deviations, `agent-wrap-up`, and `pass-the-parcel` v10→v11. `parcel`'s `task` block gains an exact `ptp-parcel-fast: deny` after the `ptp-*` glob; the host binds `task` to only `ptp-parcel-fast`. Mirrored across `opencode.json`, seeds (`opencode.template.json` v5→v6, `base-context.template.md` v8→v9, `AGENTS.template.md` v9→v10, `SPRINTS.template.md` v2→v3), `AGENTS.md`, `.devops/README.md`, `HOW-TO.md`, `agents-and-skills.md` (new "batch host" pattern), `.wiki/rules/naming.md`, `SATELLITE-BOOTSTRAP.md`, and `check-parcel-prefix.ps1` comments. Prefix PASS ×10 (was ×8). machinery-version: 38. |
| **v0.7.8** | 2026-09-13 | Patch | opencode (DeepSeek V4.1 Flash) | Parcel-Fast locked preset. New selectable orchestrator `parcel-fast` ships the fast path as a **locked preset** — `Mode=AUTO`, `Agents=SINGLE` — declared in a new `## Orchestrator Presets` table in `base-context.md`'s orchestrator block (`parcel` stays `ask`). It skips the Mode/Topology questions, plays every persona inline, and `opencode.json` binds it `task: deny`, so `SINGLE` is enforced structurally, not by restraint. Presets change who executes and which questions are asked — never the hard halts (Gate A + Gate D always halt; `AUTO` auto-clears only Gates A-C). `check-parcel-prefix.ps1` locks `parcel*.agent.md` (full prefix for both orchestrators) and `sync-architecture.ps1` hashes their unique content to avoid false DRIFT. Mirrored across `parcel.agent.md` steps 2-3, `template-plan.md`, `pass-the-parcel` v9→v10, `model-routing` v4→v5, `HOW-TO.md`, `AGENTS.md`, `.devops/README.md`, `agents-and-skills.md`, `.wiki/rules/naming.md`, `SATELLITE-BOOTSTRAP.md`, and seeds `base-context.template.md` v7→v8, `AGENTS.template.md` v8→v9, `opencode.template.json` v4→v5. Prefix PASS ×8 (was ×7). machinery-version: 37. |
| **v0.7.7** | 2026-09-13 | Patch | opencode (DeepSeek V4.1 Flash) | Concurrency + sprint lifecycle rewrite. Sprints are now **concurrent, claimed** work: a plan is committed into `.devops/sprints/sprint-{n}-<slug>/` (one `sprint.md`, open→close→retro — no `retro.md`), claimed via claim front-matter (`code`/`sprint`/`claim_status`/`owner`/`claimed_at`/`last_touch`/`touches`/`depends_on`) + `git mv` into `.devops/plans/` + `git worktree add` on `plan/<code>-<slug>`; completed plans archive to `.devops/archive/` root and the sprint record moves to `.devops/archive/sprints/`. Shared files stay trunk-only. Backlog detail moved into `t{n}-<slug>-backlog.md` theme registers (front-matter `type` is the discriminator vs parked `-backlog.md` plans); `backlog-index.md` is now Triage Panel + Themes table. Updated `plan-lifecycle` (claim protocol; rules 4/6), `sprint-plan`/`sprint-status`/`sprint-close`, `pass-the-parcel` v8→v9, `backlog` v2→v3, `agent-wrap-up` v9→v10, `spaghetti-monster`/`build-roadmap`, all seeds (`SPRINTS` v1→v2 + new `sprint.template.md`), `base-context.md` (re-inlined ×7, prefix PASS) and the orchestrator workflow, `AGENTS.md`, `.devops` READMEs, `template-plan.md` (claim front-matter), and wiki docs 17/18/00 + the worked example. machinery-version: 36. |
| **v0.7.6** | 2026-09-12 | Patch | opencode (DeepSeek V4.1 Flash) | `wiki-writer` can run as a subagent on demand. `opencode.json` rebinds it `mode: primary` → `mode: all`, so it stays user-selectable while any primary agent may also invoke it via the Task tool; `parcel`'s `task` allow-list gains `wiki-writer: allow` beside `wiki-verifier`. Mirrored into seed `opencode.template.json` (version 3→4), and the agent-surface wording in `AGENTS.md`, `.devops/templates/AGENTS.template.md`, `.devops/README.md` and `.devops/rules/agents-and-skills.md` now records the dual role. No prefix or model-binding change (wiki-writer has no Model Registry row; `check-parcel-prefix` SKIPs it). machinery-version: 35. |
| **v0.7.5** | 2026-09-12 | Patch | opencode (DeepSeek V4.1 Flash) | Plan Settings promoted to a **frozen top-of-file block** (SINGLE/AUTO parcel). `Mode` + `Agents` no longer live in the cache-anchored bottom `State & Gates` table — where resumed sessions and narrowly-prompted subagents sometimes missed them — they now sit in a `## ⚙️ Plan Settings` block at the **TOP** of the plan, read before any phase; the bottom holds only mutable runtime state (Status / Active Persona / gates). Split by mutability. Moved, not copied, across `template-plan.md`, `base-context.md` (re-inlined ×7, prefix PASS ×7), `parcel.agent.md` steps 2-3, `pass-the-parcel` v7→v8, `plan-lifecycle.md`, `HOW-TO.md`, seed `base-context.template.md` v5→v6, and the KC entry (Rule #1 + Parcel Pipeline). machinery-version: 34. |
| **v0.7.4** | 2026-09-11 | Patch | opencode (DeepSeek V4.1 Flash) | Encoding-hygiene fix from a pre-commit audit. **Frontmatter:** the six `ptp-*`/`sprint-close` SKILL descriptions carried an unquoted `: ` (invalid strict YAML; now single-quoted) and `caveman` gained its missing `version`/`updated`. **Mojibake:** two portable skills — `app-vision-north-star` and `wiki-assessment` — were corrupted with **CP437 mojibake** (UTF-8 bytes misread through code page 437, so every `—`/`–`/emoji appeared as a multi-glyph CP437 misread); repaired by reversing each high-char run through `cp437→utf-8` (114 runs; 48 runs + 2 legitimate `—` preserved). **Guard gap:** `scripts/check-utf8-agents.ps1` detected only CP1252 (`C3 A2`, `C3 B0 C2`) and U+FFFD mojibake, so it reported ALL CLEAN on the corrupted files despite scanning 190 — it now also flags the CP437 lead pairs `CE 93`+`C2`/`C3` and `E2 89 A1 C6 92`. Skills bumped: ptp-code-surgeon / ptp-context-hunter / ptp-grumpy-architect / ptp-high-visionary / ptp-smooth-operator `3→4`, sprint-close `1→2`, caveman `0→1`, app-vision-north-star `2→3`, wiki-assessment `1→2`. machinery-version: 33. |
| **v0.7.3** | 2026-09-11 | Patch | opencode (DeepSeek V4.1 Flash) | PRUNE exit-code honesty in `scripts/sync-architecture.ps1 -Check`: `PRUNE` is now counted among the out-of-sync verdicts (previously flagged in the table but excluded from the tally). Prune files are also stripped from their parent directory's hash comparison, so a retired file surfaces as `PRUNE` instead of a misleading parent-dir `DRIFT` ("locally customized — ask the user") when the directory's live content still matches upstream — without this, the parent-dir `DRIFT` already forced exit 1 for the wrong reason and masked the PRUNE tally. `-SelfTest` gains a negative assertion: a planted prune file must report `PRUNE` + `OUT OF SYNC` with no `DRIFT`, and the target is clean again after re-sync. machinery-version: 32. |
| **v0.7.2** | 2026-09-11 | Patch | opencode (DeepSeek V4.1 Flash) | OKF round-trip parity (T2-E2.02 review F4): `scripts/wiki_okf.py export` now skips navigation surfaces (`README`/`index`/`*-index.md`) using the **same `is_fm_exempt_name` predicate as `import`**, so the two directions agree file-for-file (previously export emitted 56 "concepts" while import ingested 45 — 11 `*-index.md` navigation docs were exported but dropped on ingest, making the round-trip lossy and the CI count misleading). CI's OKF step now asserts **parity** (`exported concepts == imported`) instead of a bare non-zero count. machinery-version: 31. |
| **v0.7.1** | 2026-09-11 | Patch | opencode (DeepSeek V4.1 Flash) | OKF interop hardening (T2-E2.02 review F1/F2/F3/F5): `scripts/wiki_okf.py` now emits **double-quoted, backslash-escaped** YAML scalars via a `yaml_scalar` helper for `name`/`title`/`description` in both export and import — the bridge previously dropped the local schema's quoting, so a title containing `: ` (3 docs: Design System, Security Standards, Worked Example) produced YAML that no real parser accepts. CI's round-trip smoke upgraded: it now `pip install pyyaml` and parses **every exported frontmatter block** (was a file-count-only check that could not catch the bug), then runs the import. Import into the live wiki now runs `wiki_lint.py` and exits 1 if the result is lint-dirty (unindexed `core/` concepts; areas lacking `*-index.md`). `wiki_lint._fix_unindexed` promoted to public `wiki_lint.fix_unindexed`, removing `wiki_okf.py`'s private cross-module import. machinery-version: 30. |
| **v0.7.0** | 2026-09-11 | Minor | opencode (DeepSeek V4.1 Flash) | Wiki refresh automation (T2-E2.02): closes G3 + G5. **G3** — `scripts/wiki_okf.py` gains an `import` mode bridging an OpenWiki/OKF v0.2 bundle into the local wiki: translates OKF concept frontmatter to the `format-version: 1` schema as `status: in-progress` drafts, records provenance (`okf_version: "0.2"`), registers each doc in its area spoke index via the linter's canonical `_fix_unindexed`, and skips existing targets rather than overwriting authored prose (`ponytail:` skip-on-collision). **G5** — new secret-free `.github/workflows/wiki-refresh.yml` runs `wiki_claims.py check` on a weekly cron (`0 6 * * 1`) + `workflow_dispatch` and raises/updates/closes a `wiki-drift` issue (no model key, no docs PR — the docs-PR path is deferred per the repo's no-secret principle). `validate.yml` gains an OKF export→import round-trip smoke; `wiki-refresh.yml` joins `portable_files`. Skill/docs refreshed (`@wiki-generate`/`@wiki-update` v2, HOW-TO §6, SATELLITE-BOOTSTRAP §5 v8). machinery-version: 29. |
| **v0.6.1** | 2026-09-11 | Patch | opencode (DeepSeek V4.1 Flash) | Residual hygiene (T2-E2.01 review R1-R4): `scripts/wiki_claims.py check` now prints a **per-class remediation hint** — `STALE` → `update`; `MISSING`/`UNRESOLVED-SYMBOL`/`BROKEN` → fix the claim's `source` (path#symbol) — instead of always suggesting `update` (which cannot repair a symbol/path fault). Root `CHANGELOG.md` gained the missing v0.6.0 entry plus a product↔machinery track-mapping note (product v1.0.0 = machinery-version 26). `T2-E2.02-wiki-refresh-automation-backlog.md` parked plan created (G3 OKF ingest + G5 CI drift refresh). Product release **v1.0.0** tagged at `d4c3400`. machinery-version: 28. |
| **v0.6.0** | 2026-09-11 | Minor | opencode (DeepSeek V4.1 Flash) | Wiki grounding & guard hardening (T2-E2.01): closes reassessment gaps G1/G2/G4/G6/G7 so the claims layer has teeth. **G1** — `claims.md` catalogued in both `.wiki/rules/README.md` tables (`last-reviewed` re-stamped) and `wiki_lint.py` gains a hard rules-index completeness check (`[UNCATALOGUED]` when a top-level `.wiki/rules/*.md` is unlisted; `language/` out of scope). **G2** — grounded claims on core slots 00/09/12/14/17/18 (12 claims; app-scaffold slots carry none — no repository source). **G4** — `wiki_claims.py check` resolves each `#symbol` against its source (`symbol_resolves`; identifier symbols word-boundary, section titles substring) and hard-fails `UNRESOLVED-SYMBOL`; symbol-less file-level claims stay legal. **G6** — `wiki_visualize.py --check` fails when `docs/wiki-graph.md` is stale and never writes; CI adds visualizer-freshness + OKF export smoke steps. **G7** — `check-utf8-agents.ps1` now also scans root `*.md`, `docs/**/*.md`, `.github/**/*.md`. Per-symbol hashing still deferred (whole-file `ponytail:` ceiling kept). machinery-version: 27. |
| **v0.5.0** | 2026-09-11 | Minor | GitHub Copilot (OpenCode Go / DeepSeek V4.1 Flash) | Coverage-gate symbol/claims evidence (T2-E1.03): `scripts/wiki_coverage_check.py` `covered()` upgraded from filename/folder-only to a four-route OR — (a) filename, (b) parent folder, (c) any exported identifier cited in the domain index (stdlib regex export scan; `ponytail:` ceiling for anonymous default exports, re-export aliasing, and non-standard syntax), or (d) a `source` path named by any wiki doc's `claims:` block (doc→source map inverted via a lazy `from wiki_claims import claim_docs, source_path` placed strictly after the `src/`-absent no-op guard, so the template path never parses claims). `DOMAINS`, the 5-entry `ALLOWLIST`, and the exit-code contract are unchanged; prose refreshed in `.wiki/rules/claims.md` (§Coverage Evidence), `agent-wrap-up` v8→v9, and `SATELLITE-BOOTSTRAP` §5 v6→v7. machinery-version: 26. |
| **v0.4.0** | 2026-09-11 | Minor | GitHub Copilot (OpenCode Go / DeepSeek V4.1 Flash) | Wiki self-maintenance (T2-E1.01): one unified versioned frontmatter schema (`format-version: 1`) across all 43 content docs, with `.wiki/rules/**` brought under the required-fields check in `wiki_lint.py`; a Grounded Claims layer (`claims: id` / `source: path#symbol` / `sha256(file)`; `scripts/wiki_claims.py check\|affected\|update`; `.wiki/rules/claims.md`) with a secret-free CI drift step; new `@wiki-update` skill (git-diff -> affected docs -> revise -> stamp `last-reviewed`) and native `@wiki-generate` skill with `@wiki-bootstrap` demoted to the verification pass (v2); OpenWiki/OKF v0.2 interop via `scripts/wiki_okf.py export`; static `docs/` visualizer via `scripts/wiki_visualize.py`. machinery-version: 25. |
| **v0.3.17** | 2026-09-11 | Patch | GitHub Copilot (OpenCode Go / DeepSeek V4.1 Flash) | CI self-test hotfix: `scripts/sync-architecture.ps1` `-Check`/`-SelfTest` crashed on Linux with `Get-Item: Could not find item .../.vscode` — on Unix dot-prefixed names are hidden and `Get-Item`/`Get-ChildItem` skip hidden items without `-Force` (while `Test-Path` still returns true). `.vscode` is the only portable-surface leaf that is dot-prefixed, so only it tripped. `Get-ItemHashes` now uses `-Force`, as do the `-SelfTest` mirror and nested-copy guards; the Ubuntu `SelfTest sync engine` gate goes green. machinery-version: 24. |
| **v0.3.16** | 2026-09-11 | Patch | GitHub Copilot (OpenCode Go / DeepSeek V4.1 Flash) | Agent topology axis: pass-the-parcel gains a second, orthogonal axis — `Agents: MULTI` (comprehensive plan: `ptp-*` delegation, independent Group C reviewers, 4 gates) vs `Agents: SINGLE` (fast plan: orchestrator executes each group's persona inline, no `task` spawns, Group C skipped, Gates B+C merged into one approval at Gate B with Gate C `N/A`), selected by task complexity (blast radius / contract change / risk / ambiguity / novelty) and user-confirmed at plan start. New §Agent Topology (triage table + per-topology flow) in `pass-the-parcel` v6→v7; `Agents` row + SINGLE notes on Phases 6-7/Gate C in `template-plan.md`; Agent Topology Selection step added to `parcel.agent.md`; axis documented in `base-context.md` (re-inlined into all 7 agents, prefix PASS ×7) and mirrored in the satellite seed `base-context.template.md` v4→v5, `plan-lifecycle.md`, and HOW-TO. Invariants held in both topologies: same plan file/lifecycle, one phase-group per session, Gate A + Gate D always halt for the human. machinery-version: 23. |
| **v0.3.15** | 2026-09-11 | Patch | GitHub Copilot (OpenCode Go / DeepSeek V4.1 Flash) | Wrap-up + deploy skill streamlining: `agent-wrap-up` v7→v8 (161→145 lines — dropped dead "Mandatory Tools", collapsed phase-restating "Non-Negotiable Rules" to one hard stop, merged Phases 5+6 into Backlog Reconciliation, split gate into 7a coverage / 7b state stamps); `test-and-deploy` v2→v3 (no-`package.json` applicability guard + new §2b Machinery Integrity Gate running `check-parcel-prefix`/`check-utf8-agents` before push, build folded into the concurrent block); cross-ref sweep for the phase renumber (`knowledge-consolidation` v6→v7, `wiki-verifier.subagent`, `18-knowledge-capture`); machinery-version: 22. |
| **v0.3.14** | 2026-09-11 | Patch | GitHub Copilot (OpenCode Go / DeepSeek V4.1 Flash) | T1-E2.01 review fixes: MH-15 completed (`.wiki/rules/numbering.md` top-level + sub-area tables now cover all 8 manifest areas); `wiki_lint.py` hub-reachability no longer flags `.wiki/rules/**` (governance sits above the spoke graph) and the BFS read is guarded against non-UTF-8 files; `wiki-lint` skill v3→v4 documents the exemption; residual sync/pull usage text converted to forward-slash paths; machinery-version: 21. |
| **v0.3.13** | 2026-09-11 | Minor | GitHub Copilot (OpenCode Go / DeepSeek V4.1 Flash) | T1-E2.01 machinery hardening: linter truth (`wiki_lint.py` gains frontmatter `related-to`/`dependencies` link checks, hub→spoke, `[UNINDEXED]`/`[MISSING]`, hub BFS reachability; `--fix` implemented; pattern-based exemptions); 7 broken frontmatter links repaired; `Last Verified` column added to the hub Quick Reference; transport engine Linux parity (single `Read-Manifest`, `$shellExe` re-invocation, forward-slash paths) + `-SelfTest` in CI; seeds reconciled (`opencode.template.json` ships the 9-agent block); doc truth sweep (HOW-TO, rules, wrap-up, verifier, plan-lifecycle, AGENTS); version/log discipline (descending release log + failing CI check); residue removed (Gemini genericised, sprint registers parked, `theme-linguistics` refs deleted); model-binding validation added (`opencode.json`↔registry, absent-block SKIP) + uniform rebind to DeepSeek V4.1 Flash; machinery-version: 20. |
| **v0.3.12** | 2026-09-10 | Patch | GitHub Copilot (OpenCode Go / Deepseek V4 Flash) | Documentation-structure block in `AGENTS.md` + `AGENTS.template.md` aligned (`.devops/plans/` vs `.devops/backlog/` wording); machinery-version: 19. Row reconstructed from commit `8c50a87` during T1-E2.01 — the bump had no session changelog entry. |
| **v0.3.11** | 2026-09-09 | Patch | GitHub Copilot (OpenCode Go / Deepseek V4 Flash) | Portable `.vscode/settings.json` forces UTF-8: `files.encoding: utf8` + `files.autoGuessEncoding: false` so docs never mis-decode on open (client-side guard against the cp1252/cp437 round-trip corruption); machinery-version 17→18. |
| **v0.3.10** | 2026-09-09 | Patch | GitHub Copilot (OpenCode Go / Deepseek V4 Flash) | Encoding guard added to `wiki_lint.py`: hard-fails on UTF-8/UTF-16 BOMs and non-UTF-8 files (previously reported corrupted files as "healthy"); `pull-architecture.ps1` writes `.ptp-source` via .NET UTF-8 no-BOM instead of `Set-Content -Encoding UTF8`; machinery-version 16→17. |
| **v0.3.9** | 2026-09-09 | Patch | GitHub Copilot (OpenCode Go / Deepseek V4 Flash) | UTF-8 guard extended to the full wiki: `check-utf8-agents.ps1` now scans `.wiki/**/*.md` (supersedes the KC-only scan), closing the gap where mojibake in documentation prose — invisible to `wiki_lint.py` (links/anchors/frontmatter only) — survived unchecked; machinery-version 15→16. |
| **v0.3.8** | 2026-09-09 | Patch | GitHub Copilot (OpenCode Go / Qwen3.8 Flash) | Knowledge-changelog decommission: `.devops/logs/knowledge-changelog.md` deleted and all live references purged (skills, templates, wiki, README/HOW-TO/AGENTS, `scripts/wiki_lint.py` `--changelog` flag + `LOGS` constant); `agent-changelog.md` pruned to current-session entries (git history is the permanent record); `@wiki-lint`/`@knowledge-consolidation` reports now stdout-only with no persistent log; skill versions bumped (app-vision-north-star v1→2, knowledge-capture v5→6, knowledge-consolidation v5→6, wiki-lint v1→2); machinery-version 14→15. |
| **v0.3.7** | 2026-09-07 | Patch | GitHub Copilot (OpenCode Go / Qwen3.8 Flash) | Uniform model routing per user direction: all parcel/ptp/wiki agents rebound to Qwen3.8 Flash on both surfaces (VS Code frontmatter + `opencode.json` → `opencode-go/qwen3.8-flash`); base-context Model Registry rebound + uniform-routing note added; `@model-routing` skill v2→v3 (single-model routing documented as deliberate, not drift); machinery-version 11→12. |
| **v0.3.6** | 2026-09-07 | Minor | GitHub Copilot (OpenCode Go / Glm 5.3 Flash) | Determinism overhaul: canonical 4-gate lifecycle (A Scope / B Spec & Plan / C Reviews / D Implementation) reconciled across skill, base-context, template, rules, and agents; `PHASE_8_FAILED` state + Gate A rejection path; script-enforced verbatim skill embeds (`EMBED` markers, drift now mechanically impossible); PREFIX split (orchestrator-only delegation map + registry trimmed from subagent prefixes); Phase 3 questions mandated one-at-a-time via ask-questions tool; hedged gate language replaced with operational tests; smooth-operator wiki-write mandate removed (Phase 10 owns capture); code-surgeon agent wrapper added; broken paths + numbering skips fixed; UTF-8 guard extended to plans/archive/backlog/logs. machinery-version: 11 |
| **v0.3.5** | 2026-09-06 | Patch | GitHub Copilot (OpenCode Go / Glm 5.3 Flash) | Dual-surface model binding: VS Code agent frontmatter gains `tools` (+`vscode_askQuestions`) + `model:` (orchestrator/wiki-writer → Glm 5.3 Flash; ptp-*/wiki-verifier → Deepseek V4 Flash, picker-exact labels); `opencode.json` gains 9 JSON agent bindings via `{file:...}` prompts (parcel primary with task glob-lock, subagents hidden), `default_agent` → `parcel`; machinery-version 8→9. |
| **v0.3.4** | 2026-09-05 | Patch | GitHub Copilot (OpenCode Go / Glm 5.3 Flash) | Sync `-Verify` mode: read-only post-load gate — structural checks of the satellite-authored surface (`AGENTS.md` machinery markers, `opencode.json` wiring, `base-context.md`, wiki anchor, machinery presence, `.ptp-source` advisory) + machinery gates check-only; post-sync structural report informational (bootstrap exits 0); wiki-lint gate skips wiki-less targets; machinery-version 6→7 (combined set with prune_files). |
| **v0.3.3** | 2026-09-05 | Patch | GitHub Copilot (Copilot SDK) | Sync `prune_files` mechanism: `prune_files:` key in `sync-manifest.yaml` deletes retired files from satellites (8 old `parcel-*.md` runbooks); `sync-architecture.ps1` prunes on copy, reports `PRUNE` in `-Check`, `-SelfTest` verifies; machinery-version: 6 (5→6). |
| **v0.3.2** | 2026-09-04 | Patch | GitHub Copilot (Copilot SDK) | VS Code custom agent migration: `parcel-*` runbooks → `parcel.agent.md` + `wiki-writer.agent.md` (selectable) + `ptp-*.subagent.md` + `wiki-verifier.subagent.md` (subagents); `/parcel` command folded into `parcel.agent.md`; `opencode.json` agent block removed; `check-parcel-prefix.ps1` handles frontmatter-in-file; machinery-version 4→5. |
| **v0.3.1** | 2026-09-04 | Patch | GitHub Copilot (Copilot SDK) | Sync discoverability + `.vscode` portability: AGENTS.md + seed template gain sync row; `.vscode` added to `portable_dirs` (settings.json sanitized of machine-specific `git.path`/PATH, tasks.json folder-open auto-launch removed), tracked + synced; machinery-version 2→4. |
| **v0.3.0** | 2026-09-03 | Minor | GitHub Copilot (opencode-go/qwen3.8-flash) | Independent-review hardening: template de-contamination (GRID residue removed from wiki titles + catalog indexes rebuilt as generic patterns), Model Registry replaced by capability slots (`planning`/`review-heavy`/`execution`, binding = satellite `opencode.json`), CI gate `.github/workflows/validate.yml`, sync engine `-SelfTest`, UTF-8 guard extended to skills, coverage gate graceful no-op without `src/`, hooks/ref/examples index docs, DESIGN.md folded into `09-design-system.md`. machinery-version: 2 |
| **v0.2.0** | 2026-09-03 | Minor | GitHub Copilot (opencode-go/glm) | Template sync from GRID-Deploy: spec-first parcel pipeline (Phase 4 wiki spec, Gates A/B/C, `PHASE_5_REVISION` loop), `wiki-writer` rename, `wiki-verifier` agent, Theme/Epic plan prefixes (`T{n}-E{n}.{impl}`), knowledge promotion workflow (capture v1.1 / consolidation v3.0), coverage gate (`wiki_coverage_check.py`), hardened deterministic `wiki_lint.py` (hub-spoke + BFS reachability + `--fix`/`--changelog`), `.vscode/settings.json`, UTF-8 mojibake repair, frontmatter `title` + status normalization across 31 wiki docs, integrations spoke linked from hub. |
| **v0.1.0** | 2026-08-18 | Patch | deepseek-v4-flash | Parcel orchestration overhaul: High-Visionary Phase 4 (no code snippets), Grumpy Architect → Spec & Logic Audit (Phase 5), Smooth Operator Phase 6, Code Surgeon single-pass direct-to-disk (Phases 7-8), deterministic `PHASE_4_REVISION` rejection loop at Gate C, canonical model registry (mimo-2.5 / v4-flash-max / deepseek-v4-flash), PREFIX-LOCKED cache enforcement (.gitattributes + check scripts + pre-commit hook). |

> **Convention:** newest release first. Every `machinery-version:` bump in `.devops/sync-manifest.yaml` MUST be recorded in a row above; CI fails when the current value is absent from this file.
